theory nat
imports logic
domains nat
description "Natural numbers"

header "Basic definitions"

const zero :: 'a overloaded

const one :: 'a overloaded

datatype nat =
  | zero :: nat
  | Suc (n :: nat) :: nat

def one :: nat = 1 = Suc 0 [hint_rewrite]

const of_nat :: nat ⇒ 'a overloaded

def of_nat :: nat ⇒ nat = of_nat n = n

fun Pre :: nat ⇒ nat
  | Pre 0 = 0
  | Pre (Suc n) = n

const plus :: 'a ⇒ 'a ⇒ 'a overloaded

const minus :: 'a ⇒ 'a ⇒ 'a overloaded

const times :: 'a ⇒ 'a ⇒ 'a overloaded

const power :: 'a ⇒ 'b ⇒ 'a overloaded

fun plus :: nat ⇒ nat ⇒ nat
  | 0 + n = n
  | Suc m + n = Suc (m + n)

fun times :: nat ⇒ nat ⇒ nat
  | 0 * n = 0
  | Suc m * n = n + m * n

fun power :: nat ⇒ nat ⇒ nat
  | n ^ (0::nat) = 1
  | n ^ Suc m = n * n ^ m

theorem add_0_right
  fixes x :: nat
  prop x + 0 = x
  [hint_rewrite]
proof
  ← induction x nat_induct goal=0
    #[1] (0::nat) + 0 = 0
    #[2] ∀n. n + 0 = n ⟶ Suc n + 0 = Suc n
  ← rewrite_goal nat_plus_def_1 sym=false goal=1
  ← introduction m goal=2
    #[3] _VAR m
    #[4] m + 0 = m
    #[5] Suc m + 0 = Suc m
  ← rewrite_goal nat_plus_def_2 sym=false goal=5
    #[6] Suc (m + 0) = Suc m
  ← rewrite_goal_with_prev goal=6 facts=[4]
qed

theorem add_1_left
  fixes x :: nat
  prop 1 + x = Suc x
  [hint_rewrite]
proof
  ← rewrite_goal nat_one_def goal=0
    #[1] Suc 0 + x = Suc x
  ← rewrite_goal nat_plus_def_2 goal=1
    #[2] Suc (0 + x) = Suc x
  ← rewrite_goal nat_plus_def_1 goal=2
qed

theorem add_1_right
  fixes x :: nat
  prop x + 1 = Suc x
  [hint_rewrite,hint_rewrite_sym]
proof
  ← induction x nat_induct goal=0
    #[1] 0 + 1 = Suc 0
    #[2] ∀n. n + 1 = Suc n ⟶ Suc n + 1 = Suc (Suc n)
  ← rewrite_goal nat_plus_def_1 goal=1
    #[3] 1 = Suc 0
  ← rewrite_goal nat_one_def goal=3
  ← introduction n goal=2
    #[4] _VAR n
    #[5] n + 1 = Suc n
    #[6] Suc n + 1 = Suc (Suc n)
  ← rewrite_goal nat_plus_def_2 goal=6
    #[7] Suc (n + 1) = Suc (Suc n)
  ← rewrite_goal_with_prev goal=7 facts=[5]
qed

theorem add_Suc_right
  fixes x :: nat, y :: nat
  prop x + Suc y = Suc (x + y)
  [hint_rewrite]

theorem add_comm
  fixes x :: nat, y :: nat
  prop x + y = y + x
  [hint_backward]
proof
  ← induction x nat_induct goal=0
    #[1] 0 + y = y + 0
    #[2] ∀n. n + y = y + n ⟶ Suc n + y = y + Suc n
  ← rewrite_goal add_0_right goal=1
    #[3] 0 + y = y
  ← rewrite_goal nat_plus_def_1 goal=3
  ← introduction x goal=2
    #[4] _VAR x
    #[5] x + y = y + x
    #[6] Suc x + y = y + Suc x
  ← rewrite_goal nat_plus_def_2 goal=6
    #[7] Suc (x + y) = y + Suc x
  ← rewrite_goal add_Suc_right goal=7
    #[8] Suc (x + y) = Suc (y + x)
  ← rewrite_goal_with_prev goal=8 facts=[5]
qed

theorem add_assoc
  fixes x :: nat, y :: nat, z :: nat
  prop x + y + z = x + (y + z)
  [hint_backward]
proof
  ← induction x nat_induct goal=0
    #[1] 0 + y + z = 0 + (y + z)
    #[2] ∀n. n + y + z = n + (y + z) ⟶ Suc n + y + z = Suc n + (y + z)
  ← rewrite_goal nat_plus_def_1 sym=false goal=1
  ← introduction x goal=2
    #[3] _VAR x
    #[4] x + y + z = x + (y + z)
    #[5] Suc x + y + z = Suc x + (y + z)
  ← rewrite_goal nat_plus_def_2 sym=false goal=5
    #[6] Suc (x + y) + z = Suc (x + (y + z))
  ← rewrite_goal nat_plus_def_2 sym=false goal=6
    #[7] Suc (x + y + z) = Suc (x + (y + z))
  ← rewrite_goal_with_prev goal=7 facts=[4]
qed

theorem mult_0_right
  fixes x :: nat
  prop x * 0 = 0
  [hint_rewrite]
proof
  ← induction x nat_induct goal=0
    #[1] (0::nat) * 0 = 0
    #[2] ∀n. n * 0 = 0 ⟶ Suc n * 0 = 0
  ← rewrite_goal nat_times_def_1 goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] n * 0 = 0
    #[5] Suc n * 0 = 0
  ← rewrite_goal nat_times_def_2 goal=5
    #[6] 0 + n * 0 = 0
  ← rewrite_goal nat_plus_def_1 goal=6
qed

theorem mult_1_left
  fixes x :: nat
  prop 1 * x = x
  [hint_rewrite]
proof
  ← rewrite_goal nat_one_def goal=0
    #[1] Suc 0 * x = x
  ← rewrite_goal nat_times_def_2 goal=1
    #[2] x + 0 * x = x
  ← rewrite_goal nat_times_def_1 goal=2
    #[3] x + 0 = x
  nat_norm goal=3
qed

theorem mult_1_right
  fixes x :: nat
  prop x * 1 = x
  [hint_rewrite]
proof
  ← induction x nat_induct goal=0
    #[1] (0::nat) * 1 = 0
    #[2] ∀n. n * 1 = n ⟶ Suc n * 1 = Suc n
  ← rewrite_goal nat_times_def_1 goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] n * 1 = n
    #[5] Suc n * 1 = Suc n
  ← rewrite_goal nat_times_def_2 goal=5
    #[6] 1 + n * 1 = Suc n
  ← rewrite_goal_with_prev goal=6 facts=[4]
    #[7] 1 + n = Suc n
  nat_norm goal=7
    #[8] 1 + n = Suc n
qed

theorem mult_Suc_right
  fixes x :: nat, y :: nat
  prop x * Suc y = x + x * y
  [hint_rewrite]

theorem distrib_l
  fixes x :: nat, y :: nat, z :: nat
  prop x * (y + z) = x * y + x * z
  [hint_rewrite,hint_rewrite_sym]
proof
  ← induction x nat_induct goal=0
    #[1] 0 * (y + z) = 0 * y + 0 * z
    #[2] ∀n. n * (y + z) = n * y + n * z ⟶ Suc n * (y + z) = Suc n * y + Suc n * z
  ← rewrite_goal nat_times_def_1 sym=false goal=1
    #[3] (0::nat) = 0 + 0
  ← rewrite_goal add_0_right sym=false goal=3
  ← introduction x goal=2
    #[4] _VAR x
    #[5] x * (y + z) = x * y + x * z
    #[6] Suc x * (y + z) = Suc x * y + Suc x * z
  ← rewrite_goal nat_times_def_2 sym=false goal=6
    #[7] y + z + x * (y + z) = y + x * y + (z + x * z)
  ← rewrite_goal_with_prev goal=7 facts=[5]
    #[8] y + z + (x * y + x * z) = y + x * y + (z + x * z)
  nat_norm goal=8
    #[9] y + z + (x * y + x * z) = y + x * y + (z + x * z)
qed

theorem distrib_r
  fixes x :: nat, y :: nat, z :: nat
  prop (x + y) * z = x * z + y * z
  [hint_rewrite]
proof
  ← induction z nat_induct goal=0
    #[1] (x + y) * 0 = x * 0 + y * 0
    #[2] ∀n. (x + y) * n = x * n + y * n ⟶ (x + y) * Suc n = x * Suc n + y * Suc n
  ← rewrite_goal mult_0_right sym=false goal=1
    #[3] (0::nat) = 0 + 0
  ← rewrite_goal add_0_right sym=false goal=3
  ← introduction n goal=2
    #[4] _VAR n
    #[5] (x + y) * n = x * n + y * n
    #[6] (x + y) * Suc n = x * Suc n + y * Suc n
  ← rewrite_goal mult_Suc_right sym=false goal=6
    #[7] x + y + (x + y) * n = x + x * n + (y + y * n)
  ← rewrite_goal_with_prev goal=7 facts=[5]
    #[8] x + y + (x * n + y * n) = x + x * n + (y + y * n)
  nat_norm goal=8
    #[9] x + y + (x * n + y * n) = x + x * n + (y + y * n)
qed

theorem mult_assoc
  fixes x :: nat, y :: nat, z :: nat
  prop x * y * z = x * (y * z)
  [hint_backward]
proof
  ← induction x nat_induct goal=0
    #[1] 0 * y * z = 0 * (y * z)
    #[2] ∀n. n * y * z = n * (y * z) ⟶ Suc n * y * z = Suc n * (y * z)
  ← rewrite_goal nat_times_def_1 goal=1
    #[3] 0 * z = 0
  ← rewrite_goal nat_times_def_1 goal=3
  ← introduction x goal=2
    #[4] _VAR x
    #[5] x * y * z = x * (y * z)
    #[6] Suc x * y * z = Suc x * (y * z)
  ← rewrite_goal nat_times_def_2 goal=6
    #[7] (y + x * y) * z = y * z + x * (y * z)
  ← rewrite_goal distrib_r goal=7
    #[8] y * z + x * y * z = y * z + x * (y * z)
  ← rewrite_goal_with_prev goal=8 facts=[5]
qed

theorem mult_comm
  fixes x :: nat, y :: nat
  prop x * y = y * x
  [hint_backward]
proof
  ← induction x nat_induct goal=0
    #[1] 0 * y = y * 0
    #[2] ∀n. n * y = y * n ⟶ Suc n * y = y * Suc n
  ← rewrite_goal mult_0_right goal=1
    #[3] 0 * y = 0
  ← rewrite_goal nat_times_def_1 goal=3
  ← introduction x goal=2
    #[4] _VAR x
    #[5] x * y = y * x
    #[6] Suc x * y = y * Suc x
  ← rewrite_goal nat_times_def_2 goal=6
    #[7] y + x * y = y * Suc x
  ← rewrite_goal_with_prev goal=7 facts=[5]
    #[8] y + y * x = y * Suc x
  ← rewrite_goal mult_Suc_right goal=8
    #[9] y + y * x = y * Suc x
qed

theorem add_cancel_left
  fixes x :: nat, y :: nat, z :: nat
  prop x + y = x + z --> y = z
  [hint_backward,hint_forward]
proof
  ← introduction goal=0
    #[1] x + y = x + z
    #[2] y = z
  revert_intro goal=2 facts=[1]
  ← induction x nat_induct goal=0
    #[3] 0 + y = 0 + z ⟶ y = z
    #[4] ∀n. (n + y = n + z ⟶ y = z) ⟶ Suc n + y = Suc n + z ⟶ y = z
  ← introduction goal=3
    #[5] 0 + y = 0 + z
    #[6] y = z
  -> rewrite_fact nat_plus_def_1 sym=false goal=6 facts=[5]
  ← introduction n goal=4
    #[7] _VAR n
    #[8] n + y = n + z ⟶ y = z
    #[9] Suc n + y = Suc n + z
    #[10] y = z
  ← apply_prev goal=10 facts=[8]
    #[11] n + y = n + z
  -> rewrite_fact nat_plus_def_2 sym=false goal=11 facts=[9]
    #[12] Suc (n + y) = Suc (n + z)
  ← apply_backward_step nat_Suc_inject goal=11
qed

theorem add_cancel_right
  fixes x :: nat, y :: nat, z :: nat
  prop x + z = y + z --> x = y
  [hint_backward,hint_forward]
proof
  ← introduction goal=0
    #[1] x + z = y + z
    #[2] x = y
  -> rewrite_fact add_comm goal=2 facts=[1]
    #[3] z + x = z + y
  ← apply_backward_step add_cancel_left goal=2 facts=[3]
qed

theorem add_right_eq_zero
  fixes x :: nat, y :: nat
  prop x + y = x --> y = 0
  [hint_backward]
proof
  ← introduction goal=0
    #[1] x + y = x
    #[2] y = 0
  cut "x + y = x + 0" goal=2
    #[3] x + y = x + 0
  ← rewrite_goal add_0_right goal=3
  ← apply_backward_step add_cancel_left goal=2 facts=[3]
qed

theorem add_left_eq_zero
  fixes x :: nat, y :: nat
  prop x + y = y --> x = 0
  [hint_backward]
proof
  ← introduction goal=0
    #[1] x + y = y
    #[2] x = 0
  cut "x + y = 0 + y" goal=2
    #[3] x + y = 0 + y
  ← rewrite_goal nat_plus_def_1 goal=3
  ← apply_backward_step add_cancel_right goal=2 facts=[3]
qed

theorem add_is_zero
  fixes x :: nat, y :: nat
  prop x + y = 0 --> x = 0 & y = 0
  [hint_backward,hint_backward1,hint_forward]
proof
  ← introduction goal=0
    #[1] x + y = 0
    #[2] x = 0 ∧ y = 0
  revert_intro goal=2 facts=[1]
  ← induction x nat_induct goal=0
    #[3] 0 + y = 0 ⟶ (0::nat) = 0 ∧ y = 0
    #[4] ∀n. (n + y = 0 ⟶ n = 0 ∧ y = 0) ⟶ Suc n + y = 0 ⟶ Suc n = 0 ∧ y = 0
  ← introduction goal=3
    #[5] 0 + y = 0
    #[6] (0::nat) = 0 ∧ y = 0
  ← apply_backward_step conjI goal=6
    #[7] (0::nat) = 0
    #[8] y = 0
  ← apply_backward_step eq_refl goal=7
    #[9] (0::nat) = 0
  ← apply_backward_step add_right_eq_zero goal=8 facts=[5]
  ← introduction x goal=4
    #[10] _VAR x
    #[11] x + y = 0 ⟶ x = 0 ∧ y = 0
    #[12] Suc x + y = 0
    #[13] Suc x = 0 ∧ y = 0
  -> rewrite_fact nat_plus_def_2 sym=false goal=13 facts=[12]
    #[14] Suc (x + y) = 0
  ← apply_backward_step contrapositive goal=13 facts=[14]
    #[15] ¬(Suc x = 0 ∧ y = 0) ⟶ ¬(Suc (x + y) = 0)
  ← introduction goal=15
    #[16] ¬(Suc x = 0 ∧ y = 0)
    #[17] ¬(Suc (x + y) = 0)
  ← rewrite_goal eq_sym_eq sym=false goal=17
    #[18] ¬(0 = Suc (x + y))
  ← apply_backward_step nat_zero_Suc_neq goal=18
    #[19] ¬(0 = Suc (x + y))
qed

theorem eq_add_lcancel_0
  fixes m :: nat, n :: nat
  prop m + n = m <--> n = 0
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] m + n = m ⟶ n = 0
    #[2] n = 0 ⟶ m + n = m
  ← introduction goal=1
    #[3] m + n = m
    #[4] n = 0
  ← apply_backward_step add_right_eq_zero goal=4 facts=[3]
  ← introduction goal=2
    #[5] n = 0
    #[6] m + n = m
  ← rewrite_goal_with_prev goal=6 facts=[5]
    #[7] m + 0 = m
  ← rewrite_goal add_0_right sym=false goal=7
    #[8] m + 0 = m
qed

theorem mult_nonzero
  fixes x :: nat, y :: nat
  prop x * y = 0 --> x = 0 | y = 0
  [hint_backward]
proof
  ← introduction goal=0
    #[1] x * y = 0
    #[2] x = 0 ∨ y = 0
  revert_intro goal=2 facts=[1]
  ← induction x nat_induct goal=0
    #[3] 0 * y = 0 ⟶ (0::nat) = 0 ∨ y = 0
    #[4] ∀n. (n * y = 0 ⟶ n = 0 ∨ y = 0) ⟶ Suc n * y = 0 ⟶ Suc n = 0 ∨ y = 0
  ← introduction goal=3
    #[5] 0 * y = 0
    #[6] (0::nat) = 0 ∨ y = 0
  ← apply_backward_step disjI1 goal=6
    #[7] (0::nat) = 0
  ← apply_backward_step eq_refl goal=7
    #[8] (0::nat) = 0
  ← introduction x goal=4
    #[9] _VAR x
    #[10] x * y = 0 ⟶ x = 0 ∨ y = 0
    #[11] Suc x * y = 0
    #[12] Suc x = 0 ∨ y = 0
  -> rewrite_fact nat_times_def_2 sym=false goal=12 facts=[11]
    #[13] y + x * y = 0
  -> apply_forward_step add_is_zero goal=12 facts=[13]
    #[14] y = 0 ∧ x * y = 0
  -> apply_forward_step conjD1 goal=12 facts=[14]
    #[15] y = 0
  ← apply_backward_step disjI2 goal=12
qed

theorem mult_eq_0
  fixes x :: nat, y :: nat
  prop x * y = 0 <--> x = 0 | y = 0
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] x * y = 0 ⟶ x = 0 ∨ y = 0
    #[2] x = 0 ∨ y = 0 ⟶ x * y = 0
  ← introduction goal=1
    #[3] x * y = 0
    #[4] x = 0 ∨ y = 0
  ← apply_backward_step mult_nonzero goal=4
  ← introduction goal=2
    #[5] x = 0 ∨ y = 0
    #[6] x * y = 0
  ← apply_backward_step disjE goal=6 facts=[5]
    #[7] x = 0 ⟶ x * y = 0
    #[8] y = 0 ⟶ x * y = 0
  ← introduction goal=7
    #[9] x = 0
    #[10] x * y = 0
  ← rewrite_goal_with_prev goal=10 facts=[9]
    #[11] 0 * y = 0
  ← rewrite_goal nat_times_def_1 sym=false goal=11
    #[12] 0 * y = 0
  ← introduction goal=8
    #[13] y = 0
    #[14] x * y = 0
  ← rewrite_goal_with_prev goal=14 facts=[13]
    #[15] x * 0 = 0
  ← rewrite_goal mult_0_right sym=false goal=15
    #[16] x * 0 = 0
qed

theorem eq_mult_lcancel
  fixes m :: nat, n :: nat, p :: nat
  prop m * n = m * p <--> m = 0 | n = p
  [hint_rewrite]

theorem eq_mult_rcancel
  fixes m :: nat, n :: nat, p :: nat
  prop n * m = p * m <--> m = 0 | n = p
  [hint_rewrite]
proof
  ← rewrite_goal mult_comm sym=false goal=0
    #[1] m * n = m * p ⟷ m = 0 ∨ n = p
  ← rewrite_goal eq_mult_lcancel sym=false goal=1
qed

theorem one_nonzero
  prop ~((1::nat) = 0)
proof
  ← rewrite_goal nat_one_def sym=false goal=0
    #[1] ¬(Suc 0 = 0)
  ← rewrite_goal eq_sym_eq sym=false goal=1
    #[2] ¬(0 = Suc 0)
  -> apply_forward_step nat_zero_Suc_neq param_n="(0::nat)" goal=2
qed

theorem mult_eq_1
  fixes m :: nat, n :: nat
  prop m * n = 1 <--> m = 1 & n = 1
  [hint_rewrite]

header "Binary representation"

def bit0 :: nat ⇒ nat = bit0 n = n + n

def bit1 :: nat ⇒ nat = bit1 n = n + n + 1

theorem one_Suc
  prop Suc 1 = bit0 1
proof
  ← rewrite_goal bit0_def sym="" goal=0
    #[1] Suc 1 = 1 + 1
  nat_norm goal=1
qed

theorem bit0_Suc
  fixes n :: nat
  prop Suc (bit0 n) = bit1 n
  [hint_rewrite,hint_rewrite_sym]

theorem bit1_Suc
  fixes n :: nat
  prop Suc (bit1 n) = bit0 (Suc n)
  [hint_rewrite]

theorem bit0_bit0_add
  fixes m :: nat, n :: nat
  prop bit0 m + bit0 n = bit0 (m + n)

theorem bit0_bit1_add
  fixes m :: nat, n :: nat
  prop bit0 m + bit1 n = bit1 (m + n)

theorem bit1_bit0_add
  fixes m :: nat, n :: nat
  prop bit1 m + bit0 n = bit1 (m + n)

theorem bit1_bit1_add
  fixes m :: nat, n :: nat
  prop bit1 m + bit1 n = bit0 (Suc (m + n))

theorem bit0_bit0_mult
  fixes m :: nat, n :: nat
  prop bit0 m * bit0 n = bit0 (bit0 (m * n))
proof
  ← rewrite_goal bit0_def sym="" goal=0
    #[1] (m + m) * (n + n) = bit0 (m * n) + bit0 (m * n)
  ← rewrite_goal bit0_def sym="" goal=1
    #[2] (m + m) * (n + n) = m * n + m * n + (m * n + m * n)
  nat_norm goal=2
qed

theorem bit0_bit1_mult
  fixes m :: nat, n :: nat
  prop bit0 m * bit1 n = bit0 (m * bit1 n)

theorem bit1_bit0_mult
  fixes m :: nat, n :: nat
  prop bit1 m * bit0 n = bit0 (bit1 m * n)

theorem bit1_bit1_mult
  fixes m :: nat, n :: nat
  prop bit1 m * bit1 n = bit1 (m + n + bit0 (m * n))

theorem bit0_nonzero
  fixes m :: nat
  prop ~(m = 0) --> ~(bit0 m = 0)
proof
  ← introduction goal=0
    #[1] ¬(m = 0)
    #[2] ¬(bit0 m = 0)
  ← apply_backward_step contrapositive goal=2 facts=[1]
    #[3] ¬¬(bit0 m = 0) ⟶ ¬¬(m = 0)
  ← rewrite_goal double_neg sym=false goal=3
    #[4] bit0 m = 0 ⟶ m = 0
  ← introduction goal=4
    #[5] bit0 m = 0
    #[6] m = 0
  -> rewrite_fact bit0_def sym=false goal=6 facts=[5]
    #[7] m + m = 0
  -> apply_forward_step add_is_zero goal=6 facts=[7]
    #[8] m = 0 ∧ m = 0
  ← apply_backward_step conjD1 goal=6 facts=[8]
    #[9] m = 0
qed

theorem bit1_nonzero
  fixes m :: nat
  prop ~(bit1 m = 0)
proof
  ← rewrite_goal bit1_def sym=false goal=0
    #[1] ¬(m + m + 1 = 0)
  ← rewrite_goal add_1_right sym=false goal=1
    #[2] ¬(Suc (m + m) = 0)
  ← rewrite_goal eq_sym_eq sym=false goal=2
    #[3] ¬(0 = Suc (m + m))
  ← apply_backward_step nat_zero_Suc_neq goal=3
qed

theorem bit0_neq
  fixes m :: nat, n :: nat
  prop ~(m = n) --> ~(bit0 m = bit0 n)

theorem bit1_neq
  fixes m :: nat, n :: nat
  prop ~(m = n) --> ~(bit1 m = bit1 n)

theorem bit0_bit1_neq
  fixes m :: nat, n :: nat
  prop ~(bit0 m = bit1 n)

theorem bit0_neq_one
  fixes m :: nat
  prop ~(bit0 m = 1)

theorem bit1_neq_one
  fixes m :: nat
  prop ~(m = 0) --> ~(bit1 m = 1)

header "Test of macros"

theorem nat_norm_test1
  fixes x :: nat, y :: nat, z :: nat
  prop x + y + (y + z) = y * 2 + (x + z)

theorem nat_const_ineq_test1
  prop ~((12::nat) = 13)
proof
  nat_const_ineq goal=0
qed

theorem mult_2
  fixes n :: nat
  prop 2 * n = n + n
  [hint_rewrite,hint_rewrite_sym]
proof
  ← rewrite_goal bit0_def sym="" goal=0
    #[1] of_nat (1 + 1) * n = n + n
  ← rewrite_goal nat_of_nat_def sym="" goal=1
    #[2] (1 + 1) * n = n + n
  ← rewrite_goal distrib_r sym=false goal=2
    #[3] 1 * n + 1 * n = n + n
  ← rewrite_goal mult_1_left sym=false goal=3
qed

fun Sigma :: (nat ⇒ nat) ⇒ nat ⇒ nat
  | Sigma f 0 = 0
  | Sigma f (Suc n) = Sigma f n + f n

theorem Sigma_plus
  fixes f :: nat => nat, g :: nat => nat, n :: nat
  prop Sigma (%x. f x + g x) n = Sigma f n + Sigma g n
proof
  ← induction n nat_induct goal=0
    #[1] Sigma (λx. f x + g x) 0 = Sigma f 0 + Sigma g 0
    #[2] ∀n. Sigma (λx. f x + g x) n = Sigma f n + Sigma g n ⟶ Sigma (λx. f x + g x) (Suc n) = Sigma f (Suc n) + Sigma g (Suc n)
  ← rewrite_goal Sigma_def_1 sym=false goal=1
    #[3] (0::nat) = 0 + 0
  ← rewrite_goal add_0_right sym=false goal=3
  ← introduction n goal=2
    #[4] _VAR n
    #[5] Sigma (λx. f x + g x) n = Sigma f n + Sigma g n
    #[6] Sigma (λx. f x + g x) (Suc n) = Sigma f (Suc n) + Sigma g (Suc n)
  ← rewrite_goal Sigma_def_2 sym=false goal=6
    #[7] Sigma (λx. f x + g x) n + (f n + g n) = Sigma f n + f n + (Sigma g n + g n)
  ← rewrite_goal_with_prev goal=7 facts=[5]
    #[8] Sigma f n + Sigma g n + (f n + g n) = Sigma f n + f n + (Sigma g n + g n)
  nat_norm goal=8
    #[9] Sigma f n + Sigma g n + (f n + g n) = Sigma f n + f n + (Sigma g n + g n)
qed

const less_eq :: 'a ⇒ 'a ⇒ bool overloaded

const less :: 'a ⇒ 'a ⇒ bool overloaded

def greater_eq :: 'a ⇒ 'a ⇒ bool = m ≥ n ⟷ n ≤ m [hint_rewrite]

def greater :: 'a ⇒ 'a ⇒ bool = m > n ⟷ n < m [hint_rewrite]

fun less_eq :: nat ⇒ nat ⇒ bool
  | m ≤ 0 ⟷ m = 0
  | m ≤ Suc n ⟷ m = Suc n ∨ m ≤ n

fun less :: nat ⇒ nat ⇒ bool
  | m < 0 ⟷ false
  | m < Suc n ⟷ m = n ∨ m < n

theorem lesseq_Suc_less
  fixes m :: nat, n :: nat
  prop Suc m <= n <--> m < n
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] Suc m ≤ 0 ⟷ m < 0
    #[2] ∀n. Suc m ≤ n ⟷ m < n ⟶ Suc m ≤ Suc n ⟷ m < Suc n
  ← rewrite_goal nat_less_def_1 goal=1
    #[3] Suc m ≤ 0 ⟷ false
  ← rewrite_goal nat_less_eq_def_1 goal=3
    #[4] Suc m = 0 ⟷ false
  ← apply_backward_step iffI goal=4
    #[5] Suc m = 0 ⟶ false
    #[6] false ⟶ Suc m = 0
  ← introduction goal=5
    #[7] Suc m = 0
    #[8] false
  ← apply_backward_step contrapositive goal=8 facts=[7]
    #[9] ¬false ⟶ ¬(Suc m = 0)
  ← introduction goal=9
    #[10] ¬false
    #[11] ¬(Suc m = 0)
  ← rewrite_goal eq_sym_eq sym=false goal=11
    #[12] ¬(0 = Suc m)
  ← apply_backward_step nat_zero_Suc_neq goal=12
    #[13] ¬(0 = Suc m)
  ← introduction goal=6
    #[14] false
    #[15] Suc m = 0
  ← apply_resolve_step not_false_res goal=15 facts=[14]
  ← introduction n goal=2
    #[16] _VAR n
    #[17] Suc m ≤ n ⟷ m < n
    #[18] Suc m ≤ Suc n ⟷ m < Suc n
  ← rewrite_goal nat_less_def_2 sym=false goal=18
    #[19] Suc m ≤ Suc n ⟷ m = n ∨ m < n
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=19
    #[20] Suc m = Suc n ∨ Suc m ≤ n ⟷ m = n ∨ m < n
  ← apply_backward_step iffI goal=20
    #[21] Suc m = Suc n ∨ Suc m ≤ n ⟶ m = n ∨ m < n
    #[22] m = n ∨ m < n ⟶ Suc m = Suc n ∨ Suc m ≤ n
  ← introduction goal=21
    #[23] Suc m = Suc n ∨ Suc m ≤ n
    #[24] m = n ∨ m < n
  ← apply_backward_step disjE goal=24 facts=[23]
    #[25] Suc m = Suc n ⟶ m = n ∨ m < n
    #[26] Suc m ≤ n ⟶ m = n ∨ m < n
  ← introduction goal=25
    #[27] Suc m = Suc n
    #[28] m = n ∨ m < n
  ← apply_backward_step disjI1 goal=28
    #[29] m = n
  ← apply_backward_step nat_Suc_inject goal=29
  ← introduction goal=26
    #[30] Suc m ≤ n
    #[31] m = n ∨ m < n
  ← apply_backward_step disjI2 goal=31
    #[32] m < n
  -> rewrite_fact_with_prev goal=32 facts=[17,30]
    #[33] m < n
  ← introduction goal=22
    #[34] m = n ∨ m < n
    #[35] Suc m = Suc n ∨ Suc m ≤ n
  ← apply_backward_step disjE goal=35 facts=[34]
    #[36] m = n ⟶ Suc m = Suc n ∨ Suc m ≤ n
    #[37] m < n ⟶ Suc m = Suc n ∨ Suc m ≤ n
  ← introduction goal=36
    #[38] m = n
    #[39] Suc m = Suc n ∨ Suc m ≤ n
  ← apply_backward_step disjI1 goal=39
    #[40] Suc m = Suc n
  ← rewrite_goal_with_prev goal=40 facts=[38]
    #[41] Suc m = Suc n
  ← introduction goal=37
    #[42] m < n
    #[43] Suc m = Suc n ∨ Suc m ≤ n
  ← apply_backward_step disjI2 goal=43
    #[44] Suc m ≤ n
  ← rewrite_goal_with_prev goal=44 facts=[17]
qed

theorem less_Suc_lesseq
  fixes m :: nat, n :: nat
  prop m < Suc n <--> m <= n
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] m < Suc 0 ⟷ m ≤ 0
    #[2] ∀n. m < Suc n ⟷ m ≤ n ⟶ m < Suc (Suc n) ⟷ m ≤ Suc n
  ← rewrite_goal nat_less_eq_def_1 goal=1
    #[3] m < Suc 0 ⟷ m = 0
  ← rewrite_goal nat_less_def_2 goal=3
    #[4] m = 0 ∨ m < 0 ⟷ m = 0
  ← rewrite_goal nat_less_def_1 goal=4
    #[5] m = 0 ∨ false ⟷ m = 0
  ← apply_backward_step iffI goal=5
    #[6] m = 0 ∨ false ⟶ m = 0
    #[7] m = 0 ⟶ m = 0 ∨ false
  ← introduction goal=6
    #[8] m = 0 ∨ false
    #[9] m = 0
  ← apply_backward_step disjE goal=9 facts=[8]
    #[10] m = 0 ⟶ m = 0
    #[11] false ⟶ m = 0
  ← introduction goal=11
    #[12] false
    #[13] m = 0
  ← apply_resolve_step not_false_res goal=13 facts=[12]
    #[14] m = 0
  ← introduction goal=7
    #[15] m = 0
    #[16] m = 0 ∨ false
  ← apply_backward_step disjI1 goal=16
  ← introduction n goal=2
    #[17] _VAR n
    #[18] m < Suc n ⟷ m ≤ n
    #[19] m < Suc (Suc n) ⟷ m ≤ Suc n
  ← rewrite_goal nat_less_def_2 sym=false goal=19
    #[20] m = Suc n ∨ m < Suc n ⟷ m ≤ Suc n
  ← apply_backward_step iffI goal=20
    #[21] m = Suc n ∨ m < Suc n ⟶ m ≤ Suc n
    #[22] m ≤ Suc n ⟶ m = Suc n ∨ m < Suc n
  ← introduction goal=21
    #[23] m = Suc n ∨ m < Suc n
    #[24] m ≤ Suc n
  ← apply_backward_step disjE goal=24 facts=[23]
    #[25] m = Suc n ⟶ m ≤ Suc n
    #[26] m < Suc n ⟶ m ≤ Suc n
  ← introduction goal=25
    #[27] m = Suc n
    #[28] m ≤ Suc n
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=28
    #[29] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjI1 goal=29
  ← rewrite_goal_with_prev goal=26 facts=[18]
    #[30] m ≤ n ⟶ m ≤ Suc n
  ← introduction goal=30
    #[31] m ≤ n
    #[32] m ≤ Suc n
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=32
    #[33] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjI2 goal=33
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=22
    #[34] m = Suc n ∨ m ≤ n ⟶ m = Suc n ∨ m < Suc n
  ← rewrite_goal_with_prev goal=34 facts=[18]
    #[35] m = Suc n ∨ m ≤ n ⟶ m = Suc n ∨ m ≤ n
qed

theorem le_suc
  fixes m :: nat, n :: nat
  prop Suc m <= Suc n <--> m <= n
  [hint_rewrite]
proof
  ← rewrite_goal lesseq_Suc_less sym=false goal=0
    #[1] m < Suc n ⟷ m ≤ n
  ← rewrite_goal less_Suc_lesseq sym=false goal=1
qed

theorem lt_suc
  fixes m :: nat, n :: nat
  prop Suc m < Suc n <--> m < n
  [hint_rewrite]
proof
  ← rewrite_goal less_Suc_lesseq sym=false goal=0
    #[1] Suc m ≤ n ⟷ m < n
  ← rewrite_goal lesseq_Suc_less sym=false goal=1
qed

theorem lt_imp_le
  fixes m :: nat, n :: nat
  prop m < n --> m <= n
  [hint_backward,hint_forward]
proof
  ← introduction goal=0
    #[1] m < n
    #[2] m ≤ n
  revert_intro goal=2 facts=[1]
  ← induction n nat_induct goal=0
    #[3] m < 0 ⟶ m ≤ 0
    #[4] ∀n. (m < n ⟶ m ≤ n) ⟶ m < Suc n ⟶ m ≤ Suc n
  ← introduction goal=3
    #[5] m < 0
    #[6] m ≤ 0
  -> rewrite_fact nat_less_def_1 sym=false goal=6 facts=[5]
    #[7] false
  ← apply_resolve_step not_false_res goal=6 facts=[7]
  ← introduction n goal=4
    #[8] _VAR n
    #[9] m < n ⟶ m ≤ n
    #[10] m < Suc n
    #[11] m ≤ Suc n
  -> rewrite_fact less_Suc_lesseq sym=false goal=11 facts=[10]
    #[12] m ≤ n
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=11
    #[13] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjI2 goal=13
qed

theorem le_1_1
  fixes n :: nat
  prop ~(n = 0) --> 0 < n
  [hint_backward,hint_forward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] 0 < n
  revert_intro goal=2 facts=[1]
  ← induction n nat_induct goal=0
    #[3] ¬((0::nat) = 0) ⟶ (0::nat) < 0
    #[4] ∀n. (¬(n = 0) ⟶ 0 < n) ⟶ ¬(Suc n = 0) ⟶ 0 < Suc n
  ← introduction goal=3
    #[5] ¬((0::nat) = 0)
    #[6] (0::nat) < 0
  ← apply_backward_step negE_gen goal=6 facts=[5]
    #[7] (0::nat) = 0
  ← apply_backward_step eq_refl goal=7
    #[8] (0::nat) = 0
  ← introduction n goal=4
    #[9] _VAR n
    #[10] ¬(n = 0) ⟶ 0 < n
    #[11] ¬(Suc n = 0)
    #[12] 0 < Suc n
  ← rewrite_goal less_Suc_lesseq sym=false goal=12
    #[13] 0 ≤ n
  ← cases "n = 0" goal=13
    #[14] n = 0 ⟶ 0 ≤ n
    #[15] ¬(n = 0) ⟶ 0 ≤ n
  ← introduction goal=14
    #[16] n = 0
    #[17] 0 ≤ n
  ← rewrite_goal_with_prev goal=17 facts=[16]
    #[18] (0::nat) ≤ 0
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=18
    #[19] (0::nat) ≤ 0
  ← introduction goal=15
    #[20] 0 ≤ n
  ← apply_backward_step lt_imp_le goal=20
    #[21] 0 < n
  ← apply_prev goal=21 facts=[10]
qed

theorem le_1_2
  fixes n :: nat
  prop ~(n = 0) --> 1 <= n
  [hint_backward,hint_forward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] 1 ≤ n
  ← rewrite_goal nat_one_def sym=false goal=2
    #[3] Suc 0 ≤ n
  ← rewrite_goal lesseq_Suc_less sym=false goal=3
    #[4] 0 < n
  ← apply_backward_step le_1_1 goal=4
qed

theorem less_eq_exist
  fixes m :: nat, n :: nat
  prop m <= n <--> (?p. n = m + p)
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] m ≤ 0 ⟷ (∃p. 0 = m + p)
    #[2] ∀n. m ≤ n ⟷ (∃p. n = m + p) ⟶ m ≤ Suc n ⟷ (∃p. Suc n = m + p)
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=1
    #[3] m = 0 ⟷ (∃p. 0 = m + p)
  ← apply_backward_step iffI goal=3
    #[4] m = 0 ⟶ (∃p. 0 = m + p)
    #[5] (∃p. 0 = m + p) ⟶ m = 0
  ← introduction goal=4
    #[6] m = 0
    #[7] ∃p. 0 = m + p
  ← inst_exists_goal "(0::nat)" goal=7
    #[8] 0 = m + 0
  ← rewrite_goal_with_prev goal=8 facts=[6]
    #[9] (0::nat) = 0 + 0
  ← rewrite_goal nat_plus_def_1 sym=false goal=9
    #[10] (0::nat) = 0 + 0
  ← introduction goal=5
    #[11] ∃p. 0 = m + p
    #[12] m = 0
  exists_elim p goal=12 facts=[11]
    #[13] _VAR p
    #[14] 0 = m + p
    #[15] m = 0
  -> rewrite_fact eq_sym_eq sym=false goal=15 facts=[14]
    #[16] m + p = 0
  -> apply_forward_step add_is_zero goal=15 facts=[16]
    #[17] m = 0 ∧ p = 0
  ← apply_backward_step conjD1 goal=15 facts=[17]
    #[18] m = 0
  ← introduction n goal=2
    #[19] _VAR n
    #[20] m ≤ n ⟷ (∃p. n = m + p)
    #[21] m ≤ Suc n ⟷ (∃p. Suc n = m + p)
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=21
    #[22] m = Suc n ∨ m ≤ n ⟷ (∃p. Suc n = m + p)
  ← apply_backward_step iffI goal=22
    #[23] m = Suc n ∨ m ≤ n ⟶ (∃p. Suc n = m + p)
    #[24] (∃p. Suc n = m + p) ⟶ m = Suc n ∨ m ≤ n
  ← introduction goal=23
    #[25] m = Suc n ∨ m ≤ n
    #[26] ∃p. Suc n = m + p
  ← apply_backward_step disjE goal=26 facts=[25]
    #[27] m = Suc n ⟶ (∃p. Suc n = m + p)
    #[28] m ≤ n ⟶ (∃p. Suc n = m + p)
  ← introduction goal=27
    #[29] m = Suc n
    #[30] ∃p. Suc n = m + p
  ← inst_exists_goal "(0::nat)" goal=30
    #[31] Suc n = m + 0
  ← rewrite_goal_with_prev goal=31 facts=[29]
    #[32] Suc n = Suc n + 0
  ← rewrite_goal add_0_right sym=false goal=32
    #[33] Suc n = Suc n + 0
  ← rewrite_goal_with_prev goal=28 facts=[20]
    #[34] (∃p. n = m + p) ⟶ (∃p. Suc n = m + p)
  ← introduction goal=34
    #[35] ∃p. n = m + p
    #[36] ∃p. Suc n = m + p
  exists_elim p goal=36 facts=[35]
    #[37] n = m + p
    #[38] ∃p. Suc n = m + p
  ← inst_exists_goal "Suc p" goal=38
    #[39] Suc n = m + Suc p
  ← rewrite_goal_with_prev goal=39 facts=[37]
    #[40] Suc (m + p) = m + Suc p
  ← rewrite_goal add_Suc_right sym=false goal=40
    #[41] Suc (m + p) = m + Suc p
  ← introduction goal=24
    #[42] ∃p. Suc n = m + p
    #[43] m = Suc n ∨ m ≤ n
  exists_elim p goal=43 facts=[42]
    #[44] Suc n = m + p
    #[45] m = Suc n ∨ m ≤ n
  revert_intro goal=45 facts=[44]
    #[46] Suc n = m + p ⟶ m = Suc n ∨ m ≤ n
  ← induction p nat_induct goal=46
    #[47] Suc n = m + 0 ⟶ m = Suc n ∨ m ≤ n
    #[48] ∀n1. (Suc n = m + n1 ⟶ m = Suc n ∨ m ≤ n) ⟶ Suc n = m + Suc n1 ⟶ m = Suc n ∨ m ≤ n
  ← introduction goal=47
    #[49] Suc n = m + 0
    #[50] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjI1 goal=50
    #[51] m = Suc n
  -> rewrite_fact add_0_right sym=false goal=51 facts=[49]
    #[52] Suc n = m
  ← rewrite_goal eq_sym_eq sym=false goal=51
  ← introduction p goal=48
    #[53] Suc n = m + p ⟶ m = Suc n ∨ m ≤ n
    #[54] Suc n = m + Suc p
    #[55] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjI2 goal=55
    #[56] m ≤ n
  -> rewrite_fact add_Suc_right sym=false goal=56 facts=[54]
    #[57] Suc n = Suc (m + p)
  ← rewrite_goal_with_prev goal=56 facts=[20]
    #[58] ∃p. n = m + p
  ← inst_exists_goal p goal=58
    #[59] n = m + p
  ← apply_backward_step nat_Suc_inject goal=59
qed

theorem less_exist
  fixes m :: nat, n :: nat
  prop m < n <--> (?d. n = m + Suc d)
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] m < 0 ⟷ (∃d. 0 = m + Suc d)
    #[2] ∀n. m < n ⟷ (∃d. n = m + Suc d) ⟶ m < Suc n ⟷ (∃d. Suc n = m + Suc d)
  ← apply_backward_step iffI goal=1
    #[3] m < 0 ⟶ (∃d. 0 = m + Suc d)
    #[4] (∃d. 0 = m + Suc d) ⟶ m < 0
  ← introduction goal=3
    #[5] m < 0
    #[6] ∃d. 0 = m + Suc d
  -> rewrite_fact nat_less_def_1 sym=false goal=6 facts=[5]
    #[7] false
  ← apply_resolve_step not_false_res goal=6 facts=[7]
  ← introduction goal=4
    #[8] ∃d. 0 = m + Suc d
    #[9] m < 0
  exists_elim d goal=9 facts=[8]
    #[10] _VAR d
    #[11] 0 = m + Suc d
    #[12] m < 0
  -> rewrite_fact eq_sym_eq sym=false goal=12 facts=[11]
    #[13] m + Suc d = 0
  -> apply_forward_step add_is_zero goal=12 facts=[13]
    #[14] m = 0 ∧ Suc d = 0
  -> apply_forward_step conjD2 goal=12 facts=[14]
    #[15] Suc d = 0
  ← apply_backward_step contrapositive goal=12 facts=[15]
    #[16] ¬(m < 0) ⟶ ¬(Suc d = 0)
  ← introduction goal=16
    #[17] ¬(m < 0)
    #[18] ¬(Suc d = 0)
  ← apply_backward_step ineq_sym goal=18
    #[19] ¬(0 = Suc d)
  -> apply_forward_step nat_zero_Suc_neq param_n=d goal=19
    #[20] ¬(0 = Suc d)
  ← introduction n goal=2
    #[21] _VAR n
    #[22] m < n ⟷ (∃d. n = m + Suc d)
    #[23] m < Suc n ⟷ (∃d. Suc n = m + Suc d)
  ← rewrite_goal nat_less_def_2 sym=false goal=23
    #[24] m = n ∨ m < n ⟷ (∃d. Suc n = m + Suc d)
  ← rewrite_goal_with_prev goal=24 facts=[22]
    #[25] m = n ∨ (∃d. n = m + Suc d) ⟷ (∃d. Suc n = m + Suc d)
  ← apply_backward_step iffI goal=25
    #[26] m = n ∨ (∃d. n = m + Suc d) ⟶ (∃d. Suc n = m + Suc d)
    #[27] (∃d. Suc n = m + Suc d) ⟶ m = n ∨ (∃d. n = m + Suc d)
  ← introduction goal=26
    #[28] m = n ∨ (∃d. n = m + Suc d)
    #[29] ∃d. Suc n = m + Suc d
  ← apply_backward_step disjE goal=29 facts=[28]
    #[30] m = n ⟶ (∃d. Suc n = m + Suc d)
    #[31] (∃d. n = m + Suc d) ⟶ (∃d. Suc n = m + Suc d)
  ← introduction goal=30
    #[32] m = n
    #[33] ∃d. Suc n = m + Suc d
  ← rewrite_goal_with_prev goal=33 facts=[32]
    #[34] ∃d. Suc n = n + Suc d
  ← inst_exists_goal "(0::nat)" goal=34
    #[35] Suc n = n + Suc 0
  ← rewrite_goal add_Suc_right sym=false goal=35
    #[36] Suc n = Suc (n + 0)
  ← rewrite_goal add_0_right sym=false goal=36
    #[37] Suc n = Suc (n + 0)
  ← introduction goal=31
    #[38] ∃d. n = m + Suc d
    #[39] ∃d. Suc n = m + Suc d
  exists_elim d goal=39 facts=[38]
    #[40] n = m + Suc d
    #[41] ∃d. Suc n = m + Suc d
  ← inst_exists_goal "Suc d" goal=41
    #[42] Suc n = m + Suc (Suc d)
  ← rewrite_goal_with_prev goal=42 facts=[40]
    #[43] Suc (m + Suc d) = m + Suc (Suc d)
  nat_norm goal=43
    #[44] Suc (m + Suc d) = m + Suc (Suc d)
  ← introduction goal=27
    #[45] ∃d. Suc n = m + Suc d
    #[46] m = n ∨ (∃d. n = m + Suc d)
  exists_elim d goal=46 facts=[45]
    #[47] Suc n = m + Suc d
    #[48] m = n ∨ (∃d. n = m + Suc d)
  revert_intro goal=48 facts=[47]
    #[49] Suc n = m + Suc d ⟶ m = n ∨ (∃d1. n = m + Suc d1)
  ← induction d nat_induct goal=49
    #[50] Suc n = m + Suc 0 ⟶ m = n ∨ (∃d. n = m + Suc d)
    #[51] ∀n1. (Suc n = m + Suc n1 ⟶ m = n ∨ (∃d. n = m + Suc d)) ⟶ Suc n = m + Suc (Suc n1) ⟶ m = n ∨ (∃d. n = m + Suc d)
  ← introduction goal=50
    #[52] Suc n = m + Suc 0
    #[53] m = n ∨ (∃d. n = m + Suc d)
  ← apply_backward_step disjI1 goal=53
    #[54] m = n
  -> rewrite_fact add_Suc_right sym=false goal=54 facts=[52]
    #[55] Suc n = Suc (m + 0)
  -> rewrite_fact add_0_right sym=false goal=54 facts=[55]
    #[56] Suc n = Suc m
  ← apply_backward_step nat_Suc_inject goal=54
    #[57] Suc m = Suc n
  ← rewrite_goal eq_sym_eq sym=false goal=57
  ← introduction d goal=51
    #[58] Suc n = m + Suc d ⟶ m = n ∨ (∃d1. n = m + Suc d1)
    #[59] Suc n = m + Suc (Suc d)
    #[60] m = n ∨ (∃d. n = m + Suc d)
  ← apply_backward_step disjI2 goal=60
    #[61] ∃d. n = m + Suc d
  ← inst_exists_goal d goal=61
    #[62] n = m + Suc d
  -> rewrite_fact add_Suc_right sym=false goal=62 facts=[59]
    #[63] Suc n = Suc (m + Suc d)
  ← apply_backward_step nat_Suc_inject goal=62
qed

theorem less_lesseq
  fixes m :: nat, n :: nat
  prop m < n <--> m <= n & ~(m = n)

theorem less_lesseqI
  fixes m :: nat, n :: nat
  prop m <= n --> ~(m = n) --> m < n

theorem le_0
  fixes n :: nat
  prop 0 <= n
  [hint_resolve,hint_rewrite,hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] (0::nat) ≤ 0
    #[2] ∀n. 0 ≤ n ⟶ 0 ≤ Suc n
  ← rewrite_goal nat_less_eq_def_1 goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] 0 ≤ n
    #[5] 0 ≤ Suc n
  ← rewrite_goal nat_less_eq_def_2 goal=5
    #[6] 0 = Suc n ∨ 0 ≤ n
  ← apply_backward_step disjI2 goal=6
qed

theorem less_0
  fixes n :: nat
  prop 0 < Suc n
proof
  ← rewrite_goal less_Suc_lesseq sym=false goal=0
    #[1] 0 ≤ n
  ← apply_backward_step le_0 goal=1
qed

theorem less_eq_refl
  fixes n :: nat
  prop n <= n
  [hint_backward]
proof
  ← rewrite_goal less_eq_exist goal=0
    #[1] ∃p. n = n + p
  ← inst_exists_goal "(0::nat)" goal=1
    #[2] n = n + 0
  nat_norm goal=2
qed

theorem Suc_Pre
  fixes n :: nat
  prop 0 < n --> Suc (Pre n) = n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] 0 < n
    #[2] Suc (Pre n) = n
  revert_intro goal=2 facts=[1]
  ← induction n nat_induct goal=0
    #[3] (0::nat) < 0 ⟶ Suc (Pre 0) = 0
    #[4] ∀n. (0 < n ⟶ Suc (Pre n) = n) ⟶ 0 < Suc n ⟶ Suc (Pre (Suc n)) = Suc n
  ← introduction goal=3
    #[5] (0::nat) < 0
    #[6] Suc (Pre 0) = 0
  -> rewrite_fact nat_less_def_1 sym=false goal=6 facts=[5]
    #[7] false
  ← apply_resolve_step not_false_res goal=6 facts=[7]
  ← introduction n goal=4
    #[8] _VAR n
    #[9] 0 < n ⟶ Suc (Pre n) = n
    #[10] 0 < Suc n
    #[11] Suc (Pre (Suc n)) = Suc n
  ← rewrite_goal Pre_def_2 sym=false goal=11
    #[12] Suc (Pre (Suc n)) = Suc n
qed

theorem le_Pre
  fixes m :: nat, n :: nat
  prop 0 < n --> Pre m <= Pre n <--> m <= n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] 0 < n
    #[2] Pre m ≤ Pre n ⟷ m ≤ n
  ← induction m nat_induct goal=2
    #[3] Pre 0 ≤ Pre n ⟷ 0 ≤ n
    #[4] ∀n1. Pre n1 ≤ Pre n ⟷ n1 ≤ n ⟶ Pre (Suc n1) ≤ Pre n ⟷ Suc n1 ≤ n
  ← rewrite_goal Pre_def_1 sym=false goal=3
    #[5] 0 ≤ Pre n ⟷ 0 ≤ n
  ← apply_backward_step iffI goal=5
    #[6] 0 ≤ Pre n ⟶ 0 ≤ n
    #[7] 0 ≤ n ⟶ 0 ≤ Pre n
  ← introduction goal=6
    #[8] 0 ≤ Pre n
    #[9] 0 ≤ n
  ← apply_backward_step le_0 goal=9
    #[10] 0 ≤ n
  ← introduction goal=7
    #[11] 0 ≤ n
    #[12] 0 ≤ Pre n
  ← apply_backward_step le_0 goal=12
    #[13] 0 ≤ Pre n
  ← introduction m goal=4
    #[14] _VAR m
    #[15] Pre m ≤ Pre n ⟷ m ≤ n
    #[16] Pre (Suc m) ≤ Pre n ⟷ Suc m ≤ n
  -> apply_forward_step Suc_Pre goal=16 facts=[1]
    #[17] Suc (Pre n) = n
  ← rewrite_goal Pre_def_2 sym=false goal=16
    #[18] m ≤ Pre n ⟷ Suc m ≤ n
  -> apply_forward_step le_suc param_m=m param_n="Pre n" goal=18
    #[19] Suc m ≤ Suc (Pre n) ⟷ m ≤ Pre n
  -> rewrite_fact_with_prev goal=18 facts=[17,19]
    #[20] Suc m ≤ n ⟷ m ≤ Pre n
  ← rewrite_goal eq_sym_eq sym=false goal=18
qed

theorem le_antisym
  fixes m :: nat, n :: nat
  prop m <= n & n <= m <--> m = n
  [hint_rewrite]

theorem lt_antisym
  fixes m :: nat, n :: nat
  prop ~(m < n & n < m)
  [hint_backward]

theorem let_antisym
  fixes m :: nat, n :: nat
  prop ~(m <= n & n < m)
  [hint_backward]

theorem Pre_inj
  fixes m :: nat, n :: nat
  prop 0 < m --> 0 < n --> Pre m = Pre n <--> m = n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] 0 < m
    #[2] 0 < n
    #[3] Pre m = Pre n ⟷ m = n
  ← rewrite_goal le_antisym sym=true goal=3
    #[4] Pre m ≤ Pre n ∧ Pre n ≤ Pre m ⟷ m ≤ n ∧ n ≤ m
  -> apply_forward_step le_Pre param_m=n goal=4 facts=[1]
    #[5] Pre n ≤ Pre m ⟷ n ≤ m
  -> apply_forward_step le_Pre param_m=m goal=4 facts=[2]
    #[6] Pre m ≤ Pre n ⟷ m ≤ n
  ← apply_backward_step iffI goal=4
    #[7] Pre m ≤ Pre n ∧ Pre n ≤ Pre m ⟶ m ≤ n ∧ n ≤ m
    #[8] m ≤ n ∧ n ≤ m ⟶ Pre m ≤ Pre n ∧ Pre n ≤ Pre m
  ← introduction goal=7
    #[9] Pre m ≤ Pre n ∧ Pre n ≤ Pre m
    #[10] m ≤ n ∧ n ≤ m
  -> apply_forward_step conjD1 goal=10 facts=[9]
    #[11] Pre m ≤ Pre n
  -> apply_forward_step conjD2 goal=10 facts=[9]
    #[12] Pre n ≤ Pre m
  ← apply_backward_step conjI goal=10
    #[13] m ≤ n
    #[14] n ≤ m
  -> rewrite_fact_with_prev goal=13 facts=[6,11]
    #[15] m ≤ n
  -> rewrite_fact_with_prev goal=14 facts=[5,12]
    #[16] n ≤ m
  ← introduction goal=8
    #[17] m ≤ n ∧ n ≤ m
    #[18] Pre m ≤ Pre n ∧ Pre n ≤ Pre m
  -> apply_forward_step conjD1 goal=18 facts=[17]
    #[19] m ≤ n
  -> apply_forward_step conjD2 goal=18 facts=[17]
    #[20] n ≤ m
  ← apply_backward_step conjI goal=18
    #[21] Pre m ≤ Pre n
    #[22] Pre n ≤ Pre m
  ← rewrite_goal_with_prev goal=21 facts=[6]
  ← rewrite_goal_with_prev goal=22 facts=[5]
qed

theorem less_eq_trans
  fixes k :: nat, m :: nat, n :: nat
  prop k <= m --> m <= n --> k <= n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] k ≤ m
    #[2] m ≤ n
    #[3] k ≤ n
  -> rewrite_fact less_eq_exist goal=3 facts=[1]
    #[4] ∃p. m = k + p
  -> rewrite_fact less_eq_exist goal=3 facts=[2]
    #[5] ∃p. n = m + p
  exists_elim p goal=3 facts=[4]
    #[6] _VAR p
    #[7] m = k + p
    #[8] k ≤ n
  exists_elim q goal=8 facts=[5]
    #[9] _VAR q
    #[10] n = m + q
    #[11] k ≤ n
  ← rewrite_goal less_eq_exist goal=11
    #[12] ∃p. n = k + p
  ← inst_exists_goal "p + q" goal=12
    #[13] n = k + (p + q)
  ← rewrite_goal_with_prev goal=13 facts=[10]
    #[14] m + q = k + (p + q)
  ← rewrite_goal_with_prev goal=14 facts=[7]
    #[15] k + p + q = k + (p + q)
  nat_norm goal=15
    #[16] k + p + q = k + (p + q)
qed

theorem lt_trans
  fixes k :: nat, m :: nat, n :: nat
  prop k < m --> m < n --> k < n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] k < m
    #[2] m < n
    #[3] k < n
  revert_intro goal=3 facts=[2]
    #[4] m < n ⟶ k < n
  ← induction n nat_induct goal=4
    #[5] m < 0 ⟶ k < 0
    #[6] ∀n. (m < n ⟶ k < n) ⟶ m < Suc n ⟶ k < Suc n
  ← introduction goal=5
    #[7] m < 0
    #[8] k < 0
  -> rewrite_fact nat_less_def_1 sym=false goal=8 facts=[7]
    #[9] false
  ← apply_resolve_step not_false_res goal=8 facts=[9]
    #[10] k < 0
  ← introduction n goal=6
    #[11] _VAR n
    #[12] m < n ⟶ k < n
    #[13] m < Suc n
    #[14] k < Suc n
  ← rewrite_goal less_Suc_lesseq sym=false goal=14
    #[15] k ≤ n
  -> rewrite_fact nat_less_def_2 sym=false goal=15 facts=[13]
    #[16] m = n ∨ m < n
  ← apply_backward_step disjE goal=15 facts=[16]
    #[17] m = n ⟶ k ≤ n
    #[18] m < n ⟶ k ≤ n
  ← introduction goal=17
    #[19] m = n
    #[20] k ≤ n
  -> rewrite_fact_with_prev goal=20 facts=[19,1]
    #[21] k < n
  ← apply_backward_step lt_imp_le goal=20
  ← introduction goal=18
    #[22] k ≤ n
  -> apply_fact goal=22 facts=[12,2]
    #[23] k < n
  ← apply_backward_step lt_imp_le goal=22
qed

theorem let_trans
  fixes k :: nat, m :: nat, n :: nat
  prop k <= m --> m < n --> k < n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] k ≤ m
    #[2] m < n
    #[3] k < n
  revert_intro goal=3 facts=[2]
    #[4] m < n ⟶ k < n
  ← induction n nat_induct goal=4
    #[5] m < 0 ⟶ k < 0
    #[6] ∀n. (m < n ⟶ k < n) ⟶ m < Suc n ⟶ k < Suc n
  ← rewrite_goal nat_less_def_1 sym=false goal=5
    #[7] false ⟶ false
  ← introduction n goal=6
    #[8] _VAR n
    #[9] m < n ⟶ k < n
    #[10] m < Suc n
    #[11] k < Suc n
  ← rewrite_goal less_Suc_lesseq sym=false goal=11
    #[12] k ≤ n
  -> rewrite_fact less_Suc_lesseq sym=false goal=12 facts=[10]
    #[13] m ≤ n
  ← apply_backward_step less_eq_trans goal=12 facts=[1,13]
    #[14] k ≤ n
qed

theorem lte_trans
  fixes k :: nat, m :: nat, n :: nat
  prop k < m --> m <= n --> k < n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] k < m
    #[2] m ≤ n
    #[3] k < n
  revert_intro goal=3 facts=[2]
    #[4] m ≤ n ⟶ k < n
  ← induction n nat_induct goal=4
    #[5] m ≤ 0 ⟶ k < 0
    #[6] ∀n. (m ≤ n ⟶ k < n) ⟶ m ≤ Suc n ⟶ k < Suc n
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=5
    #[7] m = 0 ⟶ k < 0
  ← introduction goal=7
    #[8] m = 0
    #[9] k < 0
  -> rewrite_fact_with_prev goal=9 facts=[8,1]
  ← introduction n goal=6
    #[10] _VAR n
    #[11] m ≤ n ⟶ k < n
    #[12] m ≤ Suc n
    #[13] k < Suc n
  -> rewrite_fact nat_less_eq_def_2 sym=false goal=13 facts=[12]
    #[14] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjE goal=13 facts=[14]
    #[15] m = Suc n ⟶ k < Suc n
    #[16] m ≤ n ⟶ k < Suc n
  ← introduction goal=15
    #[17] m = Suc n
    #[18] k < Suc n
  -> rewrite_fact_with_prev goal=18 facts=[17,1]
    #[19] k < Suc n
  ← introduction goal=16
    #[20] k < Suc n
  -> apply_fact goal=20 facts=[11,2]
    #[21] k < n
  ← rewrite_goal less_Suc_lesseq sym=false goal=20
    #[22] k ≤ n
  ← apply_backward_step lt_imp_le goal=22 facts=[21]
    #[23] k ≤ n
qed

theorem le_lt
  fixes m :: nat, n :: nat
  prop m <= n <--> m < n | m = n
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] m ≤ 0 ⟷ m < 0 ∨ m = 0
    #[2] ∀n. m ≤ n ⟷ m < n ∨ m = n ⟶ m ≤ Suc n ⟷ m < Suc n ∨ m = Suc n
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=1
    #[3] m = 0 ⟷ m < 0 ∨ m = 0
  ← apply_backward_step iffI goal=3
    #[4] m = 0 ⟶ m < 0 ∨ m = 0
    #[5] m < 0 ∨ m = 0 ⟶ m = 0
  ← introduction goal=4
    #[6] m = 0
    #[7] m < 0 ∨ m = 0
  ← apply_backward_step disjI2 goal=7
  ← introduction goal=5
    #[8] m < 0 ∨ m = 0
    #[9] m = 0
  ← apply_backward_step disjE goal=9 facts=[8]
    #[10] m < 0 ⟶ m = 0
    #[11] m = 0 ⟶ m = 0
  ← introduction goal=10
    #[12] m < 0
    #[13] m = 0
  -> rewrite_fact nat_less_def_1 sym=false goal=13 facts=[12]
    #[14] false
  ← apply_resolve_step not_false_res goal=13 facts=[14]
    #[15] m = 0
  ← introduction n goal=2
    #[16] _VAR n
    #[17] m ≤ n ⟷ m < n ∨ m = n
    #[18] m ≤ Suc n ⟷ m < Suc n ∨ m = Suc n
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=18
    #[19] m = Suc n ∨ m ≤ n ⟷ m < Suc n ∨ m = Suc n
  ← rewrite_goal_with_prev goal=19 facts=[17]
    #[20] m = Suc n ∨ m < n ∨ m = n ⟷ m < Suc n ∨ m = Suc n
  ← apply_backward_step iffI goal=20
    #[21] m = Suc n ∨ m < n ∨ m = n ⟶ m < Suc n ∨ m = Suc n
    #[22] m < Suc n ∨ m = Suc n ⟶ m = Suc n ∨ m < n ∨ m = n
  ← introduction goal=21
    #[23] m = Suc n ∨ m < n ∨ m = n
    #[24] m < Suc n ∨ m = Suc n
  ← apply_backward_step disjE goal=24 facts=[23]
    #[25] m = Suc n ⟶ m < Suc n ∨ m = Suc n
    #[26] m < n ∨ m = n ⟶ m < Suc n ∨ m = Suc n
  ← introduction goal=25
    #[27] m = Suc n
    #[28] m < Suc n ∨ m = Suc n
  ← apply_backward_step disjI2 goal=28
  ← introduction goal=26
    #[29] m < n ∨ m = n
    #[30] m < Suc n ∨ m = Suc n
  ← apply_backward_step disjI1 goal=30
    #[31] m < Suc n
  ← rewrite_goal nat_less_def_2 sym=false goal=31
    #[32] m = n ∨ m < n
  ← rewrite_goal disj_comm sym=false goal=32
  ← introduction goal=22
    #[33] m < Suc n ∨ m = Suc n
    #[34] m = Suc n ∨ m < n ∨ m = n
  ← apply_backward_step disjE goal=34 facts=[33]
    #[35] m < Suc n ⟶ m = Suc n ∨ m < n ∨ m = n
    #[36] m = Suc n ⟶ m = Suc n ∨ m < n ∨ m = n
  ← introduction goal=35
    #[37] m < Suc n
    #[38] m = Suc n ∨ m < n ∨ m = n
  ← apply_backward_step disjI2 goal=38
    #[39] m < n ∨ m = n
  -> rewrite_fact nat_less_def_2 sym=false goal=39 facts=[37]
    #[40] m = n ∨ m < n
  ← rewrite_goal disj_comm sym=false goal=39
  ← introduction goal=36
    #[41] m = Suc n ∨ m < n ∨ m = n
  ← apply_backward_step disjI1 goal=41
qed

theorem not_le
  fixes m :: nat, n :: nat
  prop ~(m <= n) <--> n < m
  [hint_rewrite]

theorem not_lt
  fixes m :: nat, n :: nat
  prop ~(m < n) <--> n <= m
  [hint_rewrite]
proof
  ← rewrite_goal not_le sym=true goal=0
    #[1] ¬¬(n ≤ m) ⟷ n ≤ m
  ← rewrite_goal double_neg sym=false goal=1
qed

theorem le_add
  fixes m :: nat, n :: nat
  prop m <= m + n
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] m ≤ m + 0
    #[2] ∀n. m ≤ m + n ⟶ m ≤ m + Suc n
  ← rewrite_goal add_0_right sym=false goal=1
    #[3] m ≤ m
  ← apply_backward_step less_eq_refl goal=3
  ← introduction n goal=2
    #[4] _VAR n
    #[5] m ≤ m + n
    #[6] m ≤ m + Suc n
  ← rewrite_goal add_comm sym=false goal=6
    #[7] m ≤ Suc n + m
  ← rewrite_goal nat_plus_def_2 sym=false goal=7
    #[8] m ≤ Suc (n + m)
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=8
    #[9] m = Suc (n + m) ∨ m ≤ n + m
  ← apply_backward_step disjI2 goal=9
    #[10] m ≤ n + m
  ← rewrite_goal add_comm sym=false goal=10
qed

theorem lt_add
  fixes m :: nat, n :: nat
  prop m < m + n <--> 0 < n
  [hint_rewrite,hint_rewrite_sym]
proof
  ← induction m nat_induct goal=0
    #[1] 0 < 0 + n ⟷ 0 < n
    #[2] ∀n1. n1 < n1 + n ⟷ 0 < n ⟶ Suc n1 < Suc n1 + n ⟷ 0 < n
  ← rewrite_goal nat_plus_def_1 sym=false goal=1
  ← introduction m goal=2
    #[3] _VAR m
    #[4] m < m + n ⟷ 0 < n
    #[5] Suc m < Suc m + n ⟷ 0 < n
  ← rewrite_goal nat_plus_def_2 sym=false goal=5
    #[6] Suc m < Suc (m + n) ⟷ 0 < n
  -> rewrite_fact eq_sym_eq sym=false goal=6 facts=[4]
    #[7] 0 < n ⟷ m < m + n
  ← rewrite_goal_with_prev goal=6 facts=[7]
    #[8] Suc m < Suc (m + n) ⟷ m < m + n
  ← rewrite_goal less_Suc_lesseq sym=false goal=8
    #[9] Suc m ≤ m + n ⟷ m < m + n
  ← rewrite_goal lesseq_Suc_less sym=false goal=9
    #[10] Suc m ≤ m + n ⟷ m < m + n
qed

theorem le_add_lcancel
  fixes m :: nat, n :: nat, p :: nat
  prop m + n <= m + p <--> n <= p
  [hint_rewrite]
proof
  ← rewrite_goal less_eq_exist sym=false goal=0
    #[1] (∃p1. m + p = m + n + p1) ⟷ (∃p1. p = n + p1)
  ← apply_backward_step iffI goal=1
    #[2] (∃p1. m + p = m + n + p1) ⟶ (∃p1. p = n + p1)
    #[3] (∃p1. p = n + p1) ⟶ (∃p1. m + p = m + n + p1)
  ← introduction goal=2
    #[4] ∃p1. m + p = m + n + p1
    #[5] ∃p1. p = n + p1
  exists_elim d goal=5 facts=[4]
    #[6] _VAR d
    #[7] m + p = m + n + d
    #[8] ∃p1. p = n + p1
  ← inst_exists_goal d goal=8
    #[9] p = n + d
  -> rewrite_fact add_assoc sym=false goal=9 facts=[7]
    #[10] m + p = m + (n + d)
  ← apply_backward_step add_cancel_left goal=9 facts=[10]
    #[11] p = n + d
  ← introduction goal=3
    #[12] ∃p1. p = n + p1
    #[13] ∃p1. m + p = m + n + p1
  exists_elim d goal=13 facts=[12]
    #[14] p = n + d
    #[15] ∃p1. m + p = m + n + p1
  ← inst_exists_goal d goal=15
    #[16] m + p = m + n + d
  ← rewrite_goal_with_prev goal=16 facts=[14]
    #[17] m + (n + d) = m + n + d
  ← rewrite_goal add_assoc sym=false goal=17
    #[18] m + (n + d) = m + n + d
qed

theorem lt_add_lcancel
  fixes m :: nat, n :: nat, p :: nat
  prop m + n < m + p <--> n < p
  [hint_rewrite]
proof
  ← rewrite_goal less_exist sym=false goal=0
    #[1] (∃d. m + p = m + n + Suc d) ⟷ (∃d. p = n + Suc d)
  ← apply_backward_step iffI goal=1
    #[2] (∃d. m + p = m + n + Suc d) ⟶ (∃d. p = n + Suc d)
    #[3] (∃d. p = n + Suc d) ⟶ (∃d. m + p = m + n + Suc d)
  ← introduction goal=2
    #[4] ∃d. m + p = m + n + Suc d
    #[5] ∃d. p = n + Suc d
  exists_elim d goal=5 facts=[4]
    #[6] _VAR d
    #[7] m + p = m + n + Suc d
    #[8] ∃d. p = n + Suc d
  ← inst_exists_goal d goal=8
    #[9] p = n + Suc d
  -> rewrite_fact add_assoc sym=false goal=9 facts=[7]
    #[10] m + p = m + (n + Suc d)
  ← apply_backward_step add_cancel_left goal=9 facts=[10]
    #[11] p = n + Suc d
  ← introduction goal=3
    #[12] ∃d. p = n + Suc d
    #[13] ∃d. m + p = m + n + Suc d
  exists_elim d goal=13 facts=[12]
    #[14] p = n + Suc d
    #[15] ∃d. m + p = m + n + Suc d
  ← inst_exists_goal d goal=15
    #[16] m + p = m + n + Suc d
  ← rewrite_goal_with_prev goal=16 facts=[14]
    #[17] m + (n + Suc d) = m + n + Suc d
  ← rewrite_goal add_assoc sym=false goal=17
    #[18] m + (n + Suc d) = m + n + Suc d
qed

theorem le_add2
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop m <= p --> n <= q --> m + n <= p + q
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m ≤ p
    #[2] n ≤ q
    #[3] m + n ≤ p + q
  -> rewrite_fact less_eq_exist sym=false goal=3 facts=[1]
    #[4] ∃p1. p = m + p1
  -> rewrite_fact less_eq_exist sym=false goal=3 facts=[2]
    #[5] ∃p. q = n + p
  exists_elim a goal=3 facts=[4]
    #[6] _VAR a
    #[7] p = m + a
    #[8] m + n ≤ p + q
  exists_elim b goal=8 facts=[5]
    #[9] _VAR b
    #[10] q = n + b
    #[11] m + n ≤ p + q
  ← rewrite_goal less_eq_exist sym=false goal=11
    #[12] ∃p1. p + q = m + n + p1
  ← inst_exists_goal a+b goal=12
    #[13] p + q = m + n + (a + b)
  ← rewrite_goal_with_prev goal=13 facts=[7]
    #[14] m + a + q = m + n + (a + b)
  ← rewrite_goal_with_prev goal=14 facts=[10]
    #[15] m + a + (n + b) = m + n + (a + b)
  nat_norm goal=15
    #[16] m + a + (n + b) = m + n + (a + b)
qed

theorem let_add2
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop m <= p --> n < q --> m + n < p + q
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m ≤ p
    #[2] n < q
    #[3] m + n < p + q
  -> rewrite_fact less_eq_exist sym=false goal=3 facts=[1]
    #[4] ∃p1. p = m + p1
  -> rewrite_fact less_exist sym=false goal=3 facts=[2]
    #[5] ∃d. q = n + Suc d
  exists_elim a goal=3 facts=[4]
    #[6] _VAR a
    #[7] p = m + a
    #[8] m + n < p + q
  exists_elim b goal=8 facts=[5]
    #[9] _VAR b
    #[10] q = n + Suc b
    #[11] m + n < p + q
  ← rewrite_goal less_exist sym=false goal=11
    #[12] ∃d. p + q = m + n + Suc d
  ← inst_exists_goal a+b goal=12
    #[13] p + q = m + n + Suc (a + b)
  ← rewrite_goal_with_prev goal=13 facts=[7]
    #[14] m + a + q = m + n + Suc (a + b)
  ← rewrite_goal_with_prev goal=14 facts=[10]
    #[15] m + a + (n + Suc b) = m + n + Suc (a + b)
  nat_norm goal=15
    #[16] m + a + (n + Suc b) = m + n + Suc (a + b)
qed

theorem lt_add2
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop m < p --> n < q --> m + n < p + q
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m < p
    #[2] n < q
    #[3] m + n < p + q
  ← apply_backward_step let_add2 goal=3
    #[4] m ≤ p
  ← apply_backward_step lt_imp_le goal=4
qed

theorem le_cases
  fixes m :: nat, n :: nat
  prop m <= n | n <= m
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] m ≤ 0 ∨ 0 ≤ m
    #[2] ∀n. m ≤ n ∨ n ≤ m ⟶ m ≤ Suc n ∨ Suc n ≤ m
  ← apply_backward_step disjI2 goal=1
    #[3] 0 ≤ m
  ← apply_backward_step le_0 goal=3
  ← induction m nat_induct goal=2
    #[4] ∀n. 0 ≤ n ∨ n ≤ 0 ⟶ 0 ≤ Suc n ∨ Suc n ≤ 0
    #[5] ∀n. (∀n1. n ≤ n1 ∨ n1 ≤ n ⟶ n ≤ Suc n1 ∨ Suc n1 ≤ n) ⟶ (∀n1. Suc n ≤ n1 ∨ n1 ≤ Suc n ⟶ Suc n ≤ Suc n1 ∨ Suc n1 ≤ Suc n)
  ← introduction n goal=4
    #[6] _VAR n
    #[7] 0 ≤ n ∨ n ≤ 0
    #[8] 0 ≤ Suc n ∨ Suc n ≤ 0
  ← apply_backward_step disjI1 goal=8
    #[9] 0 ≤ Suc n
  ← apply_backward_step le_0 goal=9
    #[10] 0 ≤ Suc n
  ← introduction n goal=5
    #[11] ∀n1. n ≤ n1 ∨ n1 ≤ n ⟶ n ≤ Suc n1 ∨ Suc n1 ≤ n
    #[12] ∀n1. Suc n ≤ n1 ∨ n1 ≤ Suc n ⟶ Suc n ≤ Suc n1 ∨ Suc n1 ≤ Suc n
  ← introduction m goal=12
    #[13] _VAR m
    #[14] Suc n ≤ m ∨ m ≤ Suc n
    #[15] Suc n ≤ Suc m ∨ Suc m ≤ Suc n
  ← apply_backward_step disjE goal=15 facts=[14]
    #[16] Suc n ≤ m ⟶ Suc n ≤ Suc m ∨ Suc m ≤ Suc n
    #[17] m ≤ Suc n ⟶ Suc n ≤ Suc m ∨ Suc m ≤ Suc n
  ← introduction goal=16
    #[18] Suc n ≤ m
    #[19] Suc n ≤ Suc m ∨ Suc m ≤ Suc n
  ← apply_backward_step disjI1 goal=19
    #[20] Suc n ≤ Suc m
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=20
    #[21] Suc n = Suc m ∨ Suc n ≤ m
  ← apply_backward_step disjI2 goal=21
  ← introduction goal=17
    #[22] m ≤ Suc n
    #[23] Suc n ≤ Suc m ∨ Suc m ≤ Suc n
  -> rewrite_fact nat_less_eq_def_2 sym=false goal=23 facts=[22]
    #[24] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjE goal=23 facts=[24]
    #[25] m = Suc n ⟶ Suc n ≤ Suc m ∨ Suc m ≤ Suc n
    #[26] m ≤ n ⟶ Suc n ≤ Suc m ∨ Suc m ≤ Suc n
  ← introduction goal=25
    #[27] m = Suc n
    #[28] Suc n ≤ Suc m ∨ Suc m ≤ Suc n
  ← apply_backward_step disjI1 goal=28
    #[29] Suc n ≤ Suc m
  -> rewrite_fact eq_sym_eq sym=false goal=29 facts=[27]
    #[30] Suc n = m
  ← rewrite_goal_with_prev goal=29 facts=[30]
    #[31] m ≤ Suc m
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=31
    #[32] m = Suc m ∨ m ≤ m
  ← apply_backward_step disjI2 goal=32
    #[33] m ≤ m
  ← apply_backward_step less_eq_refl goal=33
    #[34] m ≤ m
  ← rewrite_goal le_suc sym=false goal=26
    #[35] m ≤ n ⟶ n ≤ m ∨ m ≤ n
  ← introduction goal=35
    #[36] m ≤ n
    #[37] n ≤ m ∨ m ≤ n
  ← apply_backward_step disjI2 goal=37
qed

theorem lt_cases
  fixes m :: nat, n :: nat
  prop m < n | n < m | m = n
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] m < 0 ∨ 0 < m ∨ m = 0
    #[2] ∀n. m < n ∨ n < m ∨ m = n ⟶ m < Suc n ∨ Suc n < m ∨ m = Suc n
  ← apply_backward_step disjI2 goal=1
    #[3] 0 < m ∨ m = 0
  ← induction m nat_induct goal=3
    #[4] (0::nat) < 0 ∨ (0::nat) = 0
    #[5] ∀n. 0 < n ∨ n = 0 ⟶ 0 < Suc n ∨ Suc n = 0
  ← apply_backward_step disjI2 goal=4
    #[6] (0::nat) = 0
  ← apply_backward_step eq_refl goal=6
  ← introduction m goal=5
    #[7] _VAR m
    #[8] 0 < m ∨ m = 0
    #[9] 0 < Suc m ∨ Suc m = 0
  ← rewrite_goal nat_less_def_2 sym=false goal=9
    #[10] (0 = m ∨ 0 < m) ∨ Suc m = 0
  ← apply_backward_step disjI1 goal=10
    #[11] 0 = m ∨ 0 < m
  ← apply_backward_step disjE goal=11 facts=[8]
    #[12] 0 < m ⟶ 0 = m ∨ 0 < m
    #[13] m = 0 ⟶ 0 = m ∨ 0 < m
  ← introduction goal=12
    #[14] 0 < m
    #[15] 0 = m ∨ 0 < m
  ← apply_backward_step disjI2 goal=15
  ← introduction goal=13
    #[16] m = 0
    #[17] 0 = m ∨ 0 < m
  ← apply_backward_step disjI1 goal=17
    #[18] 0 = m
  ← rewrite_goal eq_sym_eq sym=false goal=18
  ← introduction n goal=2
    #[19] _VAR n
    #[20] m < n ∨ n < m ∨ m = n
    #[21] m < Suc n ∨ Suc n < m ∨ m = Suc n
  ← rewrite_goal nat_less_def_2 sym=false goal=21
    #[22] (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← apply_backward_step disjE goal=22 facts=[20]
    #[23] m < n ⟶ (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
    #[24] n < m ∨ m = n ⟶ (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← introduction goal=23
    #[25] m < n
    #[26] (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← apply_backward_step disjI1 goal=26
    #[27] m = n ∨ m < n
  ← apply_backward_step disjI2 goal=27
  ← introduction goal=24
    #[28] n < m ∨ m = n
    #[29] (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← apply_backward_step disjE goal=29 facts=[28]
    #[30] n < m ⟶ (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
    #[31] m = n ⟶ (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← introduction goal=30
    #[32] n < m
    #[33] (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← apply_backward_step disjI2 goal=33
    #[34] Suc n < m ∨ m = Suc n
  ← apply_backward_step contrapositive goal=34 facts=[32]
    #[35] ¬(Suc n < m ∨ m = Suc n) ⟶ ¬(n < m)
  ← rewrite_goal de_morgan_thm2 sym=false goal=35
    #[36] ¬(Suc n < m) ∧ ¬(m = Suc n) ⟶ ¬(n < m)
  ← rewrite_goal not_lt sym=false goal=36
    #[37] m ≤ Suc n ∧ ¬(m = Suc n) ⟶ m ≤ n
  ← introduction goal=37
    #[38] m ≤ Suc n ∧ ¬(m = Suc n)
    #[39] m ≤ n
  -> apply_forward_step conjD1 goal=39 facts=[38]
    #[40] m ≤ Suc n
  -> rewrite_fact nat_less_eq_def_2 sym=false goal=39 facts=[40]
    #[41] m = Suc n ∨ m ≤ n
  -> apply_forward_step conjD2 goal=39 facts=[38]
    #[42] ¬(m = Suc n)
  ← apply_backward_step disjE goal=39 facts=[41]
    #[43] m = Suc n ⟶ m ≤ n
    #[44] m ≤ n ⟶ m ≤ n
  ← introduction goal=43
    #[45] m = Suc n
    #[46] m ≤ n
  ← apply_backward_step negE_gen goal=46 facts=[42,45]
    #[47] m ≤ n
  ← introduction goal=31
    #[48] m = n
    #[49] (m = n ∨ m < n) ∨ Suc n < m ∨ m = Suc n
  ← apply_backward_step disjI1 goal=49
    #[50] m = n ∨ m < n
  ← apply_backward_step disjI1 goal=50
qed

theorem let_cases
  fixes m :: nat, n :: nat
  prop m <= n | n < m
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] m ≤ 0 ∨ 0 < m
    #[2] ∀n. m ≤ n ∨ n < m ⟶ m ≤ Suc n ∨ Suc n < m
  ← induction m nat_induct goal=1
    #[3] (0::nat) ≤ 0 ∨ (0::nat) < 0
    #[4] ∀n. n ≤ 0 ∨ 0 < n ⟶ Suc n ≤ 0 ∨ 0 < Suc n
  ← apply_backward_step disjI1 goal=3
    #[5] (0::nat) ≤ 0
  ← apply_backward_step less_eq_refl goal=5
  ← introduction m goal=4
    #[6] _VAR m
    #[7] m ≤ 0 ∨ 0 < m
    #[8] Suc m ≤ 0 ∨ 0 < Suc m
  ← apply_backward_step disjI2 goal=8
    #[9] 0 < Suc m
  ← apply_backward_step le_1_1 goal=9
    #[10] ¬(Suc m = 0)
  ← apply_backward_step ineq_sym goal=10
    #[11] ¬(0 = Suc m)
  ← apply_backward_step nat_zero_Suc_neq goal=11
    #[12] ¬(0 = Suc m)
  ← introduction n goal=2
    #[13] _VAR n
    #[14] m ≤ n ∨ n < m
    #[15] m ≤ Suc n ∨ Suc n < m
  ← rewrite_goal nat_less_eq_def_2 sym=false goal=15
    #[16] (m = Suc n ∨ m ≤ n) ∨ Suc n < m
  ← apply_backward_step disjE goal=16 facts=[14]
    #[17] m ≤ n ⟶ (m = Suc n ∨ m ≤ n) ∨ Suc n < m
    #[18] n < m ⟶ (m = Suc n ∨ m ≤ n) ∨ Suc n < m
  ← introduction goal=17
    #[19] m ≤ n
    #[20] (m = Suc n ∨ m ≤ n) ∨ Suc n < m
  ← apply_backward_step disjI1 goal=20
    #[21] m = Suc n ∨ m ≤ n
  ← apply_backward_step disjI2 goal=21
  ← induction m nat_induct goal=18
    #[22] n < 0 ⟶ (0 = Suc n ∨ 0 ≤ n) ∨ Suc n < 0
    #[23] ∀n1. (n < n1 ⟶ (n1 = Suc n ∨ n1 ≤ n) ∨ Suc n < n1) ⟶ n < Suc n1 ⟶ (Suc n1 = Suc n ∨ Suc n1 ≤ n) ∨ Suc n < Suc n1
  ← introduction goal=22
    #[24] n < 0
    #[25] (0 = Suc n ∨ 0 ≤ n) ∨ Suc n < 0
  ← apply_backward_step contrapositive goal=25 facts=[24]
    #[26] ¬((0 = Suc n ∨ 0 ≤ n) ∨ Suc n < 0) ⟶ ¬(n < 0)
  ← introduction goal=26
    #[27] ¬((0 = Suc n ∨ 0 ≤ n) ∨ Suc n < 0)
    #[28] ¬(n < 0)
  ← rewrite_goal not_lt sym=false goal=28
    #[29] 0 ≤ n
  ← apply_backward_step le_0 goal=29
    #[30] 0 ≤ n
  ← introduction m1 goal=23
    #[31] _VAR m1
    #[32] n < m1 ⟶ (m1 = Suc n ∨ m1 ≤ n) ∨ Suc n < m1
    #[33] n < Suc m1
    #[34] (Suc m1 = Suc n ∨ Suc m1 ≤ n) ∨ Suc n < Suc m1
  -> rewrite_fact nat_less_def_2 sym=false goal=34 facts=[33]
    #[35] n = m1 ∨ n < m1
  ← apply_backward_step disjE goal=34 facts=[35]
    #[36] n = m1 ⟶ (Suc m1 = Suc n ∨ Suc m1 ≤ n) ∨ Suc n < Suc m1
    #[37] n < m1 ⟶ (Suc m1 = Suc n ∨ Suc m1 ≤ n) ∨ Suc n < Suc m1
  ← introduction goal=36
    #[38] n = m1
    #[39] (Suc m1 = Suc n ∨ Suc m1 ≤ n) ∨ Suc n < Suc m1
  ← apply_backward_step disjI1 goal=39
    #[40] Suc m1 = Suc n ∨ Suc m1 ≤ n
  ← apply_backward_step disjI1 goal=40
    #[41] Suc m1 = Suc n
  ← rewrite_goal_with_prev goal=41 facts=[38]
    #[42] Suc m1 = Suc n
  ← introduction goal=37
    #[43] n < m1
    #[44] (Suc m1 = Suc n ∨ Suc m1 ≤ n) ∨ Suc n < Suc m1
  ← apply_backward_step disjI2 goal=44
    #[45] Suc n < Suc m1
  ← rewrite_goal lt_suc sym=false goal=45
qed

theorem lt_mult
  fixes m :: nat, n :: nat
  prop 0 < m * n <--> 0 < m & 0 < n
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] 0 < m * 0 ⟷ 0 < m ∧ (0::nat) < 0
    #[2] ∀n. 0 < m * n ⟷ 0 < m ∧ 0 < n ⟶ 0 < m * Suc n ⟷ 0 < m ∧ 0 < Suc n
  ← rewrite_goal mult_0_right sym=false goal=1
    #[3] (0::nat) < 0 ⟷ 0 < m ∧ (0::nat) < 0
  ← apply_backward_step iffI goal=3
    #[4] (0::nat) < 0 ⟶ 0 < m ∧ (0::nat) < 0
    #[5] 0 < m ∧ (0::nat) < 0 ⟶ (0::nat) < 0
  ← introduction goal=4
    #[6] (0::nat) < 0
    #[7] 0 < m ∧ (0::nat) < 0
  -> rewrite_fact nat_less_def_1 sym=false goal=7 facts=[6]
    #[8] false
  ← apply_resolve_step not_false_res goal=7 facts=[8]
  ← introduction goal=5
    #[9] 0 < m ∧ (0::nat) < 0
    #[10] (0::nat) < 0
  ← apply_backward_step conjD2 goal=10 facts=[9]
  ← introduction n goal=2
    #[11] _VAR n
    #[12] 0 < m * n ⟷ 0 < m ∧ 0 < n
    #[13] 0 < m * Suc n ⟷ 0 < m ∧ 0 < Suc n
  ← apply_backward_step iffI goal=13
    #[14] 0 < m * Suc n ⟶ 0 < m ∧ 0 < Suc n
    #[15] 0 < m ∧ 0 < Suc n ⟶ 0 < m * Suc n
  ← introduction goal=14
    #[16] 0 < m * Suc n
    #[17] 0 < m ∧ 0 < Suc n
  ← apply_backward_step conjI goal=17
    #[18] 0 < m
    #[19] 0 < Suc n
  ← apply_backward_step le_1_1 goal=18
    #[20] ¬(m = 0)
  ← apply_backward_step contrapositive goal=20 facts=[16]
    #[21] ¬¬(m = 0) ⟶ ¬(0 < m * Suc n)
  ← introduction goal=21
    #[22] ¬¬(m = 0)
    #[23] ¬(0 < m * Suc n)
  -> rewrite_fact double_neg sym=false goal=23 facts=[22]
    #[24] m = 0
  ← rewrite_goal_with_prev goal=23 facts=[24]
    #[25] ¬(0 < 0 * Suc n)
  ← rewrite_goal nat_times_def_1 sym=false goal=25
    #[26] ¬((0::nat) < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=26
    #[27] ¬false
  ← apply_backward_step negI goal=27
    #[28] false ⟶ false
  ← rewrite_goal less_Suc_lesseq sym=false goal=19
    #[29] 0 ≤ n
  ← apply_backward_step le_0 goal=29
    #[30] 0 ≤ n
  ← introduction goal=15
    #[31] 0 < m ∧ 0 < Suc n
    #[32] 0 < m * Suc n
  ← apply_backward_step le_1_1 goal=32
    #[33] ¬(m * Suc n = 0)
  ← apply_backward_step contrapositive goal=33 facts=[31]
    #[34] ¬¬(m * Suc n = 0) ⟶ ¬(0 < m ∧ 0 < Suc n)
  ← introduction goal=34
    #[35] ¬¬(m * Suc n = 0)
    #[36] ¬(0 < m ∧ 0 < Suc n)
  -> rewrite_fact double_neg sym=false goal=36 facts=[35]
    #[37] m * Suc n = 0
  -> apply_forward_step mult_nonzero goal=36 facts=[37]
    #[38] m = 0 ∨ Suc n = 0
  ← rewrite_goal de_morgan_thm1 sym=false goal=36
    #[39] ¬(0 < m) ∨ ¬(0 < Suc n)
  ← apply_backward_step disjE goal=39 facts=[38]
    #[40] m = 0 ⟶ ¬(0 < m) ∨ ¬(0 < Suc n)
    #[41] Suc n = 0 ⟶ ¬(0 < m) ∨ ¬(0 < Suc n)
  ← introduction goal=40
    #[42] m = 0
    #[43] ¬(0 < m) ∨ ¬(0 < Suc n)
  ← apply_backward_step disjI1 goal=43
    #[44] ¬(0 < m)
  ← rewrite_goal_with_prev goal=44 facts=[42]
    #[45] ¬((0::nat) < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=45
    #[46] ¬false
  ← apply_backward_step negI goal=46
  ← introduction goal=41
    #[47] Suc n = 0
    #[48] ¬(0 < m) ∨ ¬(0 < Suc n)
  ← apply_backward_step disjI2 goal=48
    #[49] ¬(0 < Suc n)
  ← rewrite_goal_with_prev goal=49 facts=[47]
    #[50] ¬((0::nat) < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=50
    #[51] ¬false
  ← apply_backward_step negI goal=51
qed

theorem le_mult2
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop m <= n --> p <= q --> m * p <= n * q
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m ≤ n
    #[2] p ≤ q
    #[3] m * p ≤ n * q
  -> rewrite_fact less_eq_exist sym=false goal=3 facts=[1]
    #[4] ∃p. n = m + p
  -> rewrite_fact less_eq_exist sym=false goal=3 facts=[2]
    #[5] ∃p1. q = p + p1
  exists_elim a goal=3 facts=[4]
    #[6] _VAR a
    #[7] n = m + a
    #[8] m * p ≤ n * q
  exists_elim b goal=8 facts=[5]
    #[9] _VAR b
    #[10] q = p + b
    #[11] m * p ≤ n * q
  ← rewrite_goal less_eq_exist sym=false goal=11
    #[12] ∃p1. n * q = m * p + p1
  ← inst_exists_goal "a * p + m * b + a * b" goal=12
    #[13] n * q = m * p + (a * p + m * b + a * b)
  ← rewrite_goal_with_prev goal=13 facts=[7]
    #[14] (m + a) * q = m * p + (a * p + m * b + a * b)
  ← rewrite_goal_with_prev goal=14 facts=[10]
    #[15] (m + a) * (p + b) = m * p + (a * p + m * b + a * b)
  nat_norm goal=15
    #[16] (m + a) * (p + b) = m * p + (a * p + m * b + a * b)
qed

theorem le_mult_rcancel
  fixes m :: nat, n :: nat, p :: nat
  prop m * p <= n * p <--> m <= n | p = 0
  [hint_rewrite]
proof
  ← z3 goal=0
qed

theorem lt_mult_rcancel
  fixes m :: nat, n :: nat, p :: nat
  prop m * p < n * p <--> m < n & ~(p = 0)
  [hint_rewrite]
proof
  ← z3 goal=0
qed

theorem lt_mult2
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop m < n --> p < q --> m * p < n * q
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m < n
    #[2] p < q
    #[3] m * p < n * q
  ← apply_backward_step let_trans param_m="n * p" goal=3
    #[4] m * p ≤ n * p
    #[5] n * p < n * q
  ← rewrite_goal le_mult_rcancel sym=false goal=4
    #[6] m ≤ n ∨ p = 0
  ← apply_backward_step disjI1 goal=6
    #[7] m ≤ n
  ← apply_backward_step lt_imp_le goal=7
  ← rewrite_goal mult_comm sym=false goal=5
    #[8] p * n < q * n
  ← rewrite_goal lt_mult_rcancel sym=false goal=8
    #[9] p < q ∧ ¬(n = 0)
  ← apply_backward_step conjI goal=9
    #[10] ¬(n = 0)
  ← apply_backward_step contrapositive goal=10 facts=[1]
    #[11] ¬¬(n = 0) ⟶ ¬(m < n)
  ← introduction goal=11
    #[12] ¬¬(n = 0)
    #[13] ¬(m < n)
  -> rewrite_fact double_neg sym=false goal=13 facts=[12]
    #[14] n = 0
  ← rewrite_goal_with_prev goal=13 facts=[14]
    #[15] ¬(m < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=15
    #[16] ¬false
  ← apply_backward_step negI goal=16
    #[17] false ⟶ false
qed

theorem le_square_refl
  fixes n :: nat
  prop n <= n * n
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] (0::nat) ≤ 0 * 0
    #[2] ∀n. n ≤ n * n ⟶ Suc n ≤ Suc n * Suc n
  ← apply_backward_step le_0 goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] n ≤ n * n
    #[5] Suc n ≤ Suc n * Suc n
  ← rewrite_goal mult_Suc_right sym=false goal=5
    #[6] Suc n ≤ Suc n + Suc n * n
  ← apply_backward_step le_add goal=6
    #[7] Suc n ≤ Suc n + Suc n * n
qed

fun minus :: nat ⇒ nat ⇒ nat
  | n - 0 = n
  | n - Suc m = Pre (n - m)

theorem nat_minus_l0
  fixes x :: nat
  prop 0 - x = 0
  [hint_rewrite]
proof
  ← induction x nat_induct goal=0
    #[1] (0::nat) - 0 = 0
    #[2] ∀n. 0 - n = 0 ⟶ 0 - Suc n = 0
  ← rewrite_goal nat_minus_def_1 goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] 0 - n = 0
    #[5] 0 - Suc n = 0
  ← rewrite_goal nat_minus_def_2 goal=5
    #[6] Pre (0 - n) = 0
  ← rewrite_goal_with_prev goal=6 facts=[4]
    #[7] Pre 0 = 0
  ← rewrite_goal Pre_def_1 goal=7
    #[8] Pre 0 = 0
qed

theorem nat_minus_presuc
  fixes x :: nat, y :: nat
  prop Pre (Suc x - y) = x - y
proof
  ← induction y nat_induct goal=0
    #[1] Pre (Suc x - 0) = x - 0
    #[2] ∀n. Pre (Suc x - n) = x - n ⟶ Pre (Suc x - Suc n) = x - Suc n
  ← rewrite_goal nat_minus_def_1 goal=1
    #[3] Pre (Suc x) = x
  ← rewrite_goal Pre_def_2 goal=3
  ← introduction n goal=2
    #[4] _VAR n
    #[5] Pre (Suc x - n) = x - n
    #[6] Pre (Suc x - Suc n) = x - Suc n
  ← rewrite_goal nat_minus_def_2 goal=6
    #[7] Pre (Pre (Suc x - n)) = Pre (x - n)
  ← rewrite_goal_with_prev goal=7 facts=[5]
qed

theorem nat_minus_suc
  fixes x :: nat, y :: nat
  prop Suc x - Suc y = x - y
  [hint_rewrite]

theorem nat_minus_refl
  fixes x :: nat
  prop x - x = 0
  [hint_rewrite]
proof
  ← induction x nat_induct goal=0
    #[1] (0::nat) - 0 = 0
    #[2] ∀n. n - n = 0 ⟶ Suc n - Suc n = 0
  ← rewrite_goal nat_minus_def_1 goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] n - n = 0
    #[5] Suc n - Suc n = 0
  ← rewrite_goal nat_minus_suc goal=5
qed

theorem nat_plus_minus
  fixes x :: nat, y :: nat
  prop x + y - y = x
  [hint_rewrite]
proof
  ← induction y nat_induct goal=0
    #[1] x + 0 - 0 = x
    #[2] ∀n. x + n - n = x ⟶ x + Suc n - Suc n = x
  ← rewrite_goal add_0_right goal=1
    #[3] x - 0 = x
  ← rewrite_goal nat_minus_def_1 sym=false goal=3
  ← introduction n goal=2
    #[4] _VAR n
    #[5] x + n - n = x
    #[6] x + Suc n - Suc n = x
  ← rewrite_goal add_Suc_right sym="" goal=6
    #[7] Suc (x + n) - Suc n = x
  ← rewrite_goal nat_minus_suc sym="" goal=7
qed

theorem nat_plus_minus_2
  fixes x :: nat, y :: nat
  prop x + y - x = y
  [hint_rewrite]
proof
  ← rewrite_goal add_comm goal=0
    #[1] y + x - x = y
  ← rewrite_goal nat_plus_minus goal=1
qed

theorem sub_eq_0
  fixes x :: nat, y :: nat
  prop x - y = 0 <--> x <= y
  [hint_rewrite_sym,hint_rewrite]
proof
  ← induction y nat_induct goal=0
    #[1] x - 0 = 0 ⟷ x ≤ 0
    #[2] ∀n. x - n = 0 ⟷ x ≤ n ⟶ x - Suc n = 0 ⟷ x ≤ Suc n
  ← rewrite_goal nat_minus_def_1 sym=false goal=1
    #[3] x = 0 ⟷ x ≤ 0
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=3
  ← induction x nat_induct goal=2
    #[4] ∀n. 0 - n = 0 ⟷ 0 ≤ n ⟶ 0 - Suc n = 0 ⟷ 0 ≤ Suc n
    #[5] ∀n. (∀n1. n - n1 = 0 ⟷ n ≤ n1 ⟶ n - Suc n1 = 0 ⟷ n ≤ Suc n1) ⟶ (∀n1. Suc n - n1 = 0 ⟷ Suc n ≤ n1 ⟶ Suc n - Suc n1 = 0 ⟷ Suc n ≤ Suc n1)
  ← rewrite_goal nat_minus_l0 sym=false goal=4
    #[6] ∀n. (0::nat) = 0 ⟷ 0 ≤ n ⟶ (0::nat) = 0 ⟷ 0 ≤ Suc n
  ← introduction y goal=6
    #[7] _VAR y
    #[8] (0::nat) = 0 ⟷ 0 ≤ y
    #[9] (0::nat) = 0 ⟷ 0 ≤ Suc y
  ← apply_backward_step iffI goal=9
    #[10] (0::nat) = 0 ⟶ 0 ≤ Suc y
    #[11] 0 ≤ Suc y ⟶ (0::nat) = 0
  ← introduction goal=10
    #[12] (0::nat) = 0
    #[13] 0 ≤ Suc y
  ← apply_backward_step le_0 goal=13
    #[14] 0 ≤ Suc y
  ← introduction goal=11
    #[15] 0 ≤ Suc y
    #[16] (0::nat) = 0
  ← apply_backward_step eq_refl goal=16
    #[17] (0::nat) = 0
  ← introduction y goal=5
    #[18] ∀n. y - n = 0 ⟷ y ≤ n ⟶ y - Suc n = 0 ⟷ y ≤ Suc n
    #[19] ∀n. Suc y - n = 0 ⟷ Suc y ≤ n ⟶ Suc y - Suc n = 0 ⟷ Suc y ≤ Suc n
  ← introduction x goal=19
    #[20] _VAR x
    #[21] Suc y - x = 0 ⟷ Suc y ≤ x
    #[22] Suc y - Suc x = 0 ⟷ Suc y ≤ Suc x
  ← rewrite_goal nat_minus_suc sym=false goal=22
    #[23] y - x = 0 ⟷ Suc y ≤ Suc x
qed

theorem add_subr2
  fixes x :: nat, y :: nat
  prop x - (x + y) = 0
  [hint_rewrite]
proof
  ← rewrite_goal sub_eq_0 sym=false goal=0
    #[1] x ≤ x + y
  ← apply_backward_step le_add goal=1
qed

theorem add_subr
  fixes x :: nat, y :: nat
  prop y - (x + y) = 0
  [hint_rewrite]
proof
  ← rewrite_goal add_comm sym=false goal=0
    #[1] y - (y + x) = 0
  ← rewrite_goal add_subr2 sym=false goal=1
qed

theorem sub_add
  fixes x :: nat, y :: nat
  prop y <= x --> x - y + y = x
  [hint_backward,hint_forward]
proof
  ← introduction goal=0
    #[1] y ≤ x
    #[2] x - y + y = x
  -> rewrite_fact less_eq_exist sym=false goal=2 facts=[1]
    #[3] ∃p. x = y + p
  ← rewrite_goal add_comm sym=false goal=2
    #[4] y + (x - y) = x
  exists_elim p goal=4 facts=[3]
    #[5] _VAR p
    #[6] x = y + p
    #[7] y + (x - y) = x
    #[8] x - y + y = x
  ← rewrite_goal_with_prev goal=7 facts=[6]
    #[9] y + (y + p - y) = y + p
  ← rewrite_goal nat_plus_minus_2 sym=false goal=9
    #[10] y + (y + p - y) = y + p
qed

theorem left_sub_distrib
  fixes m :: nat, n :: nat, p :: nat
  prop m * (n - p) = m * n - m * p
  [hint_rewrite]
proof
  ← rewrite_goal eq_sym_eq sym=false goal=0
    #[1] m * n - m * p = m * (n - p)
  -> apply_forward_step le_cases param_m=n param_n=p goal=1
    #[2] n ≤ p ∨ p ≤ n
  ← apply_backward_step disjE goal=1 facts=[2]
    #[3] n ≤ p ⟶ m * n - m * p = m * (n - p)
    #[4] p ≤ n ⟶ m * n - m * p = m * (n - p)
  ← rewrite_goal sub_eq_0 sym=true goal=3
    #[5] n - p = 0 ⟶ m * n - m * p = m * (n - p)
  ← introduction goal=5
    #[6] n - p = 0
    #[7] m * n - m * p = m * (n - p)
  ← z3 goal=7
  ← z3 goal=4
qed

theorem suc_sub1
  fixes n :: nat
  prop Suc n - 1 = n
  [hint_rewrite]
proof
  ← rewrite_goal nat_one_def sym=false goal=0
    #[1] Suc n - Suc 0 = n
  ← rewrite_goal nat_minus_suc sym=false goal=1
    #[2] n - 0 = n
  ← rewrite_goal nat_minus_def_1 sym=false goal=2
qed

theorem nat_le_zero
  fixes n :: nat
  prop n <= 0 --> n = 0
  [hint_forward]
proof
  ← introduction goal=0
    #[1] n ≤ 0
    #[2] n = 0
  -> rewrite_fact less_eq_exist sym=false goal=2 facts=[1]
    #[3] ∃p. 0 = n + p
  exists_elim p goal=2 facts=[3]
    #[4] _VAR p
    #[5] 0 = n + p
    #[6] n = 0
  -> rewrite_fact eq_sym_eq sym=false goal=6 facts=[5]
    #[7] n + p = 0
  -> apply_forward_step add_is_zero goal=6 facts=[7]
    #[8] n = 0 ∧ p = 0
  ← apply_backward_step conjD1 goal=6 facts=[8]
    #[9] n = 0
qed

def max :: 'a ⇒ 'a ⇒ 'a = max m n = (if m ≤ n then n else m) [hint_rewrite]

def min :: 'a ⇒ 'a ⇒ 'a = min m n = (if m ≤ n then m else n) [hint_rewrite]

theorem min_simp1
  fixes m :: nat, n :: nat
  prop m <= n --> min m n = m
proof
  ← introduction goal=0
    #[1] m ≤ n
    #[2] min m n = m
  ← rewrite_goal min_def goal=2
    #[3] (if m ≤ n then m else n) = m
  ← rewrite_goal if_P sym=false goal=3 facts=[1]
qed

fun even :: nat ⇒ bool
  | even 0 ⟷ true
  | even (Suc n) ⟷ ¬(even n)

fun odd :: nat ⇒ bool
  | odd 0 ⟷ false
  | odd (Suc n) ⟷ ¬(odd n)

theorem not_even
  fixes n :: nat
  prop ~(even n) <--> odd n
  [hint_rewrite,hint_rewrite_sym]
proof
  ← induction n nat_induct goal=0
    #[1] ¬(even 0) ⟷ odd 0
    #[2] ∀n. ¬(even n) ⟷ odd n ⟶ ¬(even (Suc n)) ⟷ odd (Suc n)
  ← rewrite_goal even_def_1 sym=false goal=1
    #[3] ¬true ⟷ odd 0
  ← rewrite_goal odd_def_1 sym=false goal=3
    #[4] ¬true ⟷ false
  ← z3 goal=4
  ← introduction n goal=2
    #[5] _VAR n
    #[6] ¬(even n) ⟷ odd n
    #[7] ¬(even (Suc n)) ⟷ odd (Suc n)
  ← rewrite_goal even_def_2 sym=false goal=7
    #[8] ¬¬(even n) ⟷ odd (Suc n)
  ← rewrite_goal odd_def_2 sym=false goal=8
    #[9] ¬¬(even n) ⟷ ¬(odd n)
  ← rewrite_goal_with_prev goal=9 facts=[6]
qed

theorem not_odd
  fixes n :: nat
  prop ~(odd n) <--> even n
  [hint_rewrite,hint_rewrite_sym]
proof
  ← induction n nat_induct goal=0
    #[1] ¬(odd 0) ⟷ even 0
    #[2] ∀n. ¬(odd n) ⟷ even n ⟶ ¬(odd (Suc n)) ⟷ even (Suc n)
  ← rewrite_goal odd_def_1 sym=false goal=1
    #[3] ¬false ⟷ even 0
  ← rewrite_goal even_def_1 sym=false goal=3
    #[4] ¬false ⟷ true
  ← z3 goal=4
  ← introduction n goal=2
    #[5] _VAR n
    #[6] ¬(odd n) ⟷ even n
    #[7] ¬(odd (Suc n)) ⟷ even (Suc n)
  ← rewrite_goal even_def_2 sym=false goal=7
    #[8] ¬(odd (Suc n)) ⟷ ¬(even n)
  ← rewrite_goal odd_def_2 sym=false goal=8
    #[9] ¬¬(odd n) ⟷ ¬(even n)
  ← rewrite_goal_with_prev goal=9 facts=[6]
qed

theorem even_or_odd
  fixes n :: nat
  prop even n | odd n
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] even 0 ∨ odd 0
    #[2] ∀n. even n ∨ odd n ⟶ even (Suc n) ∨ odd (Suc n)
  ← apply_backward_step disjI1 goal=1
    #[3] even 0
  ← rewrite_goal even_def_1 sym=false goal=3
    #[4] true
  ← apply_backward_step trueI goal=4
  ← introduction n goal=2
    #[5] _VAR n
    #[6] even n ∨ odd n
    #[7] even (Suc n) ∨ odd (Suc n)
  ← apply_backward_step disjE goal=7 facts=[6]
    #[8] even n ⟶ even (Suc n) ∨ odd (Suc n)
    #[9] odd n ⟶ even (Suc n) ∨ odd (Suc n)
  ← introduction goal=8
    #[10] even n
    #[11] even (Suc n) ∨ odd (Suc n)
  ← apply_backward_step disjI2 goal=11
    #[12] odd (Suc n)
  ← rewrite_goal odd_def_2 sym=false goal=12
    #[13] ¬(odd n)
  ← rewrite_goal not_odd sym=false goal=13
  ← introduction goal=9
    #[14] odd n
    #[15] even (Suc n) ∨ odd (Suc n)
  ← apply_backward_step disjI1 goal=15
    #[16] even (Suc n)
  ← rewrite_goal even_def_2 sym=false goal=16
    #[17] ¬(even n)
  ← rewrite_goal not_even sym=false goal=17
qed

theorem even_and_odd
  fixes n :: nat
  prop ~(even n & odd n)
  [hint_backward]
proof
  ← rewrite_goal de_morgan_thm1 sym=false goal=0
    #[1] ¬(even n) ∨ ¬(odd n)
  ← rewrite_goal not_even sym=false goal=1
    #[2] odd n ∨ ¬(odd n)
  ← rewrite_goal not_odd sym=false goal=2
    #[3] odd n ∨ even n
  ← rewrite_goal disj_comm sym=false goal=3
    #[4] even n ∨ odd n
  ← apply_backward_step even_or_odd goal=4
qed

theorem even_add
  fixes m :: nat, n :: nat
  prop even (m + n) <--> even m <--> even n
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] even (m + 0) ⟷ even m ⟷ even 0
    #[2] ∀n. even (m + n) ⟷ even m ⟷ even n ⟶ even (m + Suc n) ⟷ even m ⟷ even (Suc n)
  ← rewrite_goal add_0_right sym=false goal=1
    #[3] even m ⟷ even m ⟷ even 0
  ← rewrite_goal even_def_1 sym=false goal=3
    #[4] even m ⟷ even m ⟷ true
  ← apply_backward_step iffI goal=4
    #[5] even m ⟶ even m ⟷ true
    #[6] even m ⟷ true ⟶ even m
  ← introduction goal=5
    #[7] even m
    #[8] even m ⟷ true
  ← apply_backward_step iffI goal=8
    #[9] even m ⟶ true
    #[10] true ⟶ even m
  ← introduction goal=9
    #[11] true
  ← apply_backward_step trueI goal=11
    #[12] true
  ← introduction goal=10
    #[13] true
  ← introduction goal=6
    #[14] even m ⟷ true
    #[15] even m
  ← rewrite_goal_with_prev goal=15 facts=[14]
    #[16] true
  ← apply_backward_step trueI goal=16
  ← introduction n goal=2
    #[17] _VAR n
    #[18] even (m + n) ⟷ even m ⟷ even n
    #[19] even (m + Suc n) ⟷ even m ⟷ even (Suc n)
  ← rewrite_goal add_Suc_right sym=false goal=19
    #[20] even (Suc (m + n)) ⟷ even m ⟷ even (Suc n)
  ← rewrite_goal even_def_2 sym=false goal=20
    #[21] ¬(even (m + n)) ⟷ even m ⟷ ¬(even n)
  ← rewrite_goal_with_prev goal=21 facts=[18]
    #[22] ¬(even m ⟷ even n) ⟷ even m ⟷ ¬(even n)
  ← apply_backward_step iffI goal=22
    #[23] ¬(even m ⟷ even n) ⟶ even m ⟷ ¬(even n)
    #[24] even m ⟷ ¬(even n) ⟶ ¬(even m ⟷ even n)
  ← introduction goal=23
    #[25] ¬(even m ⟷ even n)
    #[26] even m ⟷ ¬(even n)
  ← apply_backward_step iffI goal=26
    #[27] even m ⟶ ¬(even n)
    #[28] ¬(even n) ⟶ even m
  ← introduction goal=27
    #[29] ¬(even n)
  ← apply_backward_step contrapositive goal=29 facts=[25]
    #[30] ¬¬(even n) ⟶ ¬¬(even m ⟷ even n)
  ← rewrite_goal double_neg sym=false goal=30
    #[31] even n ⟶ even m ⟷ even n
  ← introduction goal=31
    #[32] even n
    #[33] even m ⟷ even n
  ← apply_backward_step iffI goal=33
    #[34] even m ⟶ even n
    #[35] even n ⟶ even m
  ← introduction goal=34
  ← introduction goal=35
  ← introduction goal=28
    #[36] ¬(even n)
    #[37] even m
  ← apply_backward_step contrapositive goal=37 facts=[25]
    #[38] ¬(even m) ⟶ ¬¬(even m ⟷ even n)
  ← rewrite_goal double_neg sym=false goal=38
    #[39] ¬(even m) ⟶ even m ⟷ even n
  ← introduction goal=39
    #[40] ¬(even m)
    #[41] even m ⟷ even n
  ← apply_backward_step iffI goal=41
    #[42] even m ⟶ even n
    #[43] even n ⟶ even m
  ← introduction goal=42
    #[44] even n
  ← apply_backward_step negE_gen goal=44 facts=[40,7]
    #[45] even n
  ← introduction goal=43
    #[46] even m
  ← apply_backward_step negE_gen goal=46 facts=[36,32]
    #[47] even m
  ← introduction goal=24
    #[48] even m ⟷ ¬(even n)
    #[49] ¬(even m ⟷ even n)
  ← rewrite_goal_with_prev goal=49 facts=[48]
    #[50] ¬(¬(even n) ⟷ even n)
  cut "!p. ~(~p <--> p)" goal=50
    #[51] ∀p. ¬(¬p ⟷ p)
  ← z3 goal=51
    #[52] ∀p. ¬(¬p ⟷ p)
  ← apply_prev goal=50 facts=[52]
    #[53] ¬(¬(even n) ⟷ even n)
qed

theorem even_mult
  fixes m :: nat, n :: nat
  prop even (m * n) <--> even m | even n
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] even (m * 0) ⟷ even m ∨ even 0
    #[2] ∀n. even (m * n) ⟷ even m ∨ even n ⟶ even (m * Suc n) ⟷ even m ∨ even (Suc n)
  ← rewrite_goal mult_0_right sym=false goal=1
    #[3] even 0 ⟷ even m ∨ even 0
  ← apply_backward_step iffI goal=3
    #[4] even 0 ⟶ even m ∨ even 0
    #[5] even m ∨ even 0 ⟶ even 0
  ← introduction goal=4
    #[6] even 0
    #[7] even m ∨ even 0
  ← apply_backward_step disjI2 goal=7
  ← rewrite_goal even_def_1 sym=false goal=5
    #[8] even m ∨ true ⟶ true
  cut "∀p. (p ∨ true) ⟷ true" goal=8
    #[9] ∀p. p ∨ true ⟷ true
  ← z3 goal=9
  ← rewrite_goal_with_prev goal=8 facts=[9]
    #[10] true ⟶ true
  ← introduction n goal=2
    #[11] _VAR n
    #[12] even (m * n) ⟷ even m ∨ even n
    #[13] even (m * Suc n) ⟷ even m ∨ even (Suc n)
  ← rewrite_goal mult_Suc_right sym=false goal=13
    #[14] even (m + m * n) ⟷ even m ∨ even (Suc n)
  ← rewrite_goal even_add sym=false goal=14
    #[15] (even m ⟷ even (m * n)) ⟷ even m ∨ even (Suc n)
  ← rewrite_goal_with_prev goal=15 facts=[12]
    #[16] (even m ⟷ even m ∨ even n) ⟷ even m ∨ even (Suc n)
  ← rewrite_goal even_def_2 sym=false goal=16
    #[17] (even m ⟷ even m ∨ even n) ⟷ even m ∨ ¬(even n)
  ← apply_backward_step iffI goal=17
    #[18] even m ⟷ even m ∨ even n ⟶ even m ∨ ¬(even n)
    #[19] even m ∨ ¬(even n) ⟶ even m ⟷ even m ∨ even n
  ← introduction goal=18
    #[20] even m ⟷ even m ∨ even n
    #[21] even m ∨ ¬(even n)
  -> apply_forward_step even_or_odd param_n=m goal=21
    #[22] even m ∨ odd m
  ← apply_backward_step disjE goal=21 facts=[22]
    #[23] even m ⟶ even m ∨ ¬(even n)
    #[24] odd m ⟶ even m ∨ ¬(even n)
  ← introduction goal=23
    #[25] even m
    #[26] even m ∨ ¬(even n)
  ← apply_backward_step disjI1 goal=26
  ← introduction goal=24
    #[27] odd m
    #[28] even m ∨ ¬(even n)
  ← apply_backward_step disjI2 goal=28
    #[29] ¬(even n)
  ← apply_backward_step contrapositive goal=29 facts=[20]
    #[30] ¬¬(even n) ⟶ ¬(even m ⟷ even m ∨ even n)
  ← rewrite_goal double_neg sym=false goal=30
    #[31] even n ⟶ ¬(even m ⟷ even m ∨ even n)
  ← introduction goal=31
    #[32] even n
    #[33] ¬(even m ⟷ even m ∨ even n)
  -> rewrite_fact not_even sym=true goal=33 facts=[27]
    #[34] ¬(even m)
  cut "!p. !q. p --> ~q --> ~(q <--> q | p)" goal=33
    #[35] ∀p. ∀q. p ⟶ ¬q ⟶ ¬(q ⟷ q ∨ p)
  ← z3 goal=35
    #[36] ∀p. ∀q. p ⟶ ¬q ⟶ ¬(q ⟷ q ∨ p)
  ← apply_prev goal=33 facts=[36]
  ← introduction goal=19
    #[37] even m ∨ ¬(even n)
    #[38] even m ⟷ even m ∨ even n
  ← apply_backward_step iffI goal=38
    #[39] even m ⟶ even m ∨ even n
    #[40] even m ∨ even n ⟶ even m
  ← introduction goal=39
    #[41] even m ∨ even n
  ← apply_backward_step disjI1 goal=41
  ← introduction goal=40
    #[42] even m ∨ even n
    #[43] even m
  ← apply_backward_step disjE goal=43 facts=[37]
    #[44] even m ⟶ even m
    #[45] ¬(even n) ⟶ even m
  ← apply_backward_step disjE goal=45 facts=[42]
    #[46] even m ⟶ ¬(even n) ⟶ even m
    #[47] even n ⟶ ¬(even n) ⟶ even m
  ← introduction goal=47
    #[48] ¬(even n)
    #[49] even m
  ← apply_backward_step negE_gen goal=49 facts=[48,32]
    #[50] even m
qed

theorem even_exp
  fixes m :: nat, n :: nat
  prop even (m ^ n) <--> even m & ~(n = 0)
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] even (m ^ (0::nat)) ⟷ even m ∧ ¬((0::nat) = 0)
    #[2] ∀n. even (m ^ n) ⟷ even m ∧ ¬(n = 0) ⟶ even (m ^ Suc n) ⟷ even m ∧ ¬(Suc n = 0)
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
    #[3] even 1 ⟷ even m ∧ ¬((0::nat) = 0)
  ← rewrite_goal nat_one_def sym=false goal=3
    #[4] even (Suc 0) ⟷ even m ∧ ¬((0::nat) = 0)
  ← rewrite_goal even_def_2 sym=false goal=4
    #[5] ¬(even 0) ⟷ even m ∧ ¬((0::nat) = 0)
  ← rewrite_goal even_def_1 sym=false goal=5
    #[6] ¬true ⟷ even m ∧ ¬((0::nat) = 0)
  cut "∀p. ∀q. q ⟶ ¬true ⟷ p ∧ ~q" goal=6
    #[7] ∀p. ∀q. q ⟶ ¬true ⟷ p ∧ ¬q
  ← z3 goal=7
  ← apply_prev goal=6 facts=[7]
    #[8] (0::nat) = 0
  ← apply_backward_step eq_refl goal=8
  ← introduction n goal=2
    #[9] _VAR n
    #[10] even (m ^ n) ⟷ even m ∧ ¬(n = 0)
    #[11] even (m ^ Suc n) ⟷ even m ∧ ¬(Suc n = 0)
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=11
    #[12] even (m * m ^ n) ⟷ even m ∧ ¬(Suc n = 0)
  ← rewrite_goal even_mult sym=false goal=12
    #[13] even m ∨ even (m ^ n) ⟷ even m ∧ ¬(Suc n = 0)
  ← rewrite_goal_with_prev goal=13 facts=[10]
    #[14] even m ∨ even m ∧ ¬(n = 0) ⟷ even m ∧ ¬(Suc n = 0)
  ← apply_backward_step iffI goal=14
    #[15] even m ∨ even m ∧ ¬(n = 0) ⟶ even m ∧ ¬(Suc n = 0)
    #[16] even m ∧ ¬(Suc n = 0) ⟶ even m ∨ even m ∧ ¬(n = 0)
  ← introduction goal=15
    #[17] even m ∨ even m ∧ ¬(n = 0)
    #[18] even m ∧ ¬(Suc n = 0)
  ← apply_backward_step conjI goal=18
    #[19] even m
    #[20] ¬(Suc n = 0)
  ← apply_backward_step disjE goal=19 facts=[17]
    #[21] even m ⟶ even m
    #[22] even m ∧ ¬(n = 0) ⟶ even m
  ← introduction goal=22
    #[23] even m ∧ ¬(n = 0)
    #[24] even m
  ← apply_backward_step conjD1 goal=24 facts=[23]
    #[25] even m
  ← rewrite_goal eq_sym_eq sym=false goal=20
    #[26] ¬(0 = Suc n)
  ← apply_backward_step nat_zero_Suc_neq goal=26
    #[27] ¬(0 = Suc n)
  ← introduction goal=16
    #[28] even m ∧ ¬(Suc n = 0)
    #[29] even m ∨ even m ∧ ¬(n = 0)
  -> apply_forward_step conjD1 goal=29 facts=[28]
    #[30] even m
  ← apply_backward_step disjI1 goal=29
qed

theorem odd_add
  fixes m :: nat, n :: nat
  prop odd (m + n) <--> ~(odd m <--> odd n)
  [hint_rewrite]
proof
  ← rewrite_goal not_even sym=true goal=0
    #[1] ¬(even (m + n)) ⟷ ¬(¬(even m) ⟷ ¬(even n))
  ← rewrite_goal even_add sym=false goal=1
    #[2] ¬(even m ⟷ even n) ⟷ ¬(¬(even m) ⟷ ¬(even n))
  cut "∀p. ∀q. (¬p ⟷ ¬q) ⟷ p ⟷ q" goal=2
    #[3] ∀p. ∀q. (¬p ⟷ ¬q) ⟷ p ⟷ q
  ← z3 goal=3
  ← rewrite_goal_with_prev goal=2 facts=[3]
    #[4] (even m ⟷ even n) ⟷ ¬(even m) ⟷ ¬(even n)
  ← rewrite_goal_with_prev goal=4 facts=[3]
qed

theorem odd_mult
  fixes m :: nat, n :: nat
  prop odd (m * n) <--> odd m & odd n
  [hint_rewrite]
proof
  ← rewrite_goal not_even sym=true goal=0
    #[1] ¬(even (m * n)) ⟷ ¬(even m) ∧ ¬(even n)
  ← rewrite_goal even_mult sym=false goal=1
    #[2] ¬(even m ∨ even n) ⟷ ¬(even m) ∧ ¬(even n)
  ← rewrite_goal de_morgan_thm2 sym=false goal=2
qed

theorem odd_exp
  fixes m :: nat, n :: nat
  prop odd (m ^ n) <--> odd m | n = 0
  [hint_rewrite]
proof
  ← rewrite_goal not_even sym=true goal=0
    #[1] ¬(even (m ^ n)) ⟷ ¬(even m) ∨ n = 0
  ← rewrite_goal even_exp sym=false goal=1
    #[2] ¬(even m ∧ ¬(n = 0)) ⟷ ¬(even m) ∨ n = 0
  ← rewrite_goal de_morgan_thm1 sym=false goal=2
    #[3] ¬(even m) ∨ ¬¬(n = 0) ⟷ ¬(even m) ∨ n = 0
  ← rewrite_goal double_neg sym=false goal=3
qed

theorem even_double
  fixes n :: nat
  prop even (2 * n)
  [hint_backward]
proof
  ← rewrite_goal mult_2 sym=false goal=0
    #[1] even (n + n)
  ← induction n nat_induct goal=1
    #[2] even (0 + 0)
    #[3] ∀n. even (n + n) ⟶ even (Suc n + Suc n)
  ← rewrite_goal add_0_right sym=false goal=2
    #[4] even 0
  ← rewrite_goal even_def_1 sym=false goal=4
    #[5] true
  ← apply_backward_step trueI goal=5
  ← introduction n goal=3
    #[6] _VAR n
    #[7] even (n + n)
    #[8] even (Suc n + Suc n)
  ← rewrite_goal add_Suc_right sym=false goal=8
    #[9] even (Suc (Suc n + n))
  ← rewrite_goal even_def_2 sym=false goal=9
    #[10] ¬(even (Suc n + n))
  ← rewrite_goal nat_plus_def_2 sym=false goal=10
    #[11] ¬(even (Suc (n + n)))
  ← rewrite_goal even_def_2 sym=false goal=11
    #[12] ¬¬(even (n + n))
  ← rewrite_goal double_neg sym=false goal=12
qed

theorem odd_double
  fixes n :: nat
  prop odd (Suc (2 * n))
  [hint_backward]
proof
  ← rewrite_goal not_even sym=true goal=0
    #[1] ¬(even (Suc (2 * n)))
  ← rewrite_goal even_def_2 sym=false goal=1
    #[2] ¬¬(even (2 * n))
  ← rewrite_goal double_neg sym=false goal=2
    #[3] even (2 * n)
  ← apply_backward_step even_double goal=3
qed

theorem even_exists_lemma
  fixes n :: nat
  prop (even n --> (?m. n = 2 * m)) & (~(even n) --> (?m. n = Suc (2 * m)))
  [hint_backward]

theorem even_exists
  fixes n :: nat
  prop even n <--> (?m. n = 2 * m)
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] even n ⟶ (∃m. n = 2 * m)
    #[2] (∃m. n = 2 * m) ⟶ even n
  -> apply_forward_step even_exists_lemma param_n=n goal=1
    #[3] (even n ⟶ (∃m. n = 2 * m)) ∧ (¬(even n) ⟶ (∃m. n = Suc (2 * m)))
  ← apply_backward_step conjD1 goal=1 facts=[3]
  ← introduction goal=2
    #[4] ∃m. n = 2 * m
    #[5] even n
  exists_elim m goal=5 facts=[4]
    #[6] _VAR m
    #[7] n = 2 * m
    #[8] even n
  ← rewrite_goal_with_prev goal=8 facts=[7]
    #[9] even (2 * m)
  ← apply_backward_step even_double goal=9
    #[10] even (2 * m)
qed

theorem odd_exists
  fixes n :: nat
  prop odd n <--> (?m. n = Suc (2 * m))
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] odd n ⟶ (∃m. n = Suc (2 * m))
    #[2] (∃m. n = Suc (2 * m)) ⟶ odd n
  -> apply_forward_step even_exists_lemma param_n=n goal=1
    #[3] (even n ⟶ (∃m. n = 2 * m)) ∧ (¬(even n) ⟶ (∃m. n = Suc (2 * m)))
  -> rewrite_fact not_even sym=false goal=1 facts=[3]
    #[4] (even n ⟶ (∃m. n = 2 * m)) ∧ (odd n ⟶ (∃m. n = Suc (2 * m)))
  ← apply_backward_step conjD2 goal=1 facts=[4]
  ← introduction goal=2
    #[5] ∃m. n = Suc (2 * m)
    #[6] odd n
  exists_elim m goal=6 facts=[5]
    #[7] _VAR m
    #[8] n = Suc (2 * m)
    #[9] odd n
  ← rewrite_goal_with_prev goal=9 facts=[8]
    #[10] odd (Suc (2 * m))
  ← apply_backward_step odd_double goal=10
    #[11] odd (Suc (2 * m))
qed

const nat_divide :: nat ⇒ nat ⇒ nat

const nat_modulus :: nat ⇒ nat ⇒ nat

fun fact :: nat ⇒ nat
  | fact 0 = 1
  | fact (Suc n) = Suc n * fact n

theorem fact_lt
  fixes n :: nat
  prop 0 < fact n
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] 0 < fact 0
    #[2] ∀n. 0 < fact n ⟶ 0 < fact (Suc n)
  ← rewrite_goal fact_def_1 sym=false goal=1
    #[3] (0::nat) < 1
  ← apply_backward_step le_1_1 goal=3
    #[4] ¬((1::nat) = 0)
  nat_const_ineq goal=4
  ← introduction n goal=2
    #[5] _VAR n
    #[6] 0 < fact n
    #[7] 0 < fact (Suc n)
  ← rewrite_goal fact_def_2 sym=false goal=7
    #[8] 0 < Suc n * fact n
  ← rewrite_goal lt_mult sym=false goal=8
    #[9] 0 < Suc n ∧ 0 < fact n
  ← apply_backward_step conjI goal=9
    #[10] 0 < Suc n
  ← rewrite_goal less_Suc_lesseq sym=false goal=10
    #[11] 0 ≤ n
  ← apply_backward_step le_0 goal=11
    #[12] 0 ≤ n
qed

theorem fact_le
  fixes n :: nat
  prop 1 <= fact n
  [hint_backward]
proof
  ← rewrite_goal nat_one_def sym=false goal=0
    #[1] Suc 0 ≤ fact n
  ← rewrite_goal lesseq_Suc_less sym=false goal=1
    #[2] 0 < fact n
  ← apply_backward_step fact_lt goal=2
qed

theorem fact_nz
  fixes n :: nat
  prop ~(fact n = 0)
  [hint_backward]
proof
  -> apply_forward_step fact_lt param_n=n goal=0
    #[1] 0 < fact n
  ← apply_backward_step contrapositive goal=0 facts=[1]
    #[2] ¬¬(fact n = 0) ⟶ ¬(0 < fact n)
  ← rewrite_goal double_neg sym=false goal=2
    #[3] fact n = 0 ⟶ ¬(0 < fact n)
  ← introduction goal=3
    #[4] fact n = 0
    #[5] ¬(0 < fact n)
  ← rewrite_goal_with_prev goal=5 facts=[4]
    #[6] ¬((0::nat) < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=6
    #[7] ¬false
  ← apply_backward_step negI goal=7
    #[8] false ⟶ false
qed

theorem fact_mono
  fixes m :: nat, n :: nat
  prop m <= n --> fact m <= fact n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] m ≤ n
    #[2] fact m ≤ fact n
  -> rewrite_fact less_eq_exist sym=false goal=2 facts=[1]
    #[3] ∃p. n = m + p
  exists_elim d goal=2 facts=[3]
    #[4] _VAR d
    #[5] n = m + d
    #[6] fact m ≤ fact n
  ← rewrite_goal_with_prev goal=6 facts=[5]
    #[7] fact m ≤ fact (m + d)
  ← induction d nat_induct goal=7
    #[8] fact m ≤ fact (m + 0)
    #[9] ∀n. fact m ≤ fact (m + n) ⟶ fact m ≤ fact (m + Suc n)
  ← rewrite_goal add_0_right sym=false goal=8
    #[10] fact m ≤ fact m
  ← apply_backward_step less_eq_refl goal=10
    #[11] fact m ≤ fact m
  ← introduction a goal=9
    #[12] _VAR a
    #[13] fact m ≤ fact (m + a)
    #[14] fact m ≤ fact (m + Suc a)
  ← rewrite_goal add_Suc_right sym=false goal=14
    #[15] fact m ≤ fact (Suc (m + a))
  ← rewrite_goal fact_def_2 sym=false goal=15
    #[16] fact m ≤ Suc (m + a) * fact (m + a)
  ← rewrite_goal add_1_right sym=true goal=16
    #[17] fact m ≤ (m + a + 1) * fact (m + a)
  ← rewrite_goal distrib_r sym=false goal=17
    #[18] fact m ≤ (m + a) * fact (m + a) + 1 * fact (m + a)
  ← rewrite_goal mult_1_left sym=false goal=18
    #[19] fact m ≤ (m + a) * fact (m + a) + fact (m + a)
  ← rewrite_goal add_comm sym=false goal=19
    #[20] fact m ≤ fact (m + a) + (m + a) * fact (m + a)
  ← apply_backward_step less_eq_trans param_m="fact (m + a)" goal=20
    #[21] fact (m + a) ≤ fact (m + a) + (m + a) * fact (m + a)
  ← apply_backward_step le_add goal=21
    #[22] fact (m + a) ≤ fact (m + a) + (m + a) * fact (m + a)
qed

theorem exp_eq_0
  fixes m :: nat, n :: nat
  prop m ^ n = 0 <--> m = 0 & ~(n = 0)
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] m ^ (0::nat) = 0 ⟷ m = 0 ∧ ¬((0::nat) = 0)
    #[2] ∀n. m ^ n = 0 ⟷ m = 0 ∧ ¬(n = 0) ⟶ m ^ Suc n = 0 ⟷ m = 0 ∧ ¬(Suc n = 0)
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
    #[3] (1::nat) = 0 ⟷ m = 0 ∧ ¬((0::nat) = 0)
  ← apply_backward_step iffI goal=3
    #[4] (1::nat) = 0 ⟶ m = 0 ∧ ¬((0::nat) = 0)
    #[5] m = 0 ∧ ¬((0::nat) = 0) ⟶ (1::nat) = 0
  ← introduction goal=4
    #[6] (1::nat) = 0
    #[7] m = 0 ∧ ¬((0::nat) = 0)
  ← apply_backward_step contrapositive goal=7 facts=[6]
    #[8] ¬(m = 0 ∧ ¬((0::nat) = 0)) ⟶ ¬((1::nat) = 0)
  ← introduction goal=8
    #[9] ¬(m = 0 ∧ ¬((0::nat) = 0))
    #[10] ¬((1::nat) = 0)
  nat_const_ineq goal=10
    #[11] ¬((1::nat) = 0)
  ← introduction goal=5
    #[12] m = 0 ∧ ¬((0::nat) = 0)
    #[13] (1::nat) = 0
  -> apply_forward_step conjD2 goal=13 facts=[12]
    #[14] ¬((0::nat) = 0)
  ← apply_backward_step contrapositive goal=13 facts=[14]
    #[15] ¬((1::nat) = 0) ⟶ ¬¬((0::nat) = 0)
  ← introduction goal=15
    #[16] ¬((1::nat) = 0)
    #[17] ¬¬((0::nat) = 0)
  ← rewrite_goal double_neg sym=false goal=17
    #[18] ¬¬((0::nat) = 0)
  ← introduction n goal=2
    #[19] _VAR n
    #[20] m ^ n = 0 ⟷ m = 0 ∧ ¬(n = 0)
    #[21] m ^ Suc n = 0 ⟷ m = 0 ∧ ¬(Suc n = 0)
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=21
    #[22] m * m ^ n = 0 ⟷ m = 0 ∧ ¬(Suc n = 0)
  ← apply_backward_step iffI goal=22
    #[23] m * m ^ n = 0 ⟶ m = 0 ∧ ¬(Suc n = 0)
    #[24] m = 0 ∧ ¬(Suc n = 0) ⟶ m * m ^ n = 0
  ← introduction goal=23
    #[25] m * m ^ n = 0
    #[26] m = 0 ∧ ¬(Suc n = 0)
  -> apply_forward_step mult_nonzero goal=26 facts=[25]
    #[27] m = 0 ∨ m ^ n = 0
  ← apply_backward_step disjE goal=26 facts=[27]
    #[28] m = 0 ⟶ m = 0 ∧ ¬(Suc n = 0)
    #[29] m ^ n = 0 ⟶ m = 0 ∧ ¬(Suc n = 0)
  ← introduction goal=28
    #[30] m = 0
    #[31] m = 0 ∧ ¬(Suc n = 0)
  ← apply_backward_step conjI goal=31
    #[32] ¬(Suc n = 0)
  ← rewrite_goal eq_sym_eq sym=false goal=32
    #[33] ¬(0 = Suc n)
  ← apply_backward_step nat_zero_Suc_neq goal=33
    #[34] ¬(0 = Suc n)
  ← introduction goal=29
    #[35] m ^ n = 0
    #[36] m = 0 ∧ ¬(Suc n = 0)
  -> rewrite_fact_with_prev goal=36 facts=[20,35]
    #[37] m = 0 ∧ ¬(n = 0)
  -> apply_forward_step conjD1 goal=36 facts=[37]
    #[38] m = 0
  ← apply_backward_step conjI goal=36
    #[39] ¬(Suc n = 0)
  ← apply_backward_step ineq_sym goal=39
    #[40] ¬(0 = Suc n)
  ← apply_backward_step nat_zero_Suc_neq goal=40
  ← introduction goal=24
    #[41] m = 0 ∧ ¬(Suc n = 0)
    #[42] m * m ^ n = 0
  -> apply_forward_step conjD1 goal=42 facts=[41]
    #[43] m = 0
  ← rewrite_goal_with_prev goal=42 facts=[43]
    #[44] (0::nat) * 0 ^ n = 0
  ← rewrite_goal nat_times_def_1 sym=false goal=44
    #[45] (0::nat) * 0 ^ n = 0
qed

theorem exp_eq_1
  fixes m :: nat, n :: nat
  prop m ^ n = 1 <--> m = 1 | n = 0
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] m ^ (0::nat) = 1 ⟷ m = 1 ∨ (0::nat) = 0
    #[2] ∀n. m ^ n = 1 ⟷ m = 1 ∨ n = 0 ⟶ m ^ Suc n = 1 ⟷ m = 1 ∨ Suc n = 0
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
    #[3] (1::nat) = 1 ⟷ m = 1 ∨ (0::nat) = 0
  ← apply_backward_step iffI goal=3
    #[4] (1::nat) = 1 ⟶ m = 1 ∨ (0::nat) = 0
    #[5] m = 1 ∨ (0::nat) = 0 ⟶ (1::nat) = 1
  ← introduction goal=4
    #[6] (1::nat) = 1
    #[7] m = 1 ∨ (0::nat) = 0
  ← apply_backward_step disjI2 goal=7
    #[8] (0::nat) = 0
  ← apply_backward_step eq_refl goal=8
    #[9] (0::nat) = 0
  ← introduction goal=5
    #[10] m = 1 ∨ (0::nat) = 0
    #[11] (1::nat) = 1
  ← apply_backward_step eq_refl goal=11
    #[12] (1::nat) = 1
  ← introduction n goal=2
    #[13] _VAR n
    #[14] m ^ n = 1 ⟷ m = 1 ∨ n = 0
    #[15] m ^ Suc n = 1 ⟷ m = 1 ∨ Suc n = 0
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=15
    #[16] m * m ^ n = 1 ⟷ m = 1 ∨ Suc n = 0
  ← apply_backward_step iffI goal=16
    #[17] m * m ^ n = 1 ⟶ m = 1 ∨ Suc n = 0
    #[18] m = 1 ∨ Suc n = 0 ⟶ m * m ^ n = 1
  ← introduction goal=17
    #[19] m * m ^ n = 1
    #[20] m = 1 ∨ Suc n = 0
  ← apply_backward_step disjI1 goal=20
    #[21] m = 1
  -> rewrite_fact mult_eq_1 sym=false goal=21 facts=[19]
    #[22] m = 1 ∧ m ^ n = 1
  ← apply_backward_step conjD1 goal=21 facts=[22]
    #[23] m = 1
  ← introduction goal=18
    #[24] m = 1 ∨ Suc n = 0
    #[25] m * m ^ n = 1
  ← apply_backward_step disjE goal=25 facts=[24]
    #[26] m = 1 ⟶ m * m ^ n = 1
    #[27] Suc n = 0 ⟶ m * m ^ n = 1
  ← introduction goal=26
    #[28] m = 1
    #[29] m * m ^ n = 1
  ← rewrite_goal_with_prev goal=29 facts=[28]
    #[30] (1::nat) * 1 ^ n = 1
  ← induction n nat_induct goal=30
    #[31] (1::nat) * 1 ^ (0::nat) = 1
    #[32] ∀n. (1::nat) * 1 ^ n = 1 ⟶ (1::nat) * 1 ^ Suc n = 1
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=31
    #[33] (1::nat) * 1 = 1
  ← rewrite_goal mult_1_left sym=false goal=33
    #[34] (1::nat) * 1 = 1
  ← introduction n1 goal=32
    #[35] _VAR n1
    #[36] (1::nat) * 1 ^ n1 = 1
    #[37] (1::nat) * 1 ^ Suc n1 = 1
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=37
    #[38] (1::nat) * (1 * 1 ^ n1) = 1
  ← rewrite_goal_with_prev goal=38 facts=[36]
  ← introduction goal=27
    #[39] Suc n = 0
    #[40] m * m ^ n = 1
  ← apply_backward_step contrapositive goal=40 facts=[39]
    #[41] ¬(m * m ^ n = 1) ⟶ ¬(Suc n = 0)
  ← introduction goal=41
    #[42] ¬(m * m ^ n = 1)
    #[43] ¬(Suc n = 0)
  ← apply_backward_step ineq_sym goal=43
    #[44] ¬(0 = Suc n)
  ← apply_backward_step nat_zero_Suc_neq goal=44
    #[45] ¬(0 = Suc n)
qed

theorem exp_zero
  fixes n :: nat
  prop (0::nat) ^ n = (if n = 0 then 1 else 0)
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ (0::nat) ^ n = (if n = 0 then 1 else 0)
    #[2] ¬(n = 0) ⟶ (0::nat) ^ n = (if n = 0 then 1 else 0)
  ← introduction goal=1
    #[3] n = 0
    #[4] (0::nat) ^ n = (if n = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=4 facts=[3]
    #[5] (0::nat) ^ n = 1
  ← rewrite_goal_with_prev goal=5 facts=[3]
    #[6] (0::nat) ^ (0::nat) = 1
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=6
    #[7] (0::nat) ^ (0::nat) = 1
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] (0::nat) ^ n = (if n = 0 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=9 facts=[8]
    #[10] (0::nat) ^ n = 0
  ← rewrite_goal exp_eq_0 sym=false goal=10
    #[11] (0::nat) = 0 ∧ ¬(n = 0)
  ← apply_backward_step conjI goal=11
    #[12] (0::nat) = 0
  ← apply_backward_step eq_refl goal=12
    #[13] (0::nat) = 0
qed

theorem nat_power_1
  fixes m :: nat
  prop m ^ (1::nat) = m
proof
  ← rewrite_goal nat_one_def sym=false goal=0
    #[1] m ^ Suc 0 = m
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=1
    #[2] m * m ^ (0::nat) = m
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=2
    #[3] m * 1 = m
  ← rewrite_goal mult_1_right sym=false goal=3
qed

theorem nat_power_add
  fixes m :: nat, p :: nat, q :: nat
  prop m ^ (p + q) = m ^ p * m ^ q
  [hint_rewrite]
proof
  ← induction q nat_induct goal=0
    #[1] m ^ (p + 0) = m ^ p * m ^ (0::nat)
    #[2] ∀n. m ^ (p + n) = m ^ p * m ^ n ⟶ m ^ (p + Suc n) = m ^ p * m ^ Suc n
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
    #[3] m ^ (p + 0) = m ^ p * 1
  ← rewrite_goal add_0_right sym=false goal=3
    #[4] m ^ p = m ^ p * 1
  ← rewrite_goal mult_1_right sym=false goal=4
  ← introduction n goal=2
    #[5] _VAR n
    #[6] m ^ (p + n) = m ^ p * m ^ n
    #[7] m ^ (p + Suc n) = m ^ p * m ^ Suc n
  ← rewrite_goal add_Suc_right sym=false goal=7
    #[8] m ^ Suc (p + n) = m ^ p * m ^ Suc n
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=8
    #[9] m * m ^ (p + n) = m ^ p * (m * m ^ n)
  ← rewrite_goal_with_prev goal=9 facts=[6]
    #[10] m * (m ^ p * m ^ n) = m ^ p * (m * m ^ n)
  nat_norm goal=10
    #[11] m * (m ^ p * m ^ n) = m ^ p * (m * m ^ n)
qed

theorem exp_one
  fixes n :: nat
  prop (1::nat) ^ n = 1
  [hint_rewrite]
proof
  ← induction n nat_induct goal=0
    #[1] (1::nat) ^ (0::nat) = 1
    #[2] ∀n. (1::nat) ^ n = 1 ⟶ (1::nat) ^ Suc n = 1
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
  ← introduction n goal=2
    #[3] _VAR n
    #[4] (1::nat) ^ n = 1
    #[5] (1::nat) ^ Suc n = 1
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=5
    #[6] (1::nat) * 1 ^ n = 1
  ← rewrite_goal_with_prev goal=6 facts=[4]
    #[7] (1::nat) * 1 = 1
  ← rewrite_goal mult_1_left sym=false goal=7
    #[8] (1::nat) * 1 = 1
qed

theorem exp_1
  fixes n :: nat
  prop n ^ (1::nat) = n
  [hint_rewrite]
proof
  ← rewrite_goal nat_one_def sym=false goal=0
    #[1] n ^ Suc 0 = n
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=1
    #[2] n * n ^ (0::nat) = n
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=2
    #[3] n * 1 = n
  ← rewrite_goal mult_1_right sym=false goal=3
qed

theorem exp_2
  fixes n :: nat
  prop n ^ (2::nat) = n * n
  [hint_rewrite]
proof
  ← rewrite_goal bit0_def sym=false goal=0
    #[1] n ^ (of_nat::nat ⇒ nat) (1 + 1) = n * n
  ← rewrite_goal nat_of_nat_def sym="" goal=1
    #[2] n ^ ((1::nat) + 1) = n * n
  ← rewrite_goal nat_power_add sym=false goal=2
    #[3] n ^ (1::nat) * n ^ (1::nat) = n * n
  ← rewrite_goal exp_1 sym=false goal=3
qed

theorem mult_exp
  fixes m :: nat, n :: nat, p :: nat
  prop (m * n) ^ p = m ^ p * n ^ p
  [hint_rewrite]
proof
  ← induction p nat_induct goal=0
    #[1] (m * n) ^ (0::nat) = m ^ (0::nat) * n ^ (0::nat)
    #[2] ∀n1. (m * n) ^ n1 = m ^ n1 * n ^ n1 ⟶ (m * n) ^ Suc n1 = m ^ Suc n1 * n ^ Suc n1
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
    #[3] (1::nat) = 1 * 1
  ← rewrite_goal mult_1_left sym=false goal=3
  ← introduction p goal=2
    #[4] _VAR p
    #[5] (m * n) ^ p = m ^ p * n ^ p
    #[6] (m * n) ^ Suc p = m ^ Suc p * n ^ Suc p
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=6
    #[7] m * n * (m * n) ^ p = m * m ^ p * (n * n ^ p)
  ← rewrite_goal_with_prev goal=7 facts=[5]
    #[8] m * n * (m ^ p * n ^ p) = m * m ^ p * (n * n ^ p)
  nat_norm goal=8
    #[9] m * n * (m ^ p * n ^ p) = m * m ^ p * (n * n ^ p)
qed

theorem exp_mult
  fixes m :: nat, n :: nat, p :: nat
  prop m ^ (n * p) = m ^ n ^ p
  [hint_rewrite]
proof
  ← induction p nat_induct goal=0
    #[1] m ^ (n * 0) = m ^ n ^ (0::nat)
    #[2] ∀n1. m ^ (n * n1) = m ^ n ^ n1 ⟶ m ^ (n * Suc n1) = m ^ n ^ Suc n1
  ← rewrite_goal mult_0_right sym=false goal=1
    #[3] m ^ (0::nat) = m ^ n ^ (0::nat)
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=3
  ← introduction p goal=2
    #[4] _VAR p
    #[5] m ^ (n * p) = m ^ n ^ p
    #[6] m ^ (n * Suc p) = m ^ n ^ Suc p
  ← rewrite_goal mult_Suc_right sym=false goal=6
    #[7] m ^ (n + n * p) = m ^ n ^ Suc p
  ← rewrite_goal nat_power_add sym=false goal=7
    #[8] m ^ n * m ^ (n * p) = m ^ n ^ Suc p
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=8
    #[9] m ^ n * m ^ (n * p) = m ^ n * m ^ n ^ p
  ← rewrite_goal_with_prev goal=9 facts=[5]
qed

theorem exp_lt_0
  fixes n :: nat, x :: nat
  prop 0 < x ^ n <--> ~(x = 0) | n = 0
  [hint_rewrite]
proof
  ← rewrite_goal not_le sym=true goal=0
    #[1] ¬(x ^ n ≤ 0) ⟷ ¬(x = 0) ∨ n = 0
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=1
    #[2] ¬(x ^ n = 0) ⟷ ¬(x = 0) ∨ n = 0
  ← rewrite_goal exp_eq_0 sym=false goal=2
    #[3] ¬(x = 0 ∧ ¬(n = 0)) ⟷ ¬(x = 0) ∨ n = 0
  ← rewrite_goal de_morgan_thm1 sym=false goal=3
    #[4] ¬(x = 0) ∨ ¬¬(n = 0) ⟷ ¬(x = 0) ∨ n = 0
  ← rewrite_goal double_neg sym=false goal=4
qed

theorem lt_pow2_refl
  fixes n :: nat
  prop n < 2 ^ n
  [hint_backward]
proof
  ← induction n nat_induct goal=0
    #[1] (0::nat) < 2 ^ (0::nat)
    #[2] ∀n. n < 2 ^ n ⟶ Suc n < 2 ^ Suc n
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=1
    #[3] (0::nat) < 1
  ← apply_backward_step le_1_1 goal=3
    #[4] ¬((1::nat) = 0)
  nat_const_ineq goal=4
  ← introduction n goal=2
    #[5] _VAR n
    #[6] n < 2 ^ n
    #[7] Suc n < 2 ^ Suc n
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=7
    #[8] Suc n < 2 * 2 ^ n
  ← rewrite_goal mult_2 sym=false goal=8
    #[9] Suc n < 2 ^ n + 2 ^ n
  ← rewrite_goal add_1_right sym=true goal=9
    #[10] n + 1 < 2 ^ n + 2 ^ n
  ← rewrite_goal add_comm sym=false goal=10
    #[11] 1 + n < 2 ^ n + 2 ^ n
  ← apply_backward_step let_add2 goal=11
    #[12] (1::nat) ≤ 2 ^ n
  ← apply_backward_step le_1_2 goal=12
    #[13] ¬((2::nat) ^ n = 0)
  ← rewrite_goal exp_eq_0 sym=false goal=13
    #[14] ¬((2::nat) = 0 ∧ ¬(n = 0))
  ← rewrite_goal de_morgan_thm1 sym=false goal=14
    #[15] ¬((2::nat) = 0) ∨ ¬¬(n = 0)
  ← apply_backward_step disjI1 goal=15
    #[16] ¬((2::nat) = 0)
  nat_const_ineq goal=16
    #[17] ¬((2::nat) = 0)
qed

theorem lt_exp
  fixes m :: nat, n :: nat, x :: nat
  prop x ^ m < x ^ n <--> 2 <= x & m < n | x = 0 & ~(m = 0) & n = 0
  [hint_rewrite]
proof
  ← cases "x=0" goal=0
    #[1] x = 0 ⟶ x ^ m < x ^ n ⟷ 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
    #[2] ¬(x = 0) ⟶ x ^ m < x ^ n ⟷ 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
  ← introduction goal=1
    #[3] x = 0
    #[4] x ^ m < x ^ n ⟷ 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] (0::nat) ^ m < 0 ^ n ⟷ (2::nat) ≤ 0 ∧ m < n ∨ (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0
  ← apply_backward_step iffI goal=5
    #[6] (0::nat) ^ m < 0 ^ n ⟶ (2::nat) ≤ 0 ∧ m < n ∨ (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0
    #[7] (2::nat) ≤ 0 ∧ m < n ∨ (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0 ⟶ (0::nat) ^ m < 0 ^ n
  ← introduction goal=6
    #[8] (0::nat) ^ m < 0 ^ n
    #[9] (2::nat) ≤ 0 ∧ m < n ∨ (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0
  ← apply_backward_step disjI2 goal=9
    #[10] (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0
  ← apply_backward_step conjI goal=10
    #[11] (0::nat) = 0
    #[12] ¬(m = 0) ∧ n = 0
  ← apply_backward_step eq_refl goal=11
    #[13] (0::nat) = 0
  ← apply_backward_step contrapositive goal=12 facts=[8]
    #[14] ¬(¬(m = 0) ∧ n = 0) ⟶ ¬((0::nat) ^ m < 0 ^ n)
  ← introduction goal=14
    #[15] ¬(¬(m = 0) ∧ n = 0)
    #[16] ¬((0::nat) ^ m < 0 ^ n)
  -> rewrite_fact de_morgan_thm1 sym=false goal=16 facts=[15]
    #[17] ¬¬(m = 0) ∨ ¬(n = 0)
  -> rewrite_fact double_neg sym=false goal=16 facts=[17]
    #[18] m = 0 ∨ ¬(n = 0)
  ← apply_backward_step disjE goal=16 facts=[18]
    #[19] m = 0 ⟶ ¬((0::nat) ^ m < 0 ^ n)
    #[20] ¬(n = 0) ⟶ ¬((0::nat) ^ m < 0 ^ n)
  ← introduction goal=19
    #[21] m = 0
    #[22] ¬((0::nat) ^ m < 0 ^ n)
  ← rewrite_goal exp_zero sym=false goal=22
    #[23] ¬((if m = 0 then (1::nat) else 0) < (if n = 0 then 1 else 0))
  ← rewrite_goal if_P sym=false goal=23 facts=[21]
    #[24] ¬((1::nat) < (if n = 0 then 1 else 0))
  ← cases "n=0" goal=24
    #[25] n = 0 ⟶ ¬((1::nat) < (if n = 0 then 1 else 0))
    #[26] ¬(n = 0) ⟶ ¬((1::nat) < (if n = 0 then 1 else 0))
  ← introduction goal=25
    #[27] n = 0
    #[28] ¬((1::nat) < (if n = 0 then 1 else 0))
  ← rewrite_goal if_P sym=false goal=28 facts=[27]
    #[29] ¬((1::nat) < 1)
  ← rewrite_goal not_le sym=true goal=29
    #[30] ¬¬((1::nat) ≤ 1)
  ← rewrite_goal double_neg sym=false goal=30
    #[31] (1::nat) ≤ 1
  ← apply_backward_step less_eq_refl goal=31
    #[32] (1::nat) ≤ 1
  ← introduction goal=26
    #[33] ¬(n = 0)
    #[34] ¬((1::nat) < (if n = 0 then 1 else 0))
  ← rewrite_goal if_not_P sym=false goal=34 facts=[33]
    #[35] ¬((1::nat) < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=35
    #[36] ¬false
  ← apply_backward_step negI goal=36
    #[37] false ⟶ false
  ← introduction goal=20
    #[38] ¬((0::nat) ^ m < 0 ^ n)
  ← rewrite_goal exp_zero sym=false goal=38
    #[39] ¬((if m = 0 then (1::nat) else 0) < (if n = 0 then 1 else 0))
  ← rewrite_goal if_not_P sym=false goal=39 facts=[33]
    #[40] ¬((if m = 0 then (1::nat) else 0) < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=40
    #[41] ¬false
  ← apply_backward_step negI goal=41
  ← introduction goal=7
    #[42] (2::nat) ≤ 0 ∧ m < n ∨ (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0
    #[43] (0::nat) ^ m < 0 ^ n
  ← apply_backward_step disjE goal=43 facts=[42]
    #[44] (2::nat) ≤ 0 ∧ m < n ⟶ (0::nat) ^ m < 0 ^ n
    #[45] (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0 ⟶ (0::nat) ^ m < 0 ^ n
  ← introduction goal=44
    #[46] (2::nat) ≤ 0 ∧ m < n
    #[47] (0::nat) ^ m < 0 ^ n
  -> apply_forward_step conjD1 goal=47 facts=[46]
    #[48] (2::nat) ≤ 0
  ← z3 goal=47 facts=[48]
    #[49] (0::nat) ^ m < 0 ^ n
  ← introduction goal=45
    #[50] (0::nat) = 0 ∧ ¬(m = 0) ∧ n = 0
    #[51] (0::nat) ^ m < 0 ^ n
  -> apply_forward_step conjD2 goal=51 facts=[50]
    #[52] ¬(m = 0) ∧ n = 0
  -> apply_forward_step conjD1 goal=51 facts=[52]
    #[53] ¬(m = 0)
  -> apply_forward_step conjD2 goal=51 facts=[52]
    #[54] n = 0
  ← rewrite_goal exp_zero sym=false goal=51
    #[55] (if m = 0 then (1::nat) else 0) < (if n = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=55 facts=[54]
    #[56] (if m = 0 then (1::nat) else 0) < 1
  ← rewrite_goal if_not_P sym=false goal=56 facts=[53]
    #[57] (0::nat) < 1
  ← apply_backward_step le_1_1 goal=57
    #[58] ¬((1::nat) = 0)
  nat_const_ineq goal=58
    #[59] ¬((1::nat) = 0)
  ← introduction goal=2
    #[60] ¬(x = 0)
    #[61] x ^ m < x ^ n ⟷ 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
  ← apply_backward_step iffI goal=61
    #[62] x ^ m < x ^ n ⟶ 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
    #[63] 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0 ⟶ x ^ m < x ^ n
  ← introduction goal=62
    #[64] x ^ m < x ^ n
    #[65] 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
  ← apply_backward_step contrapositive goal=65 facts=[64]
    #[66] ¬(2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0) ⟶ ¬(x ^ m < x ^ n)
  ← rewrite_goal de_morgan_thm2 sym=false goal=66
    #[67] ¬(2 ≤ x ∧ m < n) ∧ ¬(x = 0 ∧ ¬(m = 0) ∧ n = 0) ⟶ ¬(x ^ m < x ^ n)
  ← rewrite_goal not_le sym=true goal=67
    #[68] ¬(2 ≤ x ∧ ¬(n ≤ m)) ∧ ¬(x = 0 ∧ ¬(m = 0) ∧ n = 0) ⟶ ¬¬(x ^ n ≤ x ^ m)
  ← rewrite_goal de_morgan_thm1 sym=false goal=68
    #[69] (¬(2 ≤ x) ∨ ¬¬(n ≤ m)) ∧ (¬(x = 0) ∨ ¬(¬(m = 0) ∧ n = 0)) ⟶ ¬¬(x ^ n ≤ x ^ m)
  ← rewrite_goal de_morgan_thm1 sym=false goal=69
    #[70] (¬(2 ≤ x) ∨ ¬¬(n ≤ m)) ∧ (¬(x = 0) ∨ ¬¬(m = 0) ∨ ¬(n = 0)) ⟶ ¬¬(x ^ n ≤ x ^ m)
  ← rewrite_goal double_neg sym=false goal=70
    #[71] (¬(2 ≤ x) ∨ n ≤ m) ∧ (¬(x = 0) ∨ m = 0 ∨ ¬(n = 0)) ⟶ x ^ n ≤ x ^ m
  ← introduction goal=71
    #[72] (¬(2 ≤ x) ∨ n ≤ m) ∧ (¬(x = 0) ∨ m = 0 ∨ ¬(n = 0))
    #[73] x ^ n ≤ x ^ m
  -> apply_forward_step conjD1 goal=73 facts=[72]
    #[74] ¬(2 ≤ x) ∨ n ≤ m
  -> apply_forward_step conjD2 goal=73 facts=[72]
    #[75] ¬(x = 0) ∨ m = 0 ∨ ¬(n = 0)
  ← apply_backward_step disjE goal=73 facts=[74]
    #[76] ¬(2 ≤ x) ⟶ x ^ n ≤ x ^ m
    #[77] n ≤ m ⟶ x ^ n ≤ x ^ m
  ← introduction goal=76
    #[78] ¬(2 ≤ x)
    #[79] x ^ n ≤ x ^ m
  -> rewrite_fact not_le sym=false goal=79 facts=[78]
    #[80] x < 2
  cut "x = 0 | x =1" goal=79 facts=[80]
    #[81] x = 0 ∨ x = 1
  ← z3 goal=81 facts=[80]
    #[82] x = 0 ∨ x = 1
  ← apply_backward_step disjE goal=79 facts=[82]
    #[83] x = 0 ⟶ x ^ n ≤ x ^ m
    #[84] x = 1 ⟶ x ^ n ≤ x ^ m
  ← introduction goal=83
    #[85] x ^ n ≤ x ^ m
  ← rewrite_goal_with_prev goal=85 facts=[3]
    #[86] (0::nat) ^ n ≤ 0 ^ m
  ← rewrite_goal exp_zero sym=false goal=86
    #[87] (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← apply_backward_step disjE goal=87 facts=[75]
    #[88] ¬(x = 0) ⟶ (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
    #[89] m = 0 ∨ ¬(n = 0) ⟶ (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← z3 goal=88 facts=[3]
    #[90] ¬(x = 0) ⟶ (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← introduction goal=89
    #[91] m = 0 ∨ ¬(n = 0)
    #[92] (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← apply_backward_step disjE goal=92 facts=[91]
    #[93] m = 0 ⟶ (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
    #[94] ¬(n = 0) ⟶ (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← introduction goal=93
    #[95] (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=95 facts=[21]
    #[96] (if n = 0 then (1::nat) else 0) ≤ 1
  ← cases "n = 0" goal=96
    #[97] n = 0 ⟶ (if n = 0 then (1::nat) else 0) ≤ 1
    #[98] ¬(n = 0) ⟶ (if n = 0 then (1::nat) else 0) ≤ 1
  ← introduction goal=97
    #[99] (if n = 0 then (1::nat) else 0) ≤ 1
  ← rewrite_goal if_P sym=false goal=99 facts=[27]
    #[100] (1::nat) ≤ 1
  ← apply_backward_step less_eq_refl goal=100
  ← introduction goal=98
    #[101] (if n = 0 then (1::nat) else 0) ≤ 1
  ← rewrite_goal if_not_P sym=false goal=101 facts=[33]
    #[102] (0::nat) ≤ 1
  ← apply_backward_step le_0 goal=102
    #[103] (0::nat) ≤ 1
  ← introduction goal=94
    #[104] (if n = 0 then (1::nat) else 0) ≤ (if m = 0 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=104 facts=[33]
    #[105] (0::nat) ≤ (if m = 0 then 1 else 0)
  ← apply_backward_step le_0 goal=105
    #[106] (0::nat) ≤ (if m = 0 then 1 else 0)
  ← introduction goal=84
    #[107] x = 1
    #[108] x ^ n ≤ x ^ m
  ← rewrite_goal_with_prev goal=108 facts=[107]
    #[109] (1::nat) ^ n ≤ 1 ^ m
  ← rewrite_goal exp_one sym=false goal=109
    #[110] (1::nat) ≤ 1
  ← apply_backward_step less_eq_refl goal=110
  ← introduction goal=77
    #[111] n ≤ m
    #[112] x ^ n ≤ x ^ m
  -> rewrite_fact less_eq_exist sym=false goal=112 facts=[111]
    #[113] ∃p. m = n + p
  exists_elim p goal=112 facts=[113]
    #[114] _VAR p
    #[115] m = n + p
    #[116] x ^ n ≤ x ^ m
  ← rewrite_goal_with_prev goal=116 facts=[115]
    #[117] x ^ n ≤ x ^ (n + p)
  ← induction p nat_induct goal=117
    #[118] x ^ n ≤ x ^ (n + 0)
    #[119] ∀n1. x ^ n ≤ x ^ (n + n1) ⟶ x ^ n ≤ x ^ (n + Suc n1)
  ← rewrite_goal add_0_right sym=false goal=118
    #[120] x ^ n ≤ x ^ n
  ← apply_backward_step less_eq_refl goal=120
    #[121] x ^ n ≤ x ^ n
  ← introduction d goal=119
    #[122] _VAR d
    #[123] x ^ n ≤ x ^ (n + d)
    #[124] x ^ n ≤ x ^ (n + Suc d)
  ← rewrite_goal add_Suc_right sym=false goal=124
    #[125] x ^ n ≤ x ^ Suc (n + d)
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=125
    #[126] x ^ n ≤ x * x ^ (n + d)
  ← apply_backward_step less_eq_trans param_m="1 * x ^ (n + d)" goal=126
    #[127] x ^ n ≤ 1 * x ^ (n + d)
    #[128] 1 * x ^ (n + d) ≤ x * x ^ (n + d)
  ← rewrite_goal mult_1_left sym=false goal=127
  ← rewrite_goal le_mult_rcancel sym=false goal=128
    #[129] 1 ≤ x ∨ x ^ (n + d) = 0
  ← apply_backward_step disjI1 goal=129
    #[130] 1 ≤ x
  ← apply_backward_step le_1_2 goal=130 facts=[60]
    #[131] 1 ≤ x
  ← introduction goal=63
    #[132] 2 ≤ x ∧ m < n ∨ x = 0 ∧ ¬(m = 0) ∧ n = 0
    #[133] x ^ m < x ^ n
  ← apply_backward_step disjE goal=133 facts=[132]
    #[134] 2 ≤ x ∧ m < n ⟶ x ^ m < x ^ n
    #[135] x = 0 ∧ ¬(m = 0) ∧ n = 0 ⟶ x ^ m < x ^ n
  ← introduction goal=134
    #[136] 2 ≤ x ∧ m < n
    #[137] x ^ m < x ^ n
  -> apply_forward_step conjD1 goal=137 facts=[136]
    #[138] 2 ≤ x
  -> apply_forward_step conjD2 goal=137 facts=[136]
    #[139] m < n
  -> rewrite_fact less_exist sym=false goal=137 facts=[139]
    #[140] ∃d. n = m + Suc d
  exists_elim d goal=137 facts=[140]
    #[141] n = m + Suc d
    #[142] x ^ m < x ^ n
  ← rewrite_goal_with_prev goal=142 facts=[141]
    #[143] x ^ m < x ^ (m + Suc d)
  ← induction d nat_induct goal=143
    #[144] x ^ m < x ^ (m + Suc 0)
    #[145] ∀n. x ^ m < x ^ (m + Suc n) ⟶ x ^ m < x ^ (m + Suc (Suc n))
  ← rewrite_goal add_Suc_right sym=false goal=144
    #[146] x ^ m < x ^ Suc (m + 0)
  ← rewrite_goal add_0_right sym=false goal=146
    #[147] x ^ m < x ^ Suc m
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=147
    #[148] x ^ m < x * x ^ m
  ← apply_backward_step let_trans param_m="1 * x ^ m" goal=148
    #[149] x ^ m ≤ 1 * x ^ m
    #[150] 1 * x ^ m < x * x ^ m
  ← rewrite_goal mult_1_left sym=false goal=149
    #[151] x ^ m ≤ x ^ m
  ← apply_backward_step less_eq_refl goal=151
    #[152] x ^ m ≤ x ^ m
  ← rewrite_goal lt_mult_rcancel sym=false goal=150
    #[153] 1 < x ∧ ¬(x ^ m = 0)
  ← apply_backward_step conjI goal=153
    #[154] 1 < x
    #[155] ¬(x ^ m = 0)
  ← z3 goal=154 facts=[138]
    #[156] 1 < x
  ← apply_backward_step negI goal=155
    #[157] x ^ m = 0 ⟶ false
  ← rewrite_goal exp_eq_0 sym=false goal=157
    #[158] x = 0 ∧ ¬(m = 0) ⟶ false
  ← introduction goal=158
    #[159] x = 0 ∧ ¬(m = 0)
    #[160] false
  ← z3 goal=160 facts=[159,138]
    #[161] false
  ← introduction p goal=145
    #[162] x ^ m < x ^ (m + Suc p)
    #[163] x ^ m < x ^ (m + Suc (Suc p))
  ← rewrite_goal add_Suc_right sym=false goal=163
    #[164] x ^ m < x ^ Suc (m + Suc p)
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=164
    #[165] x ^ m < x * x ^ (m + Suc p)
  ← apply_backward_step lt_trans param_m="1 * x ^ (m + Suc p)" goal=165
    #[166] x ^ m < 1 * x ^ (m + Suc p)
    #[167] 1 * x ^ (m + Suc p) < x * x ^ (m + Suc p)
  ← rewrite_goal mult_1_left sym=false goal=166
  ← rewrite_goal lt_mult_rcancel sym=false goal=167
    #[168] 1 < x ∧ ¬(x ^ (m + Suc p) = 0)
  ← apply_backward_step conjI goal=168
    #[169] ¬(x ^ (m + Suc p) = 0)
  ← apply_backward_step negI goal=169
    #[170] x ^ (m + Suc p) = 0 ⟶ false
  ← rewrite_goal exp_eq_0 sym=false goal=170
    #[171] x = 0 ∧ ¬(m + Suc p = 0) ⟶ false
  ← introduction goal=171
    #[172] x = 0 ∧ ¬(m + Suc p = 0)
    #[173] false
  -> apply_forward_step conjD1 goal=173 facts=[172]
    #[174] x = 0
  ← z3 goal=173 facts=[174,138]
    #[175] false
  ← introduction goal=135
    #[176] x = 0 ∧ ¬(m = 0) ∧ n = 0
    #[177] x ^ m < x ^ n
  -> apply_forward_step conjD1 goal=177 facts=[176]
    #[178] x = 0
  -> apply_forward_step conjD2 goal=177 facts=[176]
    #[179] ¬(m = 0) ∧ n = 0
  -> apply_forward_step conjD1 goal=177 facts=[179]
    #[180] ¬(m = 0)
  -> apply_forward_step conjD2 goal=177 facts=[179]
    #[181] n = 0
  ← rewrite_goal_with_prev goal=177 facts=[178]
    #[182] (0::nat) ^ m < 0 ^ n
  ← rewrite_goal exp_zero sym=false goal=182
    #[183] (if m = 0 then (1::nat) else 0) < (if n = 0 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=183 facts=[180]
    #[184] (0::nat) < (if n = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=184 facts=[181]
    #[185] (0::nat) < 1
  ← apply_backward_step le_1_1 goal=185
    #[186] ¬((1::nat) = 0)
  nat_const_ineq goal=186
qed

theorem le_exp
  fixes m :: nat, n :: nat, x :: nat
  prop x ^ m <= x ^ n <--> (if x = 0 then m = 0 --> n = 0 else x = 1 | m <= n)
  [hint_rewrite]
proof
  ← rewrite_goal not_lt sym=true goal=0
    #[1] ¬(x ^ n < x ^ m) ⟷ (if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ ¬(n < m))
  ← rewrite_goal lt_exp sym=false goal=1
    #[2] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ (if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ ¬(n < m))
  ← cases "x = 0" goal=2
    #[3] x = 0 ⟶ ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ (if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ ¬(n < m))
    #[4] ¬(x = 0) ⟶ ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ (if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ ¬(n < m))
  ← introduction goal=3
    #[5] x = 0
    #[6] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ (if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ ¬(n < m))
  ← rewrite_goal if_P sym=false goal=6 facts=[5]
    #[7] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ (m = 0 ⟶ n = 0)
  ← apply_backward_step iffI goal=7
    #[8] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟶ m = 0 ⟶ n = 0
    #[9] (m = 0 ⟶ n = 0) ⟶ ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← introduction goal=8
    #[10] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0)
    #[11] m = 0
    #[12] n = 0
  -> rewrite_fact de_morgan_thm2 sym=false goal=12 facts=[10]
    #[13] ¬(2 ≤ x ∧ n < m) ∧ ¬(x = 0 ∧ ¬(n = 0) ∧ m = 0)
  -> apply_forward_step conjD1 goal=12 facts=[13]
    #[14] ¬(2 ≤ x ∧ n < m)
  -> apply_forward_step conjD2 goal=12 facts=[13]
    #[15] ¬(x = 0 ∧ ¬(n = 0) ∧ m = 0)
  -> rewrite_fact de_morgan_thm1 sym=false goal=12 facts=[14]
    #[16] ¬(2 ≤ x) ∨ ¬(n < m)
  -> rewrite_fact not_lt sym=false goal=12 facts=[16]
    #[17] ¬(2 ≤ x) ∨ m ≤ n
  -> rewrite_fact de_morgan_thm1 sym=false goal=12 facts=[15]
    #[18] ¬(x = 0) ∨ ¬(¬(n = 0) ∧ m = 0)
  ← apply_backward_step disjE goal=12 facts=[18]
    #[19] ¬(x = 0) ⟶ n = 0
    #[20] ¬(¬(n = 0) ∧ m = 0) ⟶ n = 0
  ← introduction goal=19
    #[21] ¬(x = 0)
    #[22] n = 0
  ← apply_backward_step negE_gen goal=22 facts=[21,5]
    #[23] n = 0
  ← rewrite_goal de_morgan_thm1 sym=false goal=20
    #[24] ¬¬(n = 0) ∨ ¬(m = 0) ⟶ n = 0
  ← introduction goal=24
    #[25] ¬¬(n = 0) ∨ ¬(m = 0)
    #[26] n = 0
  ← apply_backward_step disjE goal=26 facts=[25]
    #[27] ¬¬(n = 0) ⟶ n = 0
    #[28] ¬(m = 0) ⟶ n = 0
  ← rewrite_goal double_neg sym=false goal=27
    #[29] n = 0 ⟶ n = 0
  ← introduction goal=28
    #[30] ¬(m = 0)
    #[31] n = 0
  ← apply_backward_step negE_gen goal=31 facts=[30,11]
    #[32] n = 0
  ← introduction goal=9
    #[33] m = 0 ⟶ n = 0
    #[34] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← rewrite_goal de_morgan_thm2 sym=false goal=34
    #[35] ¬(2 ≤ x ∧ n < m) ∧ ¬(x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← apply_backward_step conjI goal=35
    #[36] ¬(2 ≤ x ∧ n < m)
    #[37] ¬(x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← rewrite_goal de_morgan_thm1 sym=false goal=36
    #[38] ¬(2 ≤ x) ∨ ¬(n < m)
  ← apply_backward_step disjI1 goal=38
    #[39] ¬(2 ≤ x)
  ← rewrite_goal_with_prev goal=39 facts=[5]
    #[40] ¬((2::nat) ≤ 0)
  ← z3 goal=40
    #[41] ¬((2::nat) ≤ 0)
  ← rewrite_goal de_morgan_thm1 sym=false goal=37
    #[42] ¬(x = 0) ∨ ¬(¬(n = 0) ∧ m = 0)
  ← apply_backward_step disjI2 goal=42
    #[43] ¬(¬(n = 0) ∧ m = 0)
  ← rewrite_goal de_morgan_thm1 sym=false goal=43
    #[44] ¬¬(n = 0) ∨ ¬(m = 0)
  ← rewrite_goal double_neg sym=false goal=44
    #[45] n = 0 ∨ ¬(m = 0)
  ← z3 goal=45 facts=[33]
    #[46] n = 0 ∨ ¬(m = 0)
  ← introduction goal=4
    #[47] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ (if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ ¬(n < m))
  ← rewrite_goal if_not_P sym=false goal=47 facts=[21]
    #[48] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟷ x = 1 ∨ ¬(n < m)
  ← apply_backward_step iffI goal=48
    #[49] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0) ⟶ x = 1 ∨ ¬(n < m)
    #[50] x = 1 ∨ ¬(n < m) ⟶ ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← introduction goal=49
    #[51] x = 1 ∨ ¬(n < m)
  -> rewrite_fact de_morgan_thm2 sym=false goal=51 facts=[10]
  -> apply_forward_step conjD1 goal=51 facts=[13]
  -> rewrite_fact de_morgan_thm1 sym=false goal=51 facts=[14]
  ← apply_backward_step disjE goal=51 facts=[16]
    #[52] ¬(2 ≤ x) ⟶ x = 1 ∨ ¬(n < m)
    #[53] ¬(n < m) ⟶ x = 1 ∨ ¬(n < m)
  ← introduction goal=52
    #[54] ¬(2 ≤ x)
    #[55] x = 1 ∨ ¬(n < m)
  ← apply_backward_step disjI1 goal=55
    #[56] x = 1
  ← z3 goal=56 facts=[21,54]
    #[57] x = 1
  ← introduction goal=53
    #[58] ¬(n < m)
    #[59] x = 1 ∨ ¬(n < m)
  ← apply_backward_step disjI2 goal=59
  ← introduction goal=50
    #[60] x = 1 ∨ ¬(n < m)
    #[61] ¬(2 ≤ x ∧ n < m ∨ x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← rewrite_goal de_morgan_thm2 sym=false goal=61
    #[62] ¬(2 ≤ x ∧ n < m) ∧ ¬(x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← apply_backward_step conjI goal=62
    #[63] ¬(2 ≤ x ∧ n < m)
    #[64] ¬(x = 0 ∧ ¬(n = 0) ∧ m = 0)
  ← rewrite_goal de_morgan_thm1 sym=false goal=63
    #[65] ¬(2 ≤ x) ∨ ¬(n < m)
  ← apply_backward_step disjE goal=65 facts=[60]
    #[66] x = 1 ⟶ ¬(2 ≤ x) ∨ ¬(n < m)
    #[67] ¬(n < m) ⟶ ¬(2 ≤ x) ∨ ¬(n < m)
  ← introduction goal=66
    #[68] x = 1
    #[69] ¬(2 ≤ x) ∨ ¬(n < m)
  ← apply_backward_step disjI1 goal=69
    #[70] ¬(2 ≤ x)
  ← rewrite_goal_with_prev goal=70 facts=[68]
    #[71] ¬((2::nat) ≤ 1)
  ← z3 goal=71
    #[72] ¬((2::nat) ≤ 1)
  ← introduction goal=67
    #[73] ¬(2 ≤ x) ∨ ¬(n < m)
  ← apply_backward_step disjI2 goal=73
  ← rewrite_goal de_morgan_thm1 sym=false goal=64
    #[74] ¬(x = 0) ∨ ¬(¬(n = 0) ∧ m = 0)
  ← apply_backward_step disjI1 goal=74
qed

theorem eq_exp
  fixes m :: nat, n :: nat, x :: nat
  prop x ^ m = x ^ n <--> (if x = 0 then m = 0 <--> n = 0 else x = 1 | m = n)
  [hint_rewrite]
proof
  -> apply_forward_step le_antisym param_m="x ^ m" param_n="x ^ n" goal=0
    #[1] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m ⟷ x ^ m = x ^ n
  -> rewrite_fact eq_sym_eq sym=false goal=0 facts=[1]
    #[2] x ^ m = x ^ n ⟷ x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
  ← rewrite_goal_with_prev goal=0 facts=[2]
    #[3] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m ⟷ (if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n)
  ← apply_backward_step iffI goal=3
    #[4] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m ⟶ (if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n)
    #[5] (if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n) ⟶ x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
  ← introduction goal=4
    #[6] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
    #[7] if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n
  -> apply_forward_step conjD1 goal=7 facts=[6]
    #[8] x ^ m ≤ x ^ n
  -> apply_forward_step conjD2 goal=7 facts=[6]
    #[9] x ^ n ≤ x ^ m
  -> rewrite_fact le_exp sym=false goal=7 facts=[8]
    #[10] if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ m ≤ n
  -> rewrite_fact le_exp sym=false goal=7 facts=[9]
    #[11] if x = 0 then n = 0 ⟶ m = 0 else x = 1 ∨ n ≤ m
  ← cases "x=0" goal=7
    #[12] x = 0 ⟶ (if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n)
    #[13] ¬(x = 0) ⟶ (if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n)
  ← introduction goal=12
    #[14] x = 0
    #[15] if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n
  ← rewrite_goal if_P sym=false goal=15 facts=[14]
    #[16] m = 0 ⟷ n = 0
  -> rewrite_fact if_P sym=false goal=16 facts=[10,14]
    #[17] m = 0 ⟶ n = 0
  -> rewrite_fact if_P sym=false goal=16 facts=[11,14]
    #[18] n = 0 ⟶ m = 0
  ← apply_backward_step iffI goal=16
  ← introduction goal=13
    #[19] ¬(x = 0)
    #[20] if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n
  ← rewrite_goal if_not_P sym=false goal=20 facts=[19]
    #[21] x = 1 ∨ m = n
  -> rewrite_fact if_not_P sym=false goal=21 facts=[10,19]
    #[22] x = 1 ∨ m ≤ n
  -> rewrite_fact if_not_P sym=false goal=21 facts=[11,19]
    #[23] x = 1 ∨ n ≤ m
  ← apply_backward_step disjE goal=21 facts=[23]
    #[24] x = 1 ⟶ x = 1 ∨ m = n
    #[25] n ≤ m ⟶ x = 1 ∨ m = n
  ← introduction goal=24
    #[26] x = 1
    #[27] x = 1 ∨ m = n
  ← apply_backward_step disjI1 goal=27
  ← introduction goal=25
    #[28] n ≤ m
    #[29] x = 1 ∨ m = n
  ← apply_backward_step disjE goal=29 facts=[22]
    #[30] m ≤ n ⟶ x = 1 ∨ m = n
  ← introduction goal=30
    #[31] m ≤ n
    #[32] x = 1 ∨ m = n
  ← apply_backward_step disjI2 goal=32
    #[33] m = n
  ← rewrite_goal le_antisym sym=true goal=33
    #[34] m ≤ n ∧ n ≤ m
  ← apply_backward_step conjI goal=34
  ← introduction goal=5
    #[35] if x = 0 then m = 0 ⟷ n = 0 else x = 1 ∨ m = n
    #[36] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
  ← cases "x=0" goal=36
    #[37] x = 0 ⟶ x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
    #[38] ¬(x = 0) ⟶ x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
  ← introduction goal=37
    #[39] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
  ← apply_backward_step conjI goal=39
    #[40] x ^ m ≤ x ^ n
    #[41] x ^ n ≤ x ^ m
  ← rewrite_goal le_exp sym=false goal=40
    #[42] if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ m ≤ n
  -> rewrite_fact if_P sym=false goal=42 facts=[35,14]
    #[43] m = 0 ⟷ n = 0
  ← rewrite_goal if_P sym=false goal=42 facts=[14]
    #[44] m = 0 ⟶ n = 0
  ← introduction goal=44
    #[45] m = 0
    #[46] n = 0
  -> rewrite_fact_with_prev goal=46 facts=[43,45]
  ← rewrite_goal le_exp sym=false goal=41
    #[47] if x = 0 then n = 0 ⟶ m = 0 else x = 1 ∨ n ≤ m
  ← rewrite_goal if_P sym=false goal=47 facts=[14]
    #[48] n = 0 ⟶ m = 0
  ← introduction goal=48
    #[49] n = 0
    #[50] m = 0
  ← rewrite_goal_with_prev goal=50 facts=[43]
  ← introduction goal=38
    #[51] x ^ m ≤ x ^ n ∧ x ^ n ≤ x ^ m
  ← apply_backward_step conjI goal=51
    #[52] x ^ m ≤ x ^ n
    #[53] x ^ n ≤ x ^ m
  ← rewrite_goal le_exp sym=false goal=52
    #[54] if x = 0 then m = 0 ⟶ n = 0 else x = 1 ∨ m ≤ n
  -> rewrite_fact if_not_P sym=false goal=54 facts=[35,19]
    #[55] x = 1 ∨ m = n
  ← rewrite_goal if_not_P sym=false goal=54 facts=[19]
    #[56] x = 1 ∨ m ≤ n
  ← apply_backward_step disjE goal=56 facts=[55]
    #[57] x = 1 ⟶ x = 1 ∨ m ≤ n
    #[58] m = n ⟶ x = 1 ∨ m ≤ n
  ← introduction goal=57
    #[59] x = 1 ∨ m ≤ n
  ← apply_backward_step disjI1 goal=59
  ← introduction goal=58
    #[60] m = n
    #[61] x = 1 ∨ m ≤ n
  ← apply_backward_step disjI2 goal=61
    #[62] m ≤ n
  ← rewrite_goal_with_prev goal=62 facts=[60]
    #[63] n ≤ n
  ← apply_backward_step less_eq_refl goal=63
    #[64] n ≤ n
  ← rewrite_goal le_exp sym=false goal=53
    #[65] if x = 0 then n = 0 ⟶ m = 0 else x = 1 ∨ n ≤ m
  ← rewrite_goal if_not_P sym=false goal=65 facts=[19]
    #[66] x = 1 ∨ n ≤ m
  ← apply_backward_step disjE goal=66 facts=[55]
    #[67] x = 1 ⟶ x = 1 ∨ n ≤ m
    #[68] m = n ⟶ x = 1 ∨ n ≤ m
  ← introduction goal=67
    #[69] x = 1 ∨ n ≤ m
  ← apply_backward_step disjI1 goal=69
  ← introduction goal=68
    #[70] x = 1 ∨ n ≤ m
  ← apply_backward_step disjI2 goal=70
    #[71] n ≤ m
  ← rewrite_goal_with_prev goal=71 facts=[60]
  ← apply_backward_step less_eq_refl goal=63
qed

theorem exp_mono_le_imp
  fixes n :: nat, x :: nat, y :: nat
  prop x <= y --> x ^ n <= y ^ n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] x ≤ y
    #[2] x ^ n ≤ y ^ n
  ← induction n nat_induct goal=2
    #[3] x ^ (0::nat) ≤ y ^ (0::nat)
    #[4] ∀n. x ^ n ≤ y ^ n ⟶ x ^ Suc n ≤ y ^ Suc n
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=3
    #[5] (1::nat) ≤ 1
  ← apply_backward_step less_eq_refl goal=5
    #[6] (1::nat) ≤ 1
  ← introduction n goal=4
    #[7] _VAR n
    #[8] x ^ n ≤ y ^ n
    #[9] x ^ Suc n ≤ y ^ Suc n
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=9
    #[10] x * x ^ n ≤ y * y ^ n
  ← apply_backward_step le_mult2 goal=10
qed

theorem exp_mono_lt_imp
  fixes n :: nat, x :: nat, y :: nat
  prop x < y --> ~(n = 0) --> x ^ n < y ^ n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] x < y
    #[2] ¬(n = 0)
    #[3] x ^ n < y ^ n
  revert_intro goal=3 facts=[2]
    #[4] ¬(n = 0) ⟶ x ^ n < y ^ n
  ← induction n nat_induct goal=4
    #[5] ¬((0::nat) = 0) ⟶ x ^ (0::nat) < y ^ (0::nat)
    #[6] ∀n. (¬(n = 0) ⟶ x ^ n < y ^ n) ⟶ ¬(Suc n = 0) ⟶ x ^ Suc n < y ^ Suc n
  ← introduction goal=5
    #[7] ¬((0::nat) = 0)
    #[8] x ^ (0::nat) < y ^ (0::nat)
  ← apply_backward_step contrapositive goal=8 facts=[7]
    #[9] ¬(x ^ (0::nat) < y ^ (0::nat)) ⟶ ¬¬((0::nat) = 0)
  ← introduction goal=9
    #[10] ¬(x ^ (0::nat) < y ^ (0::nat))
    #[11] ¬¬((0::nat) = 0)
  ← rewrite_goal double_neg sym=false goal=11
    #[12] ¬¬((0::nat) = 0)
  ← introduction n goal=6
    #[13] _VAR n
    #[14] ¬(n = 0) ⟶ x ^ n < y ^ n
    #[15] ¬(Suc n = 0)
    #[16] x ^ Suc n < y ^ Suc n
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=16
    #[17] x * x ^ n < y * y ^ n
  ← apply_backward_step let_trans param_m="x * y ^ n" goal=17
    #[18] x * x ^ n ≤ x * y ^ n
    #[19] x * y ^ n < y * y ^ n
  ← rewrite_goal mult_comm sym=false goal=18
    #[20] x ^ n * x ≤ y ^ n * x
  ← rewrite_goal le_mult_rcancel sym=false goal=20
    #[21] x ^ n ≤ y ^ n ∨ x = 0
  ← apply_backward_step disjI1 goal=21
    #[22] x ^ n ≤ y ^ n
  ← apply_backward_step exp_mono_le_imp goal=22
    #[23] x ≤ y
  ← apply_backward_step lt_imp_le goal=23
  ← rewrite_goal lt_mult_rcancel sym=false goal=19
    #[24] x < y ∧ ¬(y ^ n = 0)
  ← apply_backward_step conjI goal=24
    #[25] ¬(y ^ n = 0)
  ← rewrite_goal exp_eq_0 sym=false goal=25
    #[26] ¬(y = 0 ∧ ¬(n = 0))
  ← rewrite_goal de_morgan_thm1 sym=false goal=26
    #[27] ¬(y = 0) ∨ ¬¬(n = 0)
  ← apply_backward_step disjI1 goal=27
    #[28] ¬(y = 0)
  ← apply_backward_step contrapositive goal=28 facts=[1]
    #[29] ¬¬(y = 0) ⟶ ¬(x < y)
  ← introduction goal=29
    #[30] ¬¬(y = 0)
    #[31] ¬(x < y)
  -> rewrite_fact double_neg sym=false goal=31 facts=[30]
    #[32] y = 0
  ← rewrite_goal_with_prev goal=31 facts=[32]
    #[33] ¬(x < 0)
  ← rewrite_goal not_lt sym=false goal=33
    #[34] 0 ≤ x
  ← apply_backward_step le_0 goal=34
    #[35] 0 ≤ x
qed

theorem exp_mono_le
  fixes n :: nat, x :: nat, y :: nat
  prop x ^ n <= y ^ n <--> x <= y | n = 0
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] x ^ n ≤ y ^ n ⟶ x ≤ y ∨ n = 0
    #[2] x ≤ y ∨ n = 0 ⟶ x ^ n ≤ y ^ n
  ← introduction goal=1
    #[3] x ^ n ≤ y ^ n
    #[4] x ≤ y ∨ n = 0
  ← apply_backward_step contrapositive goal=4 facts=[3]
    #[5] ¬(x ≤ y ∨ n = 0) ⟶ ¬(x ^ n ≤ y ^ n)
  ← introduction goal=5
    #[6] ¬(x ≤ y ∨ n = 0)
    #[7] ¬(x ^ n ≤ y ^ n)
  ← rewrite_goal not_le sym=false goal=7
    #[8] y ^ n < x ^ n
  ← apply_backward_step exp_mono_lt_imp goal=8
    #[9] y < x
    #[10] ¬(n = 0)
  -> rewrite_fact de_morgan_thm2 sym=false goal=9 facts=[6]
    #[11] ¬(x ≤ y) ∧ ¬(n = 0)
  -> rewrite_fact not_le sym=false goal=9 facts=[11]
    #[12] y < x ∧ ¬(n = 0)
  ← apply_backward_step conjD1 goal=9 facts=[12]
    #[13] y < x
  ← apply_backward_step conjD2 goal=10 facts=[12]
    #[14] ¬(n = 0)
  ← introduction goal=2
    #[15] x ≤ y ∨ n = 0
    #[16] x ^ n ≤ y ^ n
  ← apply_backward_step disjE goal=16 facts=[15]
    #[17] x ≤ y ⟶ x ^ n ≤ y ^ n
    #[18] n = 0 ⟶ x ^ n ≤ y ^ n
  ← introduction goal=17
    #[19] x ≤ y
    #[20] x ^ n ≤ y ^ n
  ← apply_backward_step exp_mono_le_imp goal=20
  ← introduction goal=18
    #[21] n = 0
    #[22] x ^ n ≤ y ^ n
  ← rewrite_goal_with_prev goal=22 facts=[21]
    #[23] x ^ (0::nat) ≤ y ^ (0::nat)
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=23
    #[24] (1::nat) ≤ 1
  ← apply_backward_step less_eq_refl goal=24
    #[25] (1::nat) ≤ 1
qed

theorem exp_mono_lt
  fixes n :: nat, x :: nat, y :: nat
  prop x ^ n < y ^ n <--> x < y & ~(n = 0)
  [hint_rewrite]
proof
  ← rewrite_goal not_le sym=true goal=0
    #[1] ¬(y ^ n ≤ x ^ n) ⟷ ¬(y ≤ x) ∧ ¬(n = 0)
  ← rewrite_goal exp_mono_le sym=false goal=1
    #[2] ¬(y ≤ x ∨ n = 0) ⟷ ¬(y ≤ x) ∧ ¬(n = 0)
  ← rewrite_goal de_morgan_thm2 sym=false goal=2
qed

theorem exp_mono_eq
  fixes n :: nat, x :: nat, y :: nat
  prop x ^ n = y ^ n <--> x = y | n = 0
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] x ^ n = y ^ n ⟶ x = y ∨ n = 0
    #[2] x = y ∨ n = 0 ⟶ x ^ n = y ^ n
  ← introduction goal=1
    #[3] x ^ n = y ^ n
    #[4] x = y ∨ n = 0
  -> rewrite_fact le_antisym sym=true goal=4 facts=[3]
    #[5] x ^ n ≤ y ^ n ∧ y ^ n ≤ x ^ n
  -> apply_forward_step conjD1 goal=4 facts=[5]
    #[6] x ^ n ≤ y ^ n
  -> apply_forward_step conjD2 goal=4 facts=[5]
    #[7] y ^ n ≤ x ^ n
  -> rewrite_fact exp_mono_le sym=false goal=4 facts=[6]
    #[8] x ≤ y ∨ n = 0
  -> rewrite_fact exp_mono_le sym=false goal=4 facts=[7]
    #[9] y ≤ x ∨ n = 0
  ← apply_backward_step disjE goal=4 facts=[8]
    #[10] x ≤ y ⟶ x = y ∨ n = 0
    #[11] n = 0 ⟶ x = y ∨ n = 0
  ← introduction goal=10
    #[12] x ≤ y
    #[13] x = y ∨ n = 0
  ← apply_backward_step disjE goal=13 facts=[9]
    #[14] y ≤ x ⟶ x = y ∨ n = 0
    #[15] n = 0 ⟶ x = y ∨ n = 0
  ← introduction goal=14
    #[16] y ≤ x
    #[17] x = y ∨ n = 0
  ← apply_backward_step disjI1 goal=17
    #[18] x = y
  ← rewrite_goal le_antisym sym=true goal=18
    #[19] x ≤ y ∧ y ≤ x
  ← apply_backward_step conjI goal=19
  ← introduction goal=15
    #[20] n = 0
    #[21] x = y ∨ n = 0
  ← apply_backward_step disjI2 goal=21
  ← introduction goal=11
    #[22] x = y ∨ n = 0
  ← apply_backward_step disjI2 goal=22
  ← introduction goal=2
    #[23] x = y ∨ n = 0
    #[24] x ^ n = y ^ n
  ← apply_backward_step disjE goal=24 facts=[23]
    #[25] x = y ⟶ x ^ n = y ^ n
    #[26] n = 0 ⟶ x ^ n = y ^ n
  ← introduction goal=25
    #[27] x = y
    #[28] x ^ n = y ^ n
  ← rewrite_goal_with_prev goal=28 facts=[27]
    #[29] x ^ n = y ^ n
  ← introduction goal=26
    #[30] x ^ n = y ^ n
  ← rewrite_goal_with_prev goal=30 facts=[20]
    #[31] x ^ (0::nat) = y ^ (0::nat)
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=31
    #[32] x ^ (0::nat) = y ^ (0::nat)
qed

theorem nat_WF
  fixes P :: nat => bool
  prop (!n. (!m. m < n --> P m) --> P n) --> (!n. P n)
proof
  ← introduction goal=0
    #[1] ∀n. (∀m. m < n ⟶ P m) ⟶ P n
    #[2] ∀n. P n
  ← introduction x goal=2
    #[3] _VAR x
    #[4] P x
  ← apply_prev goal=4 facts=[1]
    #[5] ∀m. m < x ⟶ P m
  ← induction x nat_induct goal=5
    #[6] ∀m. m < 0 ⟶ P m
    #[7] ∀n. (∀m. m < n ⟶ P m) ⟶ (∀m. m < Suc n ⟶ P m)
  ← rewrite_goal nat_less_def_1 sym=false goal=6
    #[8] ∀m. false ⟶ P m
  ← introduction m goal=8
    #[9] _VAR m
    #[10] false
    #[11] P m
  ← apply_resolve_step not_false_res goal=11 facts=[10]
    #[12] P m
  ← introduction x goal=7
    #[13] ∀m. m < x ⟶ P m
    #[14] ∀m. m < Suc x ⟶ P m
  ← rewrite_goal nat_less_def_2 sym=false goal=14
    #[15] ∀m. m = x ∨ m < x ⟶ P m
  ← introduction m goal=15
    #[16] m = x ∨ m < x
    #[17] P m
  ← apply_backward_step disjE goal=17 facts=[16]
    #[18] m = x ⟶ P m
    #[19] m < x ⟶ P m
  ← introduction goal=18
    #[20] m = x
    #[21] P m
  ← rewrite_goal_with_prev goal=21 facts=[20]
    #[22] P x
  ← apply_prev goal=22 facts=[1]
  ← introduction goal=19
    #[23] m < x
    #[24] P m
  ← apply_prev goal=24 facts=[13]
qed

theorem nat_WOP
  fixes P :: nat => bool
  prop (?n. P n) <--> (?n. P n & (!m. m < n --> ~(P m)))
proof
  ← apply_backward_step iffI goal=0
    #[1] (∃n. P n) ⟶ (∃n. P n ∧ (∀m. m < n ⟶ ¬(P m)))
    #[2] (∃n. P n ∧ (∀m. m < n ⟶ ¬(P m))) ⟶ (∃n. P n)
  ← introduction goal=1
    #[3] ∃n. P n
    #[4] ∃n. P n ∧ (∀m. m < n ⟶ ¬(P m))
  ← apply_backward_step contrapositive goal=4 facts=[3]
    #[5] ¬(∃n. P n ∧ (∀m. m < n ⟶ ¬(P m))) ⟶ ¬(∃n. P n)
  ← rewrite_goal not_exists sym=false goal=5
    #[6] (∀n. ¬(P n ∧ (∀m. m < n ⟶ ¬(P m)))) ⟶ (∀n. ¬(P n))
  ← introduction goal=6
    #[7] ∀n. ¬(P n ∧ (∀m. m < n ⟶ ¬(P m)))
    #[8] ∀n. ¬(P n)
  ← apply_backward_step nat_WF goal=8
    #[9] ∀n. (∀m. m < n ⟶ ¬(P m)) ⟶ ¬(P n)
  ← introduction n goal=9
    #[10] _VAR n
    #[11] ∀m. m < n ⟶ ¬(P m)
    #[12] ¬(P n)
  -> forall_elim n goal=12 facts=[7]
    #[13] ¬(P n ∧ (∀m. m < n ⟶ ¬(P m)))
  -> rewrite_fact de_morgan_thm1 sym=false goal=12 facts=[13]
    #[14] ¬(P n) ∨ ¬(∀m. m < n ⟶ ¬(P m))
  ← apply_backward_step disjE goal=12 facts=[14]
    #[15] ¬(P n) ⟶ ¬(P n)
    #[16] ¬(∀m. m < n ⟶ ¬(P m)) ⟶ ¬(P n)
  ← introduction goal=16
    #[17] ¬(∀m. m < n ⟶ ¬(P m))
    #[18] ¬(P n)
  ← apply_backward_step negE_gen goal=18 facts=[17,11]
    #[19] ¬(P n)
  ← introduction goal=2
    #[20] ∃n. P n ∧ (∀m. m < n ⟶ ¬(P m))
    #[21] ∃n. P n
  exists_elim n goal=21 facts=[20]
    #[22] P n ∧ (∀m. m < n ⟶ ¬(P m))
    #[23] ∃n. P n
  ← inst_exists_goal n goal=23
    #[24] P n
  ← apply_backward_step conjD1 goal=24 facts=[22]
    #[25] P n
qed

theorem nat_MAX
  fixes P :: nat => bool
  prop (?x. P x) & (?M. !x. P x --> x <= M) <--> (?m. P m & (!x. P x --> x <= m))
proof
  ← apply_backward_step iffI goal=0
    #[1] (∃x. P x) ∧ (∃M. ∀x. P x ⟶ x ≤ M) ⟶ (∃m. P m ∧ (∀x. P x ⟶ x ≤ m))
    #[2] (∃m. P m ∧ (∀x. P x ⟶ x ≤ m)) ⟶ (∃x. P x) ∧ (∃M. ∀x. P x ⟶ x ≤ M)
  ← introduction goal=1
    #[3] (∃x. P x) ∧ (∃M. ∀x. P x ⟶ x ≤ M)
    #[4] ∃m. P m ∧ (∀x. P x ⟶ x ≤ m)
  -> apply_forward_step conjD1 goal=4 facts=[3]
    #[5] ∃x. P x
  -> apply_forward_step conjD2 goal=4 facts=[3]
    #[6] ∃M. ∀x. P x ⟶ x ≤ M
  exists_elim a goal=4 facts=[5]
    #[7] _VAR a
    #[8] P a
    #[9] ∃m. P m ∧ (∀x. P x ⟶ x ≤ m)
  -> rewrite_fact nat_WOP sym="" goal=9 facts=[6]
    #[10] ∃M. (∀x. P x ⟶ x ≤ M) ∧ (∀m. m < M ⟶ ¬(∀x. P x ⟶ x ≤ m))
  exists_elim m goal=9 facts=[10]
    #[11] _VAR m
    #[12] (∀x. P x ⟶ x ≤ m) ∧ (∀m1. m1 < m ⟶ ¬(∀x. P x ⟶ x ≤ m1))
    #[13] ∃m. P m ∧ (∀x. P x ⟶ x ≤ m)
  ← inst_exists_goal m goal=13
    #[14] P m ∧ (∀x. P x ⟶ x ≤ m)
  -> apply_forward_step conjD1 goal=14 facts=[12]
    #[15] ∀x. P x ⟶ x ≤ m
  ← apply_backward_step conjI goal=14
    #[16] P m
  revert_intro goal=16 facts=[12]
    #[17] (∀x. P x ⟶ x ≤ m) ∧ (∀m1. m1 < m ⟶ ¬(∀x. P x ⟶ x ≤ m1)) ⟶ P m
  ← induction m nat_induct goal=17
    #[18] (∀x. P x ⟶ x ≤ 0) ∧ (∀m. m < 0 ⟶ ¬(∀x. P x ⟶ x ≤ m)) ⟶ P 0
    #[19] ∀n. ((∀x. P x ⟶ x ≤ n) ∧ (∀m. m < n ⟶ ¬(∀x. P x ⟶ x ≤ m)) ⟶ P n) ⟶ (∀x. P x ⟶ x ≤ Suc n) ∧ (∀m. m < Suc n ⟶ ¬(∀x. P x ⟶ x ≤ m)) ⟶ P (Suc n)
  ← introduction goal=18
    #[20] (∀x. P x ⟶ x ≤ 0) ∧ (∀m. m < 0 ⟶ ¬(∀x. P x ⟶ x ≤ m))
    #[21] P 0
  -> apply_forward_step conjD1 goal=21 facts=[20]
    #[22] ∀x. P x ⟶ x ≤ 0
  -> rewrite_fact nat_less_eq_def_1 sym=false goal=21 facts=[22]
    #[23] ∀x. P x ⟶ x = 0
  -> apply_fact goal=21 facts=[23,8]
    #[24] a = 0
  -> rewrite_fact_with_prev goal=21 facts=[24,8]
    #[25] P 0
  ← introduction m goal=19
    #[26] (∀x. P x ⟶ x ≤ m) ∧ (∀m1. m1 < m ⟶ ¬(∀x. P x ⟶ x ≤ m1)) ⟶ P m
    #[27] (∀x. P x ⟶ x ≤ Suc m) ∧ (∀m1. m1 < Suc m ⟶ ¬(∀x. P x ⟶ x ≤ m1))
    #[28] P (Suc m)
  -> apply_forward_step conjD2 goal=28 facts=[27]
    #[29] ∀m1. m1 < Suc m ⟶ ¬(∀x. P x ⟶ x ≤ m1)
  -> forall_elim m goal=28 facts=[29]
    #[30] m < Suc m ⟶ ¬(∀x. P x ⟶ x ≤ m)
  cut "m < Suc m " goal=28 facts=[30]
    #[31] m < Suc m
  ← rewrite_goal less_Suc_lesseq sym=false goal=31
    #[32] m ≤ m
  ← apply_backward_step less_eq_refl goal=32
    #[33] m ≤ m
  -> apply_fact goal=28 facts=[30,31]
    #[34] ¬(∀x. P x ⟶ x ≤ m)
  ← apply_backward_step contrapositive goal=28 facts=[34]
    #[35] ¬(P (Suc m)) ⟶ ¬¬(∀x. P x ⟶ x ≤ m)
  ← introduction goal=35
    #[36] ¬(P (Suc m))
    #[37] ¬¬(∀x. P x ⟶ x ≤ m)
  ← rewrite_goal double_neg sym=false goal=37
    #[38] ∀x. P x ⟶ x ≤ m
  ← introduction p goal=38
    #[39] _VAR p
    #[40] P p
    #[41] p ≤ m
  -> apply_forward_step conjD1 goal=41 facts=[27]
    #[42] ∀x. P x ⟶ x ≤ Suc m
  -> forall_elim p goal=41 facts=[42]
    #[43] P p ⟶ p ≤ Suc m
  -> apply_fact goal=41 facts=[43,40]
    #[44] p ≤ Suc m
  -> rewrite_fact nat_less_eq_def_2 sym=false goal=41 facts=[44]
    #[45] p = Suc m ∨ p ≤ m
  ← apply_backward_step disjE goal=41 facts=[45]
    #[46] p = Suc m ⟶ p ≤ m
    #[47] p ≤ m ⟶ p ≤ m
  ← introduction goal=46
    #[48] p = Suc m
    #[49] p ≤ m
  -> rewrite_fact_with_prev goal=49 facts=[48,40]
    #[50] P (Suc m)
  ← apply_backward_step negE_gen goal=49 facts=[36,50]
    #[51] p ≤ m
  ← introduction goal=2
    #[52] ∃m. P m ∧ (∀x. P x ⟶ x ≤ m)
    #[53] (∃x. P x) ∧ (∃M. ∀x. P x ⟶ x ≤ M)
  ← apply_backward_step ex_conj_distrib goal=53 facts=[52]
qed

theorem divmod_exist
  fixes m :: nat, n :: nat
  prop ~(n = 0) --> (?q. ?r. m = q * n + r & r < n)
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] ∃q. ∃r. m = q * n + r ∧ r < n
  -> apply_forward_step nat_WOP param_P="%r. ?q. m = q * n + r" goal=2
    #[3] (∃n1. ∃q. m = q * n + n1) ⟷ (∃n1. (∃q. m = q * n + n1) ∧ (∀m1. m1 < n1 ⟶ ¬(∃q. m = q * n + m1)))
  cut "!A. !B. (A<-->B)-->(A-->B)" goal=2
    #[4] ∀A. ∀B. A ⟷ B ⟶ A ⟶ B
  ← z3 goal=4
    #[5] ∀A. ∀B. A ⟷ B ⟶ A ⟶ B
  -> apply_fact goal=2 facts=[5,3]
    #[6] (∃n1. ∃q. m = q * n + n1) ⟶ (∃n1. (∃q. m = q * n + n1) ∧ (∀m1. m1 < n1 ⟶ ¬(∃q. m = q * n + m1)))
  -> rewrite_fact left_imp_exists_thm sym=false goal=2 facts=[6]
    #[7] ∀n1. (∃q. m = q * n + n1) ⟶ (∃n11. (∃q. m = q * n + n11) ∧ (∀m1. m1 < n11 ⟶ ¬(∃q. m = q * n + m1)))
  -> rewrite_fact left_imp_exists_thm sym=false goal=2 facts=[7]
    #[8] ∀n1. ∀q. m = q * n + n1 ⟶ (∃n11. (∃q1. m = q1 * n + n11) ∧ (∀m1. m1 < n11 ⟶ ¬(∃q1. m = q1 * n + m1)))
  -> forall_elim m goal=2 facts=[8]
    #[9] ∀q. m = q * n + m ⟶ (∃n1. (∃q1. m = q1 * n + n1) ∧ (∀m1. m1 < n1 ⟶ ¬(∃q1. m = q1 * n + m1)))
  -> forall_elim "(0::nat)" goal=2 facts=[9]
    #[10] m = 0 * n + m ⟶ (∃n1. (∃q. m = q * n + n1) ∧ (∀m1. m1 < n1 ⟶ ¬(∃q. m = q * n + m1)))
  cut "m = 0 * n + m" goal=2 facts=[10]
    #[11] m = 0 * n + m
  nat_norm goal=11
    #[12] m = 0 * n + m
  -> apply_fact goal=2 facts=[10,12]
    #[13] ∃n1. (∃q. m = q * n + n1) ∧ (∀m1. m1 < n1 ⟶ ¬(∃q. m = q * n + m1))
  exists_elim r goal=2 facts=[13]
    #[14] _VAR r
    #[15] (∃q. m = q * n + r) ∧ (∀m1. m1 < r ⟶ ¬(∃q. m = q * n + m1))
    #[16] ∃q. ∃r. m = q * n + r ∧ r < n
  -> apply_forward_step conjD1 goal=16 facts=[15]
    #[17] ∃q. m = q * n + r
  -> apply_forward_step conjD2 goal=16 facts=[15]
    #[18] ∀m1. m1 < r ⟶ ¬(∃q. m = q * n + m1)
  exists_elim q goal=16 facts=[17]
    #[19] _VAR q
    #[20] m = q * n + r
    #[21] ∃q. ∃r. m = q * n + r ∧ r < n
  ← inst_exists_goal q goal=21
    #[22] ∃r. m = q * n + r ∧ r < n
  ← inst_exists_goal r goal=22
    #[23] m = q * n + r ∧ r < n
  ← apply_backward_step conjI goal=23
    #[24] r < n
  ← apply_backward_step contrapositive goal=24 facts=[18]
    #[25] ¬(r < n) ⟶ ¬(∀m1. m1 < r ⟶ ¬(∃q. m = q * n + m1))
  ← introduction goal=25
    #[26] ¬(r < n)
    #[27] ¬(∀m1. m1 < r ⟶ ¬(∃q. m = q * n + m1))
  -> rewrite_fact not_lt sym=false goal=27 facts=[26]
    #[28] n ≤ r
  -> rewrite_fact less_eq_exist sym=false goal=27 facts=[28]
    #[29] ∃p. r = n + p
  exists_elim d goal=27 facts=[29]
    #[30] _VAR d
    #[31] r = n + d
    #[32] ¬(∀m1. m1 < r ⟶ ¬(∃q. m = q * n + m1))
  ← rewrite_goal not_all sym=false goal=32
    #[33] ∃m1. ¬(m1 < r ⟶ ¬(∃q. m = q * n + m1))
  ← inst_exists_goal d goal=33
    #[34] ¬(d < r ⟶ ¬(∃q. m = q * n + d))
  ← rewrite_goal not_imp sym=false goal=34
    #[35] d < r ∧ ¬¬(∃q. m = q * n + d)
  ← apply_backward_step conjI goal=35
    #[36] d < r
    #[37] ¬¬(∃q. m = q * n + d)
  ← z3 goal=36 facts=[1,31]
    #[38] d < r
  ← rewrite_goal double_neg sym=false goal=37
    #[39] ∃q. m = q * n + d
  ← inst_exists_goal q+1 goal=39
    #[40] m = (q + 1) * n + d
  ← rewrite_goal_with_prev goal=40 facts=[20]
    #[41] q * n + r = (q + 1) * n + d
  ← rewrite_goal_with_prev goal=41 facts=[31]
    #[42] q * n + (n + d) = (q + 1) * n + d
  nat_norm goal=42
    #[43] q * n + (n + d) = (q + 1) * n + d
qed

theorem divmod_exist_0
  fixes m :: nat, n :: nat
  prop ?q. ?r. if n = 0 then q = 0 & r = m else m = q * n + r & r < n
  [hint_backward]
proof
  ← cases "n= 0" goal=0
    #[1] n = 0 ⟶ (∃q. ∃r. if n = 0 then q = 0 ∧ r = m else m = q * n + r ∧ r < n)
    #[2] ¬(n = 0) ⟶ (∃q. ∃r. if n = 0 then q = 0 ∧ r = m else m = q * n + r ∧ r < n)
  ← introduction goal=1
    #[3] n = 0
    #[4] ∃q. ∃r. if n = 0 then q = 0 ∧ r = m else m = q * n + r ∧ r < n
  ← rewrite_goal if_P sym=false goal=4 facts=[3]
    #[5] ∃q::nat. ∃r. q = 0 ∧ r = m
  ← inst_exists_goal "(0::nat)" goal=5
    #[6] ∃r. (0::nat) = 0 ∧ r = m
  ← inst_exists_goal m goal=6
    #[7] (0::nat) = 0 ∧ m = m
  ← apply_backward_step conjI goal=7
    #[8] (0::nat) = 0
    #[9] m = m
  ← apply_backward_step eq_refl goal=8
    #[10] (0::nat) = 0
  ← apply_backward_step eq_refl goal=9
    #[11] m = m
  ← introduction goal=2
    #[12] ¬(n = 0)
    #[13] ∃q. ∃r. if n = 0 then q = 0 ∧ r = m else m = q * n + r ∧ r < n
  ← rewrite_goal if_not_P sym=false goal=13 facts=[12]
    #[14] ∃q. ∃r. m = q * n + r ∧ r < n
  ← apply_backward_step divmod_exist goal=14
qed

axiom division_0
  fixes m :: nat, n :: nat
  prop if n = 0 then m DIV n = 0 ∧ m MOD n = m else m = m DIV n * n + m MOD n ∧ m MOD n < n
  [hint_backward]

theorem division
  fixes m :: nat, n :: nat
  prop ~(n = 0) --> m = m DIV n * n + m MOD n & m MOD n < n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> apply_forward_step division_0 param_m=m param_n=n goal=2
    #[3] if n = 0 then m DIV n = 0 ∧ m MOD n = m else m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> rewrite_fact if_not_P sym=false goal=2 facts=[3,1]
qed

theorem div_zero
  fixes n :: nat
  prop n DIV 0 = 0
  [hint_rewrite]
proof
  -> apply_forward_step division_0 param_m=n param_n="(0::nat)" goal=0
    #[1] if (0::nat) = 0 then n DIV 0 = 0 ∧ n MOD 0 = n else n = n DIV 0 * 0 + n MOD 0 ∧ n MOD 0 < 0
  cut "(0::nat) = 0" goal=0 facts=[1]
    #[2] (0::nat) = 0
  ← apply_backward_step eq_refl goal=2
  -> rewrite_fact if_P sym=false goal=0 facts=[1,2]
    #[3] n DIV 0 = 0 ∧ n MOD 0 = n
  ← apply_backward_step conjD1 goal=0 facts=[3]
qed

theorem mod_zero
  fixes n :: nat
  prop n MOD 0 = n
  [hint_rewrite]
proof
  -> apply_forward_step division_0 param_m=n param_n="(0::nat)" goal=0
    #[1] if (0::nat) = 0 then n DIV 0 = 0 ∧ n MOD 0 = n else n = n DIV 0 * 0 + n MOD 0 ∧ n MOD 0 < 0
  cut "(0::nat) = 0" goal=0
    #[2] (0::nat) = 0
  ← apply_backward_step eq_refl goal=2
  -> rewrite_fact if_P sym=false goal=0 facts=[1,2]
    #[3] n DIV 0 = 0 ∧ n MOD 0 = n
  ← apply_backward_step conjD2 goal=0 facts=[3]
qed

theorem division_simp1
  fixes m :: nat, n :: nat
  prop m DIV n * n + m MOD n = m
  [hint_rewrite]
proof
  ← cases "n = 0" goal=0
    #[1] n = 0 ⟶ m DIV n * n + m MOD n = m
    #[2] ¬(n = 0) ⟶ m DIV n * n + m MOD n = m
  ← introduction goal=1
    #[3] n = 0
    #[4] m DIV n * n + m MOD n = m
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m DIV 0 * 0 + m MOD 0 = m
  ← rewrite_goal div_zero sym=false goal=5
    #[6] 0 * 0 + m MOD 0 = m
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] 0 * 0 + m = m
  ← rewrite_goal nat_times_def_1 sym=false goal=7
    #[8] 0 + m = m
  ← rewrite_goal nat_plus_def_1 sym=false goal=8
    #[9] 0 + m = m
  ← introduction goal=2
    #[10] ¬(n = 0)
    #[11] m DIV n * n + m MOD n = m
  -> apply_forward_step division param_m=m goal=11 facts=[10]
    #[12] m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> apply_forward_step conjD1 goal=11 facts=[12]
    #[13] m = m DIV n * n + m MOD n
  ← rewrite_goal eq_sym_eq sym=false goal=11
qed

theorem division_simp2
  fixes m :: nat, n :: nat
  prop n * (m DIV n) + m MOD n = m
  [hint_rewrite]
proof
  ← rewrite_goal mult_comm sym=false goal=0
    #[1] m DIV n * n + m MOD n = m
  ← rewrite_goal division_simp1 sym=false goal=1
qed

theorem eq_divmod
  fixes m :: nat, n :: nat, p :: nat
  prop m DIV p = n DIV p & m MOD p = n MOD p <--> m = n
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] m DIV p = n DIV p ∧ m MOD p = n MOD p ⟶ m = n
    #[2] m = n ⟶ m DIV p = n DIV p ∧ m MOD p = n MOD p
  ← introduction goal=1
    #[3] m DIV p = n DIV p ∧ m MOD p = n MOD p
    #[4] m = n
  -> apply_forward_step division_simp1 param_m=m param_n=p goal=4
    #[5] m DIV p * p + m MOD p = m
  -> apply_forward_step division_simp1 param_m=n param_n=p goal=4
    #[6] n DIV p * p + n MOD p = n
  -> rewrite_fact eq_sym_eq sym=false goal=4 facts=[5]
    #[7] m = m DIV p * p + m MOD p
  -> rewrite_fact eq_sym_eq sym=false goal=4 facts=[6]
    #[8] n = n DIV p * p + n MOD p
  ← rewrite_goal_with_prev goal=4 facts=[7]
    #[9] m DIV p * p + m MOD p = n
  ← rewrite_goal_with_prev goal=9 facts=[8]
    #[10] m DIV p * p + m MOD p = n DIV p * p + n MOD p
  -> apply_forward_step conjD1 goal=10 facts=[3]
    #[11] m DIV p = n DIV p
  -> apply_forward_step conjD2 goal=10 facts=[3]
    #[12] m MOD p = n MOD p
  ← rewrite_goal_with_prev goal=10 facts=[11]
    #[13] n DIV p * p + m MOD p = n DIV p * p + n MOD p
  ← rewrite_goal_with_prev goal=13 facts=[12]
  ← introduction goal=2
    #[14] m = n
    #[15] m DIV p = n DIV p ∧ m MOD p = n MOD p
  ← rewrite_goal_with_prev goal=15 facts=[14]
    #[16] n DIV p = n DIV p ∧ n MOD p = n MOD p
  ← apply_backward_step conjI goal=16
    #[17] n DIV p = n DIV p
    #[18] n MOD p = n MOD p
  ← apply_backward_step eq_refl goal=17
    #[19] n DIV p = n DIV p
  ← apply_backward_step eq_refl goal=18
    #[20] n MOD p = n MOD p
qed

theorem mod_lt_eq
  fixes m :: nat, n :: nat
  prop m MOD n < n <--> ~(n = 0)
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] m MOD n < n ⟶ ¬(n = 0)
    #[2] ¬(n = 0) ⟶ m MOD n < n
  ← introduction goal=1
    #[3] m MOD n < n
    #[4] ¬(n = 0)
  ← apply_backward_step contrapositive goal=4 facts=[3]
    #[5] ¬¬(n = 0) ⟶ ¬(m MOD n < n)
  ← introduction goal=5
    #[6] ¬¬(n = 0)
    #[7] ¬(m MOD n < n)
  -> rewrite_fact double_neg sym=false goal=7 facts=[6]
    #[8] n = 0
  ← rewrite_goal_with_prev goal=7 facts=[8]
    #[9] ¬(m MOD 0 < 0)
  ← rewrite_goal nat_less_def_1 sym=false goal=9
    #[10] ¬false
  ← apply_backward_step negI goal=10
    #[11] false ⟶ false
  ← introduction goal=2
    #[12] ¬(n = 0)
    #[13] m MOD n < n
  -> apply_forward_step division param_m=m goal=13 facts=[12]
    #[14] m = m DIV n * n + m MOD n ∧ m MOD n < n
  ← apply_backward_step conjD2 goal=13 facts=[14]
qed

theorem mod_lt_eq_lt
  fixes m :: nat, n :: nat
  prop m MOD n < n <--> 0 < n
  [hint_rewrite]
proof
  ← rewrite_goal mod_lt_eq sym=false goal=0
    #[1] ¬(n = 0) ⟷ 0 < n
  ← apply_backward_step iffI goal=1
    #[2] ¬(n = 0) ⟶ 0 < n
    #[3] 0 < n ⟶ ¬(n = 0)
  ← introduction goal=2
    #[4] ¬(n = 0)
    #[5] 0 < n
  ← apply_backward_step le_1_1 goal=5
  ← introduction goal=3
    #[6] 0 < n
    #[7] ¬(n = 0)
  ← apply_backward_step contrapositive goal=7 facts=[6]
    #[8] ¬¬(n = 0) ⟶ ¬(0 < n)
  ← rewrite_goal not_lt sym=false goal=8
    #[9] ¬¬(n = 0) ⟶ n ≤ 0
  ← introduction goal=9
    #[10] ¬¬(n = 0)
    #[11] n ≤ 0
  -> rewrite_fact double_neg sym=false goal=11 facts=[10]
    #[12] n = 0
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=11
qed

theorem divmod_uniq_lemma
  fixes m :: nat, n :: nat, q1 :: nat, q2 :: nat, r1 :: nat, r2 :: nat
  prop m = q1 * n + r1 --> r1 < n --> m = q2 * n + r2 --> r2 < n --> q1 = q2 & r1 = r2
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m = q1 * n + r1
    #[2] r1 < n
    #[3] m = q2 * n + r2
    #[4] r2 < n
    #[5] q1 = q2 ∧ r1 = r2
  cut "r1 = r2" goal=5
    #[6] r1 = r2
  -> rewrite_fact_with_prev goal=6 facts=[1,3]
    #[7] q1 * n + r1 = q2 * n + r2
  -> apply_forward_step le_cases param_m=q1 param_n=q2 goal=6
    #[8] q1 ≤ q2 ∨ q2 ≤ q1
  ← apply_backward_step disjE goal=6 facts=[8]
    #[9] q1 ≤ q2 ⟶ r1 = r2
    #[10] q2 ≤ q1 ⟶ r1 = r2
  ← introduction goal=9
    #[11] q1 ≤ q2
    #[12] r1 = r2
  -> rewrite_fact less_eq_exist sym=false goal=12 facts=[11]
    #[13] ∃p. q2 = q1 + p
  exists_elim d goal=12 facts=[13]
    #[14] _VAR d
    #[15] q2 = q1 + d
    #[16] r1 = r2
  revert_intro goal=16 facts=[15]
    #[17] q2 = q1 + d ⟶ r1 = r2
  ← induction d nat_induct goal=17
    #[18] q2 = q1 + 0 ⟶ r1 = r2
    #[19] ∀n. (q2 = q1 + n ⟶ r1 = r2) ⟶ q2 = q1 + Suc n ⟶ r1 = r2
  ← introduction goal=18
    #[20] q2 = q1 + 0
    #[21] r1 = r2
  -> rewrite_fact add_0_right sym=false goal=21 facts=[20]
    #[22] q2 = q1
  -> rewrite_fact_with_prev goal=21 facts=[22,7]
    #[23] q1 * n + r1 = q1 * n + r2
  ← apply_backward_step add_cancel_left goal=21 facts=[23]
    #[24] r1 = r2
  ← introduction d goal=19
    #[25] q2 = q1 + d ⟶ r1 = r2
    #[26] q2 = q1 + Suc d
    #[27] r1 = r2
  -> rewrite_fact_with_prev goal=27 facts=[26,7]
    #[28] q1 * n + r1 = (q1 + Suc d) * n + r2
  -> rewrite_fact distrib_r sym=false goal=27 facts=[28]
    #[29] q1 * n + r1 = q1 * n + Suc d * n + r2
  -> rewrite_fact add_assoc sym=false goal=27 facts=[29]
    #[30] q1 * n + r1 = q1 * n + (Suc d * n + r2)
  -> apply_forward_step add_cancel_left goal=27 facts=[30]
    #[31] r1 = Suc d * n + r2
  cut "Suc d * n <= r1" goal=27 facts=[31]
    #[32] Suc d * n ≤ r1
  ← rewrite_goal_with_prev goal=32 facts=[31]
    #[33] Suc d * n ≤ Suc d * n + r2
  ← apply_backward_step le_add goal=33
    #[34] Suc d * n ≤ Suc d * n + r2
  -> rewrite_fact nat_times_def_2 sym=false goal=27 facts=[32]
    #[35] n + d * n ≤ r1
  ← z3 goal=27 facts=[35,2]
  ← introduction goal=10
    #[36] q2 ≤ q1
    #[37] r1 = r2
  -> rewrite_fact less_eq_exist sym=false goal=37 facts=[36]
    #[38] ∃p. q1 = q2 + p
  exists_elim d goal=37 facts=[38]
    #[39] q1 = q2 + d
    #[40] r1 = r2
  revert_intro goal=40 facts=[39]
    #[41] q1 = q2 + d ⟶ r1 = r2
  ← induction d nat_induct goal=41
    #[42] q1 = q2 + 0 ⟶ r1 = r2
    #[43] ∀n. (q1 = q2 + n ⟶ r1 = r2) ⟶ q1 = q2 + Suc n ⟶ r1 = r2
  ← introduction goal=42
    #[44] q1 = q2 + 0
    #[45] r1 = r2
  -> rewrite_fact add_0_right sym=false goal=45 facts=[44]
    #[46] q1 = q2
  -> rewrite_fact_with_prev goal=45 facts=[46,7]
    #[47] q2 * n + r1 = q2 * n + r2
  ← apply_backward_step add_cancel_left goal=45 facts=[47]
    #[48] r1 = r2
  ← introduction d goal=43
    #[49] q1 = q2 + d ⟶ r1 = r2
    #[50] q1 = q2 + Suc d
    #[51] r1 = r2
  -> rewrite_fact_with_prev goal=51 facts=[50,7]
    #[52] (q2 + Suc d) * n + r1 = q2 * n + r2
  -> rewrite_fact distrib_r sym=false goal=51 facts=[52]
    #[53] q2 * n + Suc d * n + r1 = q2 * n + r2
  -> rewrite_fact add_assoc sym=false goal=51 facts=[53]
    #[54] q2 * n + (Suc d * n + r1) = q2 * n + r2
  -> apply_forward_step add_cancel_left goal=51 facts=[54]
    #[55] Suc d * n + r1 = r2
  cut "Suc d * n <= r2" goal=51
    #[56] Suc d * n ≤ r2
  -> rewrite_fact eq_sym_eq sym=false goal=56 facts=[55]
    #[57] r2 = Suc d * n + r1
  ← rewrite_goal_with_prev goal=56 facts=[57]
    #[58] Suc d * n ≤ Suc d * n + r1
  ← apply_backward_step le_add goal=58
    #[59] Suc d * n ≤ Suc d * n + r1
  -> rewrite_fact nat_times_def_2 sym=false goal=51 facts=[56]
    #[60] n + d * n ≤ r2
  ← z3 goal=51 facts=[60,4]
  ← apply_backward_step conjI goal=5
    #[61] q1 = q2
  -> rewrite_fact_with_prev goal=61 facts=[6,7]
    #[62] q1 * n + r2 = q2 * n + r2
  -> apply_forward_step add_cancel_right goal=61 facts=[62]
    #[63] q1 * n = q2 * n
  -> rewrite_fact eq_mult_rcancel sym=false goal=61 facts=[63]
    #[64] n = 0 ∨ q1 = q2
  ← apply_backward_step disjE goal=61 facts=[64]
    #[65] n = 0 ⟶ q1 = q2
    #[66] q1 = q2 ⟶ q1 = q2
  ← introduction goal=65
    #[67] n = 0
    #[68] q1 = q2
  ← z3 goal=68 facts=[67,2]
    #[69] q1 = q2
qed

theorem divmod_uniq
  fixes m :: nat, n :: nat, q :: nat, r :: nat
  prop m = q * n + r --> r < n --> m DIV n = q & m MOD n = r
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m = q * n + r
    #[2] r < n
    #[3] m DIV n = q ∧ m MOD n = r
  ← apply_backward_step divmod_uniq_lemma param_m=m param_n=n goal=3
    #[4] m = m DIV n * n + m MOD n
    #[5] m MOD n < n
  ← rewrite_goal division_simp1 sym=false goal=4
    #[6] m = m DIV n * n + m MOD n
  ← rewrite_goal mod_lt_eq sym=false goal=5
    #[7] ¬(n = 0)
  ← z3 goal=7 facts=[2]
    #[8] ¬(n = 0)
qed

theorem mod_uniq
  fixes m :: nat, n :: nat, q :: nat, r :: nat
  prop m = q * n + r --> r < n --> m MOD n = r
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m = q * n + r
    #[2] r < n
    #[3] m MOD n = r
  -> apply_forward_step divmod_uniq goal=3 facts=[1,2]
    #[4] m DIV n = q ∧ m MOD n = r
  ← apply_backward_step conjD2 goal=3 facts=[4]
qed

theorem div_uniq
  fixes m :: nat, n :: nat, q :: nat, r :: nat
  prop m = q * n + r --> r < n --> m DIV n = q
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m = q * n + r
    #[2] r < n
    #[3] m DIV n = q
  -> apply_forward_step divmod_uniq goal=3 facts=[1,2]
    #[4] m DIV n = q ∧ m MOD n = r
  ← apply_backward_step conjD1 goal=3 facts=[4]
qed

theorem div_0
  fixes n :: nat
  prop 0 DIV n = 0
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ 0 DIV n = 0
    #[2] ¬(n = 0) ⟶ 0 DIV n = 0
  ← introduction goal=1
    #[3] n = 0
    #[4] 0 DIV n = 0
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] 0 DIV 0 = 0
  ← rewrite_goal div_zero sym=false goal=5
    #[6] 0 DIV 0 = 0
  ← introduction goal=2
    #[7] ¬(n = 0)
    #[8] 0 DIV n = 0
  ← apply_backward_step div_uniq param_r="(0::nat)" goal=8
    #[9] 0 = 0 * n + 0
    #[10] 0 < n
  nat_norm goal=9
    #[11] 0 = 0 * n + 0
  ← apply_backward_step le_1_1 goal=10
qed

theorem mod_0
  fixes n :: nat
  prop 0 MOD n = 0
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ 0 MOD n = 0
    #[2] ¬(n = 0) ⟶ 0 MOD n = 0
  ← introduction goal=1
    #[3] n = 0
    #[4] 0 MOD n = 0
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] 0 MOD 0 = 0
  ← rewrite_goal mod_zero sym=false goal=5
    #[6] 0 MOD 0 = 0
  ← introduction goal=2
    #[7] ¬(n = 0)
    #[8] 0 MOD n = 0
  ← apply_backward_step mod_uniq param_q="(0::nat)" goal=8
    #[9] 0 = 0 * n + 0
    #[10] 0 < n
  nat_norm goal=9
    #[11] 0 = 0 * n + 0
  ← apply_backward_step le_1_1 goal=10
qed

theorem div_mult
  fixes m :: nat, n :: nat
  prop ~(m = 0) --> m * n DIV m = n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(m = 0)
    #[2] m * n DIV m = n
  ← apply_backward_step div_uniq param_r="(0::nat)" goal=2
    #[3] m * n = n * m + 0
    #[4] 0 < m
  nat_norm goal=3
    #[5] m * n = n * m + 0
  ← apply_backward_step le_1_1 goal=4
qed

theorem mod_mult
  fixes m :: nat, n :: nat
  prop m * n MOD m = 0
  [hint_rewrite]
proof
  ← cases "m=0" goal=0
    #[1] m = 0 ⟶ m * n MOD m = 0
    #[2] ¬(m = 0) ⟶ m * n MOD m = 0
  ← introduction goal=1
    #[3] m = 0
    #[4] m * n MOD m = 0
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] 0 * n MOD 0 = 0
  ← rewrite_goal nat_times_def_1 sym=false goal=5
    #[6] 0 MOD 0 = 0
  ← rewrite_goal mod_0 sym=false goal=6
    #[7] 0 MOD 0 = 0
  ← introduction goal=2
    #[8] ¬(m = 0)
    #[9] m * n MOD m = 0
  ← apply_backward_step mod_uniq param_q=n goal=9
    #[10] m * n = n * m + 0
    #[11] 0 < m
  nat_norm goal=10
    #[12] m * n = n * m + 0
  ← apply_backward_step le_1_1 goal=11
qed

theorem mod_lt
  fixes m :: nat, n :: nat
  prop m < n --> m MOD n = m
  [hint_backward]
proof
  ← introduction goal=0
    #[1] m < n
    #[2] m MOD n = m
  ← apply_backward_step mod_uniq param_q="(0::nat)" goal=2
    #[3] m = 0 * n + m
  nat_norm goal=3
    #[4] m = 0 * n + m
qed

theorem mod_eq_self
  fixes m :: nat, n :: nat
  prop m MOD n = m <--> n = 0 | m < n
  [hint_rewrite]
proof
  ← apply_backward_step iffI goal=0
    #[1] m MOD n = m ⟶ n = 0 ∨ m < n
    #[2] n = 0 ∨ m < n ⟶ m MOD n = m
  ← introduction goal=1
    #[3] m MOD n = m
    #[4] n = 0 ∨ m < n
  ← cases "n=0" goal=4
    #[5] n = 0 ⟶ n = 0 ∨ m < n
    #[6] ¬(n = 0) ⟶ n = 0 ∨ m < n
  ← introduction goal=5
    #[7] n = 0
    #[8] n = 0 ∨ m < n
  ← apply_backward_step disjI1 goal=8 facts=[7]
    #[9] n = 0 ∨ m < n
  ← introduction goal=6
    #[10] ¬(n = 0)
    #[11] n = 0 ∨ m < n
  ← apply_backward_step disjI2 goal=11
    #[12] m < n
  -> rewrite_fact eq_sym_eq sym=false goal=12 facts=[3]
    #[13] m = m MOD n
  ← rewrite_goal_with_prev goal=12 facts=[13]
    #[14] m MOD n < n
  ← rewrite_goal mod_lt_eq sym=false goal=14
  ← introduction goal=2
    #[15] n = 0 ∨ m < n
    #[16] m MOD n = m
  ← apply_backward_step disjE goal=16 facts=[15]
    #[17] n = 0 ⟶ m MOD n = m
    #[18] m < n ⟶ m MOD n = m
  ← introduction goal=17
    #[19] m MOD n = m
  ← rewrite_goal_with_prev goal=19 facts=[7]
    #[20] m MOD 0 = m
  ← rewrite_goal mod_zero sym=false goal=20
    #[21] m MOD 0 = m
  ← introduction goal=18
    #[22] m < n
    #[23] m MOD n = m
  ← apply_backward_step mod_lt goal=23
qed

theorem mod_cases
  fixes n :: nat, p :: nat
  prop n < 2 * p --> n MOD p = (if n < p then n else n - p)
  [hint_backward]
proof
  ← introduction goal=0
    #[1] n < 2 * p
    #[2] n MOD p = (if n < p then n else n - p)
  ← cases "n < p" goal=2
    #[3] n < p ⟶ n MOD p = (if n < p then n else n - p)
    #[4] ¬(n < p) ⟶ n MOD p = (if n < p then n else n - p)
  ← introduction goal=3
    #[5] n < p
    #[6] n MOD p = (if n < p then n else n - p)
  ← rewrite_goal if_P sym=false goal=6 facts=[5]
    #[7] n MOD p = n
  ← apply_backward_step mod_lt goal=7
  ← introduction goal=4
    #[8] ¬(n < p)
    #[9] n MOD p = (if n < p then n else n - p)
  ← rewrite_goal if_not_P sym=false goal=9 facts=[8]
    #[10] n MOD p = n - p
  ← apply_backward_step mod_uniq param_q="(1::nat)" goal=10
    #[11] n = 1 * p + (n - p)
    #[12] n - p < p
  ← rewrite_goal mult_1_left sym=false goal=11
    #[13] n = p + (n - p)
  ← z3 goal=13 facts=[8]
    #[14] n = p + (n - p)
  ← z3 goal=12 facts=[1]
    #[15] n - p < p
qed

theorem mod_add_cases
  fixes m :: nat, n :: nat, p :: nat
  prop m < p --> n < p --> (m + n) MOD p = (if m + n < p then m + n else m + n - p)
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] m < p
    #[2] n < p
    #[3] (m + n) MOD p = (if m + n < p then m + n else m + n - p)
  ← apply_backward_step mod_cases goal=3
    #[4] m + n < 2 * p
  ← rewrite_goal mult_2 sym=false goal=4
    #[5] m + n < p + p
  ← apply_backward_step lt_add2 goal=5
qed

theorem mod_eq
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop m = n + q * p --> m MOD p = n MOD p
  [hint_backward]
proof
  ← introduction goal=0
    #[1] m = n + q * p
    #[2] m MOD p = n MOD p
  ← cases "p=0" goal=2
    #[3] p = 0 ⟶ m MOD p = n MOD p
    #[4] ¬(p = 0) ⟶ m MOD p = n MOD p
  ← introduction goal=3
    #[5] p = 0
    #[6] m MOD p = n MOD p
  -> rewrite_fact_with_prev goal=6 facts=[5,1]
    #[7] m = n + q * 0
  -> rewrite_fact mult_0_right sym=false goal=6 facts=[7]
    #[8] m = n + 0
  -> rewrite_fact add_0_right sym=false goal=6 facts=[8]
    #[9] m = n
  ← rewrite_goal_with_prev goal=6 facts=[9]
  ← introduction goal=4
    #[10] ¬(p = 0)
    #[11] m MOD p = n MOD p
  ← apply_backward_step mod_uniq param_q="q + n DIV p" goal=11
    #[12] m = (q + n DIV p) * p + n MOD p
    #[13] n MOD p < p
  ← rewrite_goal_with_prev goal=12 facts=[1]
    #[14] n + q * p = (q + n DIV p) * p + n MOD p
  ← rewrite_goal distrib_r sym=false goal=14
    #[15] n + q * p = q * p + n DIV p * p + n MOD p
  ← rewrite_goal add_assoc sym=false goal=15
    #[16] n + q * p = q * p + (n DIV p * p + n MOD p)
  ← rewrite_goal division_simp1 sym=false goal=16
    #[17] n + q * p = q * p + n
  nat_norm goal=17
    #[18] n + q * p = q * p + n
  ← rewrite_goal mod_lt_eq sym=false goal=13
qed

theorem div_le
  fixes m :: nat, n :: nat
  prop m DIV n <= m
  [hint_backward]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ m DIV n ≤ m
    #[2] ¬(n = 0) ⟶ m DIV n ≤ m
  ← introduction goal=1
    #[3] n = 0
    #[4] m DIV n ≤ m
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m DIV 0 ≤ m
  ← rewrite_goal div_zero sym=false goal=5
    #[6] 0 ≤ m
  ← apply_backward_step le_0 goal=6
    #[7] 0 ≤ m
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] m DIV n ≤ m
  -> apply_forward_step division param_m=m goal=9 facts=[8]
    #[10] m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> apply_forward_step conjD1 goal=9 facts=[10]
    #[11] m = m DIV n * n + m MOD n
  ← rewrite_goal_with_prev goal=9 facts=[11]
    #[12] (m DIV n * n + m MOD n) DIV n ≤ m DIV n * n + m MOD n
  cut "(m DIV n * n + m MOD n) DIV n = m DIV n" goal=12
    #[13] (m DIV n * n + m MOD n) DIV n = m DIV n
  ← rewrite_goal division_simp1 sym=false goal=13
    #[14] (m DIV n * n + m MOD n) DIV n = m DIV n
  ← rewrite_goal_with_prev goal=12 facts=[14]
    #[15] m DIV n ≤ m DIV n * n + m MOD n
  revert_intro goal=15 facts=[8]
    #[16] ¬(n = 0) ⟶ m DIV n ≤ m DIV n * n + m MOD n
  ← induction n nat_induct goal=16
    #[17] ¬((0::nat) = 0) ⟶ m DIV 0 ≤ m DIV 0 * 0 + m MOD 0
    #[18] ∀n. (¬(n = 0) ⟶ m DIV n ≤ m DIV n * n + m MOD n) ⟶ ¬(Suc n = 0) ⟶ m DIV Suc n ≤ m DIV Suc n * Suc n + m MOD Suc n
  ← introduction goal=17
    #[19] ¬((0::nat) = 0)
    #[20] m DIV 0 ≤ m DIV 0 * 0 + m MOD 0
  ← apply_backward_step negE_gen goal=20 facts=[19]
    #[21] (0::nat) = 0
  ← apply_backward_step eq_refl goal=21
    #[22] (0::nat) = 0
  ← introduction n goal=18
    #[23] _VAR n
    #[24] ¬(n = 0) ⟶ m DIV n ≤ m DIV n * n + m MOD n
    #[25] ¬(Suc n = 0)
    #[26] m DIV Suc n ≤ m DIV Suc n * Suc n + m MOD Suc n
  ← rewrite_goal mult_Suc_right sym=false goal=26
    #[27] m DIV Suc n ≤ m DIV Suc n + m DIV Suc n * n + m MOD Suc n
  ← rewrite_goal add_assoc sym=false goal=27
    #[28] m DIV Suc n ≤ m DIV Suc n + (m DIV Suc n * n + m MOD Suc n)
  ← apply_backward_step le_add goal=28
    #[29] m DIV Suc n ≤ m DIV Suc n + (m DIV Suc n * n + m MOD Suc n)
qed

theorem div_mul_le
  fixes m :: nat, n :: nat
  prop n * (m DIV n) <= m
  [hint_backward]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ n * (m DIV n) ≤ m
    #[2] ¬(n = 0) ⟶ n * (m DIV n) ≤ m
  ← introduction goal=1
    #[3] n = 0
    #[4] n * (m DIV n) ≤ m
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] 0 * (m DIV 0) ≤ m
  ← rewrite_goal nat_times_def_1 sym=false goal=5
    #[6] 0 ≤ m
  ← apply_backward_step le_0 goal=6
    #[7] 0 ≤ m
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] n * (m DIV n) ≤ m
  -> apply_forward_step division param_m=m goal=9 facts=[8]
    #[10] m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> apply_forward_step conjD1 goal=9 facts=[10]
    #[11] m = m DIV n * n + m MOD n
  ← rewrite_goal mult_comm sym=false goal=9
    #[12] m DIV n * n ≤ m
  -> apply_forward_step division param_m=m goal=12 facts=[8]
  -> apply_forward_step conjD1 goal=12 facts=[10]
  ← rewrite_goal_with_prev goal=12 facts=[11]
    #[13] (m DIV n * n + m MOD n) DIV n * n ≤ m DIV n * n + m MOD n
  cut "(m DIV n * n + m MOD n) DIV n = m DIV n" goal=13
    #[14] (m DIV n * n + m MOD n) DIV n = m DIV n
  ← rewrite_goal division_simp1 sym=false goal=14
    #[15] (m DIV n * n + m MOD n) DIV n = m DIV n
  ← rewrite_goal_with_prev goal=13 facts=[15]
    #[16] m DIV n * n ≤ m DIV n * n + m MOD n
  ← apply_backward_step le_add goal=16
    #[17] m DIV n * n ≤ m DIV n * n + m MOD n
qed

theorem mod_le_twice
  fixes m :: nat, n :: nat
  prop 0 < m --> m <= n --> 2 * (n MOD m) <= n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] 0 < m
    #[2] m ≤ n
    #[3] 2 * (n MOD m) ≤ n
  ← cases "2 * m <= n" goal=3
    #[4] 2 * m ≤ n ⟶ 2 * (n MOD m) ≤ n
    #[5] ¬(2 * m ≤ n) ⟶ 2 * (n MOD m) ≤ n
  ← introduction goal=4
    #[6] 2 * m ≤ n
    #[7] 2 * (n MOD m) ≤ n
  ← apply_backward_step less_eq_trans param_m="2 * m" goal=7
    #[8] 2 * (n MOD m) ≤ 2 * m
  ← apply_backward_step le_mult2 goal=8
    #[9] (2::nat) ≤ 2
    #[10] n MOD m ≤ m
  ← apply_backward_step less_eq_refl goal=9
    #[11] (2::nat) ≤ 2
  ← apply_backward_step lt_imp_le goal=10
    #[12] n MOD m < m
  ← rewrite_goal mod_lt_eq_lt sym=false goal=12
  ← introduction goal=5
    #[13] ¬(2 * m ≤ n)
    #[14] 2 * (n MOD m) ≤ n
  ← apply_backward_step less_eq_trans param_m="m + n MOD m" goal=14
    #[15] 2 * (n MOD m) ≤ m + n MOD m
    #[16] m + n MOD m ≤ n
  ← rewrite_goal mult_2 sym=false goal=15
    #[17] n MOD m + n MOD m ≤ m + n MOD m
  ← apply_backward_step le_add2 goal=17
    #[18] n MOD m ≤ m
    #[19] n MOD m ≤ n MOD m
  ← apply_backward_step lt_imp_le goal=18
    #[20] n MOD m < m
  ← rewrite_goal mod_lt_eq_lt sym=false goal=20
  ← apply_backward_step less_eq_refl goal=19
    #[21] n MOD m ≤ n MOD m
  ← rewrite_goal add_comm sym=false goal=16
    #[22] n MOD m + m ≤ n
  cut "n MOD m = n - m" goal=22
    #[23] n MOD m = n - m
  ← apply_backward_step mod_uniq param_q="(1::nat)" goal=23
    #[24] n = 1 * m + (n - m)
    #[25] n - m < m
  ← rewrite_goal mult_1_left sym=false goal=24
    #[26] n = m + (n - m)
  ← rewrite_goal add_comm sym=false goal=26
    #[27] n = n - m + m
  ← rewrite_goal eq_sym_eq sym=false goal=27
    #[28] n - m + m = n
  ← apply_backward_step sub_add goal=28
  ← z3 goal=25 facts=[13]
    #[29] n - m < m
  ← rewrite_goal_with_prev goal=22 facts=[23]
    #[30] n - m + m ≤ n
  -> apply_forward_step sub_add goal=30 facts=[2]
    #[31] n - m + m = n
  ← rewrite_goal_with_prev goal=30 facts=[31]
    #[32] n ≤ n
  ← apply_backward_step less_eq_refl goal=32
    #[33] n ≤ n
qed

theorem div_1
  fixes n :: nat
  prop n DIV 1 = n
  [hint_rewrite]
proof
  ← apply_backward_step div_uniq param_r="(0::nat)" goal=0
    #[1] n = n * 1 + 0
    #[2] (0::nat) < 1
  nat_norm goal=1
  ← apply_backward_step le_1_1 goal=2
    #[3] ¬((1::nat) = 0)
  nat_const_ineq goal=3
qed

theorem mod_1
  fixes n :: nat
  prop n MOD 1 = 0
  [hint_rewrite]
proof
  ← apply_backward_step mod_uniq param_q=n goal=0
    #[1] n = n * 1 + 0
    #[2] (0::nat) < 1
  nat_norm goal=1
  ← rewrite_goal nat_one_def sym=false goal=2
    #[3] 0 < Suc 0
  ← rewrite_goal less_Suc_lesseq sym=false goal=3
    #[4] (0::nat) ≤ 0
  ← apply_backward_step less_eq_refl goal=4
qed

theorem div_lt
  fixes m :: nat, n :: nat
  prop m < n --> m DIV n = 0
  [hint_backward]
proof
  ← introduction goal=0
    #[1] m < n
    #[2] m DIV n = 0
  ← apply_backward_step div_uniq param_r=m goal=2
    #[3] m = 0 * n + m
  nat_norm goal=3
    #[4] m = 0 * n + m
qed

theorem mod_mod
  fixes m :: nat, n :: nat, p :: nat
  prop m MOD (n * p) MOD n = m MOD n
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ m MOD (n * p) MOD n = m MOD n
    #[2] ¬(n = 0) ⟶ m MOD (n * p) MOD n = m MOD n
  ← introduction goal=1
    #[3] n = 0
    #[4] m MOD (n * p) MOD n = m MOD n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m MOD (0 * p) MOD 0 = m MOD 0
  ← rewrite_goal nat_times_def_1 sym=false goal=5
    #[6] m MOD 0 MOD 0 = m MOD 0
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] m MOD 0 = m
  ← rewrite_goal mod_zero sym=false goal=7
    #[8] m MOD 0 = m
  ← introduction goal=2
    #[9] ¬(n = 0)
    #[10] m MOD (n * p) MOD n = m MOD n
  ← cases "p=0" goal=10
    #[11] p = 0 ⟶ m MOD (n * p) MOD n = m MOD n
    #[12] ¬(p = 0) ⟶ m MOD (n * p) MOD n = m MOD n
  ← introduction goal=11
    #[13] p = 0
    #[14] m MOD (n * p) MOD n = m MOD n
  ← rewrite_goal_with_prev goal=14 facts=[13]
    #[15] m MOD (n * 0) MOD n = m MOD n
  ← rewrite_goal mult_0_right sym=false goal=15
    #[16] m MOD 0 MOD n = m MOD n
  ← rewrite_goal mod_zero sym=false goal=16
    #[17] m MOD 0 MOD n = m MOD n
  ← introduction goal=12
    #[18] ¬(p = 0)
    #[19] m MOD (n * p) MOD n = m MOD n
  ← rewrite_goal eq_sym_eq sym=false goal=19
    #[20] m MOD n = m MOD (n * p) MOD n
  ← apply_backward_step mod_eq param_q="m DIV (n * p) * p" goal=20
    #[21] m = m MOD (n * p) + m DIV (n * p) * p * n
  ← rewrite_goal mult_assoc sym=false goal=21
    #[22] m = m MOD (n * p) + m DIV (n * p) * (p * n)
  cut "p*n = n*p" goal=22
    #[23] p * n = n * p
  nat_norm goal=23
    #[24] p * n = n * p
  ← rewrite_goal_with_prev goal=22 facts=[24]
    #[25] m = m MOD (n * p) + m DIV (n * p) * (n * p)
  ← rewrite_goal add_comm sym=false goal=25
    #[26] m = m DIV (n * p) * (n * p) + m MOD (n * p)
  ← rewrite_goal division_simp1 sym=false goal=26
    #[27] m = m DIV (n * p) * (n * p) + m MOD (n * p)
qed

theorem mod_mod_refl
  fixes m :: nat, n :: nat
  prop m MOD n MOD n = m MOD n
  [hint_rewrite]
proof
  -> apply_forward_step mod_mod param_m=m param_n=n param_p="(1::nat)" goal=0
    #[1] m MOD (n * 1) MOD n = m MOD n
  -> rewrite_fact mult_1_right sym=false goal=0 facts=[1]
qed

theorem mod_mod_le
  fixes m :: nat, n :: nat, p :: nat
  prop ~(n = 0) --> n <= p --> m MOD n MOD p = m MOD n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] n ≤ p
    #[3] m MOD n MOD p = m MOD n
  ← apply_backward_step mod_lt goal=3
    #[4] m MOD n < p
  ← apply_backward_step lte_trans param_m=n goal=4
    #[5] m MOD n < n
  ← rewrite_goal mod_lt_eq sym=false goal=5
qed

theorem mod_even_2
  fixes m :: nat, n :: nat
  prop even n --> m MOD n MOD 2 = m MOD 2
  [hint_backward]
proof
  ← introduction goal=0
    #[1] even n
    #[2] m MOD n MOD 2 = m MOD 2
  -> rewrite_fact even_exists sym=false goal=2 facts=[1]
    #[3] ∃m. n = 2 * m
  exists_elim p goal=2 facts=[3]
    #[4] _VAR p
    #[5] n = 2 * p
    #[6] m MOD n MOD 2 = m MOD 2
  ← rewrite_goal_with_prev goal=6 facts=[5]
    #[7] m MOD (2 * p) MOD 2 = m MOD 2
  ← rewrite_goal mod_mod sym=false goal=7
    #[8] m MOD (2 * p) MOD 2 = m MOD 2
qed

theorem div_mult2
  fixes m :: nat, n :: nat, p :: nat
  prop ~(m = 0) --> m * n DIV (m * p) = n DIV p
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(m = 0)
    #[2] m * n DIV (m * p) = n DIV p
  ← cases "p=0" goal=2
    #[3] p = 0 ⟶ m * n DIV (m * p) = n DIV p
    #[4] ¬(p = 0) ⟶ m * n DIV (m * p) = n DIV p
  ← introduction goal=3
    #[5] p = 0
    #[6] m * n DIV (m * p) = n DIV p
  ← rewrite_goal_with_prev goal=6 facts=[5]
    #[7] m * n DIV (m * 0) = n DIV 0
  ← rewrite_goal mult_0_right sym=false goal=7
    #[8] m * n DIV 0 = n DIV 0
  ← rewrite_goal div_zero sym=false goal=8
    #[9] m * n DIV 0 = n DIV 0
  ← introduction goal=4
    #[10] ¬(p = 0)
    #[11] m * n DIV (m * p) = n DIV p
  ← apply_backward_step div_uniq param_r="m * (n MOD p)" goal=11
    #[12] m * n = n DIV p * (m * p) + m * (n MOD p)
    #[13] m * (n MOD p) < m * p
  cut "n DIV p * (m * p) = m * (p * (n DIV p)) " goal=12
    #[14] n DIV p * (m * p) = m * (p * (n DIV p))
  nat_norm goal=14
    #[15] n DIV p * (m * p) = m * (p * (n DIV p))
  ← rewrite_goal_with_prev goal=12 facts=[15]
    #[16] m * n = m * (p * (n DIV p)) + m * (n MOD p)
  ← rewrite_goal distrib_l sym=true goal=16
    #[17] m * n = m * (p * (n DIV p) + n MOD p)
  ← rewrite_goal division_simp2 sym=false goal=17
    #[18] m * n = m * (p * (n DIV p) + n MOD p)
  ← rewrite_goal mult_comm sym=false goal=13
    #[19] n MOD p * m < p * m
  ← rewrite_goal lt_mult_rcancel sym=false goal=19
    #[20] n MOD p < p ∧ ¬(m = 0)
  ← apply_backward_step conjI goal=20
    #[21] n MOD p < p
  ← rewrite_goal mod_lt_eq sym=false goal=21
qed

theorem mod_mult2
  fixes m :: nat, n :: nat, p :: nat
  prop m * n MOD (m * p) = m * (n MOD p)
  [hint_rewrite]
proof
  ← cases "p=0" goal=0
    #[1] p = 0 ⟶ m * n MOD (m * p) = m * (n MOD p)
    #[2] ¬(p = 0) ⟶ m * n MOD (m * p) = m * (n MOD p)
  ← introduction goal=1
    #[3] p = 0
    #[4] m * n MOD (m * p) = m * (n MOD p)
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m * n MOD (m * 0) = m * (n MOD 0)
  ← rewrite_goal mult_0_right sym=false goal=5
    #[6] m * n MOD 0 = m * (n MOD 0)
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] m * n MOD 0 = m * (n MOD 0)
  ← introduction goal=2
    #[8] ¬(p = 0)
    #[9] m * n MOD (m * p) = m * (n MOD p)
  ← cases "m=0" goal=9
    #[10] m = 0 ⟶ m * n MOD (m * p) = m * (n MOD p)
    #[11] ¬(m = 0) ⟶ m * n MOD (m * p) = m * (n MOD p)
  ← introduction goal=10
    #[12] m = 0
    #[13] m * n MOD (m * p) = m * (n MOD p)
  ← rewrite_goal_with_prev goal=13 facts=[12]
    #[14] 0 * n MOD (0 * p) = 0 * (n MOD p)
  ← rewrite_goal nat_times_def_1 sym=false goal=14
    #[15] 0 MOD 0 = 0
  ← rewrite_goal mod_0 sym=false goal=15
    #[16] 0 MOD 0 = 0
  ← introduction goal=11
    #[17] ¬(m = 0)
    #[18] m * n MOD (m * p) = m * (n MOD p)
  ← apply_backward_step mod_uniq param_q="n DIV p" goal=18
    #[19] m * n = n DIV p * (m * p) + m * (n MOD p)
    #[20] m * (n MOD p) < m * p
  cut "n DIV p * (m * p) = m * (p * (n DIV p))" goal=19
    #[21] n DIV p * (m * p) = m * (p * (n DIV p))
  nat_norm goal=21
    #[22] n DIV p * (m * p) = m * (p * (n DIV p))
  ← rewrite_goal_with_prev goal=19 facts=[22]
    #[23] m * n = m * (p * (n DIV p)) + m * (n MOD p)
  ← rewrite_goal distrib_l sym=true goal=23
    #[24] m * n = m * (p * (n DIV p) + n MOD p)
  ← rewrite_goal division_simp2 sym=false goal=24
    #[25] m * n = m * (p * (n DIV p) + n MOD p)
  ← rewrite_goal mult_comm sym=false goal=20
    #[26] n MOD p * m < p * m
  ← rewrite_goal lt_mult_rcancel sym=false goal=26
    #[27] n MOD p < p ∧ ¬(m = 0)
  ← apply_backward_step conjI goal=27
    #[28] n MOD p < p
  ← rewrite_goal mod_lt_eq sym=false goal=28
qed

theorem mod_exists
  fixes m :: nat, n :: nat
  prop (?q. m = n * q) <--> (if n = 0 then m = 0 else m MOD n = 0)
  [hint_rewrite]
proof
  ← cases "n = 0" goal=0
    #[1] n = 0 ⟶ (∃q. m = n * q) ⟷ (if n = 0 then m = 0 else m MOD n = 0)
    #[2] ¬(n = 0) ⟶ (∃q. m = n * q) ⟷ (if n = 0 then m = 0 else m MOD n = 0)
  ← introduction goal=1
    #[3] n = 0
    #[4] (∃q. m = n * q) ⟷ (if n = 0 then m = 0 else m MOD n = 0)
  ← rewrite_goal if_P sym=false goal=4 facts=[3]
    #[5] (∃q. m = n * q) ⟷ m = 0
  ← rewrite_goal_with_prev goal=5 facts=[3]
    #[6] (∃q. m = 0 * q) ⟷ m = 0
  ← rewrite_goal nat_times_def_1 sym=false goal=6
    #[7] (∃q::nat. m = 0) ⟷ m = 0
  ← z3 goal=7
    #[8] (∃q::nat. m = 0) ⟷ m = 0
  ← introduction goal=2
    #[9] ¬(n = 0)
    #[10] (∃q. m = n * q) ⟷ (if n = 0 then m = 0 else m MOD n = 0)
  ← rewrite_goal if_not_P sym=false goal=10 facts=[9]
    #[11] (∃q. m = n * q) ⟷ m MOD n = 0
  ← apply_backward_step iffI goal=11
    #[12] (∃q. m = n * q) ⟶ m MOD n = 0
    #[13] m MOD n = 0 ⟶ (∃q. m = n * q)
  ← introduction goal=12
    #[14] ∃q. m = n * q
    #[15] m MOD n = 0
  exists_elim q goal=15 facts=[14]
    #[16] _VAR q
    #[17] m = n * q
    #[18] m MOD n = 0
  ← rewrite_goal_with_prev goal=18 facts=[17]
    #[19] n * q MOD n = 0
  ← rewrite_goal mod_mult sym=false goal=19
    #[20] n * q MOD n = 0
  ← introduction goal=13
    #[21] m MOD n = 0
    #[22] ∃q. m = n * q
  ← inst_exists_goal "m DIV n" goal=22
    #[23] m = n * (m DIV n)
  cut "m = (m DIV n) * n + m MOD n" goal=23
    #[24] m = m DIV n * n + m MOD n
  ← rewrite_goal division_simp1 sym=false goal=24
    #[25] m = m DIV n * n + m MOD n
  -> rewrite_fact_with_prev goal=23 facts=[21,25]
    #[26] m = m DIV n * n + 0
  -> rewrite_fact add_0_right sym=false goal=23 facts=[26]
    #[27] m = m DIV n * n
  ← rewrite_goal mult_comm sym=false goal=23
qed

theorem le_rdiv_eq
  fixes a :: nat, b :: nat, n :: nat
  prop ~(a = 0) --> n <= b DIV a <--> a * n <= b
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(a = 0)
    #[2] n ≤ b DIV a ⟷ a * n ≤ b
  ← apply_backward_step iffI goal=2
    #[3] n ≤ b DIV a ⟶ a * n ≤ b
    #[4] a * n ≤ b ⟶ n ≤ b DIV a
  ← introduction goal=3
    #[5] n ≤ b DIV a
    #[6] a * n ≤ b
  ← apply_backward_step less_eq_trans param_m="a * (b DIV a)" goal=6
    #[7] a * n ≤ a * (b DIV a)
    #[8] a * (b DIV a) ≤ b
  ← rewrite_goal mult_comm sym=false goal=7
    #[9] n * a ≤ b DIV a * a
  ← rewrite_goal le_mult_rcancel sym=false goal=9
    #[10] n ≤ b DIV a ∨ a = 0
  ← apply_backward_step disjI1 goal=10
  ← apply_backward_step div_mul_le goal=8
    #[11] a * (b DIV a) ≤ b
  ← introduction goal=4
    #[12] a * n ≤ b
    #[13] n ≤ b DIV a
  cut "a * n < a * (b DIV a + 1)" goal=13
    #[14] a * n < a * (b DIV a + 1)
  ← apply_backward_step let_trans param_m="(b DIV a) * a + b MOD a" goal=14
    #[15] a * n ≤ b DIV a * a + b MOD a
    #[16] b DIV a * a + b MOD a < a * (b DIV a + 1)
  ← rewrite_goal division_simp1 sym=false goal=15
  ← rewrite_goal distrib_l sym=false goal=16
    #[17] b DIV a * a + b MOD a < a * (b DIV a) + a * 1
  cut "b DIV a * a = a * (b DIV a)" goal=17
    #[18] b DIV a * a = a * (b DIV a)
  nat_norm goal=18
    #[19] b DIV a * a = a * (b DIV a)
  ← rewrite_goal_with_prev goal=17 facts=[19]
    #[20] a * (b DIV a) + b MOD a < a * (b DIV a) + a * 1
  ← rewrite_goal lt_add_lcancel sym=false goal=20
    #[21] b MOD a < a * 1
  ← rewrite_goal mult_1_right sym=false goal=21
    #[22] b MOD a < a
  ← rewrite_goal mod_lt_eq sym=false goal=22
  -> rewrite_fact mult_comm sym=false goal=13 facts=[14]
    #[23] n * a < (b DIV a + 1) * a
  -> rewrite_fact lt_mult_rcancel sym=false goal=13 facts=[23]
    #[24] n < b DIV a + 1 ∧ ¬(a = 0)
  -> apply_forward_step conjD1 goal=13 facts=[24]
    #[25] n < b DIV a + 1
  -> rewrite_fact add_1_right sym=false goal=13 facts=[25]
    #[26] n < Suc (b DIV a)
  -> rewrite_fact less_Suc_lesseq sym=false goal=13 facts=[26]
qed

theorem rdiv_lt_eq
  fixes a :: nat, b :: nat, n :: nat
  prop ~(a = 0) --> b DIV a < n <--> b < a * n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(a = 0)
    #[2] b DIV a < n ⟷ b < a * n
  ← rewrite_goal not_le sym=true goal=2
    #[3] ¬(n ≤ b DIV a) ⟷ ¬(a * n ≤ b)
  -> apply_forward_step le_rdiv_eq param_b=b param_n=n goal=3 facts=[1]
    #[4] n ≤ b DIV a ⟷ a * n ≤ b
  ← rewrite_goal_with_prev goal=3 facts=[4]
qed

theorem le_ldiv_eq
  fixes a :: nat, b :: nat, n :: nat
  prop ~(a = 0) --> b DIV a <= n <--> b < a * (n + 1)
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(a = 0)
    #[2] b DIV a ≤ n ⟷ b < a * (n + 1)
  ← rewrite_goal add_1_right sym=false goal=2
    #[3] b DIV a ≤ n ⟷ b < a * Suc n
  ← rewrite_goal not_lt sym=true goal=3
    #[4] ¬(n < b DIV a) ⟷ b < a * Suc n
  cut "n < b DIV a <--> Suc n <= b DIV a" goal=4
    #[5] n < b DIV a ⟷ Suc n ≤ b DIV a
  ← rewrite_goal lesseq_Suc_less sym=false goal=5
    #[6] n < b DIV a ⟷ Suc n ≤ b DIV a
  ← rewrite_goal_with_prev goal=4 facts=[6]
    #[7] ¬(Suc n ≤ b DIV a) ⟷ b < a * Suc n
  -> apply_forward_step le_rdiv_eq param_b=b param_n="Suc n" goal=7 facts=[1]
    #[8] Suc n ≤ b DIV a ⟷ a * Suc n ≤ b
  ← rewrite_goal_with_prev goal=7 facts=[8]
    #[9] ¬(a * Suc n ≤ b) ⟷ b < a * Suc n
  ← rewrite_goal not_le sym=false goal=9
    #[10] ¬(a * Suc n ≤ b) ⟷ b < a * Suc n
qed

theorem ldiv_lt_eq
  fixes a :: nat, b :: nat, n :: nat
  prop ~(a = 0) --> n < b DIV a <--> a * (n + 1) <= b
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(a = 0)
    #[2] n < b DIV a ⟷ a * (n + 1) ≤ b
  ← rewrite_goal not_le sym=true goal=2
    #[3] ¬(b DIV a ≤ n) ⟷ a * (n + 1) ≤ b
  -> apply_forward_step le_ldiv_eq param_b=b param_n=n goal=3 facts=[1]
    #[4] b DIV a ≤ n ⟷ b < a * (n + 1)
  ← rewrite_goal_with_prev goal=3 facts=[4]
    #[5] ¬(b < a * (n + 1)) ⟷ a * (n + 1) ≤ b
  ← rewrite_goal not_lt sym=false goal=5
    #[6] ¬(b < a * (n + 1)) ⟷ a * (n + 1) ≤ b
qed

theorem le_ldiv
  fixes a :: nat, b :: nat, n :: nat
  prop ~(a = 0) --> b <= a * n --> b DIV a <= n
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] ¬(a = 0)
    #[2] b ≤ a * n
    #[3] b DIV a ≤ n
  -> apply_forward_step le_ldiv_eq param_b=b param_n=n goal=3 facts=[1]
    #[4] b DIV a ≤ n ⟷ b < a * (n + 1)
  ← rewrite_goal_with_prev goal=3 facts=[4]
    #[5] b < a * (n + 1)
  ← apply_backward_step let_trans param_m="a * n" goal=5
    #[6] a * n < a * (n + 1)
  ← rewrite_goal distrib_l sym=false goal=6
    #[7] a * n < a * n + a * 1
  ← rewrite_goal lt_add sym=false goal=7
    #[8] 0 < a * 1
  ← rewrite_goal mult_1_right sym=false goal=8
    #[9] 0 < a
  ← apply_backward_step le_1_1 goal=9
qed

theorem div_mono
  fixes m :: nat, n :: nat, p :: nat
  prop m <= n --> m DIV p <= n DIV p
  [hint_backward]
proof
  ← introduction goal=0
    #[1] m ≤ n
    #[2] m DIV p ≤ n DIV p
  ← cases "p=0" goal=2
    #[3] p = 0 ⟶ m DIV p ≤ n DIV p
    #[4] ¬(p = 0) ⟶ m DIV p ≤ n DIV p
  ← introduction goal=3
    #[5] p = 0
    #[6] m DIV p ≤ n DIV p
  ← rewrite_goal_with_prev goal=6 facts=[5]
    #[7] m DIV 0 ≤ n DIV 0
  ← rewrite_goal div_zero sym=false goal=7
    #[8] (0::nat) ≤ 0
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=8
    #[9] (0::nat) ≤ 0
  ← introduction goal=4
    #[10] ¬(p = 0)
    #[11] m DIV p ≤ n DIV p
  cut "!a::nat. !b. (!k. k <= a --> k <= b) --> a <= b " goal=11
    #[12] ∀a::nat. ∀b. (∀k. k ≤ a ⟶ k ≤ b) ⟶ a ≤ b
  ← z3 goal=12
    #[13] ∀a::nat. ∀b. (∀k. k ≤ a ⟶ k ≤ b) ⟶ a ≤ b
  ← apply_prev goal=11 facts=[13]
    #[14] ∀k. k ≤ m DIV p ⟶ k ≤ n DIV p
  ← introduction k goal=14
    #[15] _VAR k
    #[16] k ≤ m DIV p
    #[17] k ≤ n DIV p
  -> apply_forward_step le_rdiv_eq param_b=n param_n=k goal=17 facts=[10]
    #[18] k ≤ n DIV p ⟷ p * k ≤ n
  ← rewrite_goal_with_prev goal=17 facts=[18]
    #[19] p * k ≤ n
  -> apply_forward_step le_rdiv_eq param_b=m param_n=k goal=19 facts=[10]
    #[20] k ≤ m DIV p ⟷ p * k ≤ m
  -> rewrite_fact_with_prev goal=19 facts=[20,16]
    #[21] p * k ≤ m
  ← apply_backward_step less_eq_trans param_m=m goal=19
qed

theorem div_mono_lt
  fixes m :: nat, n :: nat, p :: nat
  prop ~(p = 0) --> m + p <= n --> m DIV p < n DIV p
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] ¬(p = 0)
    #[2] m + p ≤ n
    #[3] m DIV p < n DIV p
  cut "!a. !b. (!k::nat. k <= a --> k + 1 <= b) --> a < b" goal=3
    #[4] ∀a::nat. ∀b. (∀k. k ≤ a ⟶ k + 1 ≤ b) ⟶ a < b
  ← z3 goal=4
    #[5] ∀a::nat. ∀b. (∀k. k ≤ a ⟶ k + 1 ≤ b) ⟶ a < b
  ← apply_prev goal=3 facts=[5]
    #[6] ∀k. k ≤ m DIV p ⟶ k + 1 ≤ n DIV p
  ← introduction k goal=6
    #[7] _VAR k
    #[8] k ≤ m DIV p
    #[9] k + 1 ≤ n DIV p
  -> apply_forward_step le_rdiv_eq param_b=n param_n=k+1 goal=9 facts=[1]
    #[10] k + 1 ≤ n DIV p ⟷ p * (k + 1) ≤ n
  -> apply_forward_step le_rdiv_eq param_b=m param_n=k goal=9 facts=[1]
    #[11] k ≤ m DIV p ⟷ p * k ≤ m
  -> rewrite_fact_with_prev goal=9 facts=[11,8]
    #[12] p * k ≤ m
  ← rewrite_goal_with_prev goal=9 facts=[10]
    #[13] p * (k + 1) ≤ n
  ← apply_backward_step less_eq_trans param_m=m+p goal=13
    #[14] p * (k + 1) ≤ m + p
  ← rewrite_goal distrib_l sym=false goal=14
    #[15] p * k + p * 1 ≤ m + p
  ← rewrite_goal mult_1_right sym=false goal=15
    #[16] p * k + p ≤ m + p
  ← apply_backward_step le_add2 goal=16
    #[17] p ≤ p
  ← apply_backward_step less_eq_refl goal=17
    #[18] p ≤ p
qed

theorem div_eq_0
  fixes m :: nat, n :: nat
  prop ~(n = 0) --> m DIV n = 0 <--> m < n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] m DIV n = 0 ⟷ m < n
  ← apply_backward_step iffI goal=2
    #[3] m DIV n = 0 ⟶ m < n
    #[4] m < n ⟶ m DIV n = 0
  ← introduction goal=3
    #[5] m DIV n = 0
    #[6] m < n
  -> apply_forward_step division param_m=m goal=6 facts=[1]
    #[7] m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> apply_forward_step conjD1 goal=6 facts=[7]
    #[8] m = m DIV n * n + m MOD n
  -> rewrite_fact_with_prev goal=6 facts=[5,8]
    #[9] m = 0 * n + m MOD n
  -> rewrite_fact nat_times_def_1 sym=false goal=6 facts=[9]
    #[10] m = 0 + m MOD n
  -> rewrite_fact nat_plus_def_1 sym=false goal=6 facts=[10]
    #[11] m = m MOD n
  ← rewrite_goal_with_prev goal=6 facts=[11]
    #[12] m MOD n < n
  ← rewrite_goal mod_lt_eq sym=false goal=12
  ← introduction goal=4
    #[13] m < n
    #[14] m DIV n = 0
  ← apply_backward_step div_uniq param_r=m goal=14
    #[15] m = 0 * n + m
  nat_norm goal=15
    #[16] m = 0 * n + m
qed

theorem mod_eq_0
  fixes m :: nat, n :: nat
  prop m MOD n = 0 <--> (?q. m = q * n)
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ m MOD n = 0 ⟷ (∃q. m = q * n)
    #[2] ¬(n = 0) ⟶ m MOD n = 0 ⟷ (∃q. m = q * n)
  ← introduction goal=1
    #[3] n = 0
    #[4] m MOD n = 0 ⟷ (∃q. m = q * n)
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m MOD 0 = 0 ⟷ (∃q. m = q * 0)
  ← apply_backward_step iffI goal=5
    #[6] m MOD 0 = 0 ⟶ (∃q. m = q * 0)
    #[7] (∃q. m = q * 0) ⟶ m MOD 0 = 0
  ← introduction goal=6
    #[8] m MOD 0 = 0
    #[9] ∃q. m = q * 0
  -> rewrite_fact mod_zero sym=false goal=9 facts=[8]
    #[10] m = 0
  ← rewrite_goal_with_prev goal=9 facts=[10]
    #[11] ∃q::nat. 0 = q * 0
  ← rewrite_goal mult_0_right sym=false goal=11
    #[12] ∃q::nat. (0::nat) = 0
  ← z3 goal=12
    #[13] ∃q::nat. (0::nat) = 0
  ← introduction goal=7
    #[14] ∃q. m = q * 0
    #[15] m MOD 0 = 0
  ← rewrite_goal mod_zero sym=false goal=15
    #[16] m = 0
  exists_elim q goal=16 facts=[14]
    #[17] _VAR q
    #[18] m = q * 0
    #[19] m = 0
    #[20] m MOD 0 = 0
  -> rewrite_fact mult_0_right sym=false goal=19 facts=[18]
    #[21] m = 0
  ← introduction goal=2
    #[22] ¬(n = 0)
    #[23] m MOD n = 0 ⟷ (∃q. m = q * n)
  ← apply_backward_step iffI goal=23
    #[24] m MOD n = 0 ⟶ (∃q. m = q * n)
    #[25] (∃q. m = q * n) ⟶ m MOD n = 0
  ← introduction goal=24
    #[26] m MOD n = 0
    #[27] ∃q. m = q * n
  -> apply_forward_step division param_m=m goal=27 facts=[22]
    #[28] m = m DIV n * n + m MOD n ∧ m MOD n < n
  -> apply_forward_step conjD1 goal=27 facts=[28]
    #[29] m = m DIV n * n + m MOD n
  -> rewrite_fact_with_prev goal=27 facts=[26,29]
    #[30] m = m DIV n * n + 0
  -> rewrite_fact add_0_right sym=false goal=27 facts=[30]
    #[31] m = m DIV n * n
  ← apply_backward_step exI goal=27 facts=[31]
  ← introduction goal=25
    #[32] ∃q. m = q * n
    #[33] m MOD n = 0
  exists_elim q goal=33 facts=[32]
    #[34] m = q * n
    #[35] m MOD n = 0
  ← apply_backward_step mod_uniq param_q=q goal=35
    #[36] m = q * n + 0
    #[37] 0 < n
  ← rewrite_goal add_0_right sym=false goal=36
  ← apply_backward_step le_1_1 goal=37
qed

theorem div_eq_self
  fixes m :: nat, n :: nat
  prop m DIV n = m <--> m = 0 | n = 1
  [hint_rewrite]
proof
  ← cases "m=0" goal=0
    #[1] m = 0 ⟶ m DIV n = m ⟷ m = 0 ∨ n = 1
    #[2] ¬(m = 0) ⟶ m DIV n = m ⟷ m = 0 ∨ n = 1
  ← introduction goal=1
    #[3] m = 0
    #[4] m DIV n = m ⟷ m = 0 ∨ n = 1
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] 0 DIV n = 0 ⟷ (0::nat) = 0 ∨ n = 1
  ← rewrite_goal div_0 sym=false goal=5
    #[6] (0::nat) = 0 ⟷ (0::nat) = 0 ∨ n = 1
  ← apply_backward_step iffI goal=6
    #[7] (0::nat) = 0 ⟶ (0::nat) = 0 ∨ n = 1
    #[8] (0::nat) = 0 ∨ n = 1 ⟶ (0::nat) = 0
  ← introduction goal=7
    #[9] (0::nat) = 0
    #[10] (0::nat) = 0 ∨ n = 1
  ← apply_backward_step disjI1 goal=10
  ← introduction goal=8
    #[11] (0::nat) = 0 ∨ n = 1
    #[12] (0::nat) = 0
  ← apply_backward_step eq_refl goal=12
    #[13] (0::nat) = 0
  ← introduction goal=2
    #[14] ¬(m = 0)
    #[15] m DIV n = m ⟷ m = 0 ∨ n = 1
  ← cases "n=1" goal=15
    #[16] n = 1 ⟶ m DIV n = m ⟷ m = 0 ∨ n = 1
    #[17] ¬(n = 1) ⟶ m DIV n = m ⟷ m = 0 ∨ n = 1
  ← introduction goal=16
    #[18] n = 1
    #[19] m DIV n = m ⟷ m = 0 ∨ n = 1
  ← rewrite_goal_with_prev goal=19 facts=[18]
    #[20] m DIV 1 = m ⟷ m = 0 ∨ (1::nat) = 1
  ← rewrite_goal div_1 sym=false goal=20
    #[21] m = m ⟷ m = 0 ∨ (1::nat) = 1
  ← apply_backward_step iffI goal=21
    #[22] m = m ⟶ m = 0 ∨ (1::nat) = 1
    #[23] m = 0 ∨ (1::nat) = 1 ⟶ m = m
  ← introduction goal=22
    #[24] m = m
    #[25] m = 0 ∨ (1::nat) = 1
  ← apply_backward_step disjI2 goal=25
    #[26] (1::nat) = 1
  ← apply_backward_step eq_refl goal=26
    #[27] (1::nat) = 1
  ← introduction goal=23
    #[28] m = 0 ∨ (1::nat) = 1
    #[29] m = m
  ← apply_backward_step eq_refl goal=29
    #[30] m = m
  ← introduction goal=17
    #[31] ¬(n = 1)
    #[32] m DIV n = m ⟷ m = 0 ∨ n = 1
  ← cases "n=0" goal=32
    #[33] n = 0 ⟶ m DIV n = m ⟷ m = 0 ∨ n = 1
    #[34] ¬(n = 0) ⟶ m DIV n = m ⟷ m = 0 ∨ n = 1
  ← introduction goal=33
    #[35] n = 0
    #[36] m DIV n = m ⟷ m = 0 ∨ n = 1
  ← rewrite_goal_with_prev goal=36 facts=[35]
    #[37] m DIV 0 = m ⟷ m = 0 ∨ (0::nat) = 1
  ← apply_backward_step iffI goal=37
    #[38] m DIV 0 = m ⟶ m = 0 ∨ (0::nat) = 1
    #[39] m = 0 ∨ (0::nat) = 1 ⟶ m DIV 0 = m
  ← introduction goal=38
    #[40] m DIV 0 = m
    #[41] m = 0 ∨ (0::nat) = 1
  -> rewrite_fact div_zero sym=false goal=41 facts=[40]
    #[42] 0 = m
  ← z3 goal=41 facts=[42,14]
    #[43] m = 0 ∨ (0::nat) = 1
  ← introduction goal=39
    #[44] m = 0 ∨ (0::nat) = 1
    #[45] m DIV 0 = m
  ← z3 goal=45 facts=[44,14]
    #[46] m DIV 0 = m
  ← introduction goal=34
    #[47] ¬(n = 0)
    #[48] m DIV n = m ⟷ m = 0 ∨ n = 1
  ← apply_backward_step iffI goal=48
    #[49] m DIV n = m ⟶ m = 0 ∨ n = 1
    #[50] m = 0 ∨ n = 1 ⟶ m DIV n = m
  ← introduction goal=49
    #[51] m DIV n = m
    #[52] m = 0 ∨ n = 1
  ← apply_backward_step contrapositive goal=52 facts=[51]
    #[53] ¬(m = 0 ∨ n = 1) ⟶ ¬(m DIV n = m)
  ← introduction goal=53
    #[54] ¬(m = 0 ∨ n = 1)
    #[55] ¬(m DIV n = m)
  cut "!m. !n::nat. m < n --> ~(m = n)" goal=55
    #[56] ∀m::nat. ∀n. m < n ⟶ ¬(m = n)
  ← z3 goal=56
    #[57] ∀m::nat. ∀n. m < n ⟶ ¬(m = n)
  ← apply_prev goal=55 facts=[57]
    #[58] m DIV n < m
  -> apply_forward_step rdiv_lt_eq param_b=m param_n=m goal=58 facts=[47]
    #[59] m DIV n < m ⟷ m < n * m
  ← rewrite_goal_with_prev goal=58 facts=[59]
    #[60] m < n * m
  -> rewrite_fact de_morgan_thm2 sym=false goal=60 facts=[54]
    #[61] ¬(m = 0) ∧ ¬(n = 1)
  -> apply_forward_step conjD2 goal=60 facts=[61]
    #[62] ¬(n = 1)
  -> apply_forward_step conjD1 goal=60 facts=[61]
    #[63] ¬(m = 0)
  ← z3 goal=60 facts=[47,62,63]
    #[64] m < n * m
  ← introduction goal=50
    #[65] m = 0 ∨ n = 1
    #[66] m DIV n = m
  ← apply_backward_step disjE goal=66 facts=[65]
    #[67] m = 0 ⟶ m DIV n = m
    #[68] n = 1 ⟶ m DIV n = m
  ← introduction goal=67
    #[69] m DIV n = m
  ← apply_backward_step negE_gen goal=69 facts=[14,3]
    #[70] m DIV n = m
  ← introduction goal=68
    #[71] m DIV n = m
  ← rewrite_goal_with_prev goal=71 facts=[18]
    #[72] m DIV 1 = m
  ← rewrite_goal div_1 sym=false goal=72
    #[73] m DIV 1 = m
qed

theorem mod_refl
  fixes n :: nat
  prop n MOD n = 0
  [hint_rewrite]
proof
  ← rewrite_goal mod_eq_0 sym=false goal=0
    #[1] ∃q. n = q * n
  ← inst_exists_goal "(1::nat)" goal=1
    #[2] n = 1 * n
  ← rewrite_goal mult_1_left sym=false goal=2
qed

theorem even_mod
  fixes n :: nat
  prop even n <--> n MOD 2 = 0
  [hint_rewrite]
proof
  ← rewrite_goal even_exists sym=false goal=0
    #[1] (∃m. n = 2 * m) ⟷ n MOD 2 = 0
  ← rewrite_goal mod_eq_0 sym=false goal=1
    #[2] (∃m. n = 2 * m) ⟷ (∃q. n = q * 2)
  ← apply_backward_step iffI goal=2
    #[3] (∃m. n = 2 * m) ⟶ (∃q. n = q * 2)
    #[4] (∃q. n = q * 2) ⟶ (∃m. n = 2 * m)
  ← introduction goal=3
    #[5] ∃m. n = 2 * m
    #[6] ∃q. n = q * 2
  exists_elim m goal=6 facts=[5]
    #[7] _VAR m
    #[8] n = 2 * m
    #[9] ∃q. n = q * 2
  ← inst_exists_goal m goal=9
    #[10] n = m * 2
  ← rewrite_goal_with_prev goal=10 facts=[8]
    #[11] 2 * m = m * 2
  nat_norm goal=11
    #[12] 2 * m = m * 2
  ← introduction goal=4
    #[13] ∃q. n = q * 2
    #[14] ∃m. n = 2 * m
  exists_elim q goal=14 facts=[13]
    #[15] _VAR q
    #[16] n = q * 2
    #[17] ∃m. n = 2 * m
  ← inst_exists_goal q goal=17
    #[18] n = 2 * q
  ← rewrite_goal_with_prev goal=18 facts=[16]
    #[19] q * 2 = 2 * q
  nat_norm goal=19
    #[20] q * 2 = 2 * q
qed

theorem odd_mod
  fixes n :: nat
  prop odd n <--> n MOD 2 = 1
  [hint_rewrite]
proof
  ← rewrite_goal not_even sym=true goal=0
    #[1] ¬(even n) ⟷ n MOD 2 = 1
  ← rewrite_goal even_mod sym=false goal=1
    #[2] ¬(n MOD 2 = 0) ⟷ n MOD 2 = 1
  cut "n MOD 2 < 2" goal=2
    #[3] n MOD 2 < 2
  ← rewrite_goal mod_lt_eq sym=false goal=3
    #[4] ¬((2::nat) = 0)
  nat_const_ineq goal=4
  cut "∀n::nat. n < 2 ⟷ n = 0 ∨ n = 1" goal=2
    #[5] ∀n::nat. n < 2 ⟷ n = 0 ∨ n = 1
  ← z3 goal=5
  -> rewrite_fact_with_prev goal=2 facts=[5,3]
    #[6] n MOD 2 = 0 ∨ n MOD 2 = 1
  ← apply_backward_step disjE goal=2 facts=[6]
    #[7] n MOD 2 = 0 ⟶ ¬(n MOD 2 = 0) ⟷ n MOD 2 = 1
    #[8] n MOD 2 = 1 ⟶ ¬(n MOD 2 = 0) ⟷ n MOD 2 = 1
  ← introduction goal=7
    #[9] n MOD 2 = 0
    #[10] ¬(n MOD 2 = 0) ⟷ n MOD 2 = 1
  ← apply_backward_step iffI goal=10
    #[11] ¬(n MOD 2 = 0) ⟶ n MOD 2 = 1
    #[12] n MOD 2 = 1 ⟶ ¬(n MOD 2 = 0)
  ← introduction goal=11
    #[13] ¬(n MOD 2 = 0)
    #[14] n MOD 2 = 1
  ← apply_backward_step negE_gen goal=14 facts=[13,9]
  ← introduction goal=12
    #[15] n MOD 2 = 1
    #[16] ¬(n MOD 2 = 0)
  -> rewrite_fact_with_prev goal=16 facts=[15,9]
    #[17] (1::nat) = 0
  ← z3 goal=16 facts=[17]
  ← introduction goal=8
    #[18] ¬(n MOD 2 = 0) ⟷ n MOD 2 = 1
  ← apply_backward_step iffI goal=18
    #[19] ¬(n MOD 2 = 0) ⟶ n MOD 2 = 1
    #[20] n MOD 2 = 1 ⟶ ¬(n MOD 2 = 0)
  ← introduction goal=19
  ← introduction goal=20
    #[21] ¬(n MOD 2 = 0)
  ← rewrite_goal_with_prev goal=21 facts=[15]
    #[22] ¬((1::nat) = 0)
  nat_const_ineq goal=22
    #[23] ¬((1::nat) = 0)
qed

theorem mod_2_cases
  fixes n :: nat
  prop n MOD 2 = (if even n then 0 else 1)
  [hint_rewrite]
proof
  ← cases "even n" goal=0
    #[1] even n ⟶ n MOD 2 = (if even n then 0 else 1)
    #[2] ¬(even n) ⟶ n MOD 2 = (if even n then 0 else 1)
  ← introduction goal=1
    #[3] even n
    #[4] n MOD 2 = (if even n then 0 else 1)
  ← rewrite_goal if_P sym=false goal=4 facts=[3]
    #[5] n MOD 2 = 0
  -> rewrite_fact even_mod sym=false goal=5 facts=[3]
  ← introduction goal=2
    #[6] ¬(even n)
    #[7] n MOD 2 = (if even n then 0 else 1)
  ← rewrite_goal if_not_P sym=false goal=7 facts=[6]
    #[8] n MOD 2 = 1
  -> rewrite_fact not_even sym=false goal=8 facts=[6]
    #[9] odd n
  -> rewrite_fact odd_mod sym=false goal=8 facts=[9]
qed

theorem even_mod_even
  fixes m :: nat, n :: nat
  prop even n --> even (m MOD n) <--> even m
  [hint_backward]
proof
  ← introduction goal=0
    #[1] even n
    #[2] even (m MOD n) ⟷ even m
  ← rewrite_goal even_mod sym=false goal=2
    #[3] m MOD n MOD 2 = 0 ⟷ m MOD 2 = 0
  -> apply_forward_step mod_even_2 param_m=m goal=3 facts=[1]
    #[4] m MOD n MOD 2 = m MOD 2
  ← rewrite_goal_with_prev goal=3 facts=[4]
qed

theorem odd_mod_even
  fixes m :: nat, n :: nat
  prop even n --> odd (m MOD n) <--> odd m
  [hint_backward]
proof
  ← introduction goal=0
    #[1] even n
    #[2] odd (m MOD n) ⟷ odd m
  ← rewrite_goal not_even sym=true goal=2
    #[3] ¬(even (m MOD n)) ⟷ ¬(even m)
  -> apply_forward_step even_mod_even param_m=m goal=3 facts=[1]
    #[4] even (m MOD n) ⟷ even m
  ← rewrite_goal_with_prev goal=3 facts=[4]
qed

theorem mod_mult_rmod
  fixes m :: nat, n :: nat, p :: nat
  prop m * (p MOD n) MOD n = m * p MOD n
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ m * (p MOD n) MOD n = m * p MOD n
    #[2] ¬(n = 0) ⟶ m * (p MOD n) MOD n = m * p MOD n
  ← introduction goal=1
    #[3] n = 0
    #[4] m * (p MOD n) MOD n = m * p MOD n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m * (p MOD 0) MOD 0 = m * p MOD 0
  ← rewrite_goal mod_zero sym=false goal=5
    #[6] m * (p MOD 0) = m * p
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] m * (p MOD 0) = m * p
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] m * (p MOD n) MOD n = m * p MOD n
  ← rewrite_goal eq_sym_eq sym=false goal=9
    #[10] m * p MOD n = m * (p MOD n) MOD n
  ← apply_backward_step mod_eq param_q="m * (p DIV n)" goal=10
    #[11] m * p = m * (p MOD n) + m * (p DIV n) * n
  ← rewrite_goal mult_assoc sym=false goal=11
    #[12] m * p = m * (p MOD n) + m * (p DIV n * n)
  ← rewrite_goal distrib_l sym=true goal=12
    #[13] m * p = m * (p MOD n + p DIV n * n)
  ← rewrite_goal eq_mult_lcancel sym=false goal=13
    #[14] m = 0 ∨ p = p MOD n + p DIV n * n
  ← apply_backward_step disjI2 goal=14
    #[15] p = p MOD n + p DIV n * n
  ← rewrite_goal add_comm sym=false goal=15
    #[16] p = p DIV n * n + p MOD n
  ← rewrite_goal division_simp1 sym=false goal=16
    #[17] p = p DIV n * n + p MOD n
qed

theorem mod_mult_lmod
  fixes m :: nat, n :: nat, p :: nat
  prop m MOD n * p MOD n = m * p MOD n
  [hint_rewrite]
proof
  ← rewrite_goal mult_comm sym=false goal=0
    #[1] p * (m MOD n) MOD n = p * m MOD n
  ← rewrite_goal mod_mult_rmod sym=false goal=1
qed

theorem mod_mult_mod2
  fixes m :: nat, n :: nat, p :: nat
  prop m MOD n * (p MOD n) MOD n = m * p MOD n
  [hint_rewrite]
proof
  ← rewrite_goal mod_mult_rmod sym=false goal=0
    #[1] m MOD n * p MOD n = m * p MOD n
  ← rewrite_goal mod_mult_lmod sym=false goal=1
qed

theorem mod_exp_mod
  fixes m :: nat, n :: nat, p :: nat
  prop (m MOD n) ^ p MOD n = m ^ p MOD n
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ (m MOD n) ^ p MOD n = m ^ p MOD n
    #[2] ¬(n = 0) ⟶ (m MOD n) ^ p MOD n = m ^ p MOD n
  ← introduction goal=1
    #[3] n = 0
    #[4] (m MOD n) ^ p MOD n = m ^ p MOD n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] (m MOD 0) ^ p MOD 0 = m ^ p MOD 0
  ← rewrite_goal mod_zero sym=false goal=5
    #[6] (m MOD 0) ^ p = m ^ p
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] (m MOD 0) ^ p = m ^ p
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] (m MOD n) ^ p MOD n = m ^ p MOD n
  ← induction p nat_induct goal=9
    #[10] (m MOD n) ^ (0::nat) MOD n = m ^ (0::nat) MOD n
    #[11] ∀n1. (m MOD n) ^ n1 MOD n = m ^ n1 MOD n ⟶ (m MOD n) ^ Suc n1 MOD n = m ^ Suc n1 MOD n
  ← rewrite_goal nat_nat_power_def_1 sym=false goal=10
    #[12] (m MOD n) ^ (0::nat) MOD n = m ^ (0::nat) MOD n
  ← introduction p goal=11
    #[13] _VAR p
    #[14] (m MOD n) ^ p MOD n = m ^ p MOD n
    #[15] (m MOD n) ^ Suc p MOD n = m ^ Suc p MOD n
  ← rewrite_goal nat_nat_power_def_2 sym=false goal=15
    #[16] m MOD n * (m MOD n) ^ p MOD n = m * m ^ p MOD n
  ← rewrite_goal mod_mult_lmod sym=false goal=16
    #[17] m * (m MOD n) ^ p MOD n = m * m ^ p MOD n
  cut "m * (m MOD n) ^ p MOD n = m * ((m MOD n) ^ p MOD n) MOD n" goal=17
    #[18] m * (m MOD n) ^ p MOD n = m * ((m MOD n) ^ p MOD n) MOD n
  ← rewrite_goal mod_mult_rmod sym=false goal=18
    #[19] m * (m MOD n) ^ p MOD n = m * ((m MOD n) ^ p MOD n) MOD n
  ← rewrite_goal_with_prev goal=17 facts=[19]
    #[20] m * ((m MOD n) ^ p MOD n) MOD n = m * m ^ p MOD n
  ← rewrite_goal_with_prev goal=20 facts=[14]
    #[21] m * (m ^ p MOD n) MOD n = m * m ^ p MOD n
  ← rewrite_goal mod_mult_rmod sym=false goal=21
    #[22] m * (m ^ p MOD n) MOD n = m * m ^ p MOD n
qed

theorem mod_mult_add_1
  fixes m :: nat, n :: nat, p :: nat
  prop (m * n + p) MOD n = p MOD n
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ (m * n + p) MOD n = p MOD n
    #[2] ¬(n = 0) ⟶ (m * n + p) MOD n = p MOD n
  ← introduction goal=1
    #[3] n = 0
    #[4] (m * n + p) MOD n = p MOD n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] (m * 0 + p) MOD 0 = p MOD 0
  ← rewrite_goal mult_0_right sym=false goal=5
    #[6] (0 + p) MOD 0 = p MOD 0
  ← rewrite_goal nat_plus_def_1 sym=false goal=6
    #[7] (0 + p) MOD 0 = p MOD 0
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] (m * n + p) MOD n = p MOD n
  ← apply_backward_step mod_uniq param_q="m+p DIV n" goal=9
    #[10] m * n + p = (m + p DIV n) * n + p MOD n
    #[11] p MOD n < n
  ← rewrite_goal distrib_r sym=false goal=10
    #[12] m * n + p = m * n + p DIV n * n + p MOD n
  ← rewrite_goal add_assoc sym=false goal=12
    #[13] m * n + p = m * n + (p DIV n * n + p MOD n)
  ← rewrite_goal division_simp1 sym=false goal=13
    #[14] m * n + p = m * n + (p DIV n * n + p MOD n)
  ← rewrite_goal mod_lt_eq sym=false goal=11
qed

theorem mod_mult_add_2
  fixes m :: nat, n :: nat, p :: nat
  prop (n * m + p) MOD n = p MOD n
  [hint_rewrite]
proof
  ← rewrite_goal mult_comm sym=false goal=0
    #[1] (m * n + p) MOD n = p MOD n
  ← rewrite_goal mod_mult_add_1 sym=false goal=1
qed

theorem mod_mult_add_3
  fixes m :: nat, n :: nat, p :: nat
  prop (p + m * n) MOD n = p MOD n
  [hint_rewrite]
proof
  ← rewrite_goal add_comm sym=false goal=0
    #[1] (m * n + p) MOD n = p MOD n
  ← rewrite_goal mod_mult_add_1 sym=false goal=1
qed

theorem mod_mult_add_4
  fixes m :: nat, n :: nat, p :: nat
  prop (p + n * m) MOD n = p MOD n
  [hint_rewrite]
proof
  ← rewrite_goal add_comm sym=false goal=0
    #[1] (n * m + p) MOD n = p MOD n
  ← rewrite_goal mod_mult_add_2 sym=false goal=1
qed

theorem div_mult_add1
  fixes a :: nat, b :: nat, n :: nat
  prop ~(n = 0) --> (a * n + b) DIV n = a + b DIV n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] (a * n + b) DIV n = a + b DIV n
  ← apply_backward_step div_uniq param_r="b MOD n" goal=2
    #[3] a * n + b = (a + b DIV n) * n + b MOD n
    #[4] b MOD n < n
  ← rewrite_goal distrib_r sym=false goal=3
    #[5] a * n + b = a * n + b DIV n * n + b MOD n
  ← rewrite_goal add_assoc sym=false goal=5
    #[6] a * n + b = a * n + (b DIV n * n + b MOD n)
  ← rewrite_goal division_simp1 sym=false goal=6
    #[7] a * n + b = a * n + (b DIV n * n + b MOD n)
  ← rewrite_goal mod_lt_eq sym=false goal=4
qed

theorem div_mult_add2
  fixes a :: nat, b :: nat, n :: nat
  prop ~(n = 0) --> (n * a + b) DIV n = a + b DIV n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] (n * a + b) DIV n = a + b DIV n
  ← rewrite_goal mult_comm sym=false goal=2
    #[3] (a * n + b) DIV n = a + b DIV n
  ← apply_backward_step div_mult_add1 goal=3 facts=[1]
qed

theorem div_mult_add3
  fixes a :: nat, b :: nat, n :: nat
  prop ~(n = 0) --> (b + a * n) DIV n = b DIV n + a
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] (b + a * n) DIV n = b DIV n + a
  ← rewrite_goal add_comm sym=false goal=2
    #[3] (a * n + b) DIV n = a + b DIV n
  ← apply_backward_step div_mult_add1 goal=3 facts=[1]
qed

theorem div_mult_add4
  fixes a :: nat, b :: nat, n :: nat
  prop ~(n = 0) --> (b + n * a) DIV n = b DIV n + a
  [hint_backward]

theorem mod_add_mod
  fixes a :: nat, b :: nat, n :: nat
  prop (a MOD n + b MOD n) MOD n = (a + b) MOD n
  [hint_rewrite]
proof
  ← cases "n = 0" goal=0
    #[1] n = 0 ⟶ (a MOD n + b MOD n) MOD n = (a + b) MOD n
    #[2] ¬(n = 0) ⟶ (a MOD n + b MOD n) MOD n = (a + b) MOD n
  ← introduction goal=1
    #[3] n = 0
    #[4] (a MOD n + b MOD n) MOD n = (a + b) MOD n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] (a MOD 0 + b MOD 0) MOD 0 = (a + b) MOD 0
  ← rewrite_goal mod_zero sym=false goal=5
    #[6] a MOD 0 + b MOD 0 = a + b
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] a MOD 0 + b MOD 0 = a + b
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] (a MOD n + b MOD n) MOD n = (a + b) MOD n
  ← rewrite_goal eq_sym_eq sym=false goal=9
    #[10] (a + b) MOD n = (a MOD n + b MOD n) MOD n
  ← apply_backward_step mod_eq param_q="a DIV n + b DIV n" goal=10
    #[11] a + b = a MOD n + b MOD n + (a DIV n + b DIV n) * n
  ← rewrite_goal distrib_r sym=false goal=11
    #[12] a + b = a MOD n + b MOD n + (a DIV n * n + b DIV n * n)
  cut "!a. !b. !c. !d::nat. (a + b) + (c + d) = (c + a) + (d + b)" goal=12
    #[13] ∀a::nat. ∀b. ∀c. ∀d. a + b + (c + d) = c + a + (d + b)
  ← z3 goal=13
    #[14] ∀a::nat. ∀b. ∀c. ∀d. a + b + (c + d) = c + a + (d + b)
  ← rewrite_goal_with_prev goal=12 facts=[14]
    #[15] a + b = a DIV n * n + a MOD n + (b DIV n * n + b MOD n)
  ← rewrite_goal division_simp1 sym=false goal=15
    #[16] a + b = a DIV n * n + a MOD n + (b DIV n * n + b MOD n)
qed

theorem div_add_mod
  fixes a :: nat, b :: nat, n :: nat
  prop ~(n = 0) --> (a + b) MOD n = a MOD n + b MOD n <--> (a + b) DIV n = a DIV n + b DIV n
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] (a + b) MOD n = a MOD n + b MOD n ⟷ (a + b) DIV n = a DIV n + b DIV n
  -> apply_forward_step division param_m=a+b goal=2 facts=[1]
    #[3] a + b = (a + b) DIV n * n + (a + b) MOD n ∧ (a + b) MOD n < n
  -> apply_forward_step division param_m=a goal=2 facts=[1]
    #[4] a = a DIV n * n + a MOD n ∧ a MOD n < n
  -> apply_forward_step division param_m=b goal=2 facts=[1]
    #[5] b = b DIV n * n + b MOD n ∧ b MOD n < n
  -> apply_forward_step conjD1 goal=2 facts=[3]
    #[6] a + b = (a + b) DIV n * n + (a + b) MOD n
  -> apply_forward_step conjD1 goal=2 facts=[4]
    #[7] a = a DIV n * n + a MOD n
  -> apply_forward_step conjD1 goal=2 facts=[5]
    #[8] b = b DIV n * n + b MOD n
  cut "!x::nat. !y. !z. !w. x = y --> z= w --> x +z = y + w" goal=2
    #[9] ∀x::nat. ∀y. ∀z. ∀w. x = y ⟶ z = w ⟶ x + z = y + w
  ← z3 goal=9
    #[10] ∀x::nat. ∀y. ∀z. ∀w. x = y ⟶ z = w ⟶ x + z = y + w
  -> apply_fact goal=2 facts=[10,7,8]
    #[11] a + b = a DIV n * n + a MOD n + (b DIV n * n + b MOD n)
  -> rewrite_fact_with_prev goal=2 facts=[6,11]
    #[12] (a + b) DIV n * n + (a + b) MOD n = a DIV n * n + a MOD n + (b DIV n * n + b MOD n)
  cut "!a::nat. !b::nat. !c. !d. (a + b) + c + d = (a + c) + (b + d)" goal=2
    #[13] ∀a::nat. ∀b. ∀c. ∀d. a + b + c + d = a + c + (b + d)
  ← z3 goal=13
    #[14] ∀a::nat. ∀b. ∀c. ∀d. a + b + c + d = a + c + (b + d)
  -> rewrite_fact eq_sym_eq sym=false goal=2 facts=[14]
    #[15] ∀a::nat. ∀b. ∀c. ∀d. a + c + (b + d) = a + b + c + d
  -> rewrite_fact_with_prev goal=2 facts=[15,12]
    #[16] (a + b) DIV n * n + (a + b) MOD n = a DIV n * n + b DIV n * n + a MOD n + b MOD n
  ← apply_backward_step iffI goal=2
    #[17] (a + b) MOD n = a MOD n + b MOD n ⟶ (a + b) DIV n = a DIV n + b DIV n
    #[18] (a + b) DIV n = a DIV n + b DIV n ⟶ (a + b) MOD n = a MOD n + b MOD n
  ← introduction goal=17
    #[19] (a + b) MOD n = a MOD n + b MOD n
    #[20] (a + b) DIV n = a DIV n + b DIV n
  -> rewrite_fact_with_prev goal=20 facts=[19,16]
    #[21] (a + b) DIV n * n + (a MOD n + b MOD n) = a DIV n * n + b DIV n * n + a MOD n + b MOD n
  -> rewrite_fact add_assoc sym=false goal=20 facts=[21]
    #[22] (a + b) DIV n * n + (a MOD n + b MOD n) = a DIV n * n + b DIV n * n + (a MOD n + b MOD n)
  -> apply_forward_step add_cancel_right goal=20 facts=[22]
    #[23] (a + b) DIV n * n = a DIV n * n + b DIV n * n
  -> rewrite_fact distrib_r sym=true goal=20 facts=[23]
    #[24] (a + b) DIV n * n = (a DIV n + b DIV n) * n
  -> rewrite_fact eq_mult_rcancel sym=false goal=20 facts=[24]
    #[25] n = 0 ∨ (a + b) DIV n = a DIV n + b DIV n
  ← apply_backward_step disjE goal=20 facts=[25]
    #[26] n = 0 ⟶ (a + b) DIV n = a DIV n + b DIV n
    #[27] (a + b) DIV n = a DIV n + b DIV n ⟶ (a + b) DIV n = a DIV n + b DIV n
  ← introduction goal=26
    #[28] n = 0
    #[29] (a + b) DIV n = a DIV n + b DIV n
  ← apply_backward_step negE_gen goal=29 facts=[1,28]
    #[30] (a + b) DIV n = a DIV n + b DIV n
  ← introduction goal=18
    #[31] (a + b) DIV n = a DIV n + b DIV n
    #[32] (a + b) MOD n = a MOD n + b MOD n
  -> rewrite_fact_with_prev goal=32 facts=[31,16]
    #[33] (a DIV n + b DIV n) * n + (a + b) MOD n = a DIV n * n + b DIV n * n + a MOD n + b MOD n
  -> rewrite_fact distrib_r sym=false goal=32 facts=[33]
    #[34] a DIV n * n + b DIV n * n + (a + b) MOD n = a DIV n * n + b DIV n * n + a MOD n + b MOD n
  -> rewrite_fact add_assoc sym=false goal=32 facts=[34]
    #[35] a DIV n * n + (b DIV n * n + (a + b) MOD n) = a DIV n * n + b DIV n * n + (a MOD n + b MOD n)
  -> rewrite_fact add_assoc sym=false goal=32 facts=[35]
    #[36] a DIV n * n + (b DIV n * n + (a + b) MOD n) = a DIV n * n + (b DIV n * n + (a MOD n + b MOD n))
  -> apply_forward_step add_cancel_left goal=32 facts=[36]
    #[37] b DIV n * n + (a + b) MOD n = b DIV n * n + (a MOD n + b MOD n)
  ← apply_backward_step add_cancel_left goal=32 facts=[37]
qed

theorem div_refl
  fixes n :: nat
  prop ~(n = 0) --> n DIV n = 1
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(n = 0)
    #[2] n DIV n = 1
  ← apply_backward_step div_uniq param_r="(0::nat)" goal=2
    #[3] n = 1 * n + 0
    #[4] 0 < n
  nat_norm goal=3
    #[5] n = 1 * n + 0
  ← apply_backward_step le_1_1 goal=4
qed

theorem mod_le
  fixes m :: nat, n :: nat
  prop m MOD n <= m
  [hint_backward]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ m MOD n ≤ m
    #[2] ¬(n = 0) ⟶ m MOD n ≤ m
  ← introduction goal=1
    #[3] n = 0
    #[4] m MOD n ≤ m
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m MOD 0 ≤ m
  ← rewrite_goal mod_zero sym=false goal=5
    #[6] m ≤ m
  ← apply_backward_step less_eq_refl goal=6
    #[7] m ≤ m
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] m MOD n ≤ m
  ← apply_backward_step less_eq_trans param_m="m DIV n * n + m MOD n" goal=9
    #[10] m MOD n ≤ m DIV n * n + m MOD n
    #[11] m DIV n * n + m MOD n ≤ m
  ← rewrite_goal add_comm sym=false goal=10
    #[12] m MOD n ≤ m MOD n + m DIV n * n
  ← apply_backward_step le_add goal=12
    #[13] m MOD n ≤ m MOD n + m DIV n * n
  ← rewrite_goal division_simp1 sym=false goal=11
    #[14] m ≤ m
  ← apply_backward_step less_eq_refl goal=14
qed

theorem div_mono2
  fixes m :: nat, n :: nat, p :: nat
  prop ~(p = 0) --> p <= m --> n DIV m <= n DIV p
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] ¬(p = 0)
    #[2] p ≤ m
    #[3] n DIV m ≤ n DIV p
  -> apply_forward_step le_rdiv_eq param_b=n param_n="n DIV m" goal=3 facts=[1]
    #[4] n DIV m ≤ n DIV p ⟷ p * (n DIV m) ≤ n
  ← rewrite_goal_with_prev goal=3 facts=[4]
    #[5] p * (n DIV m) ≤ n
  ← apply_backward_step less_eq_trans param_m="m * (n DIV m)" goal=5
    #[6] p * (n DIV m) ≤ m * (n DIV m)
    #[7] m * (n DIV m) ≤ n
  ← rewrite_goal le_mult_rcancel sym=false goal=6
    #[8] p ≤ m ∨ n DIV m = 0
  ← apply_backward_step disjI1 goal=8
  ← apply_backward_step div_mul_le goal=7
    #[9] m * (n DIV m) ≤ n
qed

theorem div_le_exclusion
  fixes a :: nat, b :: nat, c :: nat, d :: nat
  prop ~(b = 0) --> b * c < (a + 1) * d --> c DIV d <= a DIV b
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] ¬(b = 0)
    #[2] b * c < (a + 1) * d
    #[3] c DIV d ≤ a DIV b
  ← cases "d=0" goal=3
    #[4] d = 0 ⟶ c DIV d ≤ a DIV b
    #[5] ¬(d = 0) ⟶ c DIV d ≤ a DIV b
  ← introduction goal=4
    #[6] d = 0
    #[7] c DIV d ≤ a DIV b
  ← rewrite_goal_with_prev goal=7 facts=[6]
    #[8] c DIV 0 ≤ a DIV b
  ← rewrite_goal div_zero sym=false goal=8
    #[9] 0 ≤ a DIV b
  ← apply_backward_step le_0 goal=9
    #[10] 0 ≤ a DIV b
  ← introduction goal=5
    #[11] ¬(d = 0)
    #[12] c DIV d ≤ a DIV b
  cut "!a. !b. (!k::nat. k <= a --> k <= b) --> a <= b" goal=12
    #[13] ∀a::nat. ∀b. (∀k. k ≤ a ⟶ k ≤ b) ⟶ a ≤ b
  ← z3 goal=13
    #[14] ∀a::nat. ∀b. (∀k. k ≤ a ⟶ k ≤ b) ⟶ a ≤ b
  ← apply_prev goal=12 facts=[14]
    #[15] ∀k. k ≤ c DIV d ⟶ k ≤ a DIV b
  ← introduction k goal=15
    #[16] _VAR k
    #[17] k ≤ c DIV d
    #[18] k ≤ a DIV b
  cut "b * d * k <= b * c --> (b * k) * d < (a + 1) * d" goal=18
    #[19] b * d * k ≤ b * c ⟶ b * k * d < (a + 1) * d
  ← introduction goal=19
    #[20] b * d * k ≤ b * c
    #[21] b * k * d < (a + 1) * d
  cut "b * k * d = b * d * k" goal=21
    #[22] b * k * d = b * d * k
  nat_norm goal=22
    #[23] b * k * d = b * d * k
  ← rewrite_goal_with_prev goal=21 facts=[23]
    #[24] b * d * k < (a + 1) * d
  ← apply_backward_step let_trans goal=24 facts=[20,2]
    #[25] b * d * k < (a + 1) * d
  -> apply_forward_step le_rdiv_eq param_b=c param_n=k goal=18 facts=[11]
    #[26] k ≤ c DIV d ⟷ d * k ≤ c
  -> rewrite_fact_with_prev goal=18 facts=[26,17]
    #[27] d * k ≤ c
  -> apply_forward_step le_rdiv_eq param_b=a param_n=k goal=18 facts=[1]
    #[28] k ≤ a DIV b ⟷ b * k ≤ a
  ← rewrite_goal_with_prev goal=18 facts=[28]
    #[29] b * k ≤ a
  -> rewrite_fact lt_mult_rcancel sym=false goal=29 facts=[19]
    #[30] b * d * k ≤ b * c ⟶ b * k < a + 1 ∧ ¬(d = 0)
  -> rewrite_fact mult_assoc sym=false goal=29 facts=[30]
    #[31] b * (d * k) ≤ b * c ⟶ b * k < a + 1 ∧ ¬(d = 0)
  -> rewrite_fact mult_comm sym=false goal=29 facts=[31]
    #[32] d * k * b ≤ c * b ⟶ k * b < a + 1 ∧ ¬(d = 0)
  -> rewrite_fact le_mult_rcancel sym=false goal=29 facts=[32]
    #[33] d * k ≤ c ∨ b = 0 ⟶ k * b < a + 1 ∧ ¬(d = 0)
  ← rewrite_goal less_Suc_lesseq sym=true goal=29
    #[34] b * k < Suc a
  ← rewrite_goal add_1_right sym=true goal=34
    #[35] b * k < a + 1
  cut "k * b < a + 1 ∧ ¬(d = 0) --> b * k < a + 1" goal=35
    #[36] k * b < a + 1 ∧ ¬(d = 0) ⟶ b * k < a + 1
  ← introduction goal=36
    #[37] k * b < a + 1 ∧ ¬(d = 0)
    #[38] b * k < a + 1
  -> apply_forward_step conjD1 goal=38 facts=[37]
    #[39] k * b < a + 1
  cut "b * k = k * b" goal=38
    #[40] b * k = k * b
  nat_norm goal=40
    #[41] b * k = k * b
  ← rewrite_goal_with_prev goal=38 facts=[41]
  ← apply_prev goal=35 facts=[36]
    #[42] k * b < a + 1 ∧ ¬(d = 0)
  ← apply_prev goal=42 facts=[33]
    #[43] d * k ≤ c ∨ b = 0
  ← apply_backward_step disjI1 goal=43
qed

theorem div_eq_exclusion
  fixes a :: nat, b :: nat, c :: nat, d :: nat
  prop b * c < (a + 1) * d --> a * d < (c + 1) * b --> a DIV b = c DIV d
  [hint_backward,hint_backward1]
proof
  ← introduction goal=0
    #[1] b * c < (a + 1) * d
    #[2] a * d < (c + 1) * b
    #[3] a DIV b = c DIV d
  ← cases "b=0" goal=3
    #[4] b = 0 ⟶ a DIV b = c DIV d
    #[5] ¬(b = 0) ⟶ a DIV b = c DIV d
  ← introduction goal=4
    #[6] b = 0
    #[7] a DIV b = c DIV d
  ← rewrite_goal_with_prev goal=7 facts=[6]
    #[8] a DIV 0 = c DIV d
  ← cases "d=0" goal=8
    #[9] d = 0 ⟶ a DIV 0 = c DIV d
    #[10] ¬(d = 0) ⟶ a DIV 0 = c DIV d
  ← introduction goal=9
    #[11] d = 0
    #[12] a DIV 0 = c DIV d
  ← rewrite_goal_with_prev goal=12 facts=[11]
    #[13] a DIV 0 = c DIV 0
  ← rewrite_goal div_zero sym=false goal=13
    #[14] a DIV 0 = c DIV 0
  ← introduction goal=10
    #[15] ¬(d = 0)
    #[16] a DIV 0 = c DIV d
  -> rewrite_fact_with_prev goal=16 facts=[6,2]
    #[17] a * d < (c + 1) * 0
  -> rewrite_fact mult_0_right sym=false goal=16 facts=[17]
    #[18] a * d < 0
  ← apply_backward_step contrapositive goal=16 facts=[18]
    #[19] ¬(a DIV 0 = c DIV d) ⟶ ¬(a * d < 0)
  ← introduction goal=19
    #[20] ¬(a DIV 0 = c DIV d)
    #[21] ¬(a * d < 0)
  ← rewrite_goal not_lt sym=false goal=21
    #[22] 0 ≤ a * d
  ← apply_backward_step le_0 goal=22
    #[23] 0 ≤ a * d
  ← introduction goal=5
    #[24] ¬(b = 0)
    #[25] a DIV b = c DIV d
  ← cases "d=0" goal=25
    #[26] d = 0 ⟶ a DIV b = c DIV d
    #[27] ¬(d = 0) ⟶ a DIV b = c DIV d
  ← introduction goal=26
    #[28] a DIV b = c DIV d
  -> rewrite_fact_with_prev goal=28 facts=[11,1]
    #[29] b * c < (a + 1) * 0
  -> rewrite_fact mult_0_right sym=false goal=28 facts=[29]
    #[30] b * c < 0
  ← apply_backward_step contrapositive goal=28 facts=[30]
    #[31] ¬(a DIV b = c DIV d) ⟶ ¬(b * c < 0)
  ← introduction goal=31
    #[32] ¬(a DIV b = c DIV d)
    #[33] ¬(b * c < 0)
  ← rewrite_goal not_lt sym=false goal=33
    #[34] 0 ≤ b * c
  ← apply_backward_step le_0 goal=34
    #[35] 0 ≤ b * c
  ← introduction goal=27
    #[36] a DIV b = c DIV d
  ← rewrite_goal le_antisym sym=true goal=36
    #[37] a DIV b ≤ c DIV d ∧ c DIV d ≤ a DIV b
  ← apply_backward_step conjI goal=37
    #[38] a DIV b ≤ c DIV d
    #[39] c DIV d ≤ a DIV b
  ← apply_backward_step div_le_exclusion goal=38
    #[40] d * a < (c + 1) * b
  cut "d * a = a*d" goal=40
    #[41] d * a = a * d
  nat_norm goal=41
    #[42] d * a = a * d
  ← rewrite_goal_with_prev goal=40 facts=[42]
  ← apply_backward_step div_le_exclusion goal=39
qed

theorem mult_div_le
  fixes m :: nat, n :: nat, p :: nat
  prop m * (n DIV p) <= m * n DIV p
  [hint_backward]
proof
  ← cases "p=0" goal=0
    #[1] p = 0 ⟶ m * (n DIV p) ≤ m * n DIV p
    #[2] ¬(p = 0) ⟶ m * (n DIV p) ≤ m * n DIV p
  ← introduction goal=1
    #[3] p = 0
    #[4] m * (n DIV p) ≤ m * n DIV p
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m * (n DIV 0) ≤ m * n DIV 0
  ← rewrite_goal div_zero sym=false goal=5
    #[6] m * 0 ≤ 0
  ← rewrite_goal mult_0_right sym=false goal=6
    #[7] (0::nat) ≤ 0
  ← apply_backward_step le_0 goal=7
    #[8] (0::nat) ≤ 0
  ← introduction goal=2
    #[9] ¬(p = 0)
    #[10] m * (n DIV p) ≤ m * n DIV p
  -> apply_forward_step le_rdiv_eq param_b="m * n" param_n="m * (n DIV p)" goal=10 facts=[9]
    #[11] m * (n DIV p) ≤ m * n DIV p ⟷ p * (m * (n DIV p)) ≤ m * n
  ← rewrite_goal_with_prev goal=10 facts=[11]
    #[12] p * (m * (n DIV p)) ≤ m * n
  cut "p * (m * (n DIV p)) = m * (n DIV p) * p" goal=12
    #[13] p * (m * (n DIV p)) = m * (n DIV p) * p
  nat_norm goal=13
    #[14] p * (m * (n DIV p)) = m * (n DIV p) * p
  ← rewrite_goal_with_prev goal=12 facts=[14]
    #[15] m * (n DIV p) * p ≤ m * n
  ← rewrite_goal mult_assoc sym=false goal=15
    #[16] m * (n DIV p * p) ≤ m * n
  ← apply_backward_step le_mult2 goal=16
    #[17] m ≤ m
    #[18] n DIV p * p ≤ n
  ← apply_backward_step less_eq_refl goal=17
    #[19] m ≤ m
  ← apply_backward_step less_eq_trans param_m="n DIV p * p + n MOD p" goal=18
    #[20] n DIV p * p ≤ n DIV p * p + n MOD p
    #[21] n DIV p * p + n MOD p ≤ n
  ← apply_backward_step le_add goal=20
    #[22] n DIV p * p ≤ n DIV p * p + n MOD p
  ← rewrite_goal division_simp1 sym=false goal=21
    #[23] n ≤ n
  ← apply_backward_step less_eq_refl goal=23
    #[24] n ≤ n
qed

theorem div_div
  fixes m :: nat, n :: nat, p :: nat
  prop m DIV n DIV p = m DIV (n * p)
  [hint_rewrite]
proof
  ← cases "p=0" goal=0
    #[1] p = 0 ⟶ m DIV n DIV p = m DIV (n * p)
    #[2] ¬(p = 0) ⟶ m DIV n DIV p = m DIV (n * p)
  ← introduction goal=1
    #[3] p = 0
    #[4] m DIV n DIV p = m DIV (n * p)
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m DIV n DIV 0 = m DIV (n * 0)
  ← rewrite_goal mult_0_right sym=false goal=5
    #[6] m DIV n DIV 0 = m DIV 0
  ← rewrite_goal div_zero sym=false goal=6
    #[7] m DIV n DIV 0 = m DIV 0
  ← introduction goal=2
    #[8] ¬(p = 0)
    #[9] m DIV n DIV p = m DIV (n * p)
  ← cases "n=0" goal=9
    #[10] n = 0 ⟶ m DIV n DIV p = m DIV (n * p)
    #[11] ¬(n = 0) ⟶ m DIV n DIV p = m DIV (n * p)
  ← introduction goal=10
    #[12] n = 0
    #[13] m DIV n DIV p = m DIV (n * p)
  ← rewrite_goal_with_prev goal=13 facts=[12]
    #[14] m DIV 0 DIV p = m DIV (0 * p)
  ← rewrite_goal nat_times_def_1 sym=false goal=14
    #[15] m DIV 0 DIV p = m DIV 0
  ← rewrite_goal div_zero sym=false goal=15
    #[16] 0 DIV p = 0
  ← rewrite_goal div_0 sym=false goal=16
    #[17] 0 DIV p = 0
  ← introduction goal=11
    #[18] ¬(n = 0)
    #[19] m DIV n DIV p = m DIV (n * p)
  cut "!m::nat. !n. (!k. k <= m <--> k <= n) --> m = n" goal=19
    #[20] ∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n
  ← z3 goal=20
    #[21] ∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n
  ← apply_prev goal=19 facts=[21]
    #[22] ∀k. k ≤ m DIV n DIV p ⟷ k ≤ m DIV (n * p)
  ← introduction k goal=22
    #[23] _VAR k
    #[24] k ≤ m DIV n DIV p ⟷ k ≤ m DIV (n * p)
  -> apply_forward_step le_rdiv_eq param_b="m DIV n" param_n=k goal=24 facts=[8]
    #[25] k ≤ m DIV n DIV p ⟷ p * k ≤ m DIV n
  cut "~(n * p =0)" goal=24
    #[26] ¬(n * p = 0)
  ← rewrite_goal mult_eq_0 sym=false goal=26
    #[27] ¬(n = 0 ∨ p = 0)
  ← rewrite_goal de_morgan_thm2 sym=false goal=27
    #[28] ¬(n = 0) ∧ ¬(p = 0)
  ← apply_backward_step conjI goal=28
  -> apply_forward_step le_rdiv_eq param_b=m param_n=k goal=24 facts=[26]
    #[29] k ≤ m DIV (n * p) ⟷ n * p * k ≤ m
  -> apply_forward_step le_rdiv_eq param_b=m param_n=p*k goal=24 facts=[18]
    #[30] p * k ≤ m DIV n ⟷ n * (p * k) ≤ m
  ← rewrite_goal_with_prev goal=24 facts=[25]
    #[31] p * k ≤ m DIV n ⟷ k ≤ m DIV (n * p)
  ← rewrite_goal_with_prev goal=31 facts=[30]
    #[32] n * (p * k) ≤ m ⟷ k ≤ m DIV (n * p)
  ← rewrite_goal_with_prev goal=32 facts=[29]
    #[33] n * (p * k) ≤ m ⟷ n * p * k ≤ m
  ← rewrite_goal mult_assoc sym=false goal=33
    #[34] n * (p * k) ≤ m ⟷ n * p * k ≤ m
qed

theorem div_mod
  fixes m :: nat, n :: nat, p :: nat
  prop m DIV n MOD p = m MOD (n * p) DIV n
  [hint_rewrite]
proof
  ← cases "p=0" goal=0
    #[1] p = 0 ⟶ m DIV n MOD p = m MOD (n * p) DIV n
    #[2] ¬(p = 0) ⟶ m DIV n MOD p = m MOD (n * p) DIV n
  ← introduction goal=1
    #[3] p = 0
    #[4] m DIV n MOD p = m MOD (n * p) DIV n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m DIV n MOD 0 = m MOD (n * 0) DIV n
  ← rewrite_goal mult_0_right sym=false goal=5
    #[6] m DIV n MOD 0 = m MOD 0 DIV n
  ← rewrite_goal mod_zero sym=false goal=6
    #[7] m DIV n MOD 0 = m MOD 0 DIV n
  ← introduction goal=2
    #[8] ¬(p = 0)
    #[9] m DIV n MOD p = m MOD (n * p) DIV n
  ← cases "n=0" goal=9
    #[10] n = 0 ⟶ m DIV n MOD p = m MOD (n * p) DIV n
    #[11] ¬(n = 0) ⟶ m DIV n MOD p = m MOD (n * p) DIV n
  ← introduction goal=10
    #[12] n = 0
    #[13] m DIV n MOD p = m MOD (n * p) DIV n
  ← rewrite_goal_with_prev goal=13 facts=[12]
    #[14] m DIV 0 MOD p = m MOD (0 * p) DIV 0
  ← rewrite_goal div_zero sym=false goal=14
    #[15] 0 MOD p = 0
  ← rewrite_goal mod_0 sym=false goal=15
    #[16] 0 MOD p = 0
  ← introduction goal=11
    #[17] ¬(n = 0)
    #[18] m DIV n MOD p = m MOD (n * p) DIV n
  cut "∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n" goal=18
    #[19] ∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n
  ← z3 goal=19
    #[20] ∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n
  ← apply_prev goal=18 facts=[20]
    #[21] ∀k. k ≤ m DIV n MOD p ⟷ k ≤ m MOD (n * p) DIV n
  ← introduction k goal=21
    #[22] _VAR k
    #[23] k ≤ m DIV n MOD p ⟷ k ≤ m MOD (n * p) DIV n
  cut "k ≤ m DIV n MOD p ⟷ k + p * ((m DIV n) DIV p) <= (m DIV n)" goal=23
    #[24] k ≤ m DIV n MOD p ⟷ k + p * (m DIV n DIV p) ≤ m DIV n
  ← rewrite_goal add_comm sym=false goal=24
    #[25] k ≤ m DIV n MOD p ⟷ p * (m DIV n DIV p) + k ≤ m DIV n
  -> apply_forward_step le_add_lcancel param_m="p * (m DIV n DIV p)" param_n=k param_p="m DIV n MOD p" goal=25
    #[26] p * (m DIV n DIV p) + k ≤ p * (m DIV n DIV p) + m DIV n MOD p ⟷ k ≤ m DIV n MOD p
  -> rewrite_fact division_simp2 sym=false goal=25 facts=[26]
    #[27] p * (m DIV n DIV p) + k ≤ m DIV n ⟷ k ≤ m DIV n MOD p
  ← rewrite_goal_with_prev goal=25 facts=[27]
    #[28] k ≤ m DIV n MOD p ⟷ p * (m DIV n DIV p) + k ≤ m DIV n
  ← rewrite_goal_with_prev goal=23 facts=[24]
    #[29] k + p * (m DIV n DIV p) ≤ m DIV n ⟷ k ≤ m MOD (n * p) DIV n
  ← rewrite_goal div_div sym=false goal=29
    #[30] k + p * (m DIV (n * p)) ≤ m DIV n ⟷ k ≤ m MOD (n * p) DIV n
  cut "~(n * p = 0)" goal=30
    #[31] ¬(n * p = 0)
  ← rewrite_goal mult_eq_0 sym=false goal=31
    #[32] ¬(n = 0 ∨ p = 0)
  ← rewrite_goal de_morgan_thm2 sym=false goal=32
    #[33] ¬(n = 0) ∧ ¬(p = 0)
  ← apply_backward_step conjI goal=33
  -> apply_forward_step division param_m=m goal=30 facts=[31]
    #[34] m = m DIV (n * p) * (n * p) + m MOD (n * p) ∧ m MOD (n * p) < n * p
  -> apply_forward_step conjD1 goal=30 facts=[34]
    #[35] m = m DIV (n * p) * (n * p) + m MOD (n * p)
  cut "m DIV n = ((n * p) * (m DIV (n * p)) + m MOD (n * p)) DIV n" goal=30
    #[36] m DIV n = (n * p * (m DIV (n * p)) + m MOD (n * p)) DIV n
  ← rewrite_goal division_simp2 sym=false goal=36
    #[37] m DIV n = (n * p * (m DIV (n * p)) + m MOD (n * p)) DIV n
  ← rewrite_goal_with_prev goal=30 facts=[37]
    #[38] k + p * (m DIV (n * p)) ≤ (n * p * (m DIV (n * p)) + m MOD (n * p)) DIV n ⟷ k ≤ m MOD (n * p) DIV n
  ← rewrite_goal mult_assoc sym=false goal=38
    #[39] k + p * (m DIV (n * p)) ≤ (n * (p * (m DIV (n * p))) + m MOD (n * p)) DIV n ⟷ k ≤ m MOD (n * p) DIV n
  -> apply_forward_step div_mult_add2 param_a="p * (m DIV (n * p))" param_b="m MOD (n * p)" goal=39 facts=[17]
    #[40] (n * (p * (m DIV (n * p))) + m MOD (n * p)) DIV n = p * (m DIV (n * p)) + m MOD (n * p) DIV n
  ← rewrite_goal_with_prev goal=39 facts=[40]
    #[41] k + p * (m DIV (n * p)) ≤ p * (m DIV (n * p)) + m MOD (n * p) DIV n ⟷ k ≤ m MOD (n * p) DIV n
  cut "k + p * (m DIV (n * p)) = p * (m DIV (n * p)) + k" goal=41
    #[42] k + p * (m DIV (n * p)) = p * (m DIV (n * p)) + k
  nat_norm goal=42
    #[43] k + p * (m DIV (n * p)) = p * (m DIV (n * p)) + k
  ← rewrite_goal_with_prev goal=41 facts=[43]
    #[44] p * (m DIV (n * p)) + k ≤ p * (m DIV (n * p)) + m MOD (n * p) DIV n ⟷ k ≤ m MOD (n * p) DIV n
  ← rewrite_goal le_add_lcancel sym=false goal=44
    #[45] p * (m DIV (n * p)) + k ≤ p * (m DIV (n * p)) + m MOD (n * p) DIV n ⟷ k ≤ m MOD (n * p) DIV n
qed

theorem mod_mult_mod
  fixes m :: nat, n :: nat, p :: nat
  prop m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
    #[2] ¬(n = 0) ⟶ m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  ← introduction goal=1
    #[3] n = 0
    #[4] m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] m MOD (0 * p) = 0 * (m DIV 0 MOD p) + m MOD 0
  ← rewrite_goal nat_times_def_1 sym=false goal=5
    #[6] m MOD 0 = 0 + m MOD 0
  ← rewrite_goal nat_plus_def_1 sym=false goal=6
    #[7] m MOD 0 = 0 + m MOD 0
  ← introduction goal=2
    #[8] ¬(n = 0)
    #[9] m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  ← cases "p=0" goal=9
    #[10] p = 0 ⟶ m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
    #[11] ¬(p = 0) ⟶ m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  ← introduction goal=10
    #[12] p = 0
    #[13] m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  ← rewrite_goal_with_prev goal=13 facts=[12]
    #[14] m MOD (n * 0) = n * (m DIV n MOD 0) + m MOD n
  ← rewrite_goal mult_0_right sym=false goal=14
    #[15] m MOD 0 = n * (m DIV n MOD 0) + m MOD n
  ← rewrite_goal mod_zero sym=false goal=15
    #[16] m = n * (m DIV n) + m MOD n
  ← rewrite_goal division_simp2 sym=false goal=16
    #[17] m = n * (m DIV n) + m MOD n
  ← introduction goal=11
    #[18] ¬(p = 0)
    #[19] m MOD (n * p) = n * (m DIV n MOD p) + m MOD n
  ← apply_backward_step add_cancel_left param_x="n * (m DIV n DIV p * p)" goal=19
    #[20] n * (m DIV n DIV p * p) + m MOD (n * p) = n * (m DIV n DIV p * p) + (n * (m DIV n MOD p) + m MOD n)
  ← rewrite_goal add_assoc sym=true goal=20
    #[21] n * (m DIV n DIV p * p) + m MOD (n * p) = n * (m DIV n DIV p * p) + n * (m DIV n MOD p) + m MOD n
  ← rewrite_goal distrib_l sym=true goal=21
    #[22] n * (m DIV n DIV p * p) + m MOD (n * p) = n * (m DIV n DIV p * p + m DIV n MOD p) + m MOD n
  ← rewrite_goal division_simp1 sym=false goal=22
    #[23] n * (m DIV n DIV p * p) + m MOD (n * p) = n * (m DIV n) + m MOD n
  ← rewrite_goal division_simp2 sym=false goal=23
    #[24] n * (m DIV n DIV p * p) + m MOD (n * p) = m
  cut "n * (m DIV n DIV p * p) = m DIV n DIV p * (n * p)" goal=24
    #[25] n * (m DIV n DIV p * p) = m DIV n DIV p * (n * p)
  nat_norm goal=25
    #[26] n * (m DIV n DIV p * p) = m DIV n DIV p * (n * p)
  ← rewrite_goal_with_prev goal=24 facts=[26]
    #[27] m DIV n DIV p * (n * p) + m MOD (n * p) = m
  ← rewrite_goal div_div sym=false goal=27
    #[28] m DIV (n * p) * (n * p) + m MOD (n * p) = m
  ← rewrite_goal division_simp1 sym=false goal=28
    #[29] m DIV (n * p) * (n * p) + m MOD (n * p) = m
qed

theorem mod_mod_exp_min
  fixes m :: nat, n :: nat, p :: nat, x :: nat
  prop x MOD p ^ m MOD p ^ n = x MOD p ^ min m n
proof
  ← cases "p=0" goal=0
    #[1] p = 0 ⟶ x MOD p ^ m MOD p ^ n = x MOD p ^ min m n
    #[2] ¬(p = 0) ⟶ x MOD p ^ m MOD p ^ n = x MOD p ^ min m n
  ← introduction goal=1
    #[3] p = 0
    #[4] x MOD p ^ m MOD p ^ n = x MOD p ^ min m n
  ← rewrite_goal_with_prev goal=4 facts=[3]
    #[5] x MOD 0 ^ m MOD 0 ^ n = x MOD 0 ^ min m n
  ← rewrite_goal exp_zero sym=false goal=5
    #[6] x MOD (if m = 0 then 1 else 0) MOD (if n = 0 then 1 else 0) = x MOD (if min m n = 0 then 1 else 0)
  ← rewrite_goal min_def sym=false goal=6
    #[7] x MOD (if m = 0 then 1 else 0) MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← cases "m=0" goal=7
    #[8] m = 0 ⟶ x MOD (if m = 0 then 1 else 0) MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
    #[9] ¬(m = 0) ⟶ x MOD (if m = 0 then 1 else 0) MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← introduction goal=8
    #[10] m = 0
    #[11] x MOD (if m = 0 then 1 else 0) MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=11 facts=[10]
    #[12] x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← cases "m ≤ n" goal=12
    #[13] m ≤ n ⟶ x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
    #[14] ¬(m ≤ n) ⟶ x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← introduction goal=13
    #[15] m ≤ n
    #[16] x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=16 facts=[15]
    #[17] x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD (if m = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=17 facts=[10]
    #[18] x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD 1
  ← rewrite_goal mod_1 sym=false goal=18
    #[19] 0 MOD (if n = 0 then 1 else 0) = 0
  ← rewrite_goal mod_0 sym=false goal=19
    #[20] 0 MOD (if n = 0 then 1 else 0) = 0
  ← introduction goal=14
    #[21] ¬(m ≤ n)
    #[22] x MOD 1 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← apply_backward_step negE_gen goal=22 facts=[21]
    #[23] m ≤ n
  ← rewrite_goal_with_prev goal=23 facts=[10]
    #[24] 0 ≤ n
  ← apply_backward_step le_0 goal=24
    #[25] 0 ≤ n
  ← introduction goal=9
    #[26] ¬(m = 0)
    #[27] x MOD (if m = 0 then 1 else 0) MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=27 facts=[26]
    #[28] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← cases "m ≤ n" goal=28
    #[29] m ≤ n ⟶ x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
    #[30] ¬(m ≤ n) ⟶ x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← introduction goal=29
    #[31] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=31 facts=[15]
    #[32] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if m = 0 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=32 facts=[26]
    #[33] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD 0
  ← rewrite_goal mod_zero sym=false goal=33
    #[34] x MOD (if n = 0 then 1 else 0) = x
  ← cases "n = 0" goal=34
    #[35] n = 0 ⟶ x MOD (if n = 0 then 1 else 0) = x
    #[36] ¬(n = 0) ⟶ x MOD (if n = 0 then 1 else 0) = x
  ← introduction goal=35
    #[37] n = 0
    #[38] x MOD (if n = 0 then 1 else 0) = x
  -> rewrite_fact_with_prev goal=38 facts=[37,15]
    #[39] m ≤ 0
  ← apply_backward_step negE_gen goal=38 facts=[26]
    #[40] m = 0
  -> rewrite_fact nat_less_eq_def_1 sym=false goal=40 facts=[39]
    #[41] m = 0
  ← introduction goal=36
    #[42] ¬(n = 0)
    #[43] x MOD (if n = 0 then 1 else 0) = x
  ← rewrite_goal if_not_P sym=false goal=43 facts=[42]
    #[44] x MOD 0 = x
  ← rewrite_goal mod_zero sym=false goal=44
    #[45] x MOD 0 = x
  ← introduction goal=30
    #[46] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if (if m ≤ n then m else n) = 0 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=46 facts=[21]
    #[47] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if n = 0 then 1 else 0)
  ← rewrite_goal mod_zero sym=false goal=47
    #[48] x MOD 0 MOD (if n = 0 then 1 else 0) = x MOD (if n = 0 then 1 else 0)
  ← rewrite_goal min_def sym=false goal=2
    #[49] ¬(p = 0) ⟶ x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← introduction goal=49
    #[50] ¬(p = 0)
    #[51] x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← cases "m ≤ n" goal=51
    #[52] m ≤ n ⟶ x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
    #[53] ¬(m ≤ n) ⟶ x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← introduction goal=52
    #[54] x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← rewrite_goal if_P sym=false goal=54 facts=[15]
    #[55] x MOD p ^ m MOD p ^ n = x MOD p ^ m
  -> rewrite_fact less_eq_exist sym=false goal=55 facts=[15]
    #[56] ∃p. n = m + p
  exists_elim d goal=55 facts=[56]
    #[57] _VAR d
    #[58] n = m + d
    #[59] x MOD p ^ m MOD p ^ n = x MOD p ^ m
    #[60] x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← rewrite_goal_with_prev goal=59 facts=[58]
    #[61] x MOD p ^ m MOD p ^ (m + d) = x MOD p ^ m
  ← apply_backward_step mod_lt goal=61
    #[62] x MOD p ^ m < p ^ (m + d)
  ← apply_backward_step lte_trans param_m="p ^ m" goal=62
    #[63] x MOD p ^ m < p ^ m
    #[64] p ^ m ≤ p ^ (m + d)
  ← rewrite_goal mod_lt_eq_lt sym=false goal=63
    #[65] 0 < p ^ m
  ← rewrite_goal exp_lt_0 sym=false goal=65
    #[66] ¬(p = 0) ∨ m = 0
  ← apply_backward_step disjI1 goal=66
  ← rewrite_goal le_exp sym=false goal=64
    #[67] if p = 0 then m = 0 ⟶ m + d = 0 else p = 1 ∨ m ≤ m + d
  ← rewrite_goal if_not_P sym=false goal=67 facts=[50]
    #[68] p = 1 ∨ m ≤ m + d
  ← apply_backward_step disjI2 goal=68
    #[69] m ≤ m + d
  ← apply_backward_step le_add goal=69
    #[70] m ≤ m + d
  ← introduction goal=53
    #[71] x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← rewrite_goal if_not_P sym=false goal=71 facts=[21]
    #[72] x MOD p ^ m MOD p ^ n = x MOD p ^ n
  -> rewrite_fact not_le sym=false goal=72 facts=[21]
    #[73] n < m
  -> apply_forward_step lt_imp_le goal=72 facts=[73]
    #[74] n ≤ m
  -> rewrite_fact less_eq_exist sym=false goal=72 facts=[74]
    #[75] ∃p. m = n + p
  exists_elim d goal=72 facts=[75]
    #[76] m = n + d
    #[77] x MOD p ^ m MOD p ^ n = x MOD p ^ n
    #[78] x MOD p ^ m MOD p ^ n = x MOD p ^ (if m ≤ n then m else n)
  ← rewrite_goal_with_prev goal=77 facts=[76]
    #[79] x MOD p ^ (n + d) MOD p ^ n = x MOD p ^ n
  ← rewrite_goal nat_power_add sym=false goal=79
    #[80] x MOD (p ^ n * p ^ d) MOD p ^ n = x MOD p ^ n
  ← rewrite_goal mod_mod sym=false goal=80
    #[81] x MOD (p ^ n * p ^ d) MOD p ^ n = x MOD p ^ n
qed

theorem div_exp
  fixes m :: nat, n :: nat, p :: nat
  prop ~(m = 0) --> m ^ n DIV m ^ p = (if p <= n then m ^ (n - p) else if m = 1 then 1 else 0)
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(m = 0)
    #[2] m ^ n DIV m ^ p = (if p ≤ n then m ^ (n - p) else if m = 1 then 1 else 0)
  ← cases "p ≤ n" goal=2
    #[3] p ≤ n ⟶ m ^ n DIV m ^ p = (if p ≤ n then m ^ (n - p) else if m = 1 then 1 else 0)
    #[4] ¬(p ≤ n) ⟶ m ^ n DIV m ^ p = (if p ≤ n then m ^ (n - p) else if m = 1 then 1 else 0)
  ← introduction goal=3
    #[5] p ≤ n
    #[6] m ^ n DIV m ^ p = (if p ≤ n then m ^ (n - p) else if m = 1 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=6 facts=[5]
    #[7] m ^ n DIV m ^ p = m ^ (n - p)
  ← apply_backward_step div_uniq param_r="(0::nat)" goal=7
    #[8] m ^ n = m ^ (n - p) * m ^ p + 0
    #[9] 0 < m ^ p
  ← rewrite_goal add_0_right sym=false goal=8
    #[10] m ^ n = m ^ (n - p) * m ^ p
  ← rewrite_goal nat_power_add sym=true goal=10
    #[11] m ^ n = m ^ (n - p + p)
  -> apply_forward_step sub_add goal=11 facts=[5]
    #[12] n - p + p = n
  ← rewrite_goal_with_prev goal=11 facts=[12]
    #[13] m ^ n = m ^ (n - p + p)
  ← rewrite_goal exp_lt_0 sym=false goal=9
    #[14] ¬(m = 0) ∨ p = 0
  ← apply_backward_step disjI1 goal=14
  ← introduction goal=4
    #[15] ¬(p ≤ n)
    #[16] m ^ n DIV m ^ p = (if p ≤ n then m ^ (n - p) else if m = 1 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=16 facts=[15]
    #[17] m ^ n DIV m ^ p = (if m = 1 then 1 else 0)
  ← cases "m =1" goal=17
    #[18] m = 1 ⟶ m ^ n DIV m ^ p = (if m = 1 then 1 else 0)
    #[19] ¬(m = 1) ⟶ m ^ n DIV m ^ p = (if m = 1 then 1 else 0)
  ← introduction goal=18
    #[20] m = 1
    #[21] m ^ n DIV m ^ p = (if m = 1 then 1 else 0)
  ← rewrite_goal if_P sym=false goal=21 facts=[20]
    #[22] m ^ n DIV m ^ p = 1
  ← rewrite_goal_with_prev goal=22 facts=[20]
    #[23] 1 ^ n DIV 1 ^ p = 1
  ← rewrite_goal exp_one sym=false goal=23
    #[24] 1 DIV 1 = 1
  ← rewrite_goal div_1 sym=false goal=24
    #[25] 1 DIV 1 = 1
  ← introduction goal=19
    #[26] ¬(m = 1)
    #[27] m ^ n DIV m ^ p = (if m = 1 then 1 else 0)
  ← rewrite_goal if_not_P sym=false goal=27 facts=[26]
    #[28] m ^ n DIV m ^ p = 0
  ← apply_backward_step div_lt goal=28
    #[29] m ^ n < m ^ p
  ← rewrite_goal lt_exp sym=false goal=29
    #[30] 2 ≤ m ∧ n < p ∨ m = 0 ∧ ¬(n = 0) ∧ p = 0
  ← apply_backward_step disjI1 goal=30
    #[31] 2 ≤ m ∧ n < p
  ← apply_backward_step conjI goal=31
    #[32] 2 ≤ m
    #[33] n < p
  ← z3 goal=32 facts=[26,1]
    #[34] 2 ≤ m
  -> rewrite_fact not_le sym=false goal=33 facts=[15]
    #[35] n < p
qed

theorem mod_exp
  fixes m :: nat, n :: nat, p :: nat
  prop ~(m = 0) --> m ^ n MOD m ^ p = (if p <= n | m = 1 then 0 else m ^ n)
  [hint_backward]
proof
  ← introduction goal=0
    #[1] ¬(m = 0)
    #[2] m ^ n MOD m ^ p = (if p ≤ n ∨ m = 1 then 0 else m ^ n)
  ← cases "p ≤ n ∨ m = 1" goal=2
    #[3] p ≤ n ∨ m = 1 ⟶ m ^ n MOD m ^ p = (if p ≤ n ∨ m = 1 then 0 else m ^ n)
    #[4] ¬(p ≤ n ∨ m = 1) ⟶ m ^ n MOD m ^ p = (if p ≤ n ∨ m = 1 then 0 else m ^ n)
  ← introduction goal=3
    #[5] p ≤ n ∨ m = 1
    #[6] m ^ n MOD m ^ p = (if p ≤ n ∨ m = 1 then 0 else m ^ n)
  ← rewrite_goal if_P sym=false goal=6 facts=[5]
    #[7] m ^ n MOD m ^ p = 0
  ← apply_backward_step disjE goal=7 facts=[5]
    #[8] p ≤ n ⟶ m ^ n MOD m ^ p = 0
    #[9] m = 1 ⟶ m ^ n MOD m ^ p = 0
  ← introduction goal=8
    #[10] p ≤ n
    #[11] m ^ n MOD m ^ p = 0
  ← rewrite_goal mod_eq_0 sym=false goal=11
    #[12] ∃q. m ^ n = q * m ^ p
  ← inst_exists_goal "m ^ (n-p)" goal=12
    #[13] m ^ n = m ^ (n - p) * m ^ p
  ← rewrite_goal nat_power_add sym=true goal=13
    #[14] m ^ n = m ^ (n - p + p)
  -> apply_forward_step sub_add goal=14 facts=[10]
    #[15] n - p + p = n
  ← rewrite_goal_with_prev goal=14 facts=[15]
    #[16] m ^ n = m ^ (n - p + p)
  ← introduction goal=9
    #[17] m = 1
    #[18] m ^ n MOD m ^ p = 0
  ← rewrite_goal_with_prev goal=18 facts=[17]
    #[19] 1 ^ n MOD 1 ^ p = 0
  ← rewrite_goal exp_one sym=false goal=19
    #[20] 1 MOD 1 = 0
  ← rewrite_goal mod_1 sym=false goal=20
    #[21] 1 MOD 1 = 0
  ← introduction goal=4
    #[22] ¬(p ≤ n ∨ m = 1)
    #[23] m ^ n MOD m ^ p = (if p ≤ n ∨ m = 1 then 0 else m ^ n)
  ← rewrite_goal if_not_P sym=false goal=23 facts=[22]
    #[24] m ^ n MOD m ^ p = m ^ n
  ← rewrite_goal mod_eq_self sym=false goal=24
    #[25] m ^ p = 0 ∨ m ^ n < m ^ p
  ← apply_backward_step disjI2 goal=25
    #[26] m ^ n < m ^ p
  ← rewrite_goal lt_exp sym=false goal=26
    #[27] 2 ≤ m ∧ n < p ∨ m = 0 ∧ ¬(n = 0) ∧ p = 0
  ← apply_backward_step disjI1 goal=27
    #[28] 2 ≤ m ∧ n < p
  ← apply_backward_step conjI goal=28
    #[29] 2 ≤ m
    #[30] n < p
  -> rewrite_fact de_morgan_thm2 sym=false goal=29 facts=[22]
    #[31] ¬(p ≤ n) ∧ ¬(m = 1)
  -> apply_forward_step conjD1 goal=29 facts=[31]
    #[32] ¬(p ≤ n)
  -> apply_forward_step conjD2 goal=29 facts=[31]
    #[33] ¬(m = 1)
  cut "2 = Suc 1" goal=29
    #[34] 2 = Suc 1
  ← rewrite_goal add_1_right sym=true goal=34
    #[35] (2::nat) = 1 + 1
  ← rewrite_goal mult_2 sym=true goal=35
    #[36] (2::nat) = 2 * 1
  ← rewrite_goal mult_1_right sym=false goal=36
    #[37] (2::nat) = 2 * 1
  ← rewrite_goal_with_prev goal=29 facts=[34]
    #[38] Suc 1 ≤ m
  ← rewrite_goal nat_one_def sym=false goal=38
    #[39] Suc (Suc 0) ≤ m
  ← rewrite_goal lesseq_Suc_less sym=false goal=39
    #[40] Suc 0 < m
  ← rewrite_goal not_le sym=true goal=40
    #[41] ¬(m ≤ Suc 0)
  ← rewrite_goal le_lt sym=false goal=41
    #[42] ¬(m < Suc 0 ∨ m = Suc 0)
  ← rewrite_goal de_morgan_thm2 sym=false goal=42
    #[43] ¬(m < Suc 0) ∧ ¬(m = Suc 0)
  ← rewrite_goal less_Suc_lesseq sym=false goal=43
    #[44] ¬(m ≤ 0) ∧ ¬(m = Suc 0)
  ← apply_backward_step conjI goal=44
    #[45] ¬(m ≤ 0)
    #[46] ¬(m = Suc 0)
  ← rewrite_goal nat_less_eq_def_1 sym=false goal=45
  ← rewrite_goal add_1_right sym=true goal=46
    #[47] ¬(m = 0 + 1)
  ← rewrite_goal nat_plus_def_1 sym=false goal=47
  -> rewrite_fact not_le sym=false goal=30 facts=[32]
    #[48] n < p
qed

