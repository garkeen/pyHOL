theory mult_loop
imports hoare

program mult_loop
  vars: a: nat, b: nat, A: nat, B: nat
  pre: a == 0 & b == 0
  post: b == A * B
  body:
    while (a != A) {
      [b == a * B] b := b + B;
      a := a + 1
    }
