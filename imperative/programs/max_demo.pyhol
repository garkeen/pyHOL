theory max_demo
imports hoare
description "translated from .imp"

theorem max_demo_vc_0
  fixes m :: nat, n :: nat
  prop ∀s::nat ⇒ nat. true ⟶ (if m ≤ n then n = m ∨ n = n else m = m ∨ m = n)
  proof
    0: z3
  qed
