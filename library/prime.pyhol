theory prime
imports gcd
description "Basic theory of divisibility, gcd, coprimality and primality (over N)."

def coprime :: nat ⇒ nat ⇒ bool = coprime a b ⟷ (∀d. divides d a ∧ divides d b ⟷ d = 1) [hint_rewrite]

theorem coprime_gcd
  fixes a :: nat, b :: nat
  prop coprime a b <--> gcd a b = 1
  [hint_rewrite]
proof
  ← rewrite coprime_def sym=false goal=0
    #[1] (∀d. divides d a ∧ divides d b ⟷ d = 1) ⟷ gcd a b = 1
  ← rule iffI goal=1
    #[2] (∀d. divides d a ∧ divides d b ⟷ d = 1) ⟶ gcd a b = 1
    #[3] gcd a b = 1 ⟶ (∀d. divides d a ∧ divides d b ⟷ d = 1)
  ← intro goal=2
    #[4] ∀d. divides d a ∧ divides d b ⟷ d = 1
    #[5] gcd a b = 1
  ← rule gcd_equals goal=5
    #[6] divides 1 a
    #[7] divides 1 b
    #[8] ∀b1. divides b1 a ⟶ divides b1 b ⟶ divides b1 1
  ← rule divides_1 goal=6
    #[9] divides 1 a
  ← rule divides_1 goal=7
    #[10] divides 1 b
  ← intro d goal=8
    #[11] _VAR d
    #[12] divides d a
    #[13] divides d b
    #[14] divides d 1
  → forward conjI goal=14 facts=[12,13]
    #[15] divides d a ∧ divides d b
  → rewrite target=fact source=prev goal=14 facts=[4,15]
    #[16] d = 1
  ← rewrite source=prev goal=14 facts=[16]
    #[17] divides 1 1
  ← rewrite divides_one sym=false goal=17
    #[18] divides 1 1
  ← intro goal=3
    #[19] gcd a b = 1
    #[20] ∀d. divides d a ∧ divides d b ⟷ d = 1
  ← intro d goal=20
    #[21] divides d a ∧ divides d b ⟷ d = 1
  ← rule iffI goal=21
    #[22] divides d a ∧ divides d b ⟶ d = 1
    #[23] d = 1 ⟶ divides d a ∧ divides d b
  ← intro goal=22
    #[24] divides d a ∧ divides d b
    #[25] d = 1
  → forward conjD1 goal=25 facts=[24]
    #[26] divides d a
  → forward conjD2 goal=25 facts=[24]
    #[27] divides d b
  → forward gcd_is_max goal=25 facts=[26,27]
    #[28] divides d (gcd a b)
  → rewrite target=fact source=prev goal=25 facts=[19,28]
    #[29] divides d 1
  → rewrite target=fact divides_one sym=false goal=25 facts=[29]
  ← intro goal=23
    #[30] d = 1
    #[31] divides d a ∧ divides d b
  ← rewrite source=prev goal=31 facts=[30]
    #[32] divides 1 a ∧ divides 1 b
  ← rule conjI goal=32
    #[33] divides 1 a
    #[34] divides 1 b
  ← rule divides_1 goal=33
  ← rule divides_1 goal=34
qed

theorem coprime_sym
  fixes a :: nat, b :: nat
  prop coprime a b <--> coprime b a
  [hint_rewrite,hint_backward]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd a b = 1 ⟷ gcd b a = 1
  ← rule iffI goal=1
    #[2] gcd a b = 1 ⟶ gcd b a = 1
    #[3] gcd b a = 1 ⟶ gcd a b = 1
  ← intro goal=2
    #[4] gcd a b = 1
    #[5] gcd b a = 1
  ← rewrite gcd_comm sym=false goal=5
  ← intro goal=3
    #[6] gcd b a = 1
    #[7] gcd a b = 1
  ← rewrite gcd_comm sym=false goal=7
qed

theorem coprime_bezout
  fixes a :: nat, b :: nat
  prop coprime a b <--> (?x. ?y. a * x - b * y = 1 | b * x - a * y = 1)
  [hint_rewrite]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd a b = 1 ⟷ (∃x. ∃y. a * x - b * y = 1 ∨ b * x - a * y = 1)
  ← rewrite gcd_bezout sym=false goal=1
    #[2] gcd a b = 1 ⟷ divides (gcd a b) 1
  ← rewrite divides_one sym=false goal=2
qed

theorem coprime_divprod
  fixes a :: nat, b :: nat, d :: nat
  prop divides d (a * b) --> coprime d a --> divides d b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] divides d (a * b)
    #[2] coprime d a
    #[3] divides d b
  → rewrite target=fact divides_lmul_gcd sym=true goal=3 facts=[1]
    #[4] divides d (gcd d a * b)
  → rewrite target=fact coprime_gcd sym=false goal=3 facts=[2]
    #[5] gcd d a = 1
  → rewrite target=fact source=prev goal=3 facts=[5,4]
    #[6] divides d (1 * b)
  → rewrite target=fact mult_1_left sym=false goal=3 facts=[6]
qed

theorem coprime_rmod
  fixes a :: nat, n :: nat
  prop coprime n (a MOD n) <--> coprime n a
  [hint_rewrite]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd n (a MOD n) = 1 ⟷ gcd n a = 1
  → forward division_simp1 param_m=a param_n=n goal=1
    #[2] a DIV n * n + a MOD n = a
  → rewrite target=fact eq_sym_eq sym=false goal=1 facts=[2]
    #[3] a = a DIV n * n + a MOD n
  ← rewrite source=prev goal=1 facts=[3]
    #[4] gcd n ((a DIV n * n + a MOD n) MOD n) = 1 ⟷ gcd n (a DIV n * n + a MOD n) = 1
  cut "(a DIV n * n + a MOD n) MOD n = a MOD n" goal=4
    #[5] (a DIV n * n + a MOD n) MOD n = a MOD n
  ← rewrite division_simp1 sym=false goal=5
  ← rewrite source=prev goal=4 facts=[5]
    #[6] gcd n (a MOD n) = 1 ⟷ gcd n (a DIV n * n + a MOD n) = 1
  ← rewrite gcd_mod sym=false goal=6
qed

theorem coprime_lmod
  fixes a :: nat, n :: nat
  prop coprime (a MOD n) n <--> coprime a n
  [hint_rewrite]
proof
  ← rewrite coprime_sym sym=false goal=0
    #[1] coprime n (a MOD n) ⟷ coprime n a
  ← rewrite coprime_rmod sym=false goal=1
qed

theorem gcd_mul_coprime_1
  fixes a :: nat, b :: nat, c :: nat
  prop coprime a b --> gcd a (b * c) = gcd a c
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] gcd a (b * c) = gcd a c
  ← rule gcd_eq goal=2
    #[3] ∀d. divides d a ∧ divides d (b * c) ⟷ divides d a ∧ divides d c
  ← intro d goal=3
    #[4] _VAR d
    #[5] divides d a ∧ divides d (b * c) ⟷ divides d a ∧ divides d c
  ← rule iffI goal=5
    #[6] divides d a ∧ divides d (b * c) ⟶ divides d a ∧ divides d c
    #[7] divides d a ∧ divides d c ⟶ divides d a ∧ divides d (b * c)
  ← intro goal=6
    #[8] divides d a ∧ divides d (b * c)
    #[9] divides d a ∧ divides d c
  ← rule conjI goal=9
    #[10] divides d a
    #[11] divides d c
  ← rule conjD1 goal=10 facts=[8]
    #[12] divides d a
  → forward conjD2 goal=11 facts=[8]
    #[13] divides d (b * c)
  ← rule coprime_divprod goal=11 facts=[13]
    #[14] coprime d b
  → rewrite target=fact coprime_gcd sym=false goal=14 facts=[1]
    #[15] gcd a b = 1
  ← rewrite coprime_gcd sym=false goal=14
    #[16] gcd d b = 1
  → rewrite target=fact divides_gcd_right sym=false goal=16 facts=[12]
    #[17] gcd a d = d
  → rewrite target=fact eq_sym_eq sym=false goal=16 facts=[17]
    #[18] d = gcd a d
  ← rewrite source=prev goal=16 facts=[18]
    #[19] gcd (gcd a d) b = 1
  ← rewrite gcd_comm sym=false goal=19
    #[20] gcd b (gcd a d) = 1
  ← rewrite gcd_assoc sym=true goal=20
    #[21] gcd (gcd b a) d = 1
  → rewrite target=fact gcd_comm sym=false goal=21 facts=[15]
    #[22] gcd b a = 1
  ← rewrite source=prev goal=21 facts=[22]
    #[23] gcd 1 d = 1
  ← rewrite gcd_1_left sym=false goal=23
    #[24] gcd 1 d = 1
  ← intro goal=7
    #[25] divides d a ∧ divides d c
    #[26] divides d a ∧ divides d (b * c)
  ← rule conjI goal=26
    #[27] divides d a
    #[28] divides d (b * c)
  ← rule conjD1 goal=27 facts=[25]
    #[29] divides d a
  ← rule divides_lmul goal=28
    #[30] divides d c
  ← rule conjD2 goal=30 facts=[25]
    #[31] divides d c
qed

theorem gcd_mul_coprime_2
  fixes a :: nat, b :: nat, c :: nat
  prop coprime a c --> gcd a (b * c) = gcd a b
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime a c
    #[2] gcd a (b * c) = gcd a b
  ← rewrite mult_comm sym=false goal=2
    #[3] gcd a (c * b) = gcd a b
  ← rule gcd_mul_coprime_1 goal=3 facts=[1]
qed

theorem coprime_1
  fixes a :: nat
  prop coprime a 1
  [hint_backward]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd a 1 = 1
  ← rewrite gcd_1_right sym=false goal=1
qed

theorem gcd_coprime
  fixes a :: nat, b :: nat, c :: nat, d :: nat
  prop ~(gcd a b = 0) --> a = c * gcd a b --> b = d * gcd a b --> coprime c d
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] ¬(gcd a b = 0)
    #[2] a = c * gcd a b
    #[3] b = d * gcd a b
    #[4] coprime c d
  ← rewrite coprime_gcd sym=false goal=4
    #[5] gcd c d = 1
  → forward gcd_mult_right param_k="gcd a b" param_m=c param_n=d goal=5
    #[6] gcd (c * gcd a b) (d * gcd a b) = gcd a b * gcd c d
  → rewrite target=fact eq_sym_eq sym=false goal=5 facts=[2]
    #[7] c * gcd a b = a
  → rewrite target=fact eq_sym_eq sym=false goal=5 facts=[3]
    #[8] d * gcd a b = b
  → rewrite target=fact source=prev goal=5 facts=[7,6]
    #[9] gcd a (d * gcd a b) = gcd a b * gcd c d
  → rewrite target=fact source=prev goal=5 facts=[8,9]
    #[10] gcd a b = gcd a b * gcd c d
  cut "gcd a b = gcd a b * 1" goal=5 facts=[10]
    #[11] gcd a b = gcd a b * 1
  ← rewrite mult_1_right sym=false goal=11
    #[12] gcd a b = gcd a b * 1
  → rewrite target=fact source=prev goal=5 facts=[12,10]
    #[13] gcd a b * 1 = gcd a b * 1 * gcd c d
  → rewrite target=fact mult_assoc sym=false goal=5 facts=[13]
    #[14] gcd a b * 1 = gcd a b * (1 * gcd c d)
  → rewrite target=fact mult_1_left sym=false goal=5 facts=[14]
    #[15] gcd a b * 1 = gcd a b * gcd c d
  → rewrite target=fact eq_mult_lcancel sym=false goal=5 facts=[15]
    #[16] gcd a b = 0 ∨ 1 = gcd c d
  ← rule disjE goal=5 facts=[16]
    #[17] gcd a b = 0 ⟶ gcd c d = 1
    #[18] 1 = gcd c d ⟶ gcd c d = 1
  ← intro goal=17
    #[19] gcd a b = 0
    #[20] gcd c d = 1
  ← rule negE_gen goal=20 facts=[1,19]
    #[21] gcd c d = 1
  ← intro goal=18
    #[22] 1 = gcd c d
    #[23] gcd c d = 1
  ← rewrite eq_sym_eq sym=false goal=23
qed

theorem gcd_coprime_exists
  fixes a :: nat, b :: nat
  prop ?c. ?d. a = c * gcd a b & b = d * gcd a b & coprime c d
  [hint_backward]
proof
  ← cases "gcd a b = 0" goal=0
    #[1] gcd a b = 0 ⟶ (∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d)
    #[2] ¬(gcd a b = 0) ⟶ (∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d)
  ← intro goal=1
    #[3] gcd a b = 0
    #[4] ∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
  → rewrite target=fact gcd_zero sym=false goal=4 facts=[3]
    #[5] a = 0 ∧ b = 0
  → forward conjD1 goal=4 facts=[5]
    #[6] a = 0
  → forward conjD2 goal=4 facts=[5]
    #[7] b = 0
  ← inst "(0::nat)" goal=4
    #[8] ∃d. a = 0 * gcd a b ∧ b = d * gcd a b ∧ coprime 0 d
  ← rule exI param_a="(1::nat)" goal=8
    #[9] a = 0 * gcd a b ∧ b = 1 * gcd a b ∧ coprime 0 1
  ← rewrite source=prev goal=9 facts=[6]
    #[10] 0 = 0 * gcd 0 b ∧ b = 1 * gcd 0 b ∧ coprime 0 1
  ← rewrite source=prev goal=10 facts=[7]
    #[11] 0 = 0 * gcd 0 0 ∧ 0 = 1 * gcd 0 0 ∧ coprime 0 1
  ← rule conjI goal=11
    #[12] 0 = 0 * gcd 0 0
    #[13] 0 = 1 * gcd 0 0 ∧ coprime 0 1
  ← rewrite nat_times_def_1 sym=false goal=12
    #[14] 0 = 0 * gcd 0 0
  ← rule conjI goal=13
    #[15] 0 = 1 * gcd 0 0
    #[16] coprime 0 1
  ← rewrite gcd_refl sym=false goal=15
    #[17] (0::nat) = 1 * 0
  ← rewrite mult_0_right sym=false goal=17
    #[18] (0::nat) = 1 * 0
  ← rule coprime_1 goal=16
    #[19] coprime 0 1
  ← intro goal=2
    #[20] ¬(gcd a b = 0)
    #[21] ∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
  → forward gcd_divide1 param_m=a param_n=b goal=21
    #[22] divides (gcd a b) a
  → forward gcd_divides2 param_m=a param_n=b goal=21
    #[23] divides (gcd a b) b
  → rewrite target=fact divides_def sym=false goal=21 facts=[22]
    #[24] ∃k. a = gcd a b * k
  → rewrite target=fact divides_def sym=false goal=21 facts=[23]
    #[25] ∃k. b = gcd a b * k
  elim k1 goal=21 facts=[24]
    #[26] _VAR k1
    #[27] a = gcd a b * k1
    #[28] ∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
  elim k2 goal=28 facts=[25]
    #[29] _VAR k2
    #[30] b = gcd a b * k2
    #[31] ∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
  ← inst k1 goal=31
    #[32] ∃d. a = k1 * gcd a b ∧ b = d * gcd a b ∧ coprime k1 d
  ← inst k2 goal=32
    #[33] a = k1 * gcd a b ∧ b = k2 * gcd a b ∧ coprime k1 k2
  ← rule conjI goal=33
    #[34] a = k1 * gcd a b
    #[35] b = k2 * gcd a b ∧ coprime k1 k2
  ← rewrite mult_comm sym=false goal=34
  ← rule conjI goal=35
    #[36] b = k2 * gcd a b
    #[37] coprime k1 k2
  ← rewrite mult_comm sym=false goal=36
  ← rule gcd_coprime param_a=a param_b=b goal=37
qed

theorem coprime_0
  fixes d :: nat
  prop coprime d 0 <--> d = 1
  [hint_rewrite]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd d 0 = 1 ⟷ d = 1
  ← rewrite gcd_zero_right sym=false goal=1
qed

theorem coprime_mul
  fixes a :: nat, b :: nat, d :: nat
  prop coprime d a --> coprime d b --> coprime d (a * b)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] coprime d a
    #[2] coprime d b
    #[3] coprime d (a * b)
  ← rewrite coprime_gcd sym=false goal=3
    #[4] gcd d (a * b) = 1
  → rewrite target=fact coprime_gcd sym=false goal=4 facts=[1]
    #[5] gcd d a = 1
  → rewrite target=fact coprime_gcd sym=false goal=4 facts=[2]
    #[6] gcd d b = 1
  → forward gcd_mul_coprime_1 param_c=b goal=4 facts=[1]
    #[7] gcd d (a * b) = gcd d b
  → rewrite target=fact source=prev goal=4 facts=[6,7]
qed

theorem coprime_lmul2
  fixes a :: nat, b :: nat, d :: nat
  prop coprime d (a * b) --> coprime d b
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime d (a * b)
    #[2] coprime d b
  → rewrite target=fact coprime_gcd sym=false goal=2 facts=[1]
    #[3] gcd d (a * b) = 1
  ← rewrite coprime_gcd sym=false goal=2
    #[4] gcd d b = 1
  → rewrite target=fact eq_sym_eq sym=false goal=4 facts=[3]
    #[5] 1 = gcd d (a * b)
  ← rule divides_antisym goal=4
    #[6] divides (gcd d b) 1
    #[7] divides 1 (gcd d b)
  ← rewrite source=prev goal=6 facts=[5]
    #[8] divides (gcd d b) (gcd d (a * b))
  ← rule gcd_is_max goal=8
    #[9] divides (gcd d b) d
    #[10] divides (gcd d b) (a * b)
  ← rule gcd_divide1 goal=9
    #[11] divides (gcd d b) d
  ← rule divides_lmul goal=10
    #[12] divides (gcd d b) b
  ← rule gcd_divides2 goal=12
    #[13] divides (gcd d b) b
  ← rule divides_1 goal=7
    #[14] divides 1 (gcd d b)
qed

theorem coprime_rmul2
  fixes a :: nat, b :: nat, d :: nat
  prop coprime d (a * b) --> coprime d a
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime d (a * b)
    #[2] coprime d a
  → rewrite target=fact mult_comm sym=false goal=2 facts=[1]
    #[3] coprime d (b * a)
  ← rule coprime_lmul2 goal=2 facts=[3]
qed

theorem coprime_rmul
  fixes a :: nat, b :: nat, d :: nat
  prop coprime d (a * b) <--> coprime d a & coprime d b
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] coprime d (a * b) ⟶ coprime d a ∧ coprime d b
    #[2] coprime d a ∧ coprime d b ⟶ coprime d (a * b)
  ← intro goal=1
    #[3] coprime d (a * b)
    #[4] coprime d a ∧ coprime d b
  ← rule conjI goal=4
    #[5] coprime d a
    #[6] coprime d b
  ← rule coprime_rmul2 goal=5 facts=[3]
  ← rule coprime_lmul2 goal=6 facts=[3]
  ← intro goal=2
    #[7] coprime d a ∧ coprime d b
    #[8] coprime d (a * b)
  → forward conjD1 goal=8 facts=[7]
    #[9] coprime d a
  → forward conjD2 goal=8 facts=[7]
    #[10] coprime d b
  ← rule coprime_mul goal=8
qed

theorem coprime_lmul
  fixes a :: nat, b :: nat, d :: nat
  prop coprime (a * b) d <--> coprime a d & coprime b d
  [hint_rewrite]
proof
  ← rewrite coprime_sym sym=false goal=0
    #[1] coprime d (a * b) ⟷ coprime d a ∧ coprime d b
  ← rewrite coprime_rmul sym=false goal=1
qed

theorem coprime_exp
  fixes a :: nat, d :: nat, n :: nat
  prop coprime d a --> coprime d (a ^ n)
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime d a
    #[2] coprime d (a ^ n)
  ← induct n nat_induct goal=2
    #[3] coprime d (a ^ (0::nat))
    #[4] ∀n. coprime d (a ^ n) ⟶ coprime d (a ^ Suc n)
  ← rewrite nat_nat_power_def_1 sym=false goal=3
    #[5] coprime d 1
  ← rule coprime_1 goal=5
    #[6] coprime d 1
  ← intro n goal=4
    #[7] _VAR n
    #[8] coprime d (a ^ n)
    #[9] coprime d (a ^ Suc n)
  ← rewrite nat_nat_power_def_2 sym=false goal=9
    #[10] coprime d (a * a ^ n)
  ← rule coprime_mul goal=10
qed

theorem coprime_exp_imp
  fixes a :: nat, b :: nat, n :: nat
  prop coprime a b --> coprime (a ^ n) (b ^ n)
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] coprime (a ^ n) (b ^ n)
  ← rule coprime_exp goal=2
    #[3] coprime (a ^ n) b
  ← rewrite coprime_sym sym=false goal=3
    #[4] coprime b (a ^ n)
  ← rule coprime_exp goal=4
    #[5] coprime b a
  ← rewrite coprime_sym sym=false goal=5
qed

theorem coprime_rexp
  fixes k :: nat, m :: nat, n :: nat
  prop coprime m (n ^ k) <--> coprime m n | k = 0
  [hint_rewrite]
proof
  ← induct k nat_induct goal=0
    #[1] coprime m (n ^ (0::nat)) ⟷ coprime m n ∨ (0::nat) = 0
    #[2] ∀n1. coprime m (n ^ n1) ⟷ coprime m n ∨ n1 = 0 ⟶ coprime m (n ^ Suc n1) ⟷ coprime m n ∨ Suc n1 = 0
  ← rewrite nat_nat_power_def_1 sym=false goal=1
    #[3] coprime m 1 ⟷ coprime m n ∨ (0::nat) = 0
  ← rule iffI goal=3
    #[4] coprime m 1 ⟶ coprime m n ∨ (0::nat) = 0
    #[5] coprime m n ∨ (0::nat) = 0 ⟶ coprime m 1
  ← intro goal=4
    #[6] coprime m 1
    #[7] coprime m n ∨ (0::nat) = 0
  ← rule disjI2 goal=7
    #[8] (0::nat) = 0
  ← rule eq_refl goal=8
    #[9] (0::nat) = 0
  ← intro goal=5
    #[10] coprime m n ∨ (0::nat) = 0
    #[11] coprime m 1
  ← rule coprime_1 goal=11
    #[12] coprime m 1
  ← intro n goal=2
    #[13] _VAR n
    #[14] coprime m (n ^ n) ⟷ coprime m n ∨ n = 0
    #[15] coprime m (n ^ Suc n) ⟷ coprime m n ∨ Suc n = 0
  ← rewrite nat_nat_power_def_2 sym=false goal=15
    #[16] coprime m (n * n ^ n) ⟷ coprime m n ∨ Suc n = 0
  ← rewrite coprime_rmul sym=false goal=16
    #[17] coprime m n ∧ coprime m (n ^ n) ⟷ coprime m n ∨ Suc n = 0
  ← rewrite source=prev goal=17 facts=[14]
    #[18] coprime m n ∧ (coprime m n ∨ n = 0) ⟷ coprime m n ∨ Suc n = 0
  ← rule iffI goal=18
    #[19] coprime m n ∧ (coprime m n ∨ n = 0) ⟶ coprime m n ∨ Suc n = 0
    #[20] coprime m n ∨ Suc n = 0 ⟶ coprime m n ∧ (coprime m n ∨ n = 0)
  ← intro goal=19
    #[21] coprime m n ∧ (coprime m n ∨ n = 0)
    #[22] coprime m n ∨ Suc n = 0
  ← rule disjI1 goal=22
    #[23] coprime m n
  ← rule conjD1 goal=23 facts=[21]
    #[24] coprime m n
  ← intro goal=20
    #[25] coprime m n ∨ Suc n = 0
    #[26] coprime m n ∧ (coprime m n ∨ n = 0)
  ← rule disjE goal=26 facts=[25]
    #[27] coprime m n ⟶ coprime m n ∧ (coprime m n ∨ n = 0)
    #[28] Suc n = 0 ⟶ coprime m n ∧ (coprime m n ∨ n = 0)
  ← intro goal=27
    #[29] coprime m n
    #[30] coprime m n ∧ (coprime m n ∨ n = 0)
  ← rule conjI goal=30
    #[31] coprime m n ∨ n = 0
  ← rule disjI1 goal=31
  ← intro goal=28
    #[32] Suc n = 0
    #[33] coprime m n ∧ (coprime m n ∨ n = 0)
  ← rule contrapositive goal=33 facts=[32]
    #[34] ¬(coprime m n ∧ (coprime m n ∨ n = 0)) ⟶ ¬(Suc n = 0)
  ← intro goal=34
    #[35] ¬(coprime m n ∧ (coprime m n ∨ n = 0))
    #[36] ¬(Suc n = 0)
  ← rule ineq_sym goal=36
    #[37] ¬(0 = Suc n)
  ← rule nat_zero_Suc_neq goal=37
    #[38] ¬(0 = Suc n)
qed

theorem coprime_lexp
  fixes k :: nat, m :: nat, n :: nat
  prop coprime (m ^ k) n <--> coprime m n | k = 0
  [hint_rewrite]
proof
  ← rewrite coprime_sym sym=false goal=0
    #[1] coprime n (m ^ k) ⟷ coprime n m ∨ k = 0
  ← rewrite coprime_rexp sym=false goal=1
qed

theorem coprime_exp2
  fixes k :: nat, m :: nat, n :: nat
  prop coprime (m ^ k) (n ^ k) <--> coprime m n | k = 0
  [hint_rewrite]
proof
  ← rewrite coprime_lexp sym=false goal=0
    #[1] coprime m (n ^ k) ∨ k = 0 ⟷ coprime m n ∨ k = 0
  ← rewrite coprime_rexp sym=false goal=1
    #[2] (coprime m n ∨ k = 0) ∨ k = 0 ⟷ coprime m n ∨ k = 0
  ← rule iffI goal=2
    #[3] (coprime m n ∨ k = 0) ∨ k = 0 ⟶ coprime m n ∨ k = 0
    #[4] coprime m n ∨ k = 0 ⟶ (coprime m n ∨ k = 0) ∨ k = 0
  ← intro goal=3
    #[5] (coprime m n ∨ k = 0) ∨ k = 0
    #[6] coprime m n ∨ k = 0
  ← rule disjE goal=6 facts=[5]
    #[7] coprime m n ∨ k = 0 ⟶ coprime m n ∨ k = 0
    #[8] k = 0 ⟶ coprime m n ∨ k = 0
  ← intro goal=8
    #[9] k = 0
    #[10] coprime m n ∨ k = 0
  ← rule disjI2 goal=10
  ← intro goal=4
    #[11] coprime m n ∨ k = 0
    #[12] (coprime m n ∨ k = 0) ∨ k = 0
  ← rule disjI1 goal=12
qed

theorem coprime_exp2_suc
  fixes k :: nat, m :: nat, n :: nat
  prop coprime (m ^ Suc k) (n ^ Suc k) <--> coprime m n
  [hint_rewrite]
proof
  ← rewrite coprime_exp2 sym=false goal=0
    #[1] coprime m n ∨ Suc k = 0 ⟷ coprime m n
  ← rule iffI goal=1
    #[2] coprime m n ∨ Suc k = 0 ⟶ coprime m n
    #[3] coprime m n ⟶ coprime m n ∨ Suc k = 0
  ← intro goal=2
    #[4] coprime m n ∨ Suc k = 0
    #[5] coprime m n
  ← rule disjE goal=5 facts=[4]
    #[6] coprime m n ⟶ coprime m n
    #[7] Suc k = 0 ⟶ coprime m n
  ← intro goal=7
    #[8] Suc k = 0
    #[9] coprime m n
  ← rule contrapositive goal=9 facts=[8]
    #[10] ¬(coprime m n) ⟶ ¬(Suc k = 0)
  ← intro goal=10
    #[11] ¬(coprime m n)
    #[12] ¬(Suc k = 0)
  ← rule ineq_sym goal=12
    #[13] ¬(0 = Suc k)
  ← rule nat_zero_Suc_neq goal=13
    #[14] ¬(0 = Suc k)
  ← intro goal=3
    #[15] coprime m n
    #[16] coprime m n ∨ Suc k = 0
  ← rule disjI1 goal=16
qed

theorem coprime_refl
  fixes n :: nat
  prop coprime n n <--> n = 1
  [hint_rewrite]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd n n = 1 ⟷ n = 1
  ← rewrite gcd_refl sym=false goal=1
qed

theorem coprime_plus1
  fixes n :: nat
  prop coprime (n + 1) n
  [hint_backward]
proof
  ← rewrite coprime_gcd sym=false goal=0
    #[1] gcd (n + 1) n = 1
  ← rewrite gcd_add_right sym=false goal=1
    #[2] gcd n 1 = 1
  ← rewrite gcd_1_right sym=false goal=2
qed

theorem coprime_minus1
  fixes n :: nat
  prop ~(n = 0) --> coprime (n - 1) n
  [hint_backward]
proof
  ← intro goal=0
    #[1] ¬(n = 0)
    #[2] coprime (n - 1) n
  ← rewrite coprime_sym sym=false goal=2
    #[3] coprime n (n - 1)
  cut "coprime ((n - 1) + 1) (n - 1)" goal=3
    #[4] coprime (n - 1 + 1) (n - 1)
  ← rule coprime_plus1 goal=4
    #[5] coprime (n - 1 + 1) (n - 1)
  cut "n - 1 + 1 = n" goal=3
    #[6] n - 1 + 1 = n
  ← rule sub_add goal=6
    #[7] 1 ≤ n
  ← rule le_1_2 goal=7
  → rewrite target=fact source=prev goal=3 facts=[6,5]
qed

theorem gcd_exp
  fixes k :: nat, m :: nat, n :: nat
  prop gcd (m ^ k) (n ^ k) = gcd m n ^ k
  [hint_rewrite]
proof
  → forward gcd_coprime_exists param_a=m param_b=n goal=0
    #[1] ∃c. ∃d. m = c * gcd m n ∧ n = d * gcd m n ∧ coprime c d
  elim c,d goal=0 facts=[1]
    #[2] _VAR c
    #[3] _VAR d
    #[4] m = c * gcd m n ∧ n = d * gcd m n ∧ coprime c d
    #[5] gcd (m ^ k) (n ^ k) = gcd m n ^ k
  → forward conjD1 goal=5 facts=[4]
    #[6] m = c * gcd m n
  → forward conjD2 goal=5 facts=[4]
    #[7] n = d * gcd m n ∧ coprime c d
  → forward conjD1 goal=5 facts=[7]
    #[8] n = d * gcd m n
  → forward conjD2 goal=5 facts=[7]
    #[9] coprime c d
  ← rewrite source=prev goal=5 facts=[6]
    #[10] gcd ((c * gcd m n) ^ k) (n ^ k) = gcd (c * gcd m n) n ^ k
  cut "gcd ((c * gcd m n) ^ k) ((d * gcd m n) ^ k) = gcd (c * gcd m n) (d * gcd m n) ^ k" goal=10
    #[11] gcd ((c * gcd m n) ^ k) ((d * gcd m n) ^ k) = gcd (c * gcd m n) (d * gcd m n) ^ k
  ← rewrite mult_exp sym=false goal=11
    #[12] gcd (c ^ k * gcd m n ^ k) (d ^ k * gcd m n ^ k) = gcd (c * gcd m n) (d * gcd m n) ^ k
  ← rewrite gcd_mult_right sym=false goal=12
    #[13] gcd m n ^ k * gcd (c ^ k) (d ^ k) = (gcd m n * gcd c d) ^ k
  → rewrite target=fact coprime_gcd sym=false goal=13 facts=[9]
    #[14] gcd c d = 1
  ← rewrite source=prev goal=13 facts=[14]
    #[15] gcd m n ^ k * gcd (c ^ k) (d ^ k) = (gcd m n * 1) ^ k
  ← rewrite mult_1_right sym=false goal=15
    #[16] gcd m n ^ k * gcd (c ^ k) (d ^ k) = gcd m n ^ k
  cut "gcd m n ^ k = gcd m n ^ k * 1" goal=16
    #[17] gcd m n ^ k = gcd m n ^ k * 1
  ← rewrite mult_1_right sym=false goal=17
    #[18] gcd m n ^ k = gcd m n ^ k * 1
  ← rewrite source=prev goal=16 facts=[18]
    #[19] gcd m n ^ k * 1 * gcd (c ^ k) (d ^ k) = gcd m n ^ k * 1
  ← rewrite mult_assoc sym=false goal=19
    #[20] gcd m n ^ k * (1 * gcd (c ^ k) (d ^ k)) = gcd m n ^ k * 1
  ← rewrite mult_1_left sym=false goal=20
    #[21] gcd m n ^ k * gcd (c ^ k) (d ^ k) = gcd m n ^ k * 1
  ← rewrite eq_mult_lcancel sym=false goal=21
    #[22] gcd m n ^ k = 0 ∨ gcd (c ^ k) (d ^ k) = 1
  ← rule disjI2 goal=22
    #[23] gcd (c ^ k) (d ^ k) = 1
  ← rewrite coprime_gcd sym=true goal=23
    #[24] coprime (c ^ k) (d ^ k)
  ← rewrite coprime_exp2 sym=false goal=24
    #[25] coprime c d ∨ k = 0
  ← rule disjI1 goal=25
  → rewrite target=fact eq_sym_eq sym=false goal=10 facts=[8]
    #[26] d * gcd m n = n
  → rewrite target=fact source=prev goal=10 facts=[26,11]
qed

theorem division_decomp
  fixes a :: nat, b :: nat, c :: nat
  prop divides a (b * c) --> (?b1. ?c1. a = b1 * c1 & divides b1 b & divides c1 c)
  [hint_backward]
proof
  ← intro goal=0
    #[1] divides a (b * c)
    #[2] ∃b1. ∃c1. a = b1 * c1 ∧ divides b1 b ∧ divides c1 c
  ← inst "gcd a b" goal=2
    #[3] ∃c1. a = gcd a b * c1 ∧ divides (gcd a b) b ∧ divides c1 c
  ← cases "gcd a b =0" goal=3
    #[4] gcd a b = 0 ⟶ (∃c1. a = gcd a b * c1 ∧ divides (gcd a b) b ∧ divides c1 c)
    #[5] ¬(gcd a b = 0) ⟶ (∃c1. a = gcd a b * c1 ∧ divides (gcd a b) b ∧ divides c1 c)
  ← intro goal=4
    #[6] gcd a b = 0
    #[7] ∃c1. a = gcd a b * c1 ∧ divides (gcd a b) b ∧ divides c1 c
  ← inst "(1::nat)" goal=7
    #[8] a = gcd a b * 1 ∧ divides (gcd a b) b ∧ divides 1 c
  ← rule conjI goal=8
    #[9] a = gcd a b * 1
    #[10] divides (gcd a b) b ∧ divides 1 c
  ← rewrite source=prev goal=9 facts=[6]
    #[11] a = 0 * 1
  → rewrite target=fact gcd_zero sym=false goal=11 facts=[6]
    #[12] a = 0 ∧ b = 0
  → forward conjD1 goal=11 facts=[12]
    #[13] a = 0
  ← rewrite source=prev goal=11 facts=[13]
    #[14] (0::nat) = 0 * 1
  ← rewrite mult_1_right sym=false goal=14
    #[15] (0::nat) = 0 * 1
  ← rule conjI goal=10
    #[16] divides (gcd a b) b
    #[17] divides 1 c
  ← rule gcd_divides2 goal=16
    #[18] divides (gcd a b) b
  ← rule divides_1 goal=17
    #[19] divides 1 c
  ← intro goal=5
    #[20] ¬(gcd a b = 0)
    #[21] ∃c1. a = gcd a b * c1 ∧ divides (gcd a b) b ∧ divides c1 c
  → forward gcd_coprime_exists param_a=a param_b=b goal=21
    #[22] ∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
  elim e,f goal=21 facts=[22]
    #[23] _VAR e
    #[24] _VAR f
    #[25] a = e * gcd a b ∧ b = f * gcd a b ∧ coprime e f
    #[26] ∃c1. a = gcd a b * c1 ∧ divides (gcd a b) b ∧ divides c1 c
  ← inst e goal=26
    #[27] a = gcd a b * e ∧ divides (gcd a b) b ∧ divides e c
  → forward conjD1 goal=27 facts=[25]
    #[28] a = e * gcd a b
  ← rule conjI goal=27
    #[29] a = gcd a b * e
    #[30] divides (gcd a b) b ∧ divides e c
  → rewrite target=fact mult_comm sym=false goal=29 facts=[28]
    #[31] a = gcd a b * e
  ← rule conjI goal=30
    #[32] divides (gcd a b) b
    #[33] divides e c
  ← rule gcd_divides2 goal=32
  → forward conjD2 goal=33 facts=[25]
    #[34] b = f * gcd a b ∧ coprime e f
  → forward conjD1 goal=33 facts=[34]
    #[35] b = f * gcd a b
  → forward conjD2 goal=33 facts=[34]
    #[36] coprime e f
  ← rule coprime_divprod param_a=f goal=33
    #[37] divides e (f * c)
  ← rule divides_cmul2 param_c="gcd a b" goal=37
    #[38] divides (gcd a b * e) (gcd a b * (f * c))
  → rewrite target=fact eq_sym_eq sym=false goal=38 facts=[31]
    #[39] gcd a b * e = a
  → rewrite target=fact mult_comm sym=false goal=38 facts=[35]
    #[40] b = gcd a b * f
  → rewrite target=fact eq_sym_eq sym=false goal=38 facts=[40]
    #[41] gcd a b * f = b
  ← rewrite mult_assoc sym=true goal=38
    #[42] divides (gcd a b * e) (gcd a b * f * c)
  ← rewrite source=prev goal=42 facts=[39]
    #[43] divides a (gcd a b * f * c)
  ← rewrite source=prev goal=43 facts=[41]
qed

theorem divides_exp2_rev
  fixes a :: nat, b :: nat, n :: nat
  prop divides (a ^ n) (b ^ n) --> ~(n = 0) --> divides a b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] divides (a ^ n) (b ^ n)
    #[2] ¬(n = 0)
    #[3] divides a b
  ← cases "gcd a b = 0" goal=3
    #[4] gcd a b = 0 ⟶ divides a b
    #[5] ¬(gcd a b = 0) ⟶ divides a b
  ← intro goal=4
    #[6] gcd a b = 0
    #[7] divides a b
  → rewrite target=fact gcd_zero sym=false goal=7 facts=[6]
    #[8] a = 0 ∧ b = 0
  → forward conjD1 goal=7 facts=[8]
    #[9] a = 0
  → forward conjD2 goal=7 facts=[8]
    #[10] b = 0
  ← rewrite source=prev goal=7 facts=[10]
    #[11] divides a 0
  ← rule divides_zero goal=11
    #[12] divides a 0
  ← intro goal=5
    #[13] ¬(gcd a b = 0)
    #[14] divides a b
  → forward gcd_coprime_exists param_a=a param_b=b goal=14
    #[15] ∃c. ∃d. a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
  elim c,d goal=14 facts=[15]
    #[16] _VAR c
    #[17] _VAR d
    #[18] a = c * gcd a b ∧ b = d * gcd a b ∧ coprime c d
    #[19] divides a b
  → forward conjD1 goal=19 facts=[18]
    #[20] a = c * gcd a b
  → forward conjD2 goal=19 facts=[18]
    #[21] b = d * gcd a b ∧ coprime c d
  → forward conjD2 goal=19 facts=[21]
    #[22] coprime c d
  → forward conjD1 goal=19 facts=[21]
    #[23] b = d * gcd a b
  → rewrite target=fact source=prev goal=19 facts=[20,1]
    #[24] divides ((c * gcd a b) ^ n) (b ^ n)
  cut "divides ((c * gcd a b) ^ n) ((d * gcd a b) ^ n)" goal=19 facts=[24]
    #[25] divides ((c * gcd a b) ^ n) ((d * gcd a b) ^ n)
  → rewrite target=fact eq_sym_eq sym=false goal=25 facts=[23]
    #[26] d * gcd a b = b
  ← rewrite source=prev goal=25 facts=[26]
  → rewrite target=fact mult_exp sym=false goal=19 facts=[25]
    #[27] divides (c ^ n * gcd a b ^ n) (d ^ n * gcd a b ^ n)
  → rewrite target=fact mult_comm sym=false goal=19 facts=[27]
    #[28] divides (gcd a b ^ n * c ^ n) (gcd a b ^ n * d ^ n)
  → forward divides_cmul2 goal=19 facts=[28]
    #[29] ¬(gcd a b ^ n = 0) ⟶ divides (c ^ n) (d ^ n)
  cut "¬(gcd a b ^ n = 0)" goal=19
    #[30] ¬(gcd a b ^ n = 0)
  ← rewrite exp_eq_0 sym=false goal=30
    #[31] ¬(gcd a b = 0 ∧ ¬(n = 0))
  ← rewrite de_morgan_thm1 sym=false goal=31
    #[32] ¬(gcd a b = 0) ∨ ¬¬(n = 0)
  ← rule disjI1 goal=32
  → forward goal=19 facts=[29,30]
    #[33] divides (c ^ n) (d ^ n)
  ← rewrite source=prev goal=19 facts=[20]
    #[34] divides (c * gcd a b) b
  cut "divides (c * gcd a b) (d * gcd a b)" goal=34
    #[35] divides (c * gcd a b) (d * gcd a b)
  ← rule divides_mult_right goal=35
    #[36] divides c d
  → rewrite target=fact divides_gcd_left sym=false goal=36 facts=[33]
    #[37] gcd (c ^ n) (d ^ n) = c ^ n
  → rewrite target=fact gcd_exp sym=false goal=36 facts=[37]
    #[38] gcd c d ^ n = c ^ n
  → rewrite target=fact coprime_gcd sym=false goal=36 facts=[22]
    #[39] gcd c d = 1
  → rewrite target=fact source=prev goal=36 facts=[39,38]
    #[40] 1 ^ n = c ^ n
  → rewrite target=fact exp_mono_eq sym=false goal=36 facts=[40]
    #[41] 1 = c ∨ n = 0
  ← rule disjE goal=36 facts=[41]
    #[42] 1 = c ⟶ divides c d
    #[43] n = 0 ⟶ divides c d
  ← intro goal=42
    #[44] 1 = c
    #[45] divides c d
  → rewrite target=fact eq_sym_eq sym=false goal=45 facts=[44]
    #[46] c = 1
  ← rewrite source=prev goal=45 facts=[46]
    #[47] divides 1 d
  ← rule divides_1 goal=47
    #[48] divides 1 d
  ← intro goal=43
    #[49] n = 0
    #[50] divides c d
  ← rule negE_gen goal=50 facts=[2,49]
    #[51] divides c d
  → rewrite target=fact source=prev goal=34 facts=[26,35]
qed

theorem divides_exp2_eq
  fixes a :: nat, b :: nat, n :: nat
  prop ~(n = 0) --> divides (a ^ n) (b ^ n) <--> divides a b
  [hint_backward]
proof
  ← intro goal=0
    #[1] ¬(n = 0)
    #[2] divides (a ^ n) (b ^ n) ⟷ divides a b
  ← rule iffI goal=2
    #[3] divides (a ^ n) (b ^ n) ⟶ divides a b
    #[4] divides a b ⟶ divides (a ^ n) (b ^ n)
  ← intro goal=3
    #[5] divides (a ^ n) (b ^ n)
    #[6] divides a b
  ← rule divides_exp2_rev goal=6 facts=[5,1]
  ← intro goal=4
    #[7] divides a b
    #[8] divides (a ^ n) (b ^ n)
  ← rule divides_exp goal=8 facts=[7]
    #[9] divides (a ^ n) (b ^ n)
qed

theorem divides_mul
  fixes m :: nat, n :: nat, r :: nat
  prop divides m r --> divides n r --> coprime m n --> divides (m * n) r
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] divides m r
    #[2] divides n r
    #[3] coprime m n
    #[4] divides (m * n) r
  → rewrite target=fact divides_def sym=false goal=4 facts=[1]
    #[5] ∃k. r = m * k
  → rewrite target=fact divides_def sym=false goal=4 facts=[2]
    #[6] ∃k. r = n * k
  elim k1 goal=4 facts=[5]
    #[7] _VAR k1
    #[8] r = m * k1
    #[9] divides (m * n) r
  elim k2 goal=9 facts=[6]
    #[10] _VAR k2
    #[11] r = n * k2
    #[12] divides (m * n) r
  ← rewrite source=prev goal=12 facts=[8]
    #[13] divides (m * n) (m * k1)
  ← rule divides_mult_left goal=13
    #[14] divides n k1
  ← rule coprime_divprod param_a=m goal=14
    #[15] divides n (m * k1)
    #[16] coprime n m
  → rewrite target=fact eq_sym_eq sym=false goal=15 facts=[8]
    #[17] m * k1 = r
  ← rewrite source=prev goal=15 facts=[17]
  ← rewrite coprime_sym sym=false goal=16
qed

theorem gcd_coprime_lmul
  fixes a :: nat, b :: nat, c :: nat
  prop coprime a b --> gcd (a * b) c = gcd a c * gcd b c
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] gcd (a * b) c = gcd a c * gcd b c
  → forward gcd_divide1 param_m=a param_n=c goal=2
    #[3] divides (gcd a c) a
  → forward gcd_divides2 param_m=a param_n=c goal=2
    #[4] divides (gcd a c) c
  → rewrite target=fact divides_def sym=false goal=2 facts=[3]
    #[5] ∃k. a = gcd a c * k
  → rewrite target=fact divides_def sym=false goal=2 facts=[4]
    #[6] ∃k. c = gcd a c * k
  elim k1 goal=2 facts=[5]
    #[7] _VAR k1
    #[8] a = gcd a c * k1
    #[9] gcd (a * b) c = gcd a c * gcd b c
  elim k2 goal=9 facts=[6]
    #[10] _VAR k2
    #[11] c = gcd a c * k2
    #[12] gcd (a * b) c = gcd a c * gcd b c
  cut "gcd (a * b) c = gcd (gcd a c * k1 * b) (gcd a c * k2) " goal=12
    #[13] gcd (a * b) c = gcd (gcd a c * k1 * b) (gcd a c * k2)
  → rewrite target=fact eq_sym_eq sym=false goal=13 facts=[8]
    #[14] gcd a c * k1 = a
  → rewrite target=fact eq_sym_eq sym=false goal=13 facts=[11]
    #[15] gcd a c * k2 = c
  ← rewrite source=prev goal=13 facts=[14]
    #[16] gcd (a * b) c = gcd (a * b) (gcd a c * k2)
  ← rewrite source=prev goal=16 facts=[15]
    #[17] gcd (a * b) c = gcd (a * b) (gcd a c * k2)
  → rewrite target=fact mult_assoc sym=false goal=12 facts=[13]
    #[18] gcd (a * b) c = gcd (gcd a c * (k1 * b)) (gcd a c * k2)
  → rewrite target=fact gcd_mult_left sym=false goal=12 facts=[18]
    #[19] gcd (a * b) c = gcd a c * gcd (k1 * b) k2
  ← rewrite source=prev goal=12 facts=[19]
    #[20] gcd a c * gcd (k1 * b) k2 = gcd a c * gcd b c
  ← rewrite eq_mult_lcancel sym=false goal=20
    #[21] gcd a c = 0 ∨ gcd (k1 * b) k2 = gcd b c
  ← cases "gcd a c = 0" goal=21
    #[22] gcd a c = 0 ⟶ gcd a c = 0 ∨ gcd (k1 * b) k2 = gcd b c
    #[23] ¬(gcd a c = 0) ⟶ gcd a c = 0 ∨ gcd (k1 * b) k2 = gcd b c
  ← intro goal=22
    #[24] gcd a c = 0
    #[25] gcd a c = 0 ∨ gcd (k1 * b) k2 = gcd b c
  ← rule disjI1 goal=25
  ← intro goal=23
    #[26] ¬(gcd a c = 0)
    #[27] gcd a c = 0 ∨ gcd (k1 * b) k2 = gcd b c
  ← rule disjI2 goal=27
    #[28] gcd (k1 * b) k2 = gcd b c
  ← rule divides_antisym goal=28
    #[29] divides (gcd (k1 * b) k2) (gcd b c)
    #[30] divides (gcd b c) (gcd (k1 * b) k2)
  ← rule gcd_is_max goal=29
    #[31] divides (gcd (k1 * b) k2) b
    #[32] divides (gcd (k1 * b) k2) c
  ← rule coprime_divprod param_a=k1 goal=31
    #[33] divides (gcd (k1 * b) k2) (k1 * b)
    #[34] coprime (gcd (k1 * b) k2) k1
  ← rule gcd_divide1 goal=33
    #[35] divides (gcd (k1 * b) k2) (k1 * b)
  ← rewrite coprime_gcd sym=false goal=34
    #[36] gcd (gcd (k1 * b) k2) k1 = 1
  cut "gcd (k1 * b) k2 = gcd k2 (k1 * b)" goal=36
    #[37] gcd (k1 * b) k2 = gcd k2 (k1 * b)
  ← rule gcd_comm goal=37
    #[38] gcd (k1 * b) k2 = gcd k2 (k1 * b)
  ← rewrite source=prev goal=36 facts=[38]
    #[39] gcd (gcd k2 (k1 * b)) k1 = 1
  ← rewrite gcd_assoc sym=false goal=39
    #[40] gcd k2 (gcd (k1 * b) k1) = 1
  cut "divides k1 (k1 * b)" goal=40
    #[41] divides k1 (k1 * b)
  ← rule divides_rmul goal=41
    #[42] divides k1 k1
  ← rule divides_self goal=42
    #[43] divides k1 k1
  → rewrite target=fact divides_gcd_right sym=false goal=40 facts=[41]
    #[44] gcd (k1 * b) k1 = k1
  ← rewrite source=prev goal=40 facts=[44]
    #[45] gcd k2 k1 = 1
  ← rewrite coprime_gcd sym=true goal=45
    #[46] coprime k2 k1
  ← rewrite coprime_sym sym=false goal=46
    #[47] coprime k1 k2
  ← rule gcd_coprime goal=47 facts=[26]
    #[48] a = k1 * gcd a c
    #[49] c = k2 * gcd a c
  ← rewrite mult_comm sym=false goal=48
  ← rewrite mult_comm sym=false goal=49
  ← rule divides_trans param_m=k2 goal=32
    #[50] divides (gcd (k1 * b) k2) k2
    #[51] divides k2 c
  ← rule gcd_divides2 goal=50
    #[52] divides (gcd (k1 * b) k2) k2
  ← rewrite source=prev goal=51 facts=[11]
    #[53] divides k2 (gcd a c * k2)
  ← rule divides_lmul goal=53
    #[54] divides k2 k2
  ← rule divides_self goal=54
    #[55] divides k2 k2
  ← rule gcd_is_max goal=30
    #[56] divides (gcd b c) (k1 * b)
    #[57] divides (gcd b c) k2
  ← rule divides_lmul goal=56
    #[58] divides (gcd b c) b
  ← rule gcd_divide1 goal=58
    #[59] divides (gcd b c) b
  ← rule divides_cmul2 param_c="gcd a c" goal=57
    #[60] divides (gcd a c * gcd b c) (gcd a c * k2)
  ← rewrite source=prev goal=60 facts=[15]
    #[61] divides (gcd a c * gcd b c) c
  ← rule divides_mul goal=61
    #[62] divides (gcd b c) c
    #[63] coprime (gcd a c) (gcd b c)
  ← rule gcd_divides2 goal=62
    #[64] divides (gcd b c) c
  ← rewrite coprime_gcd sym=false goal=63
    #[65] gcd (gcd a c) (gcd b c) = 1
  ← rewrite gcd_assoc sym=false goal=65
    #[66] gcd a (gcd c (gcd b c)) = 1
  ← rewrite gcd_comm sym=false goal=66
    #[67] gcd (gcd c (gcd b c)) a = 1
  cut "(gcd b c) = (gcd c b)" goal=67
    #[68] gcd b c = gcd c b
  ← rule gcd_comm goal=68
    #[69] gcd b c = gcd c b
  ← rewrite source=prev goal=67 facts=[69]
    #[70] gcd (gcd c (gcd c b)) a = 1
  ← rewrite gcd_assoc sym=true goal=70
    #[71] gcd (gcd (gcd c c) b) a = 1
  ← rewrite gcd_refl sym=false goal=71
    #[72] gcd (gcd c b) a = 1
  ← rewrite gcd_assoc sym=false goal=72
    #[73] gcd c (gcd b a) = 1
  → rewrite target=fact coprime_gcd sym=false goal=73 facts=[1]
    #[74] gcd a b = 1
  → rewrite target=fact gcd_comm sym=false goal=73 facts=[74]
    #[75] gcd b a = 1
  ← rewrite source=prev goal=73 facts=[75]
    #[76] gcd c 1 = 1
  ← rewrite gcd_1_right sym=false goal=76
    #[77] gcd c 1 = 1
qed

theorem gcd_coprime_rmul
  fixes a :: nat, b :: nat, c :: nat
  prop coprime a b --> gcd c (a * b) = gcd c a * gcd c b
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] gcd c (a * b) = gcd c a * gcd c b
  ← rewrite gcd_comm sym=false goal=2
    #[3] gcd (a * b) c = gcd a c * gcd b c
  ← rule gcd_coprime_lmul goal=3 facts=[1]
qed

theorem chinese_reminder
  fixes a :: nat, b :: nat, u :: nat, v :: nat
  prop coprime a b --> ~(a = 0) --> ~(b = 0) --> (?x. ?q1. ?q2. x = u + q1 * a & x = v + q2 * b)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] ¬(a = 0)
    #[3] ¬(b = 0)
    #[4] ∃x. ∃q1. ∃q2. x = u + q1 * a ∧ x = v + q2 * b
  cut "!P::nat⇒nat⇒nat⇒bool. (?d. ?x. ?y. (d = 1) & P x y d) <--> (?x. ?y. P x y 1)" goal=4
    #[5] ∀P. (∃d::nat. ∃x::nat. ∃y::nat. d = 1 ∧ P x y d) ⟷ (∃x. ∃y. P x y 1)
  ← intro P goal=5
    #[6] _VAR P
    #[7] (∃d. ∃x. ∃y. d = 1 ∧ P x y d) ⟷ (∃x. ∃y. P x y 1)
  ← rule iffI goal=7
    #[8] (∃d. ∃x. ∃y. d = 1 ∧ P x y d) ⟶ (∃x. ∃y. P x y 1)
    #[9] (∃x. ∃y. P x y 1) ⟶ (∃d. ∃x. ∃y. d = 1 ∧ P x y d)
  ← intro goal=8
    #[10] ∃d. ∃x. ∃y. d = 1 ∧ P x y d
    #[11] ∃x. ∃y. P x y 1
  elim d,x,y goal=11 facts=[10]
    #[12] _VAR d
    #[13] _VAR x
    #[14] _VAR y
    #[15] d = 1 ∧ P x y d
    #[16] ∃x. ∃y. P x y 1
  → forward conjD1 goal=16 facts=[15]
    #[17] d = 1
  → forward conjD2 goal=16 facts=[15]
    #[18] P x y d
  → rewrite target=fact source=prev goal=16 facts=[17,18]
    #[19] P x y 1
  ← inst x goal=16
    #[20] ∃y. P x y 1
  ← inst y goal=20
  ← intro goal=9
    #[21] ∃x. ∃y. P x y 1
    #[22] ∃d. ∃x. ∃y. d = 1 ∧ P x y d
  elim x,y goal=22 facts=[21]
    #[23] P x y 1
    #[24] ∃d. ∃x. ∃y. d = 1 ∧ P x y d
  ← inst "(1::nat)" goal=24
    #[25] ∃x. ∃y. (1::nat) = 1 ∧ P x y 1
  ← inst x goal=25
    #[26] ∃y. (1::nat) = 1 ∧ P x y 1
  ← inst y goal=26
    #[27] (1::nat) = 1 ∧ P x y 1
  ← rule conjI goal=27
    #[28] (1::nat) = 1
  ← rule eq_refl goal=28
    #[29] (1::nat) = 1
  → forward bezout_add_strong param_a=b param_b=a goal=4
    #[30] ¬(b = 0) ⟶ (∃d. ∃x. ∃y. (divides d b ∧ divides d a) ∧ b * x = a * y + d)
  → forward bezout_add_strong param_a=a param_b=b goal=4
    #[31] ¬(a = 0) ⟶ (∃d. ∃x. ∃y. (divides d a ∧ divides d b) ∧ a * x = b * y + d)
  → rewrite target=fact coprime_def sym=false goal=4 facts=[1]
    #[32] ∀d. divides d a ∧ divides d b ⟷ d = 1
  → forward goal=4 facts=[31,2]
    #[33] ∃d. ∃x. ∃y. (divides d a ∧ divides d b) ∧ a * x = b * y + d
  → forward goal=4 facts=[30,3]
    #[34] ∃d. ∃x. ∃y. (divides d b ∧ divides d a) ∧ b * x = a * y + d
  → rewrite target=fact source=prev goal=4 facts=[32,33]
    #[35] ∃d. ∃x. ∃y. d = 1 ∧ a * x = b * y + d
  → rewrite target=fact conj_comm sym=false goal=4 facts=[32]
    #[36] ∀d. divides d b ∧ divides d a ⟷ d = 1
  → rewrite target=fact source=prev goal=4 facts=[36,34]
    #[37] ∃d. ∃x. ∃y. d = 1 ∧ b * x = a * y + d
  → rewrite target=fact source=prev goal=4 facts=[5,35]
    #[38] ∃x. ∃y. a * x = b * y + 1
  → rewrite target=fact source=prev goal=4 facts=[5,37]
    #[39] ∃x. ∃y. b * x = a * y + 1
  elim x1,y1 goal=4 facts=[38]
    #[40] _VAR x1
    #[41] _VAR y1
    #[42] a * x1 = b * y1 + 1
    #[43] ∃x. ∃q1. ∃q2. x = u + q1 * a ∧ x = v + q2 * b
  elim x2,y2 goal=43 facts=[39]
    #[44] _VAR x2
    #[45] _VAR y2
    #[46] b * x2 = a * y2 + 1
    #[47] ∃x. ∃q1. ∃q2. x = u + q1 * a ∧ x = v + q2 * b
  ← inst "v * a * x1 + u * b * x2" goal=47
    #[48] ∃q1. ∃q2. v * a * x1 + u * b * x2 = u + q1 * a ∧ v * a * x1 + u * b * x2 = v + q2 * b
  ← inst "v * x1 + u * y2" goal=48
    #[49] ∃q2. v * a * x1 + u * b * x2 = u + (v * x1 + u * y2) * a ∧ v * a * x1 + u * b * x2 = v + q2 * b
  ← inst "v * y1 + u * x2" goal=49
    #[50] v * a * x1 + u * b * x2 = u + (v * x1 + u * y2) * a ∧ v * a * x1 + u * b * x2 = v + (v * y1 + u * x2) * b
  ← rule conjI goal=50
    #[51] v * a * x1 + u * b * x2 = u + (v * x1 + u * y2) * a
    #[52] v * a * x1 + u * b * x2 = v + (v * y1 + u * x2) * b
  ← rewrite mult_assoc sym=false goal=51
    #[53] v * (a * x1) + u * (b * x2) = u + (v * x1 + u * y2) * a
  ← rewrite source=prev goal=53 facts=[46]
    #[54] v * (a * x1) + u * (a * y2 + 1) = u + (v * x1 + u * y2) * a
  nat_norm goal=54
    #[55] v * (a * x1) + u * (a * y2 + 1) = u + (v * x1 + u * y2) * a
  ← rewrite mult_assoc sym=false goal=52
    #[56] v * (a * x1) + u * (b * x2) = v + (v * y1 + u * x2) * b
  ← rewrite source=prev goal=56 facts=[42]
    #[57] v * (b * y1 + 1) + u * (b * x2) = v + (v * y1 + u * x2) * b
  nat_norm goal=57
    #[58] v * (b * y1 + 1) + u * (b * x2) = v + (v * y1 + u * x2) * b
qed

def prime :: nat ⇒ bool = prime p ⟷ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p) [hint_rewrite]

theorem one_or_prime
  fixes p :: nat
  prop p = 1 | prime p <--> (!n. divides n p --> n = 1 | n = p)
  [hint_rewrite]
proof
  ← rewrite prime_def sym=false goal=0
    #[1] p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p) ⟷ (∀n. divides n p ⟶ n = 1 ∨ n = p)
  ← rule iffI goal=1
    #[2] p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p) ⟶ (∀n. divides n p ⟶ n = 1 ∨ n = p)
    #[3] (∀n. divides n p ⟶ n = 1 ∨ n = p) ⟶ p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← intro goal=2
    #[4] p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
    #[5] ∀n. divides n p ⟶ n = 1 ∨ n = p
  ← rule disjE goal=5 facts=[4]
    #[6] p = 1 ⟶ (∀n. divides n p ⟶ n = 1 ∨ n = p)
    #[7] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p) ⟶ (∀n. divides n p ⟶ n = 1 ∨ n = p)
  ← intro goal=6
    #[8] p = 1
    #[9] ∀n. divides n p ⟶ n = 1 ∨ n = p
  ← intro n goal=9
    #[10] _VAR n
    #[11] divides n p
    #[12] n = 1 ∨ n = p
  → rewrite target=fact source=prev goal=12 facts=[8,11]
    #[13] divides n 1
  → rewrite target=fact source=prev goal=12 facts=[8,11]
  → rewrite target=fact divides_one sym=false goal=12 facts=[13]
    #[14] n = 1
  ← rule disjI1 goal=12
  ← intro goal=7
    #[15] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
    #[16] ∀n. divides n p ⟶ n = 1 ∨ n = p
  ← rule conjD2 goal=16 facts=[15]
    #[17] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← intro goal=3
    #[18] ∀n. divides n p ⟶ n = 1 ∨ n = p
    #[19] p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← cases "p = 1" goal=19
    #[20] p = 1 ⟶ p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
    #[21] ¬(p = 1) ⟶ p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← intro goal=20
    #[22] p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← rule disjI1 goal=22
  ← intro goal=21
    #[23] ¬(p = 1)
    #[24] p = 1 ∨ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← rule disjI2 goal=24
    #[25] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← rule conjI goal=25
qed

theorem one_or_prime_divides_or_coprime
  fixes p :: nat
  prop p = 1 | prime p <--> (!n. divides p n | coprime p n)
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] p = 1 ∨ prime p ⟶ (∀n. divides p n ∨ coprime p n)
    #[2] (∀n. divides p n ∨ coprime p n) ⟶ p = 1 ∨ prime p
  ← intro goal=1
    #[3] p = 1 ∨ prime p
    #[4] ∀n. divides p n ∨ coprime p n
  ← rule disjE goal=4 facts=[3]
    #[5] p = 1 ⟶ (∀n. divides p n ∨ coprime p n)
    #[6] prime p ⟶ (∀n. divides p n ∨ coprime p n)
  ← intro goal=5
    #[7] p = 1
    #[8] ∀n. divides p n ∨ coprime p n
  ← rewrite source=prev goal=8 facts=[7]
    #[9] ∀n. divides 1 n ∨ coprime 1 n
  ← intro n goal=9
    #[10] _VAR n
    #[11] divides 1 n ∨ coprime 1 n
  ← rule disjI1 goal=11
    #[12] divides 1 n
  ← rule divides_1 goal=12
    #[13] divides 1 n
  ← intro goal=6
    #[14] prime p
    #[15] ∀n. divides p n ∨ coprime p n
  → rewrite target=fact prime_def sym=false goal=15 facts=[14]
    #[16] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD2 goal=15 facts=[16]
    #[17] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← intro n goal=15
    #[18] divides p n ∨ coprime p n
  ← rewrite coprime_def sym=false goal=18
    #[19] divides p n ∨ (∀d. divides d p ∧ divides d n ⟷ d = 1)
  ← cases "divides p n" goal=19
    #[20] divides p n ⟶ divides p n ∨ (∀d. divides d p ∧ divides d n ⟷ d = 1)
    #[21] ¬(divides p n) ⟶ divides p n ∨ (∀d. divides d p ∧ divides d n ⟷ d = 1)
  ← intro goal=20
    #[22] divides p n
    #[23] divides p n ∨ (∀d. divides d p ∧ divides d n ⟷ d = 1)
  ← rule disjI1 goal=23
  ← intro goal=21
    #[24] ¬(divides p n)
    #[25] divides p n ∨ (∀d. divides d p ∧ divides d n ⟷ d = 1)
  ← rule disjI2 goal=25
    #[26] ∀d. divides d p ∧ divides d n ⟷ d = 1
  ← intro d goal=26
    #[27] _VAR d
    #[28] divides d p ∧ divides d n ⟷ d = 1
  ← rule iffI goal=28
    #[29] divides d p ∧ divides d n ⟶ d = 1
    #[30] d = 1 ⟶ divides d p ∧ divides d n
  ← intro goal=29
    #[31] divides d p ∧ divides d n
    #[32] d = 1
  → forward conjD1 goal=32 facts=[31]
    #[33] divides d p
  → forward goal=32 facts=[17,33]
    #[34] d = 1 ∨ d = p
  ← rule disjE goal=32 facts=[34]
    #[35] d = 1 ⟶ d = 1
    #[36] d = p ⟶ d = 1
  ← intro goal=36
    #[37] d = p
    #[38] d = 1
  → forward conjD2 goal=38 facts=[31]
    #[39] divides d n
  → rewrite target=fact source=prev goal=38 facts=[37,39]
    #[40] divides p n
  ← rule negE_gen goal=38 facts=[24,40]
    #[41] d = 1
  ← intro goal=30
    #[42] d = 1
    #[43] divides d p ∧ divides d n
  ← rewrite source=prev goal=43 facts=[42]
    #[44] divides 1 p ∧ divides 1 n
  ← rule conjI goal=44
    #[45] divides 1 p
    #[46] divides 1 n
  ← rule divides_1 goal=45
    #[47] divides 1 p
  ← rule divides_1 goal=46
  ← intro goal=2
    #[48] ∀n. divides p n ∨ coprime p n
    #[49] p = 1 ∨ prime p
  ← rewrite one_or_prime sym=false goal=49
    #[50] ∀n. divides n p ⟶ n = 1 ∨ n = p
  ← intro n goal=50
    #[51] divides n p
    #[52] n = 1 ∨ n = p
  → inst target=fact n goal=52 facts=[48]
    #[53] divides p n ∨ coprime p n
  ← rule disjE goal=52 facts=[53]
    #[54] divides p n ⟶ n = 1 ∨ n = p
    #[55] coprime p n ⟶ n = 1 ∨ n = p
  ← intro goal=54
    #[56] n = 1 ∨ n = p
  ← rule disjI2 goal=56
    #[57] n = p
  ← rule divides_antisym goal=57
  ← intro goal=55
    #[58] coprime p n
    #[59] n = 1 ∨ n = p
  → rewrite target=fact coprime_def sym=false goal=59 facts=[58]
    #[60] ∀d. divides d p ∧ divides d n ⟷ d = 1
  → inst target=fact n goal=59 facts=[60]
    #[61] divides n p ∧ divides n n ⟷ n = 1
  ← rule disjI1 goal=59
    #[62] n = 1
  → rewrite target=fact eq_sym_eq sym=false goal=62 facts=[61]
    #[63] n = 1 ⟷ divides n p ∧ divides n n
  ← rewrite source=prev goal=62 facts=[63]
    #[64] divides n p ∧ divides n n
  ← rule conjI goal=64
    #[65] divides n n
  ← rule divides_self goal=65
    #[66] divides n n
qed

theorem prime_coprime_eq_nondivisiable
  fixes p :: nat
  prop prime p <--> (!n. coprime p n <--> ~(divides p n))
  [hint_rewrite]
proof
  ← cases "p=1" goal=0
    #[1] p = 1 ⟶ prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
    #[2] ¬(p = 1) ⟶ prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  ← intro goal=1
    #[3] p = 1
    #[4] prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  ← rewrite source=prev goal=4 facts=[3]
    #[5] prime 1 ⟷ (∀n. coprime 1 n ⟷ ¬(divides 1 n))
  ← rule iffI goal=5
    #[6] prime 1 ⟶ (∀n. coprime 1 n ⟷ ¬(divides 1 n))
    #[7] (∀n. coprime 1 n ⟷ ¬(divides 1 n)) ⟶ prime 1
  ← intro goal=6
    #[8] prime 1
    #[9] ∀n. coprime 1 n ⟷ ¬(divides 1 n)
  → rewrite target=fact prime_def sym=false goal=9 facts=[8]
    #[10] ¬((1::nat) = 1) ∧ (∀x. divides x 1 ⟶ x = 1 ∨ x = 1)
  → forward conjD1 goal=9 facts=[10]
    #[11] ¬((1::nat) = 1)
  ← rule negE_gen goal=9 facts=[11]
    #[12] (1::nat) = 1
  ← rule eq_refl goal=12
    #[13] (1::nat) = 1
  ← intro goal=7
    #[14] ∀n. coprime 1 n ⟷ ¬(divides 1 n)
    #[15] prime 1
  → inst target=fact "(1::nat)" goal=15 facts=[14]
    #[16] coprime 1 1 ⟷ ¬(divides 1 1)
  → rewrite target=fact divides_one sym=false goal=15 facts=[16]
    #[17] coprime 1 1 ⟷ ¬((1::nat) = 1)
  → rewrite target=fact coprime_gcd sym=false goal=15 facts=[17]
    #[18] gcd 1 1 = 1 ⟷ ¬((1::nat) = 1)
  → rewrite target=fact gcd_1_right sym=false goal=15 facts=[18]
    #[19] (1::nat) = 1 ⟷ ¬((1::nat) = 1)
  ← z3 goal=15 facts=[19]
    #[20] prime 1
  ← intro goal=2
    #[21] ¬(p = 1)
    #[22] prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  → forward one_or_prime_divides_or_coprime param_p=p goal=22
    #[23] p = 1 ∨ prime p ⟷ (∀n. divides p n ∨ coprime p n)
  ← cases "p = 1 ∨ prime p" goal=22
    #[24] p = 1 ∨ prime p ⟶ prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
    #[25] ¬(p = 1 ∨ prime p) ⟶ prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  ← intro goal=24
    #[26] p = 1 ∨ prime p
    #[27] prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  ← rule disjE goal=27 facts=[26]
    #[28] prime p ⟶ prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  ← intro goal=28
    #[29] prime p
    #[30] prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  ← rule iffI goal=30
    #[31] prime p ⟶ (∀n. coprime p n ⟷ ¬(divides p n))
    #[32] (∀n. coprime p n ⟷ ¬(divides p n)) ⟶ prime p
  ← intro goal=31
    #[33] ∀n. coprime p n ⟷ ¬(divides p n)
  → rewrite target=fact source=prev goal=33 facts=[23,26]
    #[34] ∀n. divides p n ∨ coprime p n
  ← intro n goal=33
    #[35] _VAR n
    #[36] coprime p n ⟷ ¬(divides p n)
  → inst target=fact n goal=36 facts=[34]
    #[37] divides p n ∨ coprime p n
  ← rule disjE goal=36 facts=[37]
    #[38] divides p n ⟶ coprime p n ⟷ ¬(divides p n)
    #[39] coprime p n ⟶ coprime p n ⟷ ¬(divides p n)
  ← intro goal=38
    #[40] divides p n
    #[41] coprime p n ⟷ ¬(divides p n)
  ← rule iffI goal=41
    #[42] coprime p n ⟶ ¬(divides p n)
    #[43] ¬(divides p n) ⟶ coprime p n
  ← intro goal=42
    #[44] coprime p n
    #[45] ¬(divides p n)
  → rewrite target=fact coprime_def sym=false goal=45 facts=[44]
    #[46] ∀d. divides d p ∧ divides d n ⟷ d = 1
  → inst target=fact p goal=45 facts=[46]
    #[47] divides p p ∧ divides p n ⟷ p = 1
  ← rule contrapositive goal=45 facts=[21]
    #[48] ¬¬(divides p n) ⟶ ¬¬(p = 1)
  ← rewrite double_neg sym=false goal=48
    #[49] divides p n ⟶ p = 1
  ← intro goal=49
    #[50] p = 1
  → rewrite target=fact eq_sym_eq sym=false goal=50 facts=[47]
    #[51] p = 1 ⟷ divides p p ∧ divides p n
  ← rewrite source=prev goal=50 facts=[51]
    #[52] divides p p ∧ divides p n
  ← rule conjI goal=52
    #[53] divides p p
  ← rule divides_self goal=53
    #[54] divides p p
  ← intro goal=43
    #[55] ¬(divides p n)
    #[56] coprime p n
  ← rule negE_gen goal=56 facts=[55,40]
    #[57] coprime p n
  ← intro goal=39
    #[58] coprime p n ⟷ ¬(divides p n)
  ← rule iffI goal=58
    #[59] coprime p n ⟶ ¬(divides p n)
    #[60] ¬(divides p n) ⟶ coprime p n
  ← intro goal=59
    #[61] ¬(divides p n)
  → rewrite target=fact coprime_def sym=false goal=61 facts=[44]
  → inst target=fact p goal=61 facts=[46]
  ← rule contrapositive goal=61 facts=[21]
    #[62] ¬¬(divides p n) ⟶ ¬¬(p = 1)
  ← rewrite double_neg sym=false goal=62
    #[63] divides p n ⟶ p = 1
  ← intro goal=63
  → rewrite target=fact eq_sym_eq sym=false goal=50 facts=[47]
  ← rewrite source=prev goal=50 facts=[51]
  ← rule conjI goal=52
  ← rule divides_self goal=53
  ← intro goal=60
  ← intro goal=32
    #[64] ∀n. coprime p n ⟷ ¬(divides p n)
  ← intro goal=25
    #[65] ¬(p = 1 ∨ prime p)
    #[66] prime p ⟷ (∀n. coprime p n ⟷ ¬(divides p n))
  → rewrite target=fact source=prev goal=66 facts=[23,65]
    #[67] ¬(∀n. divides p n ∨ coprime p n)
  → rewrite target=fact de_morgan_thm2 sym=false goal=66 facts=[65]
    #[68] ¬(p = 1) ∧ ¬(prime p)
  → forward conjD2 goal=66 facts=[68]
    #[69] ¬(prime p)
  ← rule iffI goal=66
    #[70] prime p ⟶ (∀n. coprime p n ⟷ ¬(divides p n))
    #[71] (∀n. coprime p n ⟷ ¬(divides p n)) ⟶ prime p
  ← intro goal=70
    #[72] ∀n. coprime p n ⟷ ¬(divides p n)
  ← rule negE_gen goal=72 facts=[69,29]
    #[73] ∀n. coprime p n ⟷ ¬(divides p n)
  ← intro goal=71
    #[74] prime p
  ← rule negE_gen goal=74 facts=[67]
    #[75] ∀n. divides p n ∨ coprime p n
  ← intro n goal=75
    #[76] divides p n ∨ coprime p n
  → inst target=fact n goal=76 facts=[64]
    #[77] coprime p n ⟷ ¬(divides p n)
  ← rewrite source=prev goal=76 facts=[77]
    #[78] divides p n ∨ ¬(divides p n)
  ← rule classical goal=78
    #[79] divides p n ∨ ¬(divides p n)
qed

theorem zero_one_or_prime_divprod
  fixes a :: nat, b :: nat, p :: nat
  prop p = 0 | p = 1 | prime p --> divides p (a * b) <--> divides p a | divides p b
  [hint_backward]
proof
  ← intro goal=0
    #[1] p = 0 ∨ p = 1 ∨ prime p
    #[2] divides p (a * b) ⟷ divides p a ∨ divides p b
  ← rule disjE goal=2 facts=[1]
    #[3] p = 0 ⟶ divides p (a * b) ⟷ divides p a ∨ divides p b
    #[4] p = 1 ∨ prime p ⟶ divides p (a * b) ⟷ divides p a ∨ divides p b
  ← intro goal=3
    #[5] p = 0
    #[6] divides p (a * b) ⟷ divides p a ∨ divides p b
  ← rewrite source=prev goal=6 facts=[5]
    #[7] divides 0 (a * b) ⟷ divides 0 a ∨ divides 0 b
  ← rewrite divides_0 sym=false goal=7
    #[8] a * b = 0 ⟷ a = 0 ∨ b = 0
  ← rewrite mult_eq_0 sym=false goal=8
    #[9] a * b = 0 ⟷ a = 0 ∨ b = 0
  ← intro goal=4
    #[10] p = 1 ∨ prime p
    #[11] divides p (a * b) ⟷ divides p a ∨ divides p b
  ← rule disjE goal=11 facts=[10]
    #[12] p = 1 ⟶ divides p (a * b) ⟷ divides p a ∨ divides p b
    #[13] prime p ⟶ divides p (a * b) ⟷ divides p a ∨ divides p b
  ← intro goal=12
    #[14] p = 1
    #[15] divides p (a * b) ⟷ divides p a ∨ divides p b
  ← rewrite source=prev goal=15 facts=[14]
    #[16] divides 1 (a * b) ⟷ divides 1 a ∨ divides 1 b
  ← rule iffI goal=16
    #[17] divides 1 (a * b) ⟶ divides 1 a ∨ divides 1 b
    #[18] divides 1 a ∨ divides 1 b ⟶ divides 1 (a * b)
  ← intro goal=17
    #[19] divides 1 (a * b)
    #[20] divides 1 a ∨ divides 1 b
  ← rule disjI1 goal=20
    #[21] divides 1 a
  ← rule divides_1 goal=21
    #[22] divides 1 a
  ← intro goal=18
    #[23] divides 1 a ∨ divides 1 b
    #[24] divides 1 (a * b)
  ← rule divides_1 goal=24
    #[25] divides 1 (a * b)
  ← intro goal=13
    #[26] prime p
    #[27] divides p (a * b) ⟷ divides p a ∨ divides p b
  ← rule iffI goal=27
    #[28] divides p (a * b) ⟶ divides p a ∨ divides p b
    #[29] divides p a ∨ divides p b ⟶ divides p (a * b)
  ← intro goal=28
    #[30] divides p (a * b)
    #[31] divides p a ∨ divides p b
  ← cases "divides p a" goal=31
    #[32] divides p a ⟶ divides p a ∨ divides p b
    #[33] ¬(divides p a) ⟶ divides p a ∨ divides p b
  ← intro goal=32
    #[34] divides p a
    #[35] divides p a ∨ divides p b
  ← rule disjI1 goal=35
  ← intro goal=33
    #[36] ¬(divides p a)
    #[37] divides p a ∨ divides p b
  ← rule disjI2 goal=37
    #[38] divides p b
  ← rule coprime_divprod goal=38 facts=[30]
    #[39] coprime p a
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=39 facts=[26]
    #[40] ∀n. coprime p n ⟷ ¬(divides p n)
  ← rewrite source=prev goal=39 facts=[40]
  ← intro goal=29
    #[41] divides p a ∨ divides p b
    #[42] divides p (a * b)
  ← rule disjE goal=42 facts=[41]
    #[43] divides p a ⟶ divides p (a * b)
    #[44] divides p b ⟶ divides p (a * b)
  ← intro goal=43
    #[45] divides p (a * b)
  ← rule divides_rmul goal=45
  ← intro goal=44
    #[46] divides p b
    #[47] divides p (a * b)
  ← rule divides_lmul goal=47
qed

theorem zero_one_or_prime
  fixes p :: nat
  prop p = 0 | p = 1 | prime p <--> (!a. !b. divides p (a * b) --> divides p a | divides p b)
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] p = 0 ∨ p = 1 ∨ prime p ⟶ (∀a. ∀b. divides p (a * b) ⟶ divides p a ∨ divides p b)
    #[2] (∀a. ∀b. divides p (a * b) ⟶ divides p a ∨ divides p b) ⟶ p = 0 ∨ p = 1 ∨ prime p
  ← intro goal=1
    #[3] p = 0 ∨ p = 1 ∨ prime p
    #[4] ∀a. ∀b. divides p (a * b) ⟶ divides p a ∨ divides p b
  ← intro a,b goal=4
    #[5] _VAR a
    #[6] _VAR b
    #[7] divides p (a * b)
    #[8] divides p a ∨ divides p b
  → forward zero_one_or_prime_divprod param_a=a param_b=b goal=8 facts=[3]
    #[9] divides p (a * b) ⟷ divides p a ∨ divides p b
  → rewrite target=fact source=prev goal=8 facts=[9,7]
  ← intro goal=2
    #[10] ∀a. ∀b. divides p (a * b) ⟶ divides p a ∨ divides p b
    #[11] p = 0 ∨ p = 1 ∨ prime p
  ← cases "p = 0" goal=11
    #[12] p = 0 ⟶ p = 0 ∨ p = 1 ∨ prime p
    #[13] ¬(p = 0) ⟶ p = 0 ∨ p = 1 ∨ prime p
  ← intro goal=12
    #[14] p = 0
    #[15] p = 0 ∨ p = 1 ∨ prime p
  ← rule disjI1 goal=15
  ← intro goal=13
    #[16] ¬(p = 0)
    #[17] p = 0 ∨ p = 1 ∨ prime p
  ← cases "p=1" goal=17
    #[18] p = 1 ⟶ p = 0 ∨ p = 1 ∨ prime p
    #[19] ¬(p = 1) ⟶ p = 0 ∨ p = 1 ∨ prime p
  ← intro goal=18
    #[20] p = 1
    #[21] p = 0 ∨ p = 1 ∨ prime p
  ← rule disjI2 goal=21
    #[22] p = 1 ∨ prime p
  ← rule disjI1 goal=22
  ← intro goal=19
    #[23] ¬(p = 1)
    #[24] p = 0 ∨ p = 1 ∨ prime p
  ← rule disjI2 goal=24
    #[25] p = 1 ∨ prime p
  ← rule disjI2 goal=25
    #[26] prime p
  ← rewrite prime_def sym=false goal=26
    #[27] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← rule conjI goal=27
    #[28] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← intro x goal=28
    #[29] _VAR x
    #[30] divides x p
    #[31] x = 1 ∨ x = p
  → rewrite target=fact divides_def sym=false goal=31 facts=[30]
    #[32] ∃k. p = x * k
  elim k goal=31 facts=[32]
    #[33] _VAR k
    #[34] p = x * k
    #[35] x = 1 ∨ x = p
  → inst target=fact x goal=35 facts=[10]
    #[36] ∀b. divides p (x * b) ⟶ divides p x ∨ divides p b
  → inst target=fact k goal=35 facts=[36]
    #[37] divides p (x * k) ⟶ divides p x ∨ divides p k
  → rewrite target=fact source=prev goal=35 facts=[34,37]
    #[38] divides (x * k) (x * k) ⟶ divides (x * k) x ∨ divides (x * k) k
  cut "divides (x * k) (x * k)" goal=35 facts=[38]
    #[39] divides (x * k) (x * k)
  ← rule divides_self goal=39
    #[40] divides (x * k) (x * k)
  → forward goal=35 facts=[38,40]
    #[41] divides (x * k) x ∨ divides (x * k) k
  ← rule disjE goal=35 facts=[41]
    #[42] divides (x * k) x ⟶ x = 1 ∨ x = p
    #[43] divides (x * k) k ⟶ x = 1 ∨ x = p
  ← intro goal=42
    #[44] divides (x * k) x
    #[45] x = 1 ∨ x = p
  → rewrite target=fact eq_sym_eq sym=false goal=45 facts=[34]
    #[46] x * k = p
  → rewrite target=fact source=prev goal=45 facts=[46,44]
    #[47] divides p x
  ← rule disjI2 goal=45
    #[48] x = p
  ← rule divides_antisym goal=48
  ← intro goal=43
    #[49] divides (x * k) k
    #[50] x = 1 ∨ x = p
  cut "divides k (x * k)" goal=50 facts=[49]
    #[51] divides k (x * k)
  ← rule divides_lmul goal=51
    #[52] divides k k
  ← rule divides_self goal=52
    #[53] divides k k
  → forward divides_antisym goal=50 facts=[49,51]
    #[54] x * k = k
  cut "k = 1 * k" goal=50
    #[55] k = 1 * k
  ← rewrite mult_1_left sym=false goal=55
    #[56] k = 1 * k
  → rewrite target=fact source=prev goal=50 facts=[56,54]
    #[57] x * (1 * k) = 1 * k
  → rewrite target=fact mult_assoc sym=true goal=50 facts=[57]
    #[58] x * 1 * k = 1 * k
  → rewrite target=fact mult_1_right sym=false goal=50 facts=[58]
    #[59] x * k = 1 * k
  → rewrite target=fact eq_mult_rcancel sym=false goal=50 facts=[59]
    #[60] k = 0 ∨ x = 1
  ← rule disjE goal=50 facts=[60]
    #[61] k = 0 ⟶ x = 1 ∨ x = p
    #[62] x = 1 ⟶ x = 1 ∨ x = p
  ← intro goal=61
    #[63] k = 0
    #[64] x = 1 ∨ x = p
  ← rule disjI2 goal=64
    #[65] x = p
  → rewrite target=fact source=prev goal=65 facts=[63,34]
    #[66] p = x * 0
  → rewrite target=fact mult_0_right sym=false goal=65 facts=[66]
    #[67] p = 0
  ← rule negE_gen goal=65 facts=[16,67]
    #[68] x = p
  ← intro goal=62
    #[69] x = 1
    #[70] x = 1 ∨ x = p
  ← rule disjI1 goal=70
qed

theorem prime_0
  prop ~(prime 0)
  [hint_backward]
proof
  ← rewrite prime_def sym=false goal=0
    #[1] ¬(¬((0::nat) = 1) ∧ (∀x. divides x 0 ⟶ x = 1 ∨ x = 0))
  ← rewrite de_morgan_thm1 sym=false goal=1
    #[2] ¬¬((0::nat) = 1) ∨ ¬(∀x. divides x 0 ⟶ x = 1 ∨ x = 0)
  ← rule disjI2 goal=2
    #[3] ¬(∀x. divides x 0 ⟶ x = 1 ∨ x = 0)
  ← rewrite not_all sym=false goal=3
    #[4] ∃x. ¬(divides x 0 ⟶ x = 1 ∨ x = 0)
  ← inst "(2::nat)" goal=4
    #[5] ¬(divides 2 0 ⟶ (2::nat) = 1 ∨ (2::nat) = 0)
  ← rewrite not_imp sym=false goal=5
    #[6] divides 2 0 ∧ ¬((2::nat) = 1 ∨ (2::nat) = 0)
  ← rule conjI goal=6
    #[7] divides 2 0
    #[8] ¬((2::nat) = 1 ∨ (2::nat) = 0)
  ← rule divides_zero goal=7
  ← rewrite de_morgan_thm2 sym=false goal=8
    #[9] ¬((2::nat) = 1) ∧ ¬((2::nat) = 0)
  ← rule conjI goal=9
    #[10] ¬((2::nat) = 1)
    #[11] ¬((2::nat) = 0)
  nat_const_ineq goal=10
  nat_const_ineq goal=11
qed

theorem prime_1
  prop ~(prime 1)
  [hint_backward]
proof
  ← rewrite prime_def sym=false goal=0
    #[1] ¬(¬((1::nat) = 1) ∧ (∀x. divides x 1 ⟶ x = 1 ∨ x = 1))
  ← rewrite de_morgan_thm1 sym=false goal=1
    #[2] ¬¬((1::nat) = 1) ∨ ¬(∀x. divides x 1 ⟶ x = 1 ∨ x = 1)
  ← rule disjI1 goal=2
    #[3] ¬¬((1::nat) = 1)
  ← rewrite double_neg sym=false goal=3
qed

theorem prime_2
  prop prime 2
  [hint_backward]
proof
  ← rewrite prime_def sym=false goal=0
    #[1] ¬((2::nat) = 1) ∧ (∀x. divides x 2 ⟶ x = 1 ∨ x = 2)
  ← rule conjI goal=1
    #[2] ¬((2::nat) = 1)
    #[3] ∀x. divides x 2 ⟶ x = 1 ∨ x = 2
  nat_const_ineq goal=2
  ← intro x goal=3
    #[4] _VAR x
    #[5] divides x 2
    #[6] x = 1 ∨ x = 2
  → forward divides_le goal=6 facts=[5]
    #[7] x ≤ 2 ∨ (2::nat) = 0
  ← rule disjE goal=6 facts=[7]
    #[8] x ≤ 2 ⟶ x = 1 ∨ x = 2
    #[9] (2::nat) = 0 ⟶ x = 1 ∨ x = 2
  ← intro goal=8
    #[10] x ≤ 2
    #[11] x = 1 ∨ x = 2
  cut "~(x = 0)" goal=11
    #[12] ¬(x = 0)
  ← rule contrapositive goal=12 facts=[5]
    #[13] ¬¬(x = 0) ⟶ ¬(divides x 2)
  ← rewrite double_neg sym=false goal=13
    #[14] x = 0 ⟶ ¬(divides x 2)
  ← intro goal=14
    #[15] x = 0
    #[16] ¬(divides x 2)
  ← rewrite source=prev goal=16 facts=[15]
    #[17] ¬(divides 0 2)
  ← rewrite divides_0 sym=false goal=17
    #[18] ¬((2::nat) = 0)
  nat_const_ineq goal=18
    #[19] ¬((2::nat) = 0)
  ← z3 goal=11 facts=[10,12]
  ← intro goal=9
    #[20] (2::nat) = 0
    #[21] x = 1 ∨ x = 2
  ← rule contrapositive goal=21 facts=[20]
    #[22] ¬(x = 1 ∨ x = 2) ⟶ ¬((2::nat) = 0)
  ← intro goal=22
    #[23] ¬(x = 1 ∨ x = 2)
    #[24] ¬((2::nat) = 0)
  nat_const_ineq goal=24
qed

theorem prime_alt
  fixes p :: nat
  prop prime p <--> ~(p = 0) & ~(p = 1) & (!n. 1 < n --> n < p --> ~(divides n p))
  [hint_rewrite]
proof
  ← cases "p=0" goal=0
    #[1] p = 0 ⟶ prime p ⟷ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
    #[2] ¬(p = 0) ⟶ prime p ⟷ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  ← intro goal=1
    #[3] p = 0
    #[4] prime p ⟷ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  ← rule iffI goal=4
    #[5] prime p ⟶ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
    #[6] ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p)) ⟶ prime p
  ← intro goal=5
    #[7] prime p
    #[8] ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  → rewrite target=fact source=prev goal=8 facts=[3,7]
    #[9] prime 0
  → forward prime_0 goal=8
    #[10] ¬(prime 0)
  ← rule negE_gen goal=8 facts=[10,9]
  ← intro goal=6
    #[11] ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
    #[12] prime p
  → forward conjD1 goal=12 facts=[11]
    #[13] ¬(p = 0)
  ← rule negE_gen goal=12 facts=[13,3]
  ← intro goal=2
    #[14] ¬(p = 0)
    #[15] prime p ⟷ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  ← rewrite prime_def sym=false goal=15
    #[16] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p) ⟷ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  ← rule iffI goal=16
    #[17] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p) ⟶ ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
    #[18] ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p)) ⟶ ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← intro goal=17
    #[19] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
    #[20] ¬(p = 0) ∧ ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  → forward conjD1 goal=20 facts=[19]
    #[21] ¬(p = 1)
  → forward conjD2 goal=20 facts=[19]
    #[22] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← rule conjI goal=20
    #[23] ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  ← rule conjI goal=23
    #[24] ∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p)
  ← intro n goal=24
    #[25] _VAR n
    #[26] 1 < n
    #[27] n < p
    #[28] ¬(divides n p)
  ← rule negI goal=28
    #[29] divides n p ⟶ false
  ← intro goal=29
    #[30] divides n p
    #[31] false
  → forward goal=31 facts=[22,30]
    #[32] n = 1 ∨ n = p
  ← z3 goal=31 facts=[32,27,26]
    #[33] false
  ← intro goal=18
    #[34] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=34 facts=[11]
  → forward conjD2 goal=34 facts=[11]
    #[35] ¬(p = 1) ∧ (∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p))
  → forward conjD2 goal=34 facts=[35]
    #[36] ∀n. 1 < n ⟶ n < p ⟶ ¬(divides n p)
  → forward conjD1 goal=34 facts=[35]
    #[37] ¬(p = 1)
  ← rule conjI goal=34
    #[38] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← intro n goal=38
    #[39] n = 1 ∨ n = p
  → forward divides_le_strong goal=39 facts=[30]
    #[40] 1 ≤ n ∧ n ≤ p ∨ p = 0
  ← rule disjE goal=39 facts=[40]
    #[41] 1 ≤ n ∧ n ≤ p ⟶ n = 1 ∨ n = p
    #[42] p = 0 ⟶ n = 1 ∨ n = p
  ← intro goal=41
    #[43] 1 ≤ n ∧ n ≤ p
    #[44] n = 1 ∨ n = p
  ← rule contrapositive goal=44 facts=[30]
    #[45] ¬(n = 1 ∨ n = p) ⟶ ¬(divides n p)
  ← intro goal=45
    #[46] ¬(n = 1 ∨ n = p)
    #[47] ¬(divides n p)
  ← apply_prev goal=47 facts=[36]
    #[48] 1 < n
    #[49] n < p
  ← z3 goal=48 facts=[46,43]
    #[50] 1 < n
  ← z3 goal=49 facts=[46,43]
    #[51] n < p
  ← intro goal=42
    #[52] n = 1 ∨ n = p
  ← rule negE_gen goal=52 facts=[13,3]
    #[53] n = 1 ∨ n = p
qed

theorem prime_ge_2
  fixes p :: nat
  prop prime p --> 2 <= p
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] 2 ≤ p
  ← rule contrapositive goal=2 facts=[1]
    #[3] ¬(2 ≤ p) ⟶ ¬(prime p)
  ← rewrite not_le sym=false goal=3
    #[4] p < 2 ⟶ ¬(prime p)
  ← intro goal=4
    #[5] p < 2
    #[6] ¬(prime p)
  cut "p = 0 | p = 1" goal=6
    #[7] p = 0 ∨ p = 1
  ← z3 goal=7 facts=[5]
    #[8] p = 0 ∨ p = 1
  ← rule disjE goal=6 facts=[8]
    #[9] p = 0 ⟶ ¬(prime p)
    #[10] p = 1 ⟶ ¬(prime p)
  ← intro goal=9
    #[11] p = 0
    #[12] ¬(prime p)
  ← rewrite source=prev goal=12 facts=[11]
    #[13] ¬(prime 0)
  ← rule prime_0 goal=13
    #[14] ¬(prime 0)
  ← intro goal=10
    #[15] p = 1
    #[16] ¬(prime p)
  ← rewrite source=prev goal=16 facts=[15]
    #[17] ¬(prime 1)
  ← rule prime_1 goal=17
    #[18] ¬(prime 1)
qed

theorem prime_factor
  fixes n :: nat
  prop ~(n = 1) --> (?p. prime p & divides p n)
  [hint_backward]
proof
  ← intro goal=0
    #[1] ¬(n = 1)
    #[2] ∃p. prime p ∧ divides p n
  cut "!n. ¬(n = 1) ⟶ (∃p. prime p ∧ divides p n)" goal=2
    #[3] ∀n. ¬(n = 1) ⟶ (∃p. prime p ∧ divides p n)
  ← rule nat_WF goal=3
    #[4] ∀n. (∀m. m < n ⟶ ¬(m = 1) ⟶ (∃p. prime p ∧ divides p m)) ⟶ ¬(n = 1) ⟶ (∃p. prime p ∧ divides p n)
  ← intro n1 goal=4
    #[5] _VAR n1
    #[6] ∀m. m < n1 ⟶ ¬(m = 1) ⟶ (∃p. prime p ∧ divides p m)
    #[7] ¬(n1 = 1)
    #[8] ∃p. prime p ∧ divides p n1
  ← cases "prime (n1)" goal=8
    #[9] prime n1 ⟶ (∃p. prime p ∧ divides p n1)
    #[10] ¬(prime n1) ⟶ (∃p. prime p ∧ divides p n1)
  ← intro goal=9
    #[11] prime n1
    #[12] ∃p. prime p ∧ divides p n1
  ← inst n1 goal=12
    #[13] prime n1 ∧ divides n1 n1
  ← rule conjI goal=13
    #[14] divides n1 n1
  ← rule divides_self goal=14
    #[15] divides n1 n1
  ← intro goal=10
    #[16] ¬(prime n1)
    #[17] ∃p. prime p ∧ divides p n1
  → rewrite target=fact prime_def sym=false goal=17 facts=[16]
    #[18] ¬(¬(n1 = 1) ∧ (∀x. divides x n1 ⟶ x = 1 ∨ x = n1))
  → rewrite target=fact de_morgan_thm1 sym=false goal=17 facts=[18]
    #[19] ¬¬(n1 = 1) ∨ ¬(∀x. divides x n1 ⟶ x = 1 ∨ x = n1)
  → rewrite target=fact double_neg sym=false goal=17 facts=[19]
    #[20] n1 = 1 ∨ ¬(∀x. divides x n1 ⟶ x = 1 ∨ x = n1)
  ← rule disjE goal=17 facts=[20]
    #[21] n1 = 1 ⟶ (∃p. prime p ∧ divides p n1)
    #[22] ¬(∀x. divides x n1 ⟶ x = 1 ∨ x = n1) ⟶ (∃p. prime p ∧ divides p n1)
  ← intro goal=21
    #[23] n1 = 1
    #[24] ∃p. prime p ∧ divides p n1
  ← rule negE_gen goal=24 facts=[7,23]
    #[25] ∃p. prime p ∧ divides p n1
  ← intro goal=22
    #[26] ¬(∀x. divides x n1 ⟶ x = 1 ∨ x = n1)
    #[27] ∃p. prime p ∧ divides p n1
  → rewrite target=fact not_all sym=false goal=27 facts=[26]
    #[28] ∃x. ¬(divides x n1 ⟶ x = 1 ∨ x = n1)
  elim m goal=27 facts=[28]
    #[29] _VAR m
    #[30] ¬(divides m n1 ⟶ m = 1 ∨ m = n1)
    #[31] ∃p. prime p ∧ divides p n1
  → rewrite target=fact not_imp sym=false goal=31 facts=[30]
    #[32] divides m n1 ∧ ¬(m = 1 ∨ m = n1)
  → rewrite target=fact de_morgan_thm2 sym=false goal=31 facts=[32]
    #[33] divides m n1 ∧ ¬(m = 1) ∧ ¬(m = n1)
  → forward conjD1 goal=31 facts=[33]
    #[34] divides m n1
  → forward conjD2 goal=31 facts=[33]
    #[35] ¬(m = 1) ∧ ¬(m = n1)
  → forward conjD1 goal=31 facts=[35]
    #[36] ¬(m = 1)
  → forward conjD2 goal=31 facts=[35]
    #[37] ¬(m = n1)
  → forward divides_le goal=31 facts=[34]
    #[38] m ≤ n1 ∨ n1 = 0
  ← rule disjE goal=31 facts=[38]
    #[39] m ≤ n1 ⟶ (∃p. prime p ∧ divides p n1)
    #[40] n1 = 0 ⟶ (∃p. prime p ∧ divides p n1)
  ← intro goal=39
    #[41] m ≤ n1
    #[42] ∃p. prime p ∧ divides p n1
  cut "m < n1" goal=42
    #[43] m < n1
  ← z3 goal=43 facts=[41,37]
    #[44] m < n1
  → forward goal=42 facts=[6,44]
    #[45] ¬(m = 1) ⟶ (∃p. prime p ∧ divides p m)
  → forward goal=42 facts=[45,36]
    #[46] ∃p. prime p ∧ divides p m
  elim p goal=42 facts=[46]
    #[47] _VAR p
    #[48] prime p ∧ divides p m
    #[49] ∃p. prime p ∧ divides p n1
  ← inst p goal=49
    #[50] prime p ∧ divides p n1
  → forward conjD1 goal=50 facts=[48]
    #[51] prime p
  → forward conjD2 goal=50 facts=[48]
    #[52] divides p m
  ← rule conjI goal=50
    #[53] divides p n1
  ← rule divides_trans goal=53 facts=[52,34]
    #[54] divides p n1
  ← intro goal=40
    #[55] n1 = 0
    #[56] ∃p. prime p ∧ divides p n1
  ← inst "(2::nat)" goal=56
    #[57] prime 2 ∧ divides 2 n1
  ← rule conjI goal=57
    #[58] prime 2
    #[59] divides 2 n1
  ← rule prime_2 goal=58
    #[60] prime 2
  ← rewrite source=prev goal=59 facts=[55]
    #[61] divides 2 0
  ← rule divides_zero goal=61
    #[62] divides 2 0
  ← apply_prev goal=2 facts=[3]
qed

theorem prime_factor_lt
  fixes m :: nat, n :: nat, p :: nat
  prop prime p --> ~(n = 0) --> n = p * m --> m < n
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(n = 0)
    #[3] n = p * m
    #[4] m < n
  → forward prime_ge_2 goal=4 facts=[1]
    #[5] 2 ≤ p
  → rewrite target=fact less_eq_exist sym=false goal=4 facts=[5]
    #[6] ∃p1. p = 2 + p1
  elim q goal=4 facts=[6]
    #[7] _VAR q
    #[8] p = 2 + q
    #[9] m < n
  → rewrite target=fact source=prev goal=9 facts=[8,3]
    #[10] n = (2 + q) * m
  ← rewrite source=prev goal=9 facts=[10]
    #[11] m < (2 + q) * m
  ← rewrite distrib_r sym=false goal=11
    #[12] m < 2 * m + q * m
  ← rewrite mult_2 sym=false goal=12
    #[13] m < m + m + q * m
  ← rewrite add_assoc sym=false goal=13
    #[14] m < m + (m + q * m)
  ← rewrite lt_add sym=false goal=14
    #[15] 0 < m + q * m
  ← rule le_1_1 goal=15
    #[16] ¬(m + q * m = 0)
  cut "m + q * m = (1+ q) * m" goal=16
    #[17] m + q * m = (1 + q) * m
  nat_norm goal=17
    #[18] m + q * m = (1 + q) * m
  ← rewrite source=prev goal=16 facts=[18]
    #[19] ¬((1 + q) * m = 0)
  ← rewrite add_1_left sym=false goal=19
    #[20] ¬(Suc q * m = 0)
  ← rule negI goal=20
    #[21] Suc q * m = 0 ⟶ false
  ← intro goal=21
    #[22] Suc q * m = 0
    #[23] false
  → forward mult_nonzero goal=23 facts=[22]
    #[24] Suc q = 0 ∨ m = 0
  ← rule disjE goal=23 facts=[24]
    #[25] Suc q = 0 ⟶ false
    #[26] m = 0 ⟶ false
  ← intro goal=25
    #[27] Suc q = 0
    #[28] false
  ← rule contrapositive goal=28 facts=[27]
    #[29] ¬false ⟶ ¬(Suc q = 0)
  ← intro goal=29
    #[30] ¬false
    #[31] ¬(Suc q = 0)
  ← rule ineq_sym goal=31
    #[32] ¬(0 = Suc q)
  ← rule nat_zero_Suc_neq goal=32
    #[33] ¬(0 = Suc q)
  ← intro goal=26
    #[34] m = 0
    #[35] false
  ← rule contrapositive goal=35 facts=[2]
    #[36] ¬false ⟶ ¬¬(n = 0)
  ← intro goal=36
    #[37] ¬¬(n = 0)
  ← rewrite double_neg sym=false goal=37
    #[38] n = 0
  → rewrite target=fact source=prev goal=38 facts=[34,3]
    #[39] n = p * 0
  → rewrite target=fact mult_0_right sym=false goal=38 facts=[39]
    #[40] n = 0
qed

theorem prime_factor_induct
  fixes P :: nat => bool
  prop P 0 --> P 1 --> (!p. !n. prime p --> ~(n = 0) --> P n --> P (p * n)) --> (!n. P n)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] P 0
    #[2] P 1
    #[3] ∀p. ∀n. prime p ⟶ ¬(n = 0) ⟶ P n ⟶ P (p * n)
    #[4] ∀n. P n
  ← rule nat_WF goal=4
    #[5] ∀n. (∀m. m < n ⟶ P m) ⟶ P n
  ← intro n goal=5
    #[6] _VAR n
    #[7] ∀m. m < n ⟶ P m
    #[8] P n
  ← cases "n=0" goal=8
    #[9] n = 0 ⟶ P n
    #[10] ¬(n = 0) ⟶ P n
  ← intro goal=9
    #[11] n = 0
    #[12] P n
  ← rewrite source=prev goal=12 facts=[11]
  ← intro goal=10
    #[13] ¬(n = 0)
    #[14] P n
  ← cases "n=1" goal=14
    #[15] n = 1 ⟶ P n
    #[16] ¬(n = 1) ⟶ P n
  ← intro goal=15
    #[17] n = 1
    #[18] P n
  ← rewrite source=prev goal=18 facts=[17]
  ← intro goal=16
    #[19] ¬(n = 1)
    #[20] P n
  → forward prime_factor goal=20 facts=[19]
    #[21] ∃p. prime p ∧ divides p n
  elim p goal=20 facts=[21]
    #[22] _VAR p
    #[23] prime p ∧ divides p n
    #[24] P n
  → inst target=fact p goal=24 facts=[3]
    #[25] ∀n. prime p ⟶ ¬(n = 0) ⟶ P n ⟶ P (p * n)
  → forward conjD1 goal=24 facts=[23]
    #[26] prime p
  → forward goal=24 facts=[25,26]
    #[27] ∀n. ¬(n = 0) ⟶ P n ⟶ P (p * n)
  → forward conjD2 goal=24 facts=[23]
    #[28] divides p n
  → rewrite target=fact divides_def sym=false goal=24 facts=[28]
    #[29] ∃k. n = p * k
  elim d goal=24 facts=[29]
    #[30] _VAR d
    #[31] n = p * d
    #[32] P n
  → inst target=fact d goal=32 facts=[27]
    #[33] ¬(d = 0) ⟶ P d ⟶ P (p * d)
  ← rewrite source=prev goal=32 facts=[31]
    #[34] P (p * d)
  ← apply_prev goal=34 facts=[33]
    #[35] ¬(d = 0)
    #[36] P d
  → rewrite target=fact source=prev goal=35 facts=[31,13]
    #[37] ¬(p * d = 0)
  ← rule contrapositive goal=35 facts=[37]
    #[38] ¬¬(d = 0) ⟶ ¬¬(p * d = 0)
  ← rewrite double_neg sym=false goal=38
    #[39] d = 0 ⟶ p * d = 0
  ← intro goal=39
    #[40] d = 0
    #[41] p * d = 0
  ← rewrite source=prev goal=41 facts=[40]
    #[42] p * 0 = 0
  ← rewrite mult_0_right sym=false goal=42
    #[43] p * 0 = 0
  ← apply_prev goal=36 facts=[7]
    #[44] d < n
  ← rule prime_factor_lt param_p=p goal=44
qed

theorem complete_factor_induct
  fixes P :: nat => bool
  prop P 0 --> P 1 --> (!p. prime p --> P p) --> (!m. !n. P m --> P n --> P (m * n)) --> (!n. P n)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] P 0
    #[2] P 1
    #[3] ∀p. prime p ⟶ P p
    #[4] ∀m. ∀n. P m ⟶ P n ⟶ P (m * n)
    #[5] ∀n. P n
  ← rule prime_factor_induct goal=5
    #[6] ∀p. ∀n. prime p ⟶ ¬(n = 0) ⟶ P n ⟶ P (p * n)
  ← intro p,n goal=6
    #[7] _VAR p
    #[8] _VAR n
    #[9] prime p
    #[10] ¬(n = 0)
    #[11] P n
    #[12] P (p * n)
  → forward goal=12 facts=[3,9]
    #[13] P p
  ← apply_prev goal=12 facts=[4,13,11]
    #[14] P (p * n)
qed

theorem euclid_bound
  fixes n :: nat
  prop ?p. prime p & n < p & p <= Suc (fact n)
  [hint_backward]
proof
  → forward prime_factor param_n="fact n + 1" goal=0
    #[1] ¬(fact n + 1 = 1) ⟶ (∃p. prime p ∧ divides p (fact n + 1))
  cut "¬(fact n + 1 = 1)" goal=0 facts=[1]
    #[2] ¬(fact n + 1 = 1)
  cut "!n::nat. 0 < n --> ~(n + 1 = 1)" goal=2
    #[3] ∀n::nat. 0 < n ⟶ ¬(n + 1 = 1)
  ← z3 goal=3
  ← apply_prev goal=2 facts=[3]
    #[4] 0 < fact n
  ← rule fact_lt goal=4
  → forward goal=0 facts=[1,2]
    #[5] ∃p. prime p ∧ divides p (fact n + 1)
  elim p goal=0 facts=[5]
    #[6] _VAR p
    #[7] prime p ∧ divides p (fact n + 1)
    #[8] ∃p. prime p ∧ n < p ∧ p ≤ Suc (fact n)
  ← inst p goal=8
    #[9] prime p ∧ n < p ∧ p ≤ Suc (fact n)
  → forward conjD1 goal=9 facts=[7]
    #[10] prime p
  → forward conjD2 goal=9 facts=[7]
    #[11] divides p (fact n + 1)
  ← rule conjI goal=9
    #[12] n < p ∧ p ≤ Suc (fact n)
  ← rule conjI goal=12
    #[13] n < p
    #[14] p ≤ Suc (fact n)
  → forward divides_sum2 goal=13 facts=[11]
    #[15] divides p (fact n) ⟶ divides p 1
  → rewrite target=fact divides_one sym=false goal=13 facts=[15]
    #[16] divides p (fact n) ⟶ p = 1
  ← rule contrapositive goal=13 facts=[16]
    #[17] ¬(n < p) ⟶ ¬(divides p (fact n) ⟶ p = 1)
  ← rewrite not_lt sym=false goal=17
    #[18] p ≤ n ⟶ ¬(divides p (fact n) ⟶ p = 1)
  ← intro goal=18
    #[19] p ≤ n
    #[20] ¬(divides p (fact n) ⟶ p = 1)
  ← rewrite not_imp sym=false goal=20
    #[21] divides p (fact n) ∧ ¬(p = 1)
  ← rule conjI goal=21
    #[22] divides p (fact n)
    #[23] ¬(p = 1)
  ← rule divides_fact goal=22
    #[24] 1 ≤ p
  ← rule contrapositive goal=24 facts=[10]
    #[25] ¬(1 ≤ p) ⟶ ¬(prime p)
  ← intro goal=25
    #[26] ¬(1 ≤ p)
    #[27] ¬(prime p)
  → rewrite target=fact not_le sym=false goal=27 facts=[26]
    #[28] p < 1
  → rewrite target=fact nat_one_def sym=false goal=27 facts=[28]
    #[29] p < Suc 0
  → rewrite target=fact less_Suc_lesseq sym=false goal=27 facts=[29]
    #[30] p ≤ 0
  → rewrite target=fact nat_less_eq_def_1 sym=false goal=27 facts=[30]
    #[31] p = 0
  ← rewrite source=prev goal=27 facts=[31]
    #[32] ¬(prime 0)
  ← rule prime_0 goal=32
    #[33] ¬(prime 0)
  ← rule contrapositive goal=23 facts=[10]
    #[34] ¬¬(p = 1) ⟶ ¬(prime p)
  ← intro goal=34
    #[35] ¬¬(p = 1)
    #[36] ¬(prime p)
  → rewrite target=fact double_neg sym=false goal=36 facts=[35]
    #[37] p = 1
  ← rewrite source=prev goal=36 facts=[37]
    #[38] ¬(prime 1)
  ← rule prime_1 goal=38
    #[39] ¬(prime 1)
  → forward divides_le goal=14 facts=[11]
    #[40] p ≤ fact n + 1 ∨ fact n + 1 = 0
  ← rule disjE goal=14 facts=[40]
    #[41] p ≤ fact n + 1 ⟶ p ≤ Suc (fact n)
    #[42] fact n + 1 = 0 ⟶ p ≤ Suc (fact n)
  ← intro goal=41
    #[43] p ≤ fact n + 1
    #[44] p ≤ Suc (fact n)
  ← rewrite add_1_right sym=true goal=44
  ← intro goal=42
    #[45] fact n + 1 = 0
    #[46] p ≤ Suc (fact n)
  → forward add_is_zero goal=46 facts=[45]
    #[47] fact n = 0 ∧ (1::nat) = 0
  → forward conjD2 goal=46 facts=[47]
    #[48] (1::nat) = 0
  ← rule contrapositive goal=46 facts=[48]
    #[49] ¬(p ≤ Suc (fact n)) ⟶ ¬((1::nat) = 0)
  ← intro goal=49
    #[50] ¬(p ≤ Suc (fact n))
    #[51] ¬((1::nat) = 0)
  nat_const_ineq goal=51
    #[52] ¬((1::nat) = 0)
qed

theorem euclid
  fixes n :: nat
  prop ?p. prime p & p > n
  [hint_backward]
proof
  ← rewrite greater_def sym=false goal=0
    #[1] ∃p. prime p ∧ n < p
  → forward euclid_bound param_n=n goal=1
    #[2] ∃p. prime p ∧ n < p ∧ p ≤ Suc (fact n)
  elim p goal=1 facts=[2]
    #[3] _VAR p
    #[4] prime p ∧ n < p ∧ p ≤ Suc (fact n)
    #[5] ∃p. prime p ∧ n < p
    #[6] ∃p. prime p ∧ p > n
  ← inst p goal=5
    #[7] prime p ∧ n < p
  ← rule conjI goal=7
    #[8] prime p
    #[9] n < p
  ← rule conjD1 goal=8 facts=[4]
  → forward conjD2 goal=9 facts=[4]
    #[10] n < p ∧ p ≤ Suc (fact n)
  ← rule conjD1 goal=9 facts=[10]
qed

theorem prime_infinite
  prop infinite ({p. prime p})
  [hint_backward]

theorem coprime_prime
  fixes a :: nat, b :: nat, p :: nat
  prop coprime a b --> ~(prime p & divides p a & divides p b)
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] ¬(prime p ∧ divides p a ∧ divides p b)
  ← rewrite de_morgan_thm1 sym=false goal=2
    #[3] ¬(prime p) ∨ ¬(divides p a ∧ divides p b)
  ← cases "(prime p)" goal=3
    #[4] prime p ⟶ ¬(prime p) ∨ ¬(divides p a ∧ divides p b)
    #[5] ¬(prime p) ⟶ ¬(prime p) ∨ ¬(divides p a ∧ divides p b)
  ← intro goal=4
    #[6] prime p
    #[7] ¬(prime p) ∨ ¬(divides p a ∧ divides p b)
  ← rule disjI2 goal=7
    #[8] ¬(divides p a ∧ divides p b)
  ← rewrite de_morgan_thm1 sym=false goal=8
    #[9] ¬(divides p a) ∨ ¬(divides p b)
  ← cases "divides p a" goal=9
    #[10] divides p a ⟶ ¬(divides p a) ∨ ¬(divides p b)
    #[11] ¬(divides p a) ⟶ ¬(divides p a) ∨ ¬(divides p b)
  ← intro goal=10
    #[12] divides p a
    #[13] ¬(divides p a) ∨ ¬(divides p b)
  ← rule disjI2 goal=13
    #[14] ¬(divides p b)
  → rewrite target=fact coprime_def sym=false goal=14 facts=[1]
    #[15] ∀d. divides d a ∧ divides d b ⟷ d = 1
  ← rule contrapositive goal=14 facts=[6]
    #[16] ¬¬(divides p b) ⟶ ¬(prime p)
  ← rewrite double_neg sym=false goal=16
    #[17] divides p b ⟶ ¬(prime p)
  ← intro goal=17
    #[18] divides p b
    #[19] ¬(prime p)
  → forward conjI goal=19 facts=[12,18]
    #[20] divides p a ∧ divides p b
  → rewrite target=fact source=prev goal=19 facts=[15,20]
    #[21] p = 1
  ← rewrite source=prev goal=19 facts=[21]
    #[22] ¬(prime 1)
  ← rule prime_1 goal=22
    #[23] ¬(prime 1)
  ← intro goal=11
    #[24] ¬(divides p a)
    #[25] ¬(divides p a) ∨ ¬(divides p b)
  ← rule disjI1 goal=25
  ← intro goal=5
    #[26] ¬(prime p)
    #[27] ¬(prime p) ∨ ¬(divides p a ∧ divides p b)
  ← rule disjI1 goal=27
qed

theorem coprime_prime_eq
  fixes a :: nat, b :: nat
  prop coprime a b <--> (!p. ~(prime p & divides p a & divides p b))
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] coprime a b ⟶ (∀p. ¬(prime p ∧ divides p a ∧ divides p b))
    #[2] (∀p. ¬(prime p ∧ divides p a ∧ divides p b)) ⟶ coprime a b
  ← intro goal=1
    #[3] coprime a b
    #[4] ∀p. ¬(prime p ∧ divides p a ∧ divides p b)
  ← intro p goal=4
    #[5] _VAR p
    #[6] ¬(prime p ∧ divides p a ∧ divides p b)
  ← rule coprime_prime goal=6
  ← intro goal=2
    #[7] ∀p. ¬(prime p ∧ divides p a ∧ divides p b)
    #[8] coprime a b
  ← rule contrapositive goal=8 facts=[7]
    #[9] ¬(coprime a b) ⟶ ¬(∀p. ¬(prime p ∧ divides p a ∧ divides p b))
  ← intro goal=9
    #[10] ¬(coprime a b)
    #[11] ¬(∀p. ¬(prime p ∧ divides p a ∧ divides p b))
  → rewrite target=fact coprime_def sym=false goal=11 facts=[10]
    #[12] ¬(∀d. divides d a ∧ divides d b ⟷ d = 1)
  → rewrite target=fact not_all sym=false goal=11 facts=[12]
    #[13] ∃d. ¬(divides d a ∧ divides d b ⟷ d = 1)
  ← rewrite not_all sym=false goal=11
    #[14] ∃p. ¬¬(prime p ∧ divides p a ∧ divides p b)
  ← rewrite double_neg sym=false goal=14
    #[15] ∃p. prime p ∧ divides p a ∧ divides p b
  elim x goal=15 facts=[13]
    #[16] _VAR x
    #[17] ¬(divides x a ∧ divides x b ⟷ x = 1)
    #[18] ∃p. prime p ∧ divides p a ∧ divides p b
    #[19] ∃p. ¬¬(prime p ∧ divides p a ∧ divides p b)
    #[20] ¬(∀p. ¬(prime p ∧ divides p a ∧ divides p b))
  → forward prime_factor param_n=x goal=18
    #[21] ¬(x = 1) ⟶ (∃p. prime p ∧ divides p x)
  ← cases "x=1" goal=18
    #[22] x = 1 ⟶ (∃p. prime p ∧ divides p a ∧ divides p b)
    #[23] ¬(x = 1) ⟶ (∃p. prime p ∧ divides p a ∧ divides p b)
  ← intro goal=22
    #[24] x = 1
    #[25] ∃p. prime p ∧ divides p a ∧ divides p b
  ← rule negE_gen goal=25 facts=[17]
    #[26] divides x a ∧ divides x b ⟷ x = 1
  ← rewrite eq_sym_eq sym=false goal=26
    #[27] x = 1 ⟷ divides x a ∧ divides x b
  ← rewrite source=prev goal=27 facts=[24]
    #[28] (1::nat) = 1 ⟷ divides 1 a ∧ divides 1 b
  ← rule iffI goal=28
    #[29] (1::nat) = 1 ⟶ divides 1 a ∧ divides 1 b
    #[30] divides 1 a ∧ divides 1 b ⟶ (1::nat) = 1
  ← intro goal=29
    #[31] (1::nat) = 1
    #[32] divides 1 a ∧ divides 1 b
  ← rule conjI goal=32
    #[33] divides 1 a
    #[34] divides 1 b
  ← rule divides_1 goal=33
    #[35] divides 1 a
  ← rule divides_1 goal=34
    #[36] divides 1 b
  ← intro goal=30
    #[37] divides 1 a ∧ divides 1 b
    #[38] (1::nat) = 1
  ← rule eq_refl goal=38
    #[39] (1::nat) = 1
  ← intro goal=23
    #[40] ¬(x = 1)
    #[41] ∃p. prime p ∧ divides p a ∧ divides p b
  → forward goal=41 facts=[21,40]
    #[42] ∃p. prime p ∧ divides p x
  elim p goal=41 facts=[42]
    #[43] prime p ∧ divides p x
    #[44] ∃p. prime p ∧ divides p a ∧ divides p b
  ← inst p goal=44
    #[45] prime p ∧ divides p a ∧ divides p b
  → forward conjD1 goal=45 facts=[43]
    #[46] prime p
  → forward conjD2 goal=45 facts=[43]
    #[47] divides p x
  ← rule conjI goal=45
    #[48] divides p a ∧ divides p b
  ← rule conjI goal=48
    #[49] divides p a
    #[50] divides p b
  cut "!p. !q. ~(p<-->q) <--> (p <--> ~q)" goal=49
    #[51] ∀p. ∀q. ¬(p ⟷ q) ⟷ p ⟷ ¬q
  ← z3 goal=51
    #[52] ∀p. ∀q. ¬(p ⟷ q) ⟷ p ⟷ ¬q
  → rewrite target=fact source=prev goal=49 facts=[52,17]
    #[53] divides x a ∧ divides x b ⟷ ¬(x = 1)
  → rewrite target=fact eq_sym_eq sym=false goal=49 facts=[53]
    #[54] ¬(x = 1) ⟷ divides x a ∧ divides x b
  → rewrite target=fact source=prev goal=49 facts=[54,40]
    #[55] divides x a ∧ divides x b
  → forward conjD1 goal=49 facts=[55]
    #[56] divides x a
  → forward conjD2 goal=49 facts=[55]
    #[57] divides x b
  ← rule divides_trans goal=49 facts=[47,56]
    #[58] divides p a
  ← rule divides_trans goal=50 facts=[47,57]
    #[59] divides p b
qed

theorem prime_coprime_strong
  fixes n :: nat, p :: nat
  prop prime p --> divides p n | coprime p n
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides p n ∨ coprime p n
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=2 facts=[1]
    #[3] ∀n. coprime p n ⟷ ¬(divides p n)
  ← cases "divides p n" goal=2
    #[4] divides p n ⟶ divides p n ∨ coprime p n
    #[5] ¬(divides p n) ⟶ divides p n ∨ coprime p n
  ← intro goal=4
    #[6] divides p n
    #[7] divides p n ∨ coprime p n
  ← rule disjI1 goal=7
  ← intro goal=5
    #[8] ¬(divides p n)
    #[9] divides p n ∨ coprime p n
  ← rule disjI2 goal=9
    #[10] coprime p n
  ← rewrite source=prev goal=10 facts=[3]
qed

theorem prime_coprime
  fixes n :: nat, p :: nat
  prop prime p --> n = 1 | divides p n | coprime p n
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] n = 1 ∨ divides p n ∨ coprime p n
  ← rule disjI2 goal=2
    #[3] divides p n ∨ coprime p n
  ← rule prime_coprime_strong goal=3
qed

theorem gcd_prime_cases
  fixes n :: nat, p :: nat
  prop prime p --> gcd p n = (if divides p n then p else 1)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] gcd p n = (if divides p n then p else 1)
  ← cases "divides p n" goal=2
    #[3] divides p n ⟶ gcd p n = (if divides p n then p else 1)
    #[4] ¬(divides p n) ⟶ gcd p n = (if divides p n then p else 1)
  ← intro goal=3
    #[5] divides p n
    #[6] gcd p n = (if divides p n then p else 1)
  ← rewrite if_P sym=false goal=6 facts=[5]
    #[7] gcd p n = p
  ← rule gcd_equal_left goal=7 facts=[5]
    #[8] gcd p n = p
  ← intro goal=4
    #[9] ¬(divides p n)
    #[10] gcd p n = (if divides p n then p else 1)
  ← rewrite if_not_P sym=false goal=10 facts=[9]
    #[11] gcd p n = 1
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=11 facts=[1]
    #[12] ∀n. coprime p n ⟷ ¬(divides p n)
  → rewrite target=fact eq_sym_eq sym=false goal=11 facts=[12]
    #[13] ∀n. ¬(divides p n) ⟷ coprime p n
  → rewrite target=fact source=prev goal=11 facts=[13,9]
    #[14] coprime p n
  → rewrite target=fact coprime_gcd sym=false goal=11 facts=[14]
qed

theorem gcd_2_cases
  fixes n :: nat
  prop gcd 2 n = (if even n then 2 else 1)
  [hint_rewrite]
proof
  ← rewrite divides_2 sym=true goal=0
    #[1] gcd 2 n = (if divides 2 n then 2 else 1)
  ← rule gcd_prime_cases goal=1
    #[2] prime 2
  ← rule prime_2 goal=2
qed

theorem coprime_primepow
  fixes k :: nat, m :: nat, p :: nat
  prop prime p --> ~(k = 0) --> coprime m (p ^ k) <--> ~(divides p m)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(k = 0)
    #[3] coprime m (p ^ k) ⟷ ¬(divides p m)
  ← rewrite coprime_rexp sym=false goal=3
    #[4] coprime m p ∨ k = 0 ⟷ ¬(divides p m)
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=4 facts=[1]
    #[5] ∀n. coprime p n ⟷ ¬(divides p n)
  ← rule iffI goal=4
    #[6] coprime m p ∨ k = 0 ⟶ ¬(divides p m)
    #[7] ¬(divides p m) ⟶ coprime m p ∨ k = 0
  ← intro goal=6
    #[8] coprime m p ∨ k = 0
    #[9] ¬(divides p m)
  ← rule disjE goal=9 facts=[8]
    #[10] coprime m p ⟶ ¬(divides p m)
    #[11] k = 0 ⟶ ¬(divides p m)
  ← intro goal=10
    #[12] coprime m p
    #[13] ¬(divides p m)
  → rewrite target=fact coprime_sym sym=false goal=13 facts=[12]
    #[14] coprime p m
  → rewrite target=fact source=prev goal=13 facts=[5,14]
    #[15] ¬(divides p m)
  ← intro goal=11
    #[16] k = 0
    #[17] ¬(divides p m)
  ← rule negE_gen goal=17 facts=[2,16]
    #[18] ¬(divides p m)
  ← intro goal=7
    #[19] ¬(divides p m)
    #[20] coprime m p ∨ k = 0
  ← rule disjI1 goal=20
    #[21] coprime m p
  ← rewrite coprime_sym sym=false goal=21
    #[22] coprime p m
  ← rewrite source=prev goal=22 facts=[5]
qed

theorem coprime_bezout_strong
  fixes a :: nat, b :: nat
  prop coprime a b --> ~(b = 1) --> (?x. ?y. a * x = b * y + 1)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] ¬(b = 1)
    #[3] ∃x. ∃y. a * x = b * y + 1
  → rewrite target=fact coprime_gcd sym=false goal=3 facts=[1]
    #[4] gcd a b = 1
  → rewrite target=fact eq_sym_eq sym=false goal=3 facts=[4]
    #[5] 1 = gcd a b
  ← rewrite source=prev goal=3 facts=[5]
    #[6] ∃x. ∃y. a * x = b * y + gcd a b
  ← rule bezout_gcd_strong goal=6
    #[7] ¬(a = 0)
  ← rule contrapositive goal=7 facts=[2]
    #[8] ¬¬(a = 0) ⟶ ¬¬(b = 1)
  ← rewrite double_neg sym=false goal=8
    #[9] a = 0 ⟶ b = 1
  ← intro goal=9
    #[10] a = 0
    #[11] b = 1
  → rewrite target=fact source=prev goal=11 facts=[10,1]
    #[12] coprime 0 b
  → rewrite target=fact coprime_sym sym=false goal=11 facts=[12]
    #[13] coprime b 0
  → rewrite target=fact coprime_0 sym=false goal=11 facts=[13]
    #[14] b = 1
qed

theorem coprime_bezout_alt
  fixes a :: nat, b :: nat
  prop coprime a b --> ~(a = 0) --> (?x. ?y. a * x = b * y + 1)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] coprime a b
    #[2] ¬(a = 0)
    #[3] ∃x. ∃y. a * x = b * y + 1
  → rewrite target=fact coprime_gcd sym=false goal=3 facts=[1]
    #[4] gcd a b = 1
  → rewrite target=fact eq_sym_eq sym=false goal=3 facts=[4]
    #[5] 1 = gcd a b
  ← rewrite source=prev goal=3 facts=[5]
    #[6] ∃x. ∃y. a * x = b * y + gcd a b
  ← rule bezout_gcd_strong goal=6
qed

theorem bezout_prime
  fixes a :: nat, p :: nat
  prop prime p --> ~(divides p a) --> (?x. ?y. a * x = p * y + 1)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(divides p a)
    #[3] ∃x. ∃y. a * x = p * y + 1
  ← rule coprime_bezout_strong goal=3
    #[4] coprime a p
    #[5] ¬(p = 1)
  → forward prime_coprime_strong param_n=a goal=4 facts=[1]
    #[6] divides p a ∨ coprime p a
  ← rule disjE goal=4 facts=[6]
    #[7] divides p a ⟶ coprime a p
    #[8] coprime p a ⟶ coprime a p
  ← intro goal=7
    #[9] divides p a
    #[10] coprime a p
  ← rule negE_gen goal=10 facts=[2,9]
    #[11] coprime a p
  ← intro goal=8
    #[12] coprime p a
    #[13] coprime a p
  ← rewrite coprime_sym sym=false goal=13
  ← rule contrapositive goal=5 facts=[1]
    #[14] ¬¬(p = 1) ⟶ ¬(prime p)
  ← intro goal=14
    #[15] ¬¬(p = 1)
    #[16] ¬(prime p)
  → rewrite target=fact double_neg sym=false goal=16 facts=[15]
    #[17] p = 1
  ← rewrite source=prev goal=16 facts=[17]
    #[18] ¬(prime 1)
  ← rule prime_1 goal=18
    #[19] ¬(prime 1)
qed

theorem prime_divprod
  fixes a :: nat, b :: nat, p :: nat
  prop prime p --> divides p (a * b) --> divides p a | divides p b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides p (a * b)
    #[3] divides p a ∨ divides p b
  → forward prime_coprime_strong param_n=b goal=3 facts=[1]
    #[4] divides p b ∨ coprime p b
  ← rule disjE goal=3 facts=[4]
    #[5] divides p b ⟶ divides p a ∨ divides p b
    #[6] coprime p b ⟶ divides p a ∨ divides p b
  ← intro goal=5
    #[7] divides p b
    #[8] divides p a ∨ divides p b
  ← rule disjI2 goal=8
  ← intro goal=6
    #[9] coprime p b
    #[10] divides p a ∨ divides p b
  ← rule disjI1 goal=10
    #[11] divides p a
  ← rule coprime_divprod param_a=b goal=11
    #[12] divides p (b * a)
  ← rewrite mult_comm sym=false goal=12
qed

theorem prime_divprod_eq
  fixes a :: nat, b :: nat, p :: nat
  prop prime p --> divides p (a * b) <--> divides p a | divides p b
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides p (a * b) ⟷ divides p a ∨ divides p b
  ← rule zero_one_or_prime_divprod goal=2
    #[3] p = 0 ∨ p = 1 ∨ prime p
  ← rule disjI2 goal=3
    #[4] p = 1 ∨ prime p
  ← rule disjI2 goal=4
qed

theorem prime_divexp
  fixes a :: nat, n :: nat, p :: nat
  prop prime p --> divides p (a ^ n) --> divides p a
  [hint_backward,hint_backward1]

theorem prime_divexp_n
  fixes a :: nat, n :: nat, p :: nat
  prop prime p --> divides p (a ^ n) --> divides (p ^ n) (a ^ n)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides p (a ^ n)
    #[3] divides (p ^ n) (a ^ n)
  ← rule divides_exp goal=3
    #[4] divides p a
  ← rule prime_divexp goal=4 facts=[1,2]
qed

theorem prime_divexp_eq
  fixes a :: nat, n :: nat, p :: nat
  prop prime p --> divides p (a ^ n) <--> divides p a & ~(n = 0)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides p (a ^ n) ⟷ divides p a ∧ ¬(n = 0)
  ← cases "n = 0" goal=2
    #[3] n = 0 ⟶ divides p (a ^ n) ⟷ divides p a ∧ ¬(n = 0)
    #[4] ¬(n = 0) ⟶ divides p (a ^ n) ⟷ divides p a ∧ ¬(n = 0)
  ← intro goal=3
    #[5] n = 0
    #[6] divides p (a ^ n) ⟷ divides p a ∧ ¬(n = 0)
  ← rewrite source=prev goal=6 facts=[5]
    #[7] divides p (a ^ (0::nat)) ⟷ divides p a ∧ ¬((0::nat) = 0)
  ← rewrite nat_nat_power_def_1 sym=false goal=7
    #[8] divides p 1 ⟷ divides p a ∧ ¬((0::nat) = 0)
  ← rewrite divides_one sym=false goal=8
    #[9] p = 1 ⟷ divides p a ∧ ¬((0::nat) = 0)
  ← rule iffI goal=9
    #[10] p = 1 ⟶ divides p a ∧ ¬((0::nat) = 0)
    #[11] divides p a ∧ ¬((0::nat) = 0) ⟶ p = 1
  ← intro goal=10
    #[12] p = 1
    #[13] divides p a ∧ ¬((0::nat) = 0)
  → rewrite target=fact source=prev goal=13 facts=[12,1]
    #[14] prime 1
  ← rule contrapositive goal=13 facts=[14]
    #[15] ¬(divides p a ∧ ¬((0::nat) = 0)) ⟶ ¬(prime 1)
  ← intro goal=15
    #[16] ¬(divides p a ∧ ¬((0::nat) = 0))
    #[17] ¬(prime 1)
  ← rule prime_1 goal=17
    #[18] ¬(prime 1)
  ← intro goal=11
    #[19] divides p a ∧ ¬((0::nat) = 0)
    #[20] p = 1
  → forward conjD2 goal=20 facts=[19]
    #[21] ¬((0::nat) = 0)
  ← rule negE_gen goal=20 facts=[21]
    #[22] (0::nat) = 0
  ← rule eq_refl goal=22
    #[23] (0::nat) = 0
  ← intro goal=4
    #[24] ¬(n = 0)
    #[25] divides p (a ^ n) ⟷ divides p a ∧ ¬(n = 0)
  ← rule iffI goal=25
    #[26] divides p (a ^ n) ⟶ divides p a ∧ ¬(n = 0)
    #[27] divides p a ∧ ¬(n = 0) ⟶ divides p (a ^ n)
  ← intro goal=26
    #[28] divides p (a ^ n)
    #[29] divides p a ∧ ¬(n = 0)
  ← rule conjI goal=29
    #[30] divides p a
  ← rule prime_divexp goal=30 facts=[1,28]
    #[31] divides p a
  ← intro goal=27
    #[32] divides p a ∧ ¬(n = 0)
    #[33] divides p (a ^ n)
  → forward conjD1 goal=33 facts=[32]
    #[34] divides p a
  ← rule divides_rexp goal=33
qed

theorem parity_exp
  fixes a :: nat, n :: nat
  prop even (a ^ Suc n) <--> even a
  [hint_rewrite]
proof
  ← rewrite divides_2 sym=true goal=0
    #[1] divides 2 (a ^ Suc n) ⟷ divides 2 a
  ← rule iffI goal=1
    #[2] divides 2 (a ^ Suc n) ⟶ divides 2 a
    #[3] divides 2 a ⟶ divides 2 (a ^ Suc n)
  ← intro goal=2
    #[4] divides 2 (a ^ Suc n)
    #[5] divides 2 a
  ← rule prime_divexp param_n="Suc n" goal=5
    #[6] prime 2
  ← rule prime_2 goal=6
    #[7] prime 2
  ← intro goal=3
    #[8] divides 2 a
    #[9] divides 2 (a ^ Suc n)
  ← rewrite nat_nat_power_def_2 sym=false goal=9
    #[10] divides 2 (a * a ^ n)
  ← rule divides_rmul goal=10 facts=[8]
qed

theorem coprime_sos
  fixes x :: nat, y :: nat
  prop coprime x y --> coprime (x * y) (x ^ (2::nat) + y ^ (2::nat))
  [hint_backward]
proof
  ← intro goal=0
    #[1] coprime x y
    #[2] coprime (x * y) (x ^ (2::nat) + y ^ (2::nat))
  ← rewrite exp_2 sym=false goal=2
    #[3] coprime (x * y) (x * x + y * y)
  ← rewrite coprime_lmul sym=false goal=3
    #[4] coprime x (x * x + y * y) ∧ coprime y (x * x + y * y)
  ← rule conjI goal=4
    #[5] coprime x (x * x + y * y)
    #[6] coprime y (x * x + y * y)
  ← rewrite coprime_gcd sym=false goal=5
    #[7] gcd x (x * x + y * y) = 1
  ← rewrite gcd_mod sym=false goal=7
    #[8] gcd x (y * y) = 1
  ← rewrite exp_2 sym=true goal=8
    #[9] gcd x (y ^ (2::nat)) = 1
  ← rewrite coprime_gcd sym=true goal=9
    #[10] coprime x (y ^ (2::nat))
  ← rule coprime_exp goal=10
  ← rewrite coprime_gcd sym=false goal=6
    #[11] gcd y (x * x + y * y) = 1
  ← rewrite add_comm sym=false goal=11
    #[12] gcd y (y * y + x * x) = 1
  ← rewrite gcd_mod sym=false goal=12
    #[13] gcd y (x * x) = 1
  ← rewrite exp_2 sym=true goal=13
    #[14] gcd y (x ^ (2::nat)) = 1
  ← rewrite coprime_gcd sym=true goal=14
    #[15] coprime y (x ^ (2::nat))
  ← rule coprime_exp goal=15
    #[16] coprime y x
  ← rewrite coprime_sym sym=false goal=16
qed

theorem prime_imp_nz
  fixes p :: nat
  prop prime p --> ~(p = 0)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(p = 0)
  ← rule contrapositive goal=2 facts=[1]
    #[3] ¬¬(p = 0) ⟶ ¬(prime p)
  ← intro goal=3
    #[4] ¬¬(p = 0)
    #[5] ¬(prime p)
  → rewrite target=fact double_neg sym=false goal=5 facts=[4]
    #[6] p = 0
  ← rewrite source=prev goal=5 facts=[6]
    #[7] ¬(prime 0)
  ← rule prime_0 goal=7
    #[8] ¬(prime 0)
qed

theorem distinct_prime_coprime
  fixes p :: nat, q :: nat
  prop prime p --> prime q --> ~(p = q) --> coprime p q
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] prime q
    #[3] ¬(p = q)
    #[4] coprime p q
  → rewrite target=fact prime_def sym=false goal=4 facts=[1]
    #[5] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → rewrite target=fact prime_def sym=false goal=4 facts=[2]
    #[6] ¬(q = 1) ∧ (∀x. divides x q ⟶ x = 1 ∨ x = q)
  ← rewrite coprime_def sym=false goal=4
    #[7] ∀d. divides d p ∧ divides d q ⟷ d = 1
  ← intro d goal=7
    #[8] _VAR d
    #[9] divides d p ∧ divides d q ⟷ d = 1
  ← rule iffI goal=9
    #[10] divides d p ∧ divides d q ⟶ d = 1
    #[11] d = 1 ⟶ divides d p ∧ divides d q
  ← intro goal=10
    #[12] divides d p ∧ divides d q
    #[13] d = 1
  → forward conjD1 goal=13 facts=[12]
    #[14] divides d p
  → forward conjD2 goal=13 facts=[12]
    #[15] divides d q
  → forward conjD2 goal=13 facts=[5]
    #[16] ∀x. divides x p ⟶ x = 1 ∨ x = p
  → forward goal=13 facts=[16,14]
    #[17] d = 1 ∨ d = p
  ← rule disjE goal=13 facts=[17]
    #[18] d = 1 ⟶ d = 1
    #[19] d = p ⟶ d = 1
  ← intro goal=19
    #[20] d = p
    #[21] d = 1
  → forward conjD2 goal=21 facts=[6]
    #[22] ∀x. divides x q ⟶ x = 1 ∨ x = q
  → forward goal=21 facts=[22,15]
    #[23] d = 1 ∨ d = q
  ← rule disjE goal=21 facts=[23]
  ← intro goal=11
    #[24] d = 1
    #[25] divides d p ∧ divides d q
  ← rewrite source=prev goal=25 facts=[24]
    #[26] divides 1 p ∧ divides 1 q
  ← rule conjI goal=26
    #[27] divides 1 p
    #[28] divides 1 q
  ← rule divides_1 goal=27
    #[29] divides 1 p
  ← rule divides_1 goal=28
    #[30] divides 1 q
qed

theorem prime_coprime_lt
  fixes p :: nat, x :: nat
  prop prime p --> 0 < x --> x < p --> coprime x p
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] 0 < x
    #[3] x < p
    #[4] coprime x p
  → rewrite target=fact prime_def sym=false goal=4 facts=[1]
    #[5] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← rewrite coprime_def sym=false goal=4
    #[6] ∀d. divides d x ∧ divides d p ⟷ d = 1
  ← intro d goal=6
    #[7] _VAR d
    #[8] divides d x ∧ divides d p ⟷ d = 1
  ← rule iffI goal=8
    #[9] divides d x ∧ divides d p ⟶ d = 1
    #[10] d = 1 ⟶ divides d x ∧ divides d p
  ← intro goal=9
    #[11] divides d x ∧ divides d p
    #[12] d = 1
  → forward conjD2 goal=12 facts=[11]
    #[13] divides d p
  → forward conjD2 goal=12 facts=[5]
    #[14] ∀x. divides x p ⟶ x = 1 ∨ x = p
  → forward goal=12 facts=[14,13]
    #[15] d = 1 ∨ d = p
  ← rule disjE goal=12 facts=[15]
    #[16] d = 1 ⟶ d = 1
    #[17] d = p ⟶ d = 1
  ← intro goal=17
    #[18] d = p
    #[19] d = 1
  → forward conjD1 goal=19 facts=[11]
    #[20] divides d x
  → forward divides_le goal=19 facts=[20]
    #[21] d ≤ x ∨ x = 0
  ← rule disjE goal=19 facts=[21]
    #[22] d ≤ x ⟶ d = 1
    #[23] x = 0 ⟶ d = 1
  ← intro goal=22
    #[24] d ≤ x
    #[25] d = 1
  → rewrite target=fact source=prev goal=25 facts=[18,24]
    #[26] p ≤ x
  ← rule contrapositive goal=25 facts=[26]
    #[27] ¬(d = 1) ⟶ ¬(p ≤ x)
  ← rewrite not_le sym=false goal=27
    #[28] ¬(d = 1) ⟶ x < p
  ← intro goal=28
    #[29] ¬(d = 1)
  ← intro goal=23
    #[30] x = 0
    #[31] d = 1
  → rewrite target=fact source=prev goal=31 facts=[30,2]
    #[32] (0::nat) < 0
  → rewrite target=fact nat_less_def_1 sym=false goal=31 facts=[32]
    #[33] false
  ← resolve not_false_res goal=31 facts=[33]
    #[34] d = 1
  ← intro goal=10
    #[35] d = 1
    #[36] divides d x ∧ divides d p
  ← rewrite source=prev goal=36 facts=[35]
    #[37] divides 1 x ∧ divides 1 p
  ← rule conjI goal=37
    #[38] divides 1 x
    #[39] divides 1 p
  ← rule divides_1 goal=38
    #[40] divides 1 x
  ← rule divides_1 goal=39
    #[41] divides 1 p
qed

theorem divides_prime_prime
  fixes p :: nat, q :: nat
  prop prime p --> prime q --> divides p q <--> p = q
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] prime q
    #[3] divides p q ⟷ p = q
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=3 facts=[1]
    #[4] ∀n. coprime p n ⟷ ¬(divides p n)
  → rewrite target=fact eq_sym_eq sym=false goal=3 facts=[4]
    #[5] ∀n. ¬(divides p n) ⟷ coprime p n
  ← rule iffI goal=3
    #[6] divides p q ⟶ p = q
    #[7] p = q ⟶ divides p q
  ← intro goal=6
    #[8] divides p q
    #[9] p = q
  → rewrite target=fact double_neg sym=true goal=9 facts=[8]
    #[10] ¬¬(divides p q)
  → rewrite target=fact source=prev goal=9 facts=[5,10]
    #[11] ¬(coprime p q)
  ← rule contrapositive goal=9 facts=[11]
    #[12] ¬(p = q) ⟶ ¬¬(coprime p q)
  ← intro goal=12
    #[13] ¬(p = q)
    #[14] ¬¬(coprime p q)
  ← rewrite double_neg sym=false goal=14
    #[15] coprime p q
  ← rule distinct_prime_coprime goal=15
  ← intro goal=7
    #[16] p = q
    #[17] divides p q
  ← rewrite source=prev goal=17 facts=[16]
    #[18] divides q q
  ← rule divides_self goal=18
    #[19] divides q q
qed

theorem coprime_prime_prime
  fixes p :: nat, q :: nat
  prop prime p --> prime q --> coprime p q <--> ~(p = q)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] prime q
    #[3] coprime p q ⟷ ¬(p = q)
  → forward divides_prime_prime goal=3 facts=[1,2]
    #[4] divides p q ⟷ p = q
  → rewrite target=fact eq_sym_eq sym=false goal=3 facts=[4]
    #[5] p = q ⟷ divides p q
  ← rewrite source=prev goal=3 facts=[5]
    #[6] coprime p q ⟷ ¬(divides p q)
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=6 facts=[1]
    #[7] ∀n. coprime p n ⟷ ¬(divides p n)
  ← apply_prev goal=6 facts=[7]
    #[8] coprime p q ⟷ ¬(divides p q)
qed

theorem divides_prime_exp_le
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop prime p --> prime q --> divides (p ^ m) (q ^ n) <--> m = 0 | p = q & m <= n
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] prime q
    #[3] divides (p ^ m) (q ^ n) ⟷ m = 0 ∨ p = q ∧ m ≤ n
  ← induct m nat_induct goal=3
    #[4] divides (p ^ (0::nat)) (q ^ n) ⟷ (0::nat) = 0 ∨ p = q ∧ 0 ≤ n
    #[5] ∀n1. divides (p ^ n1) (q ^ n) ⟷ n1 = 0 ∨ p = q ∧ n1 ≤ n ⟶ divides (p ^ Suc n1) (q ^ n) ⟷ Suc n1 = 0 ∨ p = q ∧ Suc n1 ≤ n
  ← rewrite nat_nat_power_def_1 sym=false goal=4
    #[6] divides 1 (q ^ n) ⟷ (0::nat) = 0 ∨ p = q ∧ 0 ≤ n
  ← rule iffI goal=6
    #[7] divides 1 (q ^ n) ⟶ (0::nat) = 0 ∨ p = q ∧ 0 ≤ n
    #[8] (0::nat) = 0 ∨ p = q ∧ 0 ≤ n ⟶ divides 1 (q ^ n)
  ← intro goal=7
    #[9] divides 1 (q ^ n)
    #[10] (0::nat) = 0 ∨ p = q ∧ 0 ≤ n
  ← rule disjI1 goal=10
    #[11] (0::nat) = 0
  ← rule eq_refl goal=11
    #[12] (0::nat) = 0
  ← intro goal=8
    #[13] (0::nat) = 0 ∨ p = q ∧ 0 ≤ n
    #[14] divides 1 (q ^ n)
  ← rule divides_1 goal=14
    #[15] divides 1 (q ^ n)
  ← intro m goal=5
    #[16] _VAR m
    #[17] divides (p ^ m) (q ^ n) ⟷ m = 0 ∨ p = q ∧ m ≤ n
    #[18] divides (p ^ Suc m) (q ^ n) ⟷ Suc m = 0 ∨ p = q ∧ Suc m ≤ n
  ← rule iffI goal=18
    #[19] divides (p ^ Suc m) (q ^ n) ⟶ Suc m = 0 ∨ p = q ∧ Suc m ≤ n
    #[20] Suc m = 0 ∨ p = q ∧ Suc m ≤ n ⟶ divides (p ^ Suc m) (q ^ n)
  ← intro goal=19
    #[21] divides (p ^ Suc m) (q ^ n)
    #[22] Suc m = 0 ∨ p = q ∧ Suc m ≤ n
  ← rule disjI2 goal=22
    #[23] p = q ∧ Suc m ≤ n
  → rewrite target=fact nat_nat_power_def_2 sym=false goal=23 facts=[21]
    #[24] divides (p * p ^ m) (q ^ n)
  → forward divides_rmul2 goal=23 facts=[24]
    #[25] divides p (q ^ n)
  ← rule conjI goal=23
    #[26] p = q
    #[27] Suc m ≤ n
  → forward divides_prime_prime goal=26 facts=[1,2]
    #[28] divides p q ⟷ p = q
  → rewrite target=fact eq_sym_eq sym=false goal=26 facts=[28]
    #[29] p = q ⟷ divides p q
  ← rewrite source=prev goal=26 facts=[29]
    #[30] divides p q
  ← rule prime_divexp goal=30 facts=[1,25]
    #[31] divides p q
  → rewrite target=fact source=prev goal=27 facts=[26,21]
    #[32] divides (q ^ Suc m) (q ^ n)
  → forward prime_ge_2 goal=27 facts=[2]
    #[33] 2 ≤ q
  → forward divides_exp_le param_m="Suc m" param_n=n goal=27 facts=[33]
    #[34] divides (q ^ Suc m) (q ^ n) ⟷ Suc m ≤ n
  → rewrite target=fact source=prev goal=27 facts=[34,32]
  ← intro goal=20
    #[35] Suc m = 0 ∨ p = q ∧ Suc m ≤ n
    #[36] divides (p ^ Suc m) (q ^ n)
  ← rule disjE goal=36 facts=[35]
    #[37] Suc m = 0 ⟶ divides (p ^ Suc m) (q ^ n)
    #[38] p = q ∧ Suc m ≤ n ⟶ divides (p ^ Suc m) (q ^ n)
  ← intro goal=37
    #[39] Suc m = 0
    #[40] divides (p ^ Suc m) (q ^ n)
  ← rule contrapositive goal=40 facts=[39]
    #[41] ¬(divides (p ^ Suc m) (q ^ n)) ⟶ ¬(Suc m = 0)
  ← intro goal=41
    #[42] ¬(divides (p ^ Suc m) (q ^ n))
    #[43] ¬(Suc m = 0)
  ← rule ineq_sym goal=43
    #[44] ¬(0 = Suc m)
  ← rule nat_zero_Suc_neq goal=44
    #[45] ¬(0 = Suc m)
  ← intro goal=38
    #[46] p = q ∧ Suc m ≤ n
    #[47] divides (p ^ Suc m) (q ^ n)
  → forward conjD1 goal=47 facts=[46]
    #[48] p = q
  → forward conjD2 goal=47 facts=[46]
    #[49] Suc m ≤ n
  ← rewrite source=prev goal=47 facts=[48]
    #[50] divides (q ^ Suc m) (q ^ n)
  ← rule divides_exp_le_imp goal=50 facts=[49]
    #[51] divides (q ^ Suc m) (q ^ n)
qed

theorem eq_prime_exp
  fixes m :: nat, n :: nat, p :: nat, q :: nat
  prop prime p --> prime q --> p ^ m = q ^ n <--> m = 0 & n = 0 | p = q & m = n
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] prime q
    #[3] p ^ m = q ^ n ⟷ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← rule iffI goal=3
    #[4] p ^ m = q ^ n ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
    #[5] m = 0 ∧ n = 0 ∨ p = q ∧ m = n ⟶ p ^ m = q ^ n
  ← intro goal=4
    #[6] p ^ m = q ^ n
    #[7] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  → forward divides_prime_exp_le param_m=m param_n=n goal=7 facts=[1,2]
    #[8] divides (p ^ m) (q ^ n) ⟷ m = 0 ∨ p = q ∧ m ≤ n
  → rewrite target=fact source=prev goal=7 facts=[6,8]
    #[9] divides (q ^ n) (q ^ n) ⟷ m = 0 ∨ p = q ∧ m ≤ n
  cut "divides (q ^ n) (q ^ n)" goal=7
    #[10] divides (q ^ n) (q ^ n)
  ← rule divides_self goal=10
    #[11] divides (q ^ n) (q ^ n)
  → rewrite target=fact source=prev goal=7 facts=[9,11]
    #[12] m = 0 ∨ p = q ∧ m ≤ n
  → rewrite target=fact eq_sym_eq sym=false goal=7 facts=[6]
    #[13] q ^ n = p ^ m
  → forward divides_prime_exp_le param_m=n param_n=m goal=7 facts=[2,1]
    #[14] divides (q ^ n) (p ^ m) ⟷ n = 0 ∨ q = p ∧ n ≤ m
  → rewrite target=fact source=prev goal=7 facts=[13,14]
    #[15] divides (p ^ m) (p ^ m) ⟷ n = 0 ∨ q = p ∧ n ≤ m
  cut "divides (p ^ m) (p ^ m)" goal=7
    #[16] divides (p ^ m) (p ^ m)
  ← rule divides_self goal=16
    #[17] divides (p ^ m) (p ^ m)
  → rewrite target=fact source=prev goal=7 facts=[15,17]
    #[18] n = 0 ∨ q = p ∧ n ≤ m
  ← rule disjE goal=7 facts=[12]
    #[19] m = 0 ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
    #[20] p = q ∧ m ≤ n ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← intro goal=19
    #[21] m = 0
    #[22] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← cases "n=0" goal=22
    #[23] n = 0 ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
    #[24] ¬(n = 0) ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← intro goal=23
    #[25] n = 0
    #[26] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← rule disjI1 goal=26
    #[27] m = 0 ∧ n = 0
  ← rule conjI goal=27
  ← intro goal=24
    #[28] ¬(n = 0)
    #[29] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← rule disjE goal=29 facts=[18]
    #[30] q = p ∧ n ≤ m ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← intro goal=30
    #[31] q = p ∧ n ≤ m
    #[32] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  → forward conjD2 goal=32 facts=[31]
    #[33] n ≤ m
  → rewrite target=fact source=prev goal=32 facts=[21,33]
    #[34] n ≤ 0
  → rewrite target=fact nat_less_eq_def_1 sym=false goal=32 facts=[34]
    #[35] n = 0
  ← rule negE_gen goal=32 facts=[28,35]
    #[36] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← intro goal=20
    #[37] p = q ∧ m ≤ n
    #[38] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← rule disjE goal=38 facts=[18]
    #[39] n = 0 ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
    #[40] q = p ∧ n ≤ m ⟶ m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← intro goal=39
    #[41] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← rule disjI1 goal=41
    #[42] m = 0 ∧ n = 0
  ← rule conjI goal=42
    #[43] m = 0
  → forward conjD2 goal=43 facts=[37]
    #[44] m ≤ n
  → rewrite target=fact source=prev goal=43 facts=[25,44]
    #[45] m ≤ 0
  → rewrite target=fact nat_less_eq_def_1 sym=false goal=43 facts=[45]
    #[46] m = 0
  ← intro goal=40
    #[47] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
  ← rule disjI2 goal=47
    #[48] p = q ∧ m = n
  ← rule conjI goal=48
    #[49] p = q
    #[50] m = n
  ← rule conjD1 goal=49 facts=[37]
    #[51] p = q
  → forward conjD2 goal=50 facts=[37]
  → forward conjD2 goal=50 facts=[31]
  → forward conjI goal=50 facts=[44,33]
    #[52] m ≤ n ∧ n ≤ m
  → rewrite target=fact le_antisym sym=false goal=50 facts=[52]
    #[53] m = n
  ← intro goal=5
    #[54] m = 0 ∧ n = 0 ∨ p = q ∧ m = n
    #[55] p ^ m = q ^ n
  ← rule disjE goal=55 facts=[54]
    #[56] m = 0 ∧ n = 0 ⟶ p ^ m = q ^ n
    #[57] p = q ∧ m = n ⟶ p ^ m = q ^ n
  ← intro goal=56
    #[58] m = 0 ∧ n = 0
    #[59] p ^ m = q ^ n
  → forward conjD1 goal=59 facts=[58]
    #[60] m = 0
  → forward conjD2 goal=59 facts=[58]
    #[61] n = 0
  ← rewrite source=prev goal=59 facts=[60]
    #[62] p ^ (0::nat) = q ^ n
  ← rewrite source=prev goal=62 facts=[61]
    #[63] p ^ (0::nat) = q ^ (0::nat)
  ← rewrite nat_nat_power_def_1 sym=false goal=63
    #[64] p ^ (0::nat) = q ^ (0::nat)
  ← intro goal=57
    #[65] p = q ∧ m = n
    #[66] p ^ m = q ^ n
  → forward conjD1 goal=66 facts=[65]
    #[67] p = q
  → forward conjD2 goal=66 facts=[65]
    #[68] m = n
  ← rewrite source=prev goal=66 facts=[67]
    #[69] q ^ m = q ^ n
  ← rewrite source=prev goal=69 facts=[68]
    #[70] q ^ m = q ^ n
qed

theorem prime_odd
  fixes p :: nat
  prop prime p --> p = 2 | odd p
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] p = 2 ∨ odd p
  → rewrite target=fact prime_def sym=false goal=2 facts=[1]
    #[3] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD2 goal=2 facts=[3]
    #[4] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← rewrite not_even sym=true goal=2
    #[5] p = 2 ∨ ¬(even p)
  → inst target=fact "(2::nat)" goal=5 facts=[4]
    #[6] divides 2 p ⟶ (2::nat) = 1 ∨ 2 = p
  ← cases "even p" goal=5
    #[7] even p ⟶ p = 2 ∨ ¬(even p)
    #[8] ¬(even p) ⟶ p = 2 ∨ ¬(even p)
  ← intro goal=7
    #[9] even p
    #[10] p = 2 ∨ ¬(even p)
  → rewrite target=fact divides_2 sym=true goal=10 facts=[9]
    #[11] divides 2 p
  → forward goal=10 facts=[6,11]
    #[12] (2::nat) = 1 ∨ 2 = p
  ← rule disjI1 goal=10
    #[13] p = 2
  ← rule disjE goal=13 facts=[12]
    #[14] (2::nat) = 1 ⟶ p = 2
    #[15] 2 = p ⟶ p = 2
  ← intro goal=14
    #[16] (2::nat) = 1
    #[17] p = 2
  ← z3 goal=17 facts=[16]
    #[18] p = 2
  ← intro goal=15
    #[19] 2 = p
    #[20] p = 2
  ← rewrite eq_sym_eq sym=false goal=20
  ← intro goal=8
    #[21] ¬(even p)
    #[22] p = 2 ∨ ¬(even p)
  ← rule disjI2 goal=22
qed

theorem odd_prime
  fixes p :: nat
  prop prime p --> odd p <--> 3 <= p
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] odd p ⟷ 3 ≤ p
  ← cases "p=0" goal=2
    #[3] p = 0 ⟶ odd p ⟷ 3 ≤ p
    #[4] ¬(p = 0) ⟶ odd p ⟷ 3 ≤ p
  ← intro goal=3
    #[5] p = 0
    #[6] odd p ⟷ 3 ≤ p
  → rewrite target=fact source=prev goal=6 facts=[5,1]
    #[7] prime 0
  ← rule contrapositive goal=6 facts=[7]
    #[8] ¬(odd p ⟷ 3 ≤ p) ⟶ ¬(prime 0)
  ← intro goal=8
    #[9] ¬(odd p ⟷ 3 ≤ p)
    #[10] ¬(prime 0)
  ← rule prime_0 goal=10
    #[11] ¬(prime 0)
  ← intro goal=4
    #[12] ¬(p = 0)
    #[13] odd p ⟷ 3 ≤ p
  → forward le_1_2 goal=13 facts=[12]
    #[14] 1 ≤ p
  → rewrite target=fact le_lt sym=false goal=13 facts=[14]
    #[15] 1 < p ∨ 1 = p
  ← rule disjE goal=13 facts=[15]
    #[16] 1 < p ⟶ odd p ⟷ 3 ≤ p
    #[17] 1 = p ⟶ odd p ⟷ 3 ≤ p
  ← intro goal=16
    #[18] 1 < p
    #[19] odd p ⟷ 3 ≤ p
  ← cases "p=2" goal=19
    #[20] p = 2 ⟶ odd p ⟷ 3 ≤ p
    #[21] ¬(p = 2) ⟶ odd p ⟷ 3 ≤ p
  ← intro goal=20
    #[22] p = 2
    #[23] odd p ⟷ 3 ≤ p
  ← rewrite source=prev goal=23 facts=[22]
    #[24] odd 2 ⟷ (3::nat) ≤ 2
  ← rule iffI goal=24
    #[25] odd 2 ⟶ (3::nat) ≤ 2
    #[26] (3::nat) ≤ 2 ⟶ odd 2
  ← intro goal=25
    #[27] odd 2
    #[28] (3::nat) ≤ 2
  → rewrite target=fact not_even sym=true goal=28 facts=[27]
    #[29] ¬(even 2)
  ← rule negE_gen goal=28 facts=[29]
    #[30] even 2
  ← rewrite even_exists sym=false goal=30
    #[31] ∃m::nat. 2 = 2 * m
  ← inst "(1::nat)" goal=31
    #[32] (2::nat) = 2 * 1
  ← rewrite mult_1_right sym=false goal=32
    #[33] (2::nat) = 2 * 1
  ← intro goal=26
    #[34] (3::nat) ≤ 2
    #[35] odd 2
  ← z3 goal=35 facts=[34]
    #[36] odd 2
  ← intro goal=21
    #[37] ¬(p = 2)
    #[38] odd p ⟷ 3 ≤ p
  → forward prime_odd goal=38 facts=[1]
    #[39] p = 2 ∨ odd p
  ← rule disjE goal=38 facts=[39]
    #[40] odd p ⟶ odd p ⟷ 3 ≤ p
  ← intro goal=40
    #[41] odd p
    #[42] odd p ⟷ 3 ≤ p
  ← rule iffI goal=42
    #[43] odd p ⟶ 3 ≤ p
    #[44] 3 ≤ p ⟶ odd p
  ← intro goal=43
    #[45] 3 ≤ p
  ← z3 goal=45 facts=[37,18]
    #[46] 3 ≤ p
  ← intro goal=44
    #[47] 3 ≤ p
  ← intro goal=17
    #[48] 1 = p
    #[49] odd p ⟷ 3 ≤ p
  → rewrite target=fact eq_sym_eq sym=false goal=49 facts=[48]
    #[50] p = 1
  → rewrite target=fact source=prev goal=49 facts=[50,1]
    #[51] prime 1
  ← rule contrapositive goal=49 facts=[51]
    #[52] ¬(odd p ⟷ 3 ≤ p) ⟶ ¬(prime 1)
  ← intro goal=52
    #[53] ¬(prime 1)
  ← rule prime_1 goal=53
    #[54] ¬(prime 1)
qed

theorem divides_fact_prime
  fixes p :: nat
  prop prime p --> (!n. divides p (fact n) <--> p <= n)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ∀n. divides p (fact n) ⟷ p ≤ n
  ← intro n goal=2
    #[3] _VAR n
    #[4] divides p (fact n) ⟷ p ≤ n
  ← induct n nat_induct goal=4
    #[5] divides p (fact 0) ⟷ p ≤ 0
    #[6] ∀n. divides p (fact n) ⟷ p ≤ n ⟶ divides p (fact (Suc n)) ⟷ p ≤ Suc n
  ← rewrite nat_less_eq_def_1 sym=false goal=5
    #[7] divides p (fact 0) ⟷ p = 0
  ← rewrite fact_def_1 sym=false goal=7
    #[8] divides p 1 ⟷ p = 0
  ← rule iffI goal=8
    #[9] divides p 1 ⟶ p = 0
    #[10] p = 0 ⟶ divides p 1
  ← intro goal=9
    #[11] divides p 1
    #[12] p = 0
  → rewrite target=fact divides_one sym=false goal=12 facts=[11]
    #[13] p = 1
  → rewrite target=fact source=prev goal=12 facts=[13,1]
    #[14] prime 1
  ← rule contrapositive goal=12 facts=[14]
    #[15] ¬(p = 0) ⟶ ¬(prime 1)
  ← intro goal=15
    #[16] ¬(p = 0)
    #[17] ¬(prime 1)
  ← rule prime_1 goal=17
    #[18] ¬(prime 1)
  ← intro goal=10
    #[19] p = 0
    #[20] divides p 1
  → rewrite target=fact source=prev goal=20 facts=[19,1]
    #[21] prime 0
  ← rule contrapositive goal=20 facts=[21]
    #[22] ¬(divides p 1) ⟶ ¬(prime 0)
  ← intro goal=22
    #[23] ¬(divides p 1)
    #[24] ¬(prime 0)
  ← rule prime_0 goal=24
    #[25] ¬(prime 0)
  ← intro n goal=6
    #[26] divides p (fact n) ⟷ p ≤ n
    #[27] divides p (fact (Suc n)) ⟷ p ≤ Suc n
  ← rewrite fact_def_2 sym=false goal=27
    #[28] divides p (Suc n * fact n) ⟷ p ≤ Suc n
  ← rule iffI goal=28
    #[29] divides p (Suc n * fact n) ⟶ p ≤ Suc n
    #[30] p ≤ Suc n ⟶ divides p (Suc n * fact n)
  ← intro goal=29
    #[31] divides p (Suc n * fact n)
    #[32] p ≤ Suc n
  → forward prime_divprod_eq param_a="Suc n" param_b="fact n" goal=32 facts=[1]
    #[33] divides p (Suc n * fact n) ⟷ divides p (Suc n) ∨ divides p (fact n)
  → rewrite target=fact source=prev goal=32 facts=[33,31]
    #[34] divides p (Suc n) ∨ divides p (fact n)
  ← rule disjE goal=32 facts=[34]
    #[35] divides p (Suc n) ⟶ p ≤ Suc n
    #[36] divides p (fact n) ⟶ p ≤ Suc n
  ← intro goal=35
    #[37] divides p (Suc n)
    #[38] p ≤ Suc n
  → forward divides_le goal=38 facts=[37]
    #[39] p ≤ Suc n ∨ Suc n = 0
  ← rule disjE goal=38 facts=[39]
    #[40] p ≤ Suc n ⟶ p ≤ Suc n
    #[41] Suc n = 0 ⟶ p ≤ Suc n
  ← intro goal=41
    #[42] Suc n = 0
    #[43] p ≤ Suc n
  ← rule contrapositive goal=43 facts=[42]
    #[44] ¬(p ≤ Suc n) ⟶ ¬(Suc n = 0)
  ← intro goal=44
    #[45] ¬(p ≤ Suc n)
    #[46] ¬(Suc n = 0)
  ← rule ineq_sym goal=46
    #[47] ¬(0 = Suc n)
  ← rule nat_zero_Suc_neq goal=47
    #[48] ¬(0 = Suc n)
  ← intro goal=36
    #[49] divides p (fact n)
    #[50] p ≤ Suc n
  → rewrite target=fact source=prev goal=50 facts=[26,49]
    #[51] p ≤ n
  ← rewrite nat_less_eq_def_2 sym=false goal=50
    #[52] p = Suc n ∨ p ≤ n
  ← rule disjI2 goal=52
  ← intro goal=30
    #[53] p ≤ Suc n
    #[54] divides p (Suc n * fact n)
  → rewrite target=fact nat_less_eq_def_2 sym=false goal=54 facts=[53]
    #[55] p = Suc n ∨ p ≤ n
  ← rule disjE goal=54 facts=[55]
    #[56] p = Suc n ⟶ divides p (Suc n * fact n)
    #[57] p ≤ n ⟶ divides p (Suc n * fact n)
  ← intro goal=56
    #[58] p = Suc n
    #[59] divides p (Suc n * fact n)
  ← rewrite source=prev goal=59 facts=[58]
    #[60] divides (Suc n) (Suc n * fact n)
  ← rule divides_rmul goal=60
    #[61] divides (Suc n) (Suc n)
  ← rule divides_self goal=61
    #[62] divides (Suc n) (Suc n)
  ← intro goal=57
    #[63] p ≤ n
    #[64] divides p (Suc n * fact n)
  → rewrite target=fact eq_sym_eq sym=false goal=64 facts=[26]
    #[65] p ≤ n ⟷ divides p (fact n)
  → rewrite target=fact source=prev goal=64 facts=[65,63]
    #[66] divides p (fact n)
  ← rule divides_lmul goal=64
qed

theorem eq_prime_pow
  fixes m :: nat, n :: nat, p :: nat
  prop prime p --> p ^ m = p ^ n <--> m = n
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] p ^ m = p ^ n ⟷ m = n
  ← rewrite le_antisym sym=true goal=2
    #[3] p ^ m ≤ p ^ n ∧ p ^ n ≤ p ^ m ⟷ m ≤ n ∧ n ≤ m
  ← rewrite le_exp sym=false goal=3
    #[4] (if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m
  ← cases "p=0" goal=4
    #[5] p = 0 ⟶ (if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m
    #[6] ¬(p = 0) ⟶ (if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m
  ← intro goal=5
    #[7] p = 0
    #[8] (if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m
  → rewrite target=fact source=prev goal=8 facts=[7,1]
    #[9] prime 0
  ← rule contrapositive goal=8 facts=[9]
    #[10] ¬((if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m) ⟶ ¬(prime 0)
  ← intro goal=10
    #[11] ¬((if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m)
    #[12] ¬(prime 0)
  ← rule prime_0 goal=12
    #[13] ¬(prime 0)
  ← intro goal=6
    #[14] ¬(p = 0)
    #[15] (if p = 0 then m = 0 ⟶ n = 0 else p = 1 ∨ m ≤ n) ∧ (if p = 0 then n = 0 ⟶ m = 0 else p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m
  ← rewrite if_not_P sym=false goal=15 facts=[14]
    #[16] (p = 1 ∨ m ≤ n) ∧ (p = 1 ∨ n ≤ m) ⟷ m ≤ n ∧ n ≤ m
  ← rule iffI goal=16
    #[17] (p = 1 ∨ m ≤ n) ∧ (p = 1 ∨ n ≤ m) ⟶ m ≤ n ∧ n ≤ m
    #[18] m ≤ n ∧ n ≤ m ⟶ (p = 1 ∨ m ≤ n) ∧ (p = 1 ∨ n ≤ m)
  ← intro goal=17
    #[19] (p = 1 ∨ m ≤ n) ∧ (p = 1 ∨ n ≤ m)
    #[20] m ≤ n ∧ n ≤ m
  → forward conjD1 goal=20 facts=[19]
    #[21] p = 1 ∨ m ≤ n
  → forward conjD2 goal=20 facts=[19]
    #[22] p = 1 ∨ n ≤ m
  ← rule disjE goal=20 facts=[21]
    #[23] p = 1 ⟶ m ≤ n ∧ n ≤ m
    #[24] m ≤ n ⟶ m ≤ n ∧ n ≤ m
  ← intro goal=23
    #[25] p = 1
    #[26] m ≤ n ∧ n ≤ m
  → rewrite target=fact source=prev goal=26 facts=[25,1]
    #[27] prime 1
  ← rule contrapositive goal=26 facts=[27]
    #[28] ¬(m ≤ n ∧ n ≤ m) ⟶ ¬(prime 1)
  ← intro goal=28
    #[29] ¬(m ≤ n ∧ n ≤ m)
    #[30] ¬(prime 1)
  ← rule prime_1 goal=30
    #[31] ¬(prime 1)
  ← intro goal=24
    #[32] m ≤ n
    #[33] m ≤ n ∧ n ≤ m
  ← rule disjE goal=33 facts=[22]
    #[34] n ≤ m ⟶ m ≤ n ∧ n ≤ m
  ← intro goal=34
    #[35] n ≤ m
    #[36] m ≤ n ∧ n ≤ m
  ← rule conjI goal=36
  ← intro goal=18
    #[37] m ≤ n ∧ n ≤ m
    #[38] (p = 1 ∨ m ≤ n) ∧ (p = 1 ∨ n ≤ m)
  → forward conjD1 goal=38 facts=[37]
    #[39] m ≤ n
  → forward conjD2 goal=38 facts=[37]
    #[40] n ≤ m
  ← rule conjI goal=38
    #[41] p = 1 ∨ m ≤ n
    #[42] p = 1 ∨ n ≤ m
  ← rule disjI2 goal=41
  ← rule disjI2 goal=42
qed

theorem coprime_2
  fixes n :: nat
  prop coprime 2 n <--> odd n
  [hint_rewrite]
proof
  → forward prime_2 goal=0
    #[1] prime 2
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=0 facts=[1]
    #[2] ∀n. coprime 2 n ⟷ ¬(divides 2 n)
  ← rewrite source=prev goal=0 facts=[2]
    #[3] ¬(divides 2 n) ⟷ odd n
  ← rewrite not_even sym=true goal=3
    #[4] ¬(divides 2 n) ⟷ ¬(even n)
  ← rewrite divides_2 sym=false goal=4
qed

theorem divides_exp_plus1
  fixes k :: nat, n :: nat
  prop odd k --> divides (n + 1) (n ^ k + 1)
proof
  ← intro goal=0
    #[1] odd k
    #[2] divides (n + 1) (n ^ k + 1)
  → rewrite target=fact odd_exists sym=false goal=2 facts=[1]
    #[3] ∃m. k = Suc (2 * m)
  elim m goal=2 facts=[3]
    #[4] _VAR m
    #[5] k = Suc (2 * m)
    #[6] divides (n + 1) (n ^ k + 1)
  ← rewrite source=prev goal=6 facts=[5]
    #[7] divides (n + 1) (n ^ Suc (2 * m) + 1)
  ← induct m nat_induct goal=7
    #[8] divides (n + 1) (n ^ Suc (2 * 0) + 1)
    #[9] ∀n1. divides (n + 1) (n ^ Suc (2 * n1) + 1) ⟶ divides (n + 1) (n ^ Suc (2 * Suc n1) + 1)
  ← rewrite mult_0_right sym=false goal=8
    #[10] divides (n + 1) (n ^ Suc 0 + 1)
  ← rewrite nat_nat_power_def_2 sym=false goal=10
    #[11] divides (n + 1) (n * n ^ (0::nat) + 1)
  ← rewrite nat_nat_power_def_1 sym=false goal=11
    #[12] divides (n + 1) (n * 1 + 1)
  ← rewrite mult_1_right sym=false goal=12
    #[13] divides (n + 1) (n + 1)
  ← rule divides_self goal=13
    #[14] divides (n + 1) (n + 1)
  ← intro m1 goal=9
    #[15] _VAR m1
    #[16] divides (n + 1) (n ^ Suc (2 * m1) + 1)
    #[17] divides (n + 1) (n ^ Suc (2 * Suc m1) + 1)
  cut "Suc (2 * Suc m1) = Suc (2 * m1) + 2" goal=17
    #[18] Suc (2 * Suc m1) = Suc (2 * m1) + 2
  nat_norm goal=18
    #[19] Suc (2 * Suc m1) = Suc (2 * m1) + 2
  ← rewrite source=prev goal=17 facts=[19]
    #[20] divides (n + 1) (n ^ (Suc (2 * m1) + 2) + 1)
  ← rewrite nat_power_add sym=false goal=20
    #[21] divides (n + 1) (n ^ Suc (2 * m1) * n ^ (2::nat) + 1)
  ← rewrite exp_2 sym=false goal=21
    #[22] divides (n + 1) (n ^ Suc (2 * m1) * (n * n) + 1)
  → rewrite target=fact divides_def sym=false goal=22 facts=[16]
    #[23] ∃k. n ^ Suc (2 * m1) + 1 = (n + 1) * k
  elim d goal=22 facts=[23]
    #[24] _VAR d
    #[25] n ^ Suc (2 * m1) + 1 = (n + 1) * d
    #[26] divides (n + 1) (n ^ Suc (2 * m1) * (n * n) + 1)
    #[27] divides (n + 1) (n ^ Suc (2 * m1) * n ^ (2::nat) + 1)
    #[28] divides (n + 1) (n ^ (Suc (2 * m1) + 2) + 1)
    #[29] divides (n + 1) (n ^ Suc (2 * Suc m1) + 1)
  cut "1 <= n ^ Suc (2 * m1) + 1" goal=26 facts=[25]
    #[30] 1 ≤ n ^ Suc (2 * m1) + 1
  ← rewrite add_comm sym=false goal=30
    #[31] 1 ≤ 1 + n ^ Suc (2 * m1)
  ← rule le_add goal=31
    #[32] 1 ≤ 1 + n ^ Suc (2 * m1)
  cut "n ^ Suc (2 * m1) = (n + 1) * d - 1" goal=26
    #[33] n ^ Suc (2 * m1) = (n + 1) * d - 1
  → rewrite target=fact eq_sym_eq sym=false goal=33 facts=[25]
    #[34] (n + 1) * d = n ^ Suc (2 * m1) + 1
  ← rewrite source=prev goal=33 facts=[34]
    #[35] n ^ Suc (2 * m1) = n ^ Suc (2 * m1) + 1 - 1
  ← rewrite eq_sym_eq sym=false goal=35
    #[36] n ^ Suc (2 * m1) + 1 - 1 = n ^ Suc (2 * m1)
  ← rule nat_plus_minus goal=36
    #[37] n ^ Suc (2 * m1) + 1 - 1 = n ^ Suc (2 * m1)
  ← rewrite source=prev goal=26 facts=[33]
    #[38] divides (n + 1) (((n + 1) * d - 1) * (n * n) + 1)
  ← rewrite mult_comm sym=false goal=38
    #[39] divides (n + 1) (n * n * ((n + 1) * d - 1) + 1)
  ← rewrite left_sub_distrib sym=false goal=39
    #[40] divides (n + 1) (n * n * ((n + 1) * d) - n * n * 1 + 1)
  cut "∀x::nat. ∀y. ∀z. z ≤ y --> y <= x ⟶ x - y + z = x - (y - z)" goal=40
    #[41] ∀x::nat. ∀y. ∀z. z ≤ y ⟶ y ≤ x ⟶ x - y + z = x - (y - z)
  ← z3 goal=41
    #[42] ∀x::nat. ∀y. ∀z. z ≤ y ⟶ y ≤ x ⟶ x - y + z = x - (y - z)
  ← rewrite mult_1_right sym=false goal=40
    #[43] divides (n + 1) (n * n * ((n + 1) * d) - n * n + 1)
  ← cases "n=0" goal=43
    #[44] n = 0 ⟶ divides (n + 1) (n * n * ((n + 1) * d) - n * n + 1)
    #[45] ¬(n = 0) ⟶ divides (n + 1) (n * n * ((n + 1) * d) - n * n + 1)
  ← intro goal=44
    #[46] n = 0
    #[47] divides (n + 1) (n * n * ((n + 1) * d) - n * n + 1)
  ← rewrite source=prev goal=47 facts=[46]
    #[48] divides (0 + 1) (0 * 0 * ((0 + 1) * d) - 0 * 0 + 1)
  ← rewrite nat_plus_def_1 sym=false goal=48
    #[49] divides 1 (0 * 0 * (1 * d) - 0 * 0 + 1)
  ← rule divides_1 goal=49
    #[50] divides 1 (0 * 0 * (1 * d) - 0 * 0 + 1)
  ← intro goal=45
    #[51] ¬(n = 0)
    #[52] divides (n + 1) (n * n * ((n + 1) * d) - n * n + 1)
  cut "1 <= n*n" goal=52
    #[53] 1 ≤ n * n
  ← z3 goal=53 facts=[51]
    #[54] 1 ≤ n * n
  cut "n * n <= n * n * ((n + 1) * d)" goal=52
    #[55] n * n ≤ n * n * ((n + 1) * d)
  cut "n * n = n * n * 1" goal=55
    #[56] n * n = n * n * 1
  ← rewrite mult_1_right sym=false goal=56
    #[57] n * n = n * n * 1
  ← rewrite source=prev goal=55 facts=[57]
    #[58] n * n * 1 ≤ n * n * 1 * ((n + 1) * d)
  ← rewrite mult_assoc sym=false goal=58
    #[59] n * (n * 1) ≤ n * n * (1 * ((n + 1) * d))
  ← rewrite mult_1_left sym=false goal=59
    #[60] n * (n * 1) ≤ n * n * ((n + 1) * d)
  cut "n * (n * 1) = n * n * 1" goal=60
    #[61] n * (n * 1) = n * n * 1
  ← rewrite mult_assoc sym=false goal=61
    #[62] n * (n * 1) = n * n * 1
  ← rewrite source=prev goal=60 facts=[62]
    #[63] n * n * 1 ≤ n * n * ((n + 1) * d)
  ← rule le_mult2 goal=63
    #[64] n * n ≤ n * n
    #[65] 1 ≤ (n + 1) * d
  ← rule less_eq_refl goal=64
    #[66] n * n ≤ n * n
  → rewrite target=fact source=prev goal=65 facts=[25,30]
    #[67] 1 ≤ (n + 1) * d
  → forward goal=52 facts=[42,54,55]
    #[68] n * n * ((n + 1) * d) - n * n + 1 = n * n * ((n + 1) * d) - (n * n - 1)
  ← rewrite source=prev goal=52 facts=[68]
    #[69] divides (n + 1) (n * n * ((n + 1) * d) - (n * n - 1))
  ← rule divides_sub goal=69
    #[70] divides (n + 1) (n * n * ((n + 1) * d))
    #[71] divides (n + 1) (n * n - 1)
  ← rule divides_lmul goal=70
    #[72] divides (n + 1) ((n + 1) * d)
  ← rule divides_rmul goal=72
  cut "(n * n - 1) = (n+1)*(n-1)" goal=71
    #[73] n * n - 1 = (n + 1) * (n - 1)
  ← z3 goal=73 facts=[51]
    #[74] n * n - 1 = (n + 1) * (n - 1)
  ← rewrite source=prev goal=71 facts=[74]
    #[75] divides (n + 1) ((n + 1) * (n - 1))
  ← rule divides_rmul goal=75
qed

theorem divides_exp_minus1
  fixes k :: nat, n :: nat
  prop divides (n - 1) (n ^ k - 1)
  [hint_backward]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ divides (n - 1) (n ^ k - 1)
    #[2] ¬(n = 0) ⟶ divides (n - 1) (n ^ k - 1)
  ← intro goal=1
    #[3] n = 0
    #[4] divides (n - 1) (n ^ k - 1)
  ← rewrite source=prev goal=4 facts=[3]
    #[5] divides (0 - 1) (0 ^ k - 1)
  ← rewrite exp_zero sym=false goal=5
    #[6] divides (0 - 1) ((if k = 0 then 1 else 0) - 1)
  ← cases "k = 0" goal=6
    #[7] k = 0 ⟶ divides (0 - 1) ((if k = 0 then 1 else 0) - 1)
    #[8] ¬(k = 0) ⟶ divides (0 - 1) ((if k = 0 then 1 else 0) - 1)
  ← intro goal=7
    #[9] k = 0
    #[10] divides (0 - 1) ((if k = 0 then 1 else 0) - 1)
  ← rewrite if_P sym=false goal=10 facts=[9]
    #[11] divides (0 - 1) (1 - 1)
  ← rewrite nat_minus_l0 sym=false goal=11
    #[12] divides 0 (1 - 1)
  ← rewrite divides_0 sym=false goal=12
    #[13] (1::nat) - 1 = 0
  ← rewrite sub_eq_0 sym=false goal=13
    #[14] (1::nat) ≤ 1
  ← rule less_eq_refl goal=14
    #[15] (1::nat) ≤ 1
  ← intro goal=8
    #[16] ¬(k = 0)
    #[17] divides (0 - 1) ((if k = 0 then 1 else 0) - 1)
  ← rewrite if_not_P sym=false goal=17 facts=[16]
    #[18] divides (0 - 1) (0 - 1)
  ← rule divides_self goal=18
    #[19] divides (0 - 1) (0 - 1)
  ← intro goal=2
    #[20] ¬(n = 0)
    #[21] divides (n - 1) (n ^ k - 1)
  ← induct k nat_induct goal=21
    #[22] divides (n - 1) (n ^ (0::nat) - 1)
    #[23] ∀n1. divides (n - 1) (n ^ n1 - 1) ⟶ divides (n - 1) (n ^ Suc n1 - 1)
  ← rewrite nat_nat_power_def_1 sym=false goal=22
    #[24] divides (n - 1) (1 - 1)
  ← rewrite nat_minus_refl sym=false goal=24
    #[25] divides (n - 1) 0
  ← rule divides_zero goal=25
    #[26] divides (n - 1) 0
  ← intro k goal=23
    #[27] _VAR k
    #[28] divides (n - 1) (n ^ k - 1)
    #[29] divides (n - 1) (n ^ Suc k - 1)
  ← rewrite nat_nat_power_def_2 sym=false goal=29
    #[30] divides (n - 1) (n * n ^ k - 1)
  cut "n * n ^ k = n * n ^ k - n + n" goal=30
    #[31] n * n ^ k = n * n ^ k - n + n
  ← rewrite eq_sym_eq sym=false goal=31
    #[32] n * n ^ k - n + n = n * n ^ k
  ← rule sub_add goal=32
    #[33] n ≤ n * n ^ k
  cut "n = n * 1" goal=33
    #[34] n = n * 1
  ← rewrite mult_1_right sym=false goal=34
    #[35] n = n * 1
  ← rewrite source=prev goal=33 facts=[35]
    #[36] n * 1 ≤ n * 1 * (n * 1) ^ k
  ← rewrite mult_assoc sym=false goal=36
    #[37] n * 1 ≤ n * (1 * (n * 1) ^ k)
  ← rewrite mult_1_left sym=false goal=37
    #[38] n * 1 ≤ n * (n * 1) ^ k
  ← rule le_mult2 goal=38
    #[39] n ≤ n
    #[40] 1 ≤ (n * 1) ^ k
  ← rule less_eq_refl goal=39
    #[41] n ≤ n
  ← rewrite mult_1_right sym=false goal=40
    #[42] 1 ≤ n ^ k
  ← rule le_1_2 goal=42
    #[43] ¬(n ^ k = 0)
  ← rewrite exp_eq_0 sym=false goal=43
    #[44] ¬(n = 0 ∧ ¬(k = 0))
  ← rewrite de_morgan_thm1 sym=false goal=44
    #[45] ¬(n = 0) ∨ ¬¬(k = 0)
  ← rule disjI1 goal=45
  ← rewrite source=prev goal=30 facts=[31]
    #[46] divides (n - 1) (n * n ^ k - n + n - 1)
  cut "n * n ^ k - n = n * (n ^ k - 1)" goal=46
    #[47] n * n ^ k - n = n * (n ^ k - 1)
  ← rewrite left_sub_distrib sym=false goal=47
    #[48] n * n ^ k - n = n * n ^ k - n * 1
  ← rewrite mult_1_right sym=false goal=48
    #[49] n * n ^ k - n = n * n ^ k - n * 1
  ← rewrite source=prev goal=46 facts=[47]
    #[50] divides (n - 1) (n * (n ^ k - 1) + n - 1)
  cut "(n * (n ^ k - 1) + n - 1) = n * (n ^ k - 1) + (n - 1)" goal=50
    #[51] n * (n ^ k - 1) + n - 1 = n * (n ^ k - 1) + (n - 1)
  cut "!x. x + n - 1 = x + (n - 1)  " goal=51
    #[52] ∀x. x + n - 1 = x + (n - 1)
  ← intro x goal=52
    #[53] _VAR x
    #[54] x + n - 1 = x + (n - 1)
  ← z3 goal=54 facts=[20]
    #[55] x + n - 1 = x + (n - 1)
  ← apply_prev goal=51 facts=[52]
  ← rewrite source=prev goal=50 facts=[51]
    #[56] divides (n - 1) (n * (n ^ k - 1) + (n - 1))
  ← rule divides_sum goal=56
    #[57] divides (n - 1) (n * (n ^ k - 1))
    #[58] divides (n - 1) (n - 1)
  ← rule divides_lmul goal=57
  ← rule divides_self goal=58
    #[59] divides (n - 1) (n - 1)
qed

theorem prime_irreducible
  fixes p :: nat
  prop prime p <--> p > 1 & (!a. !b. divides p (a * b) --> divides p a | divides p b)
  [hint_rewrite]
proof
  ← rewrite zero_one_or_prime sym=true goal=0
    #[1] prime p ⟷ p > 1 ∧ (p = 0 ∨ p = 1 ∨ prime p)
  cut "p > 1 <--> ~(p=0) & ~(p=1)" goal=1
    #[2] p > 1 ⟷ ¬(p = 0) ∧ ¬(p = 1)
  ← z3 goal=2
  ← rewrite source=prev goal=1 facts=[2]
    #[3] prime p ⟷ (¬(p = 0) ∧ ¬(p = 1)) ∧ (p = 0 ∨ p = 1 ∨ prime p)
  ← rule iffI goal=3
    #[4] prime p ⟶ (¬(p = 0) ∧ ¬(p = 1)) ∧ (p = 0 ∨ p = 1 ∨ prime p)
    #[5] (¬(p = 0) ∧ ¬(p = 1)) ∧ (p = 0 ∨ p = 1 ∨ prime p) ⟶ prime p
  ← intro goal=4
    #[6] prime p
    #[7] (¬(p = 0) ∧ ¬(p = 1)) ∧ (p = 0 ∨ p = 1 ∨ prime p)
  ← rule conjI goal=7
    #[8] ¬(p = 0) ∧ ¬(p = 1)
    #[9] p = 0 ∨ p = 1 ∨ prime p
  ← rule conjI goal=8
    #[10] ¬(p = 0)
    #[11] ¬(p = 1)
  ← rule prime_imp_nz goal=10 facts=[6]
  → rewrite target=fact prime_def sym=false goal=11 facts=[6]
    #[12] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  ← rule conjD1 goal=11 facts=[12]
  ← rule disjI2 goal=9
    #[13] p = 1 ∨ prime p
  ← rule disjI2 goal=13
  ← intro goal=5
    #[14] (¬(p = 0) ∧ ¬(p = 1)) ∧ (p = 0 ∨ p = 1 ∨ prime p)
    #[15] prime p
  → forward conjD1 goal=15 facts=[14]
    #[16] ¬(p = 0) ∧ ¬(p = 1)
  → forward conjD1 goal=15 facts=[16]
    #[17] ¬(p = 0)
  → forward conjD2 goal=15 facts=[16]
    #[18] ¬(p = 1)
  → forward conjD2 goal=15 facts=[14]
    #[19] p = 0 ∨ p = 1 ∨ prime p
  ← rule disjE goal=15 facts=[19]
    #[20] p = 0 ⟶ prime p
    #[21] p = 1 ∨ prime p ⟶ prime p
  ← intro goal=20
    #[22] p = 0
    #[23] prime p
  ← rule negE_gen goal=23 facts=[17,22]
  ← intro goal=21
    #[24] p = 1 ∨ prime p
    #[25] prime p
  ← rule disjE goal=25 facts=[24]
    #[26] p = 1 ⟶ prime p
    #[27] prime p ⟶ prime p
  ← intro goal=26
    #[28] p = 1
    #[29] prime p
  ← rule negE_gen goal=29 facts=[18,28]
    #[30] prime p
qed

theorem coprime_exp_divprod
  fixes a :: nat, b :: nat, d :: nat, n :: nat
  prop divides (d ^ n) (a * b) --> coprime d a --> divides (d ^ n) b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] divides (d ^ n) (a * b)
    #[2] coprime d a
    #[3] divides (d ^ n) b
  ← rule coprime_divprod goal=3 facts=[1]
    #[4] coprime (d ^ n) a
  ← rewrite coprime_lexp sym=false goal=4
    #[5] coprime d a ∨ n = 0
  ← rule disjI1 goal=5
qed

theorem prime_coprime_cases
  fixes a :: nat, b :: nat, p :: nat
  prop prime p --> coprime a b --> coprime p a | coprime p b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] coprime a b
    #[3] coprime p a ∨ coprime p b
  → forward coprime_prime param_p=p goal=3 facts=[2]
    #[4] ¬(prime p ∧ divides p a ∧ divides p b)
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=3 facts=[1]
    #[5] ∀n. coprime p n ⟷ ¬(divides p n)
  ← rewrite source=prev goal=3 facts=[5]
    #[6] ¬(divides p a) ∨ ¬(divides p b)
  → rewrite target=fact de_morgan_thm1 sym=false goal=6 facts=[4]
    #[7] ¬(prime p) ∨ ¬(divides p a ∧ divides p b)
  ← rule disjE goal=6 facts=[7]
    #[8] ¬(prime p) ⟶ ¬(divides p a) ∨ ¬(divides p b)
    #[9] ¬(divides p a ∧ divides p b) ⟶ ¬(divides p a) ∨ ¬(divides p b)
  ← intro goal=8
    #[10] ¬(prime p)
    #[11] ¬(divides p a) ∨ ¬(divides p b)
  ← rule negE_gen goal=11 facts=[10,1]
    #[12] ¬(divides p a) ∨ ¬(divides p b)
  ← intro goal=9
    #[13] ¬(divides p a ∧ divides p b)
    #[14] ¬(divides p a) ∨ ¬(divides p b)
  → rewrite target=fact de_morgan_thm1 sym=false goal=14 facts=[13]
    #[15] ¬(divides p a) ∨ ¬(divides p b)
qed

theorem prime_divprod_pow_gen
  fixes a :: nat, b :: nat, n :: nat, p :: nat
  prop prime p --> ~(divides p (gcd a b)) --> divides (p ^ n) (a * b) --> divides (p ^ n) a | divides (p ^ n) b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(divides p (gcd a b))
    #[3] divides (p ^ n) (a * b)
    #[4] divides (p ^ n) a ∨ divides (p ^ n) b
  → rewrite target=fact prime_coprime_eq_nondivisiable sym=false goal=4 facts=[1]
    #[5] ∀n. coprime p n ⟷ ¬(divides p n)
  → rewrite target=fact divides_gcd sym=false goal=4 facts=[2]
    #[6] ¬(divides p a ∧ divides p b)
  → rewrite target=fact eq_sym_eq sym=false goal=4 facts=[5]
    #[7] ∀n. ¬(divides p n) ⟷ coprime p n
  → rewrite target=fact de_morgan_thm1 sym=false goal=4 facts=[6]
    #[8] ¬(divides p a) ∨ ¬(divides p b)
  → rewrite target=fact source=prev goal=4 facts=[7,8]
    #[9] coprime p a ∨ coprime p b
  ← rule disjE goal=4 facts=[9]
    #[10] coprime p a ⟶ divides (p ^ n) a ∨ divides (p ^ n) b
    #[11] coprime p b ⟶ divides (p ^ n) a ∨ divides (p ^ n) b
  ← intro goal=10
    #[12] coprime p a
    #[13] divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule disjI2 goal=13
    #[14] divides (p ^ n) b
  ← rule coprime_divprod param_a=a goal=14
    #[15] coprime (p ^ n) a
  ← rewrite coprime_lexp sym=false goal=15
    #[16] coprime p a ∨ n = 0
  ← rule disjI1 goal=16
  ← intro goal=11
    #[17] coprime p b
    #[18] divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule disjI1 goal=18
    #[19] divides (p ^ n) a
  ← rule coprime_divprod param_a=b goal=19
    #[20] divides (p ^ n) (b * a)
    #[21] coprime (p ^ n) b
  ← rewrite mult_comm sym=false goal=20
  ← rewrite coprime_lexp sym=false goal=21
    #[22] coprime p b ∨ n = 0
  ← rule disjI1 goal=22
qed

theorem prime_divprod_pow_gen_eq
  fixes a :: nat, b :: nat, n :: nat, p :: nat
  prop prime p --> ~(divides p (gcd a b)) --> divides (p ^ n) (a * b) <--> divides (p ^ n) a | divides (p ^ n) b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(divides p (gcd a b))
    #[3] divides (p ^ n) (a * b) ⟷ divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule iffI goal=3
    #[4] divides (p ^ n) (a * b) ⟶ divides (p ^ n) a ∨ divides (p ^ n) b
    #[5] divides (p ^ n) a ∨ divides (p ^ n) b ⟶ divides (p ^ n) (a * b)
  ← intro goal=4
    #[6] divides (p ^ n) (a * b)
    #[7] divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule prime_divprod_pow_gen goal=7
  ← intro goal=5
    #[8] divides (p ^ n) a ∨ divides (p ^ n) b
    #[9] divides (p ^ n) (a * b)
  ← rule disjE goal=9 facts=[8]
    #[10] divides (p ^ n) a ⟶ divides (p ^ n) (a * b)
    #[11] divides (p ^ n) b ⟶ divides (p ^ n) (a * b)
  ← intro goal=10
    #[12] divides (p ^ n) a
    #[13] divides (p ^ n) (a * b)
  ← rule divides_rmul goal=13
  ← intro goal=11
    #[14] divides (p ^ n) b
    #[15] divides (p ^ n) (a * b)
  ← rule divides_lmul goal=15
qed

theorem prime_divprod_pow
  fixes a :: nat, b :: nat, n :: nat, p :: nat
  prop prime p --> coprime a b --> divides (p ^ n) (a * b) --> divides (p ^ n) a | divides (p ^ n) b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] coprime a b
    #[3] divides (p ^ n) (a * b)
    #[4] divides (p ^ n) a ∨ divides (p ^ n) b
  → forward prime_coprime_cases goal=4 facts=[1,2]
    #[5] coprime p a ∨ coprime p b
  ← rule disjE goal=4 facts=[5]
    #[6] coprime p a ⟶ divides (p ^ n) a ∨ divides (p ^ n) b
    #[7] coprime p b ⟶ divides (p ^ n) a ∨ divides (p ^ n) b
  ← intro goal=6
    #[8] coprime p a
    #[9] divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule disjI2 goal=9
    #[10] divides (p ^ n) b
  ← rule coprime_exp_divprod goal=10 facts=[3,8]
    #[11] divides (p ^ n) b
  ← intro goal=7
    #[12] coprime p b
    #[13] divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule disjI1 goal=13
    #[14] divides (p ^ n) a
  → rewrite target=fact mult_comm sym=false goal=14 facts=[3]
    #[15] divides (p ^ n) (b * a)
  ← rule coprime_exp_divprod goal=14 facts=[15,12]
    #[16] divides (p ^ n) a
qed

theorem prime_divprod_pow_eq
  fixes a :: nat, b :: nat, n :: nat, p :: nat
  prop prime p --> coprime a b --> divides (p ^ n) (a * b) <--> divides (p ^ n) a | divides (p ^ n) b
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] coprime a b
    #[3] divides (p ^ n) (a * b) ⟷ divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule iffI goal=3
    #[4] divides (p ^ n) (a * b) ⟶ divides (p ^ n) a ∨ divides (p ^ n) b
    #[5] divides (p ^ n) a ∨ divides (p ^ n) b ⟶ divides (p ^ n) (a * b)
  ← intro goal=4
    #[6] divides (p ^ n) (a * b)
    #[7] divides (p ^ n) a ∨ divides (p ^ n) b
  ← rule prime_divprod_pow goal=7 facts=[1,2,6]
  ← intro goal=5
    #[8] divides (p ^ n) a ∨ divides (p ^ n) b
    #[9] divides (p ^ n) (a * b)
  ← rule disjE goal=9 facts=[8]
    #[10] divides (p ^ n) a ⟶ divides (p ^ n) (a * b)
    #[11] divides (p ^ n) b ⟶ divides (p ^ n) (a * b)
  ← intro goal=10
    #[12] divides (p ^ n) a
    #[13] divides (p ^ n) (a * b)
  ← rule divides_rmul goal=13
  ← intro goal=11
    #[14] divides (p ^ n) b
    #[15] divides (p ^ n) (a * b)
  ← rule divides_lmul goal=15
qed

theorem prime_factor_partition
  fixes Q :: nat => bool, n :: nat
  prop ~(n = 0) --> (?n1. ?n2. n1 * n2 = n & (!p. prime p --> divides p n1 --> Q p) & (!p. prime p --> divides p n2 --> ~(Q p)))
  [hint_backward]
proof
  ← intro goal=0
    #[1] ¬(n = 0)
    #[2] ∃n1. ∃n2. n1 * n2 = n ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
  cut "!n. ¬(n = 0) \n⟶ (∃n1. ∃n2. n1 * n2 = n ∧ \n        (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p)))" goal=2
    #[3] ∀n. ¬(n = 0) ⟶ (∃n1. ∃n2. n1 * n2 = n ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p)))
  ← rule prime_factor_induct goal=3
    #[4] ¬((0::nat) = 0) ⟶ (∃n1. ∃n2. n1 * n2 = 0 ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p)))
    #[5] ¬((1::nat) = 0) ⟶ (∃n1. ∃n2. n1 * n2 = 1 ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p)))
    #[6] ∀p. ∀n. prime p ⟶ ¬(n = 0) ⟶ (¬(n = 0) ⟶ (∃n1. ∃n2. n1 * n2 = n ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1)))) ⟶ ¬(p * n = 0) ⟶ (∃n1. ∃n2. n1 * n2 = p * n ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1)))
  ← intro goal=4
    #[7] ¬((0::nat) = 0)
    #[8] ∃n1. ∃n2. n1 * n2 = 0 ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
  ← rule negE_gen goal=8 facts=[7]
    #[9] (0::nat) = 0
  ← rule eq_refl goal=9
    #[10] (0::nat) = 0
  ← intro goal=5
    #[11] ¬((1::nat) = 0)
    #[12] ∃n1. ∃n2. n1 * n2 = 1 ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
  ← inst "(1::nat)" goal=12
    #[13] ∃n2. 1 * n2 = 1 ∧ (∀p. prime p ⟶ divides p 1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
  ← inst "(1::nat)" goal=13
    #[14] (1::nat) * 1 = 1 ∧ (∀p. prime p ⟶ divides p 1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p 1 ⟶ ¬(Q p))
  ← rule conjI goal=14
    #[15] (1::nat) * 1 = 1
    #[16] (∀p. prime p ⟶ divides p 1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p 1 ⟶ ¬(Q p))
  ← rewrite mult_1_left sym=false goal=15
    #[17] (1::nat) * 1 = 1
  ← rule conjI goal=16
    #[18] ∀p. prime p ⟶ divides p 1 ⟶ Q p
    #[19] ∀p. prime p ⟶ divides p 1 ⟶ ¬(Q p)
  ← intro p goal=18
    #[20] _VAR p
    #[21] prime p
    #[22] divides p 1
    #[23] Q p
  → rewrite target=fact divides_one sym=false goal=23 facts=[22]
    #[24] p = 1
  → rewrite target=fact source=prev goal=23 facts=[24,21]
    #[25] prime 1
  ← rule contrapositive goal=23 facts=[25]
    #[26] ¬(Q p) ⟶ ¬(prime 1)
  ← intro goal=26
    #[27] ¬(Q p)
    #[28] ¬(prime 1)
  ← rule prime_1 goal=28
    #[29] ¬(prime 1)
  ← intro p goal=19
    #[30] ¬(Q p)
  → rewrite target=fact divides_one sym=false goal=30 facts=[22]
  → rewrite target=fact source=prev goal=30 facts=[24,21]
  ← rule contrapositive goal=30 facts=[25]
    #[31] ¬¬(Q p) ⟶ ¬(prime 1)
  ← intro goal=31
    #[32] ¬¬(Q p)
    #[33] ¬(prime 1)
  ← rule prime_1 goal=33
  ← intro p,m goal=6
    #[34] _VAR m
    #[35] ¬(m = 0)
    #[36] ¬(m = 0) ⟶ (∃n1. ∃n2. n1 * n2 = m ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p)))
    #[37] ¬(p * m = 0)
    #[38] ∃n1. ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  → forward goal=38 facts=[36,35]
    #[39] ∃n1. ∃n2. n1 * n2 = m ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
  elim n1,n2 goal=38 facts=[39]
    #[40] _VAR n1
    #[41] _VAR n2
    #[42] n1 * n2 = m ∧ (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
    #[43] ∃n1. ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  → forward conjD1 goal=43 facts=[42]
    #[44] n1 * n2 = m
  → forward conjD2 goal=43 facts=[42]
    #[45] (∀p. prime p ⟶ divides p n1 ⟶ Q p) ∧ (∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p))
  → forward conjD1 goal=43 facts=[45]
    #[46] ∀p. prime p ⟶ divides p n1 ⟶ Q p
  → forward conjD2 goal=43 facts=[45]
    #[47] ∀p. prime p ⟶ divides p n2 ⟶ ¬(Q p)
  ← cases "Q p" goal=43
    #[48] Q p ⟶ (∃n1. ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1)))
    #[49] ¬(Q p) ⟶ (∃n1. ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1)))
  ← intro goal=48
    #[50] Q p
    #[51] ∃n1. ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  ← inst "p * n1" goal=51
    #[52] ∃n2. p * n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 (p * n1) ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  ← inst n2 goal=52
    #[53] p * n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 (p * n1) ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  ← rule conjI goal=53
    #[54] p * n1 * n2 = p * m
    #[55] (∀p1. prime p1 ⟶ divides p1 (p * n1) ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  ← rewrite mult_assoc sym=false goal=54
    #[56] p * (n1 * n2) = p * m
  ← rewrite source=prev goal=56 facts=[44]
    #[57] p * (n1 * n2) = p * m
  ← rule conjI goal=55
    #[58] ∀p1. prime p1 ⟶ divides p1 (p * n1) ⟶ Q p1
  ← intro p1 goal=58
    #[59] _VAR p1
    #[60] prime p1
    #[61] divides p1 (p * n1)
    #[62] Q p1
  → forward prime_divprod_eq param_a=p param_b=n1 goal=62 facts=[60]
    #[63] divides p1 (p * n1) ⟷ divides p1 p ∨ divides p1 n1
  → rewrite target=fact source=prev goal=62 facts=[63,61]
    #[64] divides p1 p ∨ divides p1 n1
  ← rule disjE goal=62 facts=[64]
    #[65] divides p1 p ⟶ Q p1
    #[66] divides p1 n1 ⟶ Q p1
  ← intro goal=65
    #[67] divides p1 p
    #[68] Q p1
  → forward divides_prime_prime goal=68 facts=[60,21]
    #[69] divides p1 p ⟷ p1 = p
  → rewrite target=fact source=prev goal=68 facts=[69,67]
    #[70] p1 = p
  ← rewrite source=prev goal=68 facts=[70]
  ← intro goal=66
    #[71] divides p1 n1
    #[72] Q p1
  ← apply_prev goal=72 facts=[46]
  ← intro goal=49
    #[73] ∃n1. ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  ← inst n1 goal=73
    #[74] ∃n2. n1 * n2 = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 n2 ⟶ ¬(Q p1))
  ← inst p*n2 goal=74
    #[75] n1 * (p * n2) = p * m ∧ (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 (p * n2) ⟶ ¬(Q p1))
  ← rule conjI goal=75
    #[76] n1 * (p * n2) = p * m
    #[77] (∀p1. prime p1 ⟶ divides p1 n1 ⟶ Q p1) ∧ (∀p1. prime p1 ⟶ divides p1 (p * n2) ⟶ ¬(Q p1))
  → rewrite target=fact eq_sym_eq sym=false goal=76 facts=[44]
    #[78] m = n1 * n2
  ← rewrite source=prev goal=76 facts=[78]
    #[79] n1 * (p * n2) = p * (n1 * n2)
  nat_norm goal=79
    #[80] n1 * (p * n2) = p * (n1 * n2)
  ← rule conjI goal=77
    #[81] ∀p1. prime p1 ⟶ divides p1 (p * n2) ⟶ ¬(Q p1)
  ← intro p1 goal=81
    #[82] divides p1 (p * n2)
    #[83] ¬(Q p1)
  → forward prime_divprod_eq param_a=p param_b=n2 goal=83 facts=[60]
    #[84] divides p1 (p * n2) ⟷ divides p1 p ∨ divides p1 n2
  → rewrite target=fact source=prev goal=83 facts=[84,82]
    #[85] divides p1 p ∨ divides p1 n2
  ← rule disjE goal=83 facts=[85]
    #[86] divides p1 p ⟶ ¬(Q p1)
    #[87] divides p1 n2 ⟶ ¬(Q p1)
  ← intro goal=86
    #[88] ¬(Q p1)
  → forward divides_prime_prime goal=88 facts=[60,21]
  → rewrite target=fact source=prev goal=88 facts=[69,67]
  ← rewrite source=prev goal=88 facts=[70]
  ← intro goal=87
    #[89] divides p1 n2
    #[90] ¬(Q p1)
  ← apply_prev goal=90 facts=[47]
  ← apply_prev goal=2 facts=[3]
qed

theorem coprime_pair_decomp
  fixes m :: nat, n1 :: nat, n2 :: nat
  prop coprime n1 n2 --> ~(m = 0) --> (?m1. ?m2. coprime m1 n1 & coprime m2 n2 & coprime m1 m2 & m1 * m2 = m)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] coprime n1 n2
    #[2] ¬(m = 0)
    #[3] ∃m1. ∃m2. coprime m1 n1 ∧ coprime m2 n2 ∧ coprime m1 m2 ∧ m1 * m2 = m
  → forward prime_factor_partition param_Q="(%p. divides p n2)" param_n=m goal=3
    #[4] ¬(m = 0) ⟶ (∃n1. ∃n21. n1 * n21 = m ∧ (∀p. prime p ⟶ divides p n1 ⟶ divides p n2) ∧ (∀p. prime p ⟶ divides p n21 ⟶ ¬(divides p n2)))
  → forward goal=3 facts=[4,2]
    #[5] ∃n1. ∃n21. n1 * n21 = m ∧ (∀p. prime p ⟶ divides p n1 ⟶ divides p n2) ∧ (∀p. prime p ⟶ divides p n21 ⟶ ¬(divides p n2))
  elim m1,m2 goal=3 facts=[5]
    #[6] _VAR m1
    #[7] _VAR m2
    #[8] m1 * m2 = m ∧ (∀p. prime p ⟶ divides p m1 ⟶ divides p n2) ∧ (∀p. prime p ⟶ divides p m2 ⟶ ¬(divides p n2))
    #[9] ∃m1. ∃m2. coprime m1 n1 ∧ coprime m2 n2 ∧ coprime m1 m2 ∧ m1 * m2 = m
  ← inst m1 goal=9
    #[10] ∃m2. coprime m1 n1 ∧ coprime m2 n2 ∧ coprime m1 m2 ∧ m1 * m2 = m
  ← inst m2 goal=10
    #[11] coprime m1 n1 ∧ coprime m2 n2 ∧ coprime m1 m2 ∧ m1 * m2 = m
  → forward conjD1 goal=11 facts=[8]
    #[12] m1 * m2 = m
  → forward conjD2 goal=11 facts=[8]
    #[13] (∀p. prime p ⟶ divides p m1 ⟶ divides p n2) ∧ (∀p. prime p ⟶ divides p m2 ⟶ ¬(divides p n2))
  → forward conjD1 goal=11 facts=[13]
    #[14] ∀p. prime p ⟶ divides p m1 ⟶ divides p n2
  → forward conjD2 goal=11 facts=[13]
    #[15] ∀p. prime p ⟶ divides p m2 ⟶ ¬(divides p n2)
  → rewrite target=fact coprime_prime_eq sym=false goal=11 facts=[1]
    #[16] ∀p. ¬(prime p ∧ divides p n1 ∧ divides p n2)
  ← rewrite coprime_prime_eq sym=false goal=11
    #[17] (∀p. ¬(prime p ∧ divides p m1 ∧ divides p n1)) ∧ (∀p. ¬(prime p ∧ divides p m2 ∧ divides p n2)) ∧ (∀p. ¬(prime p ∧ divides p m1 ∧ divides p m2)) ∧ m1 * m2 = m
  ← rule conjI goal=17
    #[18] ∀p. ¬(prime p ∧ divides p m1 ∧ divides p n1)
    #[19] (∀p. ¬(prime p ∧ divides p m2 ∧ divides p n2)) ∧ (∀p. ¬(prime p ∧ divides p m1 ∧ divides p m2)) ∧ m1 * m2 = m
  ← intro p goal=18
    #[20] _VAR p
    #[21] ¬(prime p ∧ divides p m1 ∧ divides p n1)
  → inst target=fact p goal=21 facts=[16]
    #[22] ¬(prime p ∧ divides p n1 ∧ divides p n2)
  ← rule contrapositive goal=21 facts=[22]
    #[23] ¬¬(prime p ∧ divides p m1 ∧ divides p n1) ⟶ ¬¬(prime p ∧ divides p n1 ∧ divides p n2)
  ← rewrite double_neg sym=false goal=23
    #[24] prime p ∧ divides p m1 ∧ divides p n1 ⟶ prime p ∧ divides p n1 ∧ divides p n2
  ← intro goal=24
    #[25] prime p ∧ divides p m1 ∧ divides p n1
    #[26] prime p ∧ divides p n1 ∧ divides p n2
  → forward conjD1 goal=26 facts=[25]
    #[27] prime p
  → forward conjD2 goal=26 facts=[25]
    #[28] divides p m1 ∧ divides p n1
  → forward conjD1 goal=26 facts=[28]
    #[29] divides p m1
  → forward conjD2 goal=26 facts=[28]
    #[30] divides p n1
  ← rule conjI goal=26
    #[31] divides p n1 ∧ divides p n2
  ← rule conjI goal=31
    #[32] divides p n2
  ← apply_prev goal=32 facts=[14]
  ← rule conjI goal=19
    #[33] ∀p. ¬(prime p ∧ divides p m2 ∧ divides p n2)
    #[34] (∀p. ¬(prime p ∧ divides p m1 ∧ divides p m2)) ∧ m1 * m2 = m
  ← intro p goal=33
    #[35] ¬(prime p ∧ divides p m2 ∧ divides p n2)
  → inst target=fact p goal=35 facts=[18]
  ← rule contrapositive goal=35 facts=[21]
    #[36] ¬¬(prime p ∧ divides p m2 ∧ divides p n2) ⟶ ¬¬(prime p ∧ divides p m1 ∧ divides p n1)
  ← rewrite double_neg sym=false goal=36
    #[37] prime p ∧ divides p m2 ∧ divides p n2 ⟶ prime p ∧ divides p m1 ∧ divides p n1
  ← intro goal=37
    #[38] prime p ∧ divides p m2 ∧ divides p n2
    #[39] prime p ∧ divides p m1 ∧ divides p n1
  → forward conjD1 goal=39 facts=[38]
    #[40] prime p
  → forward conjD2 goal=39 facts=[38]
    #[41] divides p m2 ∧ divides p n2
  → forward conjD1 goal=39 facts=[41]
    #[42] divides p m2
  → forward conjD2 goal=39 facts=[41]
    #[43] divides p n2
  → forward goal=39 facts=[15,40,42]
    #[44] ¬(divides p n2)
  ← rule negE_gen goal=39 facts=[44,43]
    #[45] prime p ∧ divides p m1 ∧ divides p n1
  ← rule conjI goal=34
    #[46] ∀p. ¬(prime p ∧ divides p m1 ∧ divides p m2)
  ← intro p goal=46
    #[47] ¬(prime p ∧ divides p m1 ∧ divides p m2)
  → inst target=fact p goal=47 facts=[16]
  ← rule contrapositive goal=47 facts=[22]
    #[48] ¬¬(prime p ∧ divides p m1 ∧ divides p m2) ⟶ ¬¬(prime p ∧ divides p n1 ∧ divides p n2)
  ← rewrite double_neg sym=false goal=48
    #[49] prime p ∧ divides p m1 ∧ divides p m2 ⟶ prime p ∧ divides p n1 ∧ divides p n2
  ← intro goal=49
    #[50] prime p ∧ divides p m1 ∧ divides p m2
    #[51] prime p ∧ divides p n1 ∧ divides p n2
  → forward conjD1 goal=51 facts=[50]
    #[52] prime p
  → forward conjD2 goal=51 facts=[50]
    #[53] divides p m1 ∧ divides p m2
  → forward conjD1 goal=51 facts=[53]
    #[54] divides p m1
  → forward conjD2 goal=51 facts=[53]
    #[55] divides p m2
  → forward goal=51 facts=[14,52,54]
    #[56] divides p n2
  → forward goal=51 facts=[15,52,55]
    #[57] ¬(divides p n2)
  ← rule negE_gen goal=51 facts=[57,56]
    #[58] prime p ∧ divides p n1 ∧ divides p n2
qed

theorem exp_mult_exists
  fixes k :: nat, m :: nat, n :: nat, p :: nat
  prop ~(m = 0) --> m ^ k * n = p ^ k --> (?q. n = q ^ k)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] ¬(m = 0)
    #[2] m ^ k * n = p ^ k
    #[3] ∃q. n = q ^ k
  ← cases "k=0" goal=3
    #[4] k = 0 ⟶ (∃q. n = q ^ k)
    #[5] ¬(k = 0) ⟶ (∃q. n = q ^ k)
  ← intro goal=4
    #[6] k = 0
    #[7] ∃q. n = q ^ k
  ← rewrite source=prev goal=7 facts=[6]
    #[8] ∃q. n = q ^ (0::nat)
  ← rewrite nat_nat_power_def_1 sym=false goal=8
    #[9] ∃q::nat. n = 1
  → rewrite target=fact source=prev goal=9 facts=[6,2]
    #[10] m ^ (0::nat) * n = p ^ (0::nat)
  → rewrite target=fact nat_nat_power_def_1 sym=false goal=9 facts=[10]
    #[11] 1 * n = 1
  → rewrite target=fact mult_1_left sym=false goal=9 facts=[11]
    #[12] n = 1
  ← inst k goal=9
  ← intro goal=5
    #[13] ¬(k = 0)
    #[14] ∃q. n = q ^ k
  → forward divides_exp2_rev param_a=m param_b=p param_n=k goal=14
    #[15] divides (m ^ k) (p ^ k) ⟶ ¬(k = 0) ⟶ divides m p
  cut "divides (m ^ k) (p ^ k)" goal=14 facts=[15]
    #[16] divides (m ^ k) (p ^ k)
  → rewrite target=fact eq_sym_eq sym=false goal=16 facts=[2]
    #[17] p ^ k = m ^ k * n
  ← rewrite source=prev goal=16 facts=[17]
    #[18] divides (m ^ k) (m ^ k * n)
  ← rule divides_rmul goal=18
    #[19] divides (m ^ k) (m ^ k)
  ← rule divides_self goal=19
    #[20] divides (m ^ k) (m ^ k)
  → forward goal=14 facts=[15,16,13]
    #[21] divides m p
  → rewrite target=fact divides_def sym=false goal=14 facts=[21]
    #[22] ∃k. p = m * k
  elim d goal=14 facts=[22]
    #[23] _VAR d
    #[24] p = m * d
    #[25] ∃q. n = q ^ k
  → rewrite target=fact source=prev goal=25 facts=[24,2]
    #[26] m ^ k * n = (m * d) ^ k
  → rewrite target=fact mult_exp sym=false goal=25 facts=[26]
    #[27] m ^ k * n = m ^ k * d ^ k
  → rewrite target=fact eq_mult_lcancel sym=false goal=25 facts=[27]
    #[28] m ^ k = 0 ∨ n = d ^ k
  ← rule disjE goal=25 facts=[28]
    #[29] m ^ k = 0 ⟶ (∃q. n = q ^ k)
    #[30] n = d ^ k ⟶ (∃q. n = q ^ k)
  ← intro goal=29
    #[31] m ^ k = 0
    #[32] ∃q. n = q ^ k
  → rewrite target=fact exp_eq_0 sym=false goal=32 facts=[31]
    #[33] m = 0 ∧ ¬(k = 0)
  → forward conjD1 goal=32 facts=[33]
    #[34] m = 0
  ← rule negE_gen goal=32 facts=[1,34]
    #[35] ∃q. n = q ^ k
  ← intro goal=30
    #[36] n = d ^ k
    #[37] ∃q. n = q ^ k
  ← rule exI goal=37 facts=[36]
    #[38] ∃q. n = q ^ k
qed

theorem coprime_pow
  fixes a :: nat, b :: nat, c :: nat, n :: nat
  prop coprime a b --> a * b = c ^ n --> (?r. ?s. a = r ^ n & b = s ^ n)
  [hint_backward,hint_backward1]

theorem prime_exp
  fixes n :: nat, p :: nat
  prop prime (p ^ n) <--> prime p & n = 1
  [hint_rewrite]
proof
  ← induct n nat_induct goal=0
    #[1] prime (p ^ (0::nat)) ⟷ prime p ∧ (0::nat) = 1
    #[2] ∀n. prime (p ^ n) ⟷ prime p ∧ n = 1 ⟶ prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1
  ← rewrite nat_nat_power_def_1 sym=false goal=1
    #[3] prime 1 ⟷ prime p ∧ (0::nat) = 1
  ← rule iffI goal=3
    #[4] prime 1 ⟶ prime p ∧ (0::nat) = 1
    #[5] prime p ∧ (0::nat) = 1 ⟶ prime 1
  ← intro goal=4
    #[6] prime 1
    #[7] prime p ∧ (0::nat) = 1
  ← rule contrapositive goal=7 facts=[6]
    #[8] ¬(prime p ∧ (0::nat) = 1) ⟶ ¬(prime 1)
  ← intro goal=8
    #[9] ¬(prime p ∧ (0::nat) = 1)
    #[10] ¬(prime 1)
  ← rule prime_1 goal=10
    #[11] ¬(prime 1)
  ← intro goal=5
    #[12] prime p ∧ (0::nat) = 1
    #[13] prime 1
  → forward conjD2 goal=13 facts=[12]
    #[14] (0::nat) = 1
  ← rule contrapositive goal=13 facts=[14]
    #[15] ¬(prime 1) ⟶ ¬((0::nat) = 1)
  ← intro goal=15
    #[16] ¬(prime 1)
    #[17] ¬((0::nat) = 1)
  nat_const_ineq goal=17
    #[18] ¬((0::nat) = 1)
  ← intro n goal=2
    #[19] _VAR n
    #[20] prime (p ^ n) ⟷ prime p ∧ n = 1
    #[21] prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1
  ← cases "p=0" goal=21
    #[22] p = 0 ⟶ prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1
    #[23] ¬(p = 0) ⟶ prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1
  ← intro goal=22
    #[24] p = 0
    #[25] prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1
  ← rewrite source=prev goal=25 facts=[24]
    #[26] prime (0 ^ Suc n) ⟷ prime 0 ∧ Suc n = 1
  ← rewrite exp_zero sym=false goal=26
    #[27] prime (if Suc n = 0 then 1 else 0) ⟷ prime 0 ∧ Suc n = 1
  cut "~(Suc n = 0)" goal=27
    #[28] ¬(Suc n = 0)
  ← rule ineq_sym goal=28
    #[29] ¬(0 = Suc n)
  ← rule nat_zero_Suc_neq goal=29
    #[30] ¬(0 = Suc n)
  ← rewrite if_not_P sym=false goal=27 facts=[28]
    #[31] prime 0 ⟷ prime 0 ∧ Suc n = 1
  ← rule iffI goal=31
    #[32] prime 0 ⟶ prime 0 ∧ Suc n = 1
    #[33] prime 0 ∧ Suc n = 1 ⟶ prime 0
  ← intro goal=32
    #[34] prime 0
    #[35] prime 0 ∧ Suc n = 1
  ← rule contrapositive goal=35 facts=[34]
    #[36] ¬(prime 0 ∧ Suc n = 1) ⟶ ¬(prime 0)
  ← intro goal=36
    #[37] ¬(prime 0 ∧ Suc n = 1)
    #[38] ¬(prime 0)
  ← rule prime_0 goal=38
    #[39] ¬(prime 0)
  ← intro goal=33
    #[40] prime 0 ∧ Suc n = 1
    #[41] prime 0
  ← rule conjD1 goal=41 facts=[40]
    #[42] prime 0
  ← intro goal=23
    #[43] ¬(p = 0)
    #[44] prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1
  ← induct n nat_induct goal=44
    #[45] prime (p ^ Suc 0) ⟷ prime p ∧ Suc 0 = 1
    #[46] ∀n. prime (p ^ Suc n) ⟷ prime p ∧ Suc n = 1 ⟶ prime (p ^ Suc (Suc n)) ⟷ prime p ∧ Suc (Suc n) = 1
  ← rewrite add_1_right sym=true goal=45
    #[47] prime (p ^ ((0::nat) + 1)) ⟷ prime p ∧ (0::nat) + 1 = 1
  ← rewrite nat_plus_def_1 sym=false goal=47
    #[48] prime (p ^ (1::nat)) ⟷ prime p ∧ (1::nat) = 1
  ← rewrite exp_1 sym=false goal=48
    #[49] prime p ⟷ prime p ∧ (1::nat) = 1
  ← rule iffI goal=49
    #[50] prime p ⟶ prime p ∧ (1::nat) = 1
    #[51] prime p ∧ (1::nat) = 1 ⟶ prime p
  ← intro goal=50
    #[52] prime p
    #[53] prime p ∧ (1::nat) = 1
  ← rule conjI goal=53
    #[54] (1::nat) = 1
  ← rule eq_refl goal=54
    #[55] (1::nat) = 1
  ← intro goal=51
    #[56] prime p ∧ (1::nat) = 1
    #[57] prime p
  ← rule conjD1 goal=57 facts=[56]
    #[58] prime p
  ← intro m goal=46
    #[59] _VAR m
    #[60] prime (p ^ Suc m) ⟷ prime p ∧ Suc m = 1
    #[61] prime (p ^ Suc (Suc m)) ⟷ prime p ∧ Suc (Suc m) = 1
  ← cases "p=1" goal=61
    #[62] p = 1 ⟶ prime (p ^ Suc (Suc m)) ⟷ prime p ∧ Suc (Suc m) = 1
    #[63] ¬(p = 1) ⟶ prime (p ^ Suc (Suc m)) ⟷ prime p ∧ Suc (Suc m) = 1
  ← intro goal=62
    #[64] p = 1
    #[65] prime (p ^ Suc (Suc m)) ⟷ prime p ∧ Suc (Suc m) = 1
  ← rewrite source=prev goal=65 facts=[64]
    #[66] prime (1 ^ Suc (Suc m)) ⟷ prime 1 ∧ Suc (Suc m) = 1
  ← rewrite exp_one sym=false goal=66
    #[67] prime 1 ⟷ prime 1 ∧ Suc (Suc m) = 1
  ← rule iffI goal=67
    #[68] prime 1 ⟶ prime 1 ∧ Suc (Suc m) = 1
    #[69] prime 1 ∧ Suc (Suc m) = 1 ⟶ prime 1
  ← intro goal=68
    #[70] prime 1 ∧ Suc (Suc m) = 1
  ← rule contrapositive goal=70 facts=[6]
    #[71] ¬(prime 1 ∧ Suc (Suc m) = 1) ⟶ ¬(prime 1)
  ← intro goal=71
    #[72] ¬(prime 1 ∧ Suc (Suc m) = 1)
    #[73] ¬(prime 1)
  ← rule prime_1 goal=73
  ← intro goal=69
    #[74] prime 1 ∧ Suc (Suc m) = 1
    #[75] prime 1
  ← rule conjD1 goal=75 facts=[74]
    #[76] prime 1
  ← intro goal=63
    #[77] ¬(p = 1)
    #[78] prime (p ^ Suc (Suc m)) ⟷ prime p ∧ Suc (Suc m) = 1
  ← rewrite nat_nat_power_def_2 sym=false goal=78
    #[79] prime (p * p ^ Suc m) ⟷ prime p ∧ Suc (Suc m) = 1
  ← rule iffI goal=79
    #[80] prime (p * p ^ Suc m) ⟶ prime p ∧ Suc (Suc m) = 1
    #[81] prime p ∧ Suc (Suc m) = 1 ⟶ prime (p * p ^ Suc m)
  ← intro goal=80
    #[82] prime (p * p ^ Suc m)
    #[83] prime p ∧ Suc (Suc m) = 1
  ← rule contrapositive goal=83 facts=[82]
    #[84] ¬(prime p ∧ Suc (Suc m) = 1) ⟶ ¬(prime (p * p ^ Suc m))
  ← intro goal=84
    #[85] ¬(prime p ∧ Suc (Suc m) = 1)
    #[86] ¬(prime (p * p ^ Suc m))
  ← rewrite prime_def sym=false goal=86
    #[87] ¬(¬(p * p ^ Suc m = 1) ∧ (∀x. divides x (p * p ^ Suc m) ⟶ x = 1 ∨ x = p * p ^ Suc m))
  ← rewrite de_morgan_thm1 sym=false goal=87
    #[88] ¬¬(p * p ^ Suc m = 1) ∨ ¬(∀x. divides x (p * p ^ Suc m) ⟶ x = 1 ∨ x = p * p ^ Suc m)
  ← rule disjI2 goal=88
    #[89] ¬(∀x. divides x (p * p ^ Suc m) ⟶ x = 1 ∨ x = p * p ^ Suc m)
  ← rewrite not_all sym=false goal=89
    #[90] ∃x. ¬(divides x (p * p ^ Suc m) ⟶ x = 1 ∨ x = p * p ^ Suc m)
  ← rewrite not_imp sym=false goal=90
    #[91] ∃x. divides x (p * p ^ Suc m) ∧ ¬(x = 1 ∨ x = p * p ^ Suc m)
  ← rewrite de_morgan_thm2 sym=false goal=91
    #[92] ∃x. divides x (p * p ^ Suc m) ∧ ¬(x = 1) ∧ ¬(x = p * p ^ Suc m)
  ← inst p goal=92
    #[93] divides p (p * p ^ Suc m) ∧ ¬(p = 1) ∧ ¬(p = p * p ^ Suc m)
  ← rule conjI goal=93
    #[94] divides p (p * p ^ Suc m)
    #[95] ¬(p = 1) ∧ ¬(p = p * p ^ Suc m)
  ← rule divides_rmul goal=94
    #[96] divides p p
  ← rule divides_self goal=96
    #[97] divides p p
  ← rule conjI goal=95
    #[98] ¬(p = p * p ^ Suc m)
  ← rule negI goal=98
    #[99] p = p * p ^ Suc m ⟶ false
  ← intro goal=99
    #[100] p = p * p ^ Suc m
    #[101] false
  cut "p = p * 1" goal=101 facts=[100]
    #[102] p = p * 1
  ← rewrite mult_1_right sym=false goal=102
    #[103] p = p * 1
  → rewrite target=fact source=prev goal=101 facts=[103,100]
    #[104] p * 1 = p * 1 * (p * 1) ^ Suc m
  → rewrite target=fact mult_assoc sym=false goal=101 facts=[104]
    #[105] p * 1 = p * (1 * (p * 1) ^ Suc m)
  → rewrite target=fact mult_1_left sym=false goal=101 facts=[105]
    #[106] p * 1 = p * (p * 1) ^ Suc m
  → rewrite target=fact eq_mult_lcancel sym=false goal=101 facts=[106]
    #[107] p = 0 ∨ 1 = (p * 1) ^ Suc m
  → rewrite target=fact mult_1_right sym=false goal=101 facts=[107]
    #[108] p = 0 ∨ 1 = p ^ Suc m
  ← rule disjE goal=101 facts=[108]
    #[109] p = 0 ⟶ false
    #[110] 1 = p ^ Suc m ⟶ false
  ← intro goal=109
    #[111] false
  ← rule contrapositive goal=111 facts=[24]
    #[112] ¬false ⟶ ¬(p = 0)
  ← intro goal=112
    #[113] ¬false
  ← intro goal=110
    #[114] 1 = p ^ Suc m
    #[115] false
  → rewrite target=fact eq_sym_eq sym=false goal=115 facts=[114]
    #[116] p ^ Suc m = 1
  → rewrite target=fact exp_eq_1 sym=false goal=115 facts=[116]
    #[117] p = 1 ∨ Suc m = 0
  ← rule disjE goal=115 facts=[117]
    #[118] p = 1 ⟶ false
    #[119] Suc m = 0 ⟶ false
  ← intro goal=118
    #[120] false
  ← rule negE_gen goal=120 facts=[77,64]
    #[121] false
  ← intro goal=119
    #[122] Suc m = 0
    #[123] false
  ← rule contrapositive goal=123 facts=[122]
    #[124] ¬false ⟶ ¬(Suc m = 0)
  ← intro goal=124
    #[125] ¬(Suc m = 0)
  ← rewrite eq_sym_eq sym=false goal=125
    #[126] ¬(0 = Suc m)
  ← rule nat_zero_Suc_neq goal=126
    #[127] ¬(0 = Suc m)
  ← intro goal=81
    #[128] prime p ∧ Suc (Suc m) = 1
    #[129] prime (p * p ^ Suc m)
  → forward conjD2 goal=129 facts=[128]
    #[130] Suc (Suc m) = 1
  → rewrite target=fact nat_one_def sym=false goal=129 facts=[130]
    #[131] Suc (Suc m) = Suc 0
  → forward nat_Suc_inject goal=129 facts=[131]
    #[132] Suc m = 0
  ← rule contrapositive goal=129 facts=[132]
    #[133] ¬(prime (p * p ^ Suc m)) ⟶ ¬(Suc m = 0)
  ← intro goal=133
    #[134] ¬(prime (p * p ^ Suc m))
    #[135] ¬(Suc m = 0)
  ← rule ineq_sym goal=135
    #[136] ¬(0 = Suc m)
  ← rule nat_zero_Suc_neq goal=136
qed

theorem prime_power_mult
  fixes k :: nat, p :: nat, x :: nat, y :: nat
  prop prime p --> x * y = p ^ k --> (?i::nat. ?j::nat. x = p ^ i & y = p ^ j)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] x * y = p ^ k
    #[3] ∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j
  cut "!x. !y. x * y = p ^ k ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j)" goal=3
    #[4] ∀x. ∀y. x * y = p ^ k ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j)
  ← induct k nat_induct goal=4
    #[5] ∀x. ∀y. x * y = p ^ (0::nat) ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j)
    #[6] ∀n. (∀x. ∀y. x * y = p ^ n ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j)) ⟶ (∀x. ∀y. x * y = p ^ Suc n ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j))
  ← intro x1,y1 goal=5
    #[7] _VAR x1
    #[8] _VAR y1
    #[9] x1 * y1 = p ^ (0::nat)
    #[10] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → rewrite target=fact nat_nat_power_def_1 sym=false goal=10 facts=[9]
    #[11] x1 * y1 = 1
  → rewrite target=fact mult_eq_1 sym=false goal=10 facts=[11]
    #[12] x1 = 1 ∧ y1 = 1
  ← inst "(0::nat)" goal=10
    #[13] ∃j::nat. x1 = p ^ (0::nat) ∧ y1 = p ^ j
  ← inst "(0::nat)" goal=13
    #[14] x1 = p ^ (0::nat) ∧ y1 = p ^ (0::nat)
  ← rewrite nat_nat_power_def_1 sym=false goal=14
  ← intro k1 goal=6
    #[15] _VAR k1
    #[16] ∀x. ∀y. x * y = p ^ k1 ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j)
    #[17] ∀x. ∀y. x * y = p ^ Suc k1 ⟶ (∃i::nat. ∃j::nat. x = p ^ i ∧ y = p ^ j)
  ← intro x1,y1 goal=17
    #[18] x1 * y1 = p ^ Suc k1
    #[19] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → rewrite target=fact nat_nat_power_def_2 sym=false goal=19 facts=[18]
    #[20] x1 * y1 = p * p ^ k1
  cut "divides p x1 | divides p y1" goal=19
    #[21] divides p x1 ∨ divides p y1
  ← rule prime_divprod goal=21 facts=[1]
    #[22] divides p (x1 * y1)
  ← rewrite source=prev goal=22 facts=[20]
    #[23] divides p (p * p ^ k1)
  ← rule divides_rmul goal=23
    #[24] divides p p
  ← rule divides_self goal=24
    #[25] divides p p
  → rewrite target=fact divides_def sym=false goal=19 facts=[21]
    #[26] (∃k. x1 = p * k) ∨ (∃k. y1 = p * k)
  cut "~(p=0)" goal=19
    #[27] ¬(p = 0)
  ← rule prime_imp_nz goal=27
  ← rule disjE goal=19 facts=[26]
    #[28] (∃k. x1 = p * k) ⟶ (∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j)
    #[29] (∃k. y1 = p * k) ⟶ (∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j)
  ← intro goal=28
    #[30] ∃k. x1 = p * k
    #[31] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  elim d goal=31 facts=[30]
    #[32] _VAR d
    #[33] x1 = p * d
    #[34] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → rewrite target=fact source=prev goal=34 facts=[33,20]
    #[35] p * d * y1 = p * p ^ k1
  → rewrite target=fact mult_assoc sym=false goal=34 facts=[35]
    #[36] p * (d * y1) = p * p ^ k1
  → rewrite target=fact eq_mult_lcancel sym=false goal=34 facts=[36]
    #[37] p = 0 ∨ d * y1 = p ^ k1
  ← rule disjE goal=34 facts=[37]
    #[38] p = 0 ⟶ (∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j)
    #[39] d * y1 = p ^ k1 ⟶ (∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j)
  ← intro goal=38
    #[40] p = 0
    #[41] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  ← rule negE_gen goal=41 facts=[27,40]
    #[42] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  ← intro goal=39
    #[43] d * y1 = p ^ k1
    #[44] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → inst target=fact d goal=44 facts=[16]
    #[45] ∀y. d * y = p ^ k1 ⟶ (∃i::nat. ∃j::nat. d = p ^ i ∧ y = p ^ j)
  → inst target=fact y1 goal=44 facts=[45]
    #[46] d * y1 = p ^ k1 ⟶ (∃i::nat. ∃j::nat. d = p ^ i ∧ y1 = p ^ j)
  → forward goal=44 facts=[46,43]
    #[47] ∃i::nat. ∃j::nat. d = p ^ i ∧ y1 = p ^ j
  elim i,j goal=44 facts=[47]
    #[48] _VAR i
    #[49] _VAR j
    #[50] d = p ^ i ∧ y1 = p ^ j
    #[51] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → forward conjD1 goal=51 facts=[50]
    #[52] d = p ^ i
  → forward conjD2 goal=51 facts=[50]
    #[53] y1 = p ^ j
  ← rewrite source=prev goal=51 facts=[33]
    #[54] ∃i::nat. ∃j::nat. p * d = p ^ i ∧ y1 = p ^ j
  ← inst "Suc i" goal=54
    #[55] ∃j::nat. p * d = p ^ Suc i ∧ y1 = p ^ j
  ← inst j goal=55
    #[56] p * d = p ^ Suc i ∧ y1 = p ^ j
  ← rule conjI goal=56
    #[57] p * d = p ^ Suc i
  ← rewrite source=prev goal=57 facts=[52]
    #[58] p * p ^ i = p ^ Suc i
  ← rewrite nat_nat_power_def_2 sym=false goal=58
    #[59] p * p ^ i = p ^ Suc i
  ← intro goal=29
    #[60] ∃k. y1 = p * k
    #[61] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  elim d goal=61 facts=[60]
    #[62] y1 = p * d
    #[63] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → rewrite target=fact source=prev goal=63 facts=[62,20]
    #[64] x1 * (p * d) = p * p ^ k1
  cut "x1 * (p * d) = p * (x1 * d)" goal=63
    #[65] x1 * (p * d) = p * (x1 * d)
  nat_norm goal=65
    #[66] x1 * (p * d) = p * (x1 * d)
  → rewrite target=fact source=prev goal=63 facts=[66,64]
    #[67] p * (x1 * d) = p * p ^ k1
  → rewrite target=fact eq_mult_lcancel sym=false goal=63 facts=[67]
    #[68] p = 0 ∨ x1 * d = p ^ k1
  ← rule disjE goal=63 facts=[68]
    #[69] p = 0 ⟶ (∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j)
    #[70] x1 * d = p ^ k1 ⟶ (∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j)
  ← intro goal=69
    #[71] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  ← rule negE_gen goal=71 facts=[27,40]
  ← intro goal=70
    #[72] x1 * d = p ^ k1
    #[73] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → forward goal=73 facts=[16,72]
    #[74] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ d = p ^ j
  elim i,j goal=73 facts=[74]
    #[75] x1 = p ^ i ∧ d = p ^ j
    #[76] ∃i::nat. ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  → forward conjD1 goal=76 facts=[75]
    #[77] x1 = p ^ i
  → forward conjD2 goal=76 facts=[75]
    #[78] d = p ^ j
  ← inst i goal=76
    #[79] ∃j::nat. x1 = p ^ i ∧ y1 = p ^ j
  ← inst "Suc j" goal=79
    #[80] x1 = p ^ i ∧ y1 = p ^ Suc j
  ← rule conjI goal=80
    #[81] y1 = p ^ Suc j
  ← rewrite source=prev goal=81 facts=[62]
    #[82] p * d = p ^ Suc j
  ← rewrite source=prev goal=82 facts=[78]
    #[83] p * p ^ j = p ^ Suc j
  ← rewrite nat_nat_power_def_2 sym=false goal=83
    #[84] p * p ^ j = p ^ Suc j
  ← apply_prev goal=3 facts=[4]
qed

theorem prime_power_exp
  fixes k :: nat, n :: nat, p :: nat, x :: nat
  prop prime p --> ~(n = 0) --> x ^ n = p ^ k --> (?i::nat. x = p ^ i)
  [hint_backward,hint_backward1]

theorem divides_primepow
  fixes k :: nat, p :: nat
  prop prime p --> (!d. divides d (p ^ k) <--> (?i. i <= k & d = p ^ i))
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ∀d. divides d (p ^ k) ⟷ (∃i. i ≤ k ∧ d = p ^ i)
  ← intro d goal=2
    #[3] _VAR d
    #[4] divides d (p ^ k) ⟷ (∃i. i ≤ k ∧ d = p ^ i)
  ← rule iffI goal=4
    #[5] divides d (p ^ k) ⟶ (∃i. i ≤ k ∧ d = p ^ i)
    #[6] (∃i. i ≤ k ∧ d = p ^ i) ⟶ divides d (p ^ k)
  ← intro goal=5
    #[7] divides d (p ^ k)
    #[8] ∃i. i ≤ k ∧ d = p ^ i
  → rewrite target=fact divides_def sym=false goal=8 facts=[7]
    #[9] ∃k1. p ^ k = d * k1
  elim e goal=8 facts=[9]
    #[10] _VAR e
    #[11] p ^ k = d * e
    #[12] ∃i. i ≤ k ∧ d = p ^ i
  → rewrite target=fact eq_sym_eq sym=false goal=12 facts=[11]
    #[13] d * e = p ^ k
  → forward prime_power_mult goal=12 facts=[1,13]
    #[14] ∃i::nat. ∃j::nat. d = p ^ i ∧ e = p ^ j
  elim i,j goal=12 facts=[14]
    #[15] _VAR i
    #[16] _VAR j
    #[17] d = p ^ i ∧ e = p ^ j
    #[18] ∃i. i ≤ k ∧ d = p ^ i
  ← inst i goal=18
    #[19] i ≤ k ∧ d = p ^ i
  → forward conjD1 goal=19 facts=[17]
    #[20] d = p ^ i
  → forward conjD2 goal=19 facts=[17]
    #[21] e = p ^ j
  ← rule conjI goal=19
    #[22] i ≤ k
  ← cases "p=0" goal=22
    #[23] p = 0 ⟶ i ≤ k
    #[24] ¬(p = 0) ⟶ i ≤ k
  ← intro goal=23
    #[25] p = 0
    #[26] i ≤ k
  → rewrite target=fact source=prev goal=26 facts=[25,1]
    #[27] prime 0
  ← rule contrapositive goal=26 facts=[27]
    #[28] ¬(i ≤ k) ⟶ ¬(prime 0)
  ← intro goal=28
    #[29] ¬(i ≤ k)
    #[30] ¬(prime 0)
  ← rule prime_0 goal=30
    #[31] ¬(prime 0)
  ← intro goal=24
    #[32] ¬(p = 0)
    #[33] i ≤ k
  ← cases "p=1" goal=33
    #[34] p = 1 ⟶ i ≤ k
    #[35] ¬(p = 1) ⟶ i ≤ k
  ← intro goal=34
    #[36] p = 1
    #[37] i ≤ k
  → rewrite target=fact source=prev goal=37 facts=[36,1]
    #[38] prime 1
  ← rule contrapositive goal=37 facts=[38]
    #[39] ¬(i ≤ k) ⟶ ¬(prime 1)
  ← intro goal=39
    #[40] ¬(prime 1)
  ← rule prime_1 goal=40
    #[41] ¬(prime 1)
  ← intro goal=35
    #[42] ¬(p = 1)
    #[43] i ≤ k
  cut "d * 1 <= d * e" goal=43
    #[44] d * 1 ≤ d * e
  ← rule le_mult2 goal=44
    #[45] d ≤ d
    #[46] 1 ≤ e
  ← rule less_eq_refl goal=45
    #[47] d ≤ d
  ← rewrite source=prev goal=46 facts=[21]
    #[48] 1 ≤ p ^ j
  ← rule le_1_2 goal=48
    #[49] ¬(p ^ j = 0)
  ← rewrite exp_eq_0 sym=false goal=49
    #[50] ¬(p = 0 ∧ ¬(j = 0))
  ← rewrite de_morgan_thm1 sym=false goal=50
    #[51] ¬(p = 0) ∨ ¬¬(j = 0)
  ← rule disjI1 goal=51
  → rewrite target=fact mult_1_right sym=false goal=43 facts=[44]
    #[52] d ≤ d * e
  → rewrite target=fact source=prev goal=43 facts=[13,52]
    #[53] d ≤ p ^ k
  → rewrite target=fact source=prev goal=43 facts=[20,53]
    #[54] p ^ i ≤ p ^ k
  → rewrite target=fact le_exp sym=false goal=43 facts=[54]
    #[55] if p = 0 then i = 0 ⟶ k = 0 else p = 1 ∨ i ≤ k
  → rewrite target=fact if_not_P sym=false goal=43 facts=[55,32]
    #[56] p = 1 ∨ i ≤ k
  ← rule disjE goal=43 facts=[56]
    #[57] i ≤ k ⟶ i ≤ k
  ← intro goal=6
    #[58] ∃i. i ≤ k ∧ d = p ^ i
    #[59] divides d (p ^ k)
  elim i goal=59 facts=[58]
    #[60] i ≤ k ∧ d = p ^ i
    #[61] divides d (p ^ k)
  → forward conjD1 goal=61 facts=[60]
    #[62] i ≤ k
  → forward conjD2 goal=61 facts=[60]
    #[63] d = p ^ i
  → rewrite target=fact less_eq_exist sym=false goal=61 facts=[62]
    #[64] ∃p. k = i + p
  elim e goal=61 facts=[64]
    #[65] k = i + e
    #[66] divides d (p ^ k)
  ← rewrite source=prev goal=66 facts=[65]
    #[67] divides d (p ^ (i + e))
  ← rewrite nat_power_add sym=false goal=67
    #[68] divides d (p ^ i * p ^ e)
  ← rewrite source=prev goal=68 facts=[63]
    #[69] divides (p ^ i) (p ^ i * p ^ e)
  ← rule divides_rmul goal=69
    #[70] divides (p ^ i) (p ^ i)
  ← rule divides_self goal=70
    #[71] divides (p ^ i) (p ^ i)
qed

theorem primepow_divides_prod
  fixes k :: nat, m :: nat, n :: nat, p :: nat
  prop prime p --> divides (p ^ k) (m * n) --> (?i. ?j. divides (p ^ i) m & divides (p ^ j) n & k = i + j)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides (p ^ k) (m * n)
    #[3] ∃i. ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  → forward division_decomp goal=3 facts=[2]
    #[4] ∃b1. ∃c1. p ^ k = b1 * c1 ∧ divides b1 m ∧ divides c1 n
  elim b,c goal=3 facts=[4]
    #[5] _VAR b
    #[6] _VAR c
    #[7] p ^ k = b * c ∧ divides b m ∧ divides c n
    #[8] ∃i. ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  → forward divides_primepow param_k=k goal=8 facts=[1]
    #[9] ∀d. divides d (p ^ k) ⟷ (∃i. i ≤ k ∧ d = p ^ i)
  → forward conjD1 goal=8 facts=[7]
    #[10] p ^ k = b * c
  → inst target=fact b goal=8 facts=[9]
    #[11] divides b (p ^ k) ⟷ (∃i. i ≤ k ∧ b = p ^ i)
  cut "divides b (p ^ k)" goal=8
    #[12] divides b (p ^ k)
  ← rewrite source=prev goal=12 facts=[10]
    #[13] divides b (b * c)
  ← rule divides_rmul goal=13
    #[14] divides b b
  ← rule divides_self goal=14
    #[15] divides b b
  → rewrite target=fact source=prev goal=8 facts=[11,12]
    #[16] ∃i. i ≤ k ∧ b = p ^ i
  → inst target=fact c goal=8 facts=[9]
    #[17] divides c (p ^ k) ⟷ (∃i. i ≤ k ∧ c = p ^ i)
  cut "divides c (p ^ k)" goal=8 facts=[17]
    #[18] divides c (p ^ k)
  ← rewrite source=prev goal=18 facts=[10]
    #[19] divides c (b * c)
  ← rule divides_lmul goal=19
    #[20] divides c c
  ← rule divides_self goal=20
    #[21] divides c c
  → rewrite target=fact source=prev goal=8 facts=[17,18]
    #[22] ∃i. i ≤ k ∧ c = p ^ i
  elim i goal=8 facts=[16]
    #[23] _VAR i
    #[24] i ≤ k ∧ b = p ^ i
    #[25] ∃i. ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  elim j goal=25 facts=[22]
    #[26] _VAR j
    #[27] j ≤ k ∧ c = p ^ j
    #[28] ∃i. ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  ← inst i goal=28
    #[29] ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  ← inst j goal=29
    #[30] divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  ← rule conjI goal=30
    #[31] divides (p ^ i) m
    #[32] divides (p ^ j) n ∧ k = i + j
  → forward conjD2 goal=31 facts=[24]
    #[33] b = p ^ i
  → rewrite target=fact eq_sym_eq sym=false goal=31 facts=[33]
    #[34] p ^ i = b
  ← rewrite source=prev goal=31 facts=[34]
    #[35] divides b m
  → forward conjD2 goal=35 facts=[7]
    #[36] divides b m ∧ divides c n
  ← rule conjD1 goal=35 facts=[36]
    #[37] divides b m
  ← rule conjI goal=32
    #[38] divides (p ^ j) n
    #[39] k = i + j
  → forward conjD2 goal=38 facts=[27]
    #[40] c = p ^ j
  → rewrite target=fact eq_sym_eq sym=false goal=38 facts=[40]
    #[41] p ^ j = c
  ← rewrite source=prev goal=38 facts=[41]
    #[42] divides c n
  ← rule conjD2 goal=42 facts=[36]
    #[43] divides c n
  → rewrite target=fact source=prev goal=39 facts=[33,10]
    #[44] p ^ k = p ^ i * c
  → rewrite target=fact source=prev goal=39 facts=[40,44]
    #[45] p ^ k = p ^ i * p ^ j
  → forward eq_prime_pow param_m=k param_n=i+j goal=39 facts=[1]
    #[46] p ^ k = p ^ (i + j) ⟷ k = i + j
  → rewrite target=fact eq_sym_eq sym=false goal=39 facts=[46]
    #[47] k = i + j ⟷ p ^ k = p ^ (i + j)
  ← rewrite source=prev goal=39 facts=[47]
    #[48] p ^ k = p ^ (i + j)
  ← rewrite nat_power_add sym=false goal=48
qed

theorem primepow_factor
  fixes n :: nat
  prop 2 <= n --> (?p. ?k::nat. ?m. prime p & 1 <= k & coprime p m & n = p ^ k * m)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] 2 ≤ n
    #[2] ∃p. ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  → forward prime_factor param_n=n goal=2
    #[3] ¬(n = 1) ⟶ (∃p. prime p ∧ divides p n)
  cut "¬(n = 1)" goal=2
    #[4] ¬(n = 1)
  ← z3 goal=4 facts=[1]
  → forward goal=2 facts=[3,4]
    #[5] ∃p. prime p ∧ divides p n
  elim p goal=2 facts=[5]
    #[6] _VAR p
    #[7] prime p ∧ divides p n
    #[8] ∃p. ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  ← inst p goal=8
    #[9] ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  → forward factorization_index param_n=n param_p=p goal=9
    #[10] ¬(n = 0) ⟶ 2 ≤ p ⟶ (∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n)))
  cut "¬(n = 0)" goal=9
    #[11] ¬(n = 0)
  ← z3 goal=11 facts=[1]
    #[12] ¬(n = 0)
  → forward goal=9 facts=[10,12]
    #[13] 2 ≤ p ⟶ (∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n)))
  → forward conjD1 goal=9 facts=[7]
    #[14] prime p
  → forward conjD2 goal=9 facts=[7]
    #[15] divides p n
  → forward prime_ge_2 goal=9 facts=[14]
    #[16] 2 ≤ p
  → forward goal=9 facts=[13,16]
    #[17] ∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n))
  elim k goal=9 facts=[17]
    #[18] _VAR k
    #[19] divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n))
    #[20] ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
    #[21] ∃p. ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  ← inst k goal=20
    #[22] ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  → forward conjD1 goal=22 facts=[19]
    #[23] divides (p ^ k) n
  → forward conjD2 goal=22 facts=[19]
    #[24] ∀l. k < l ⟶ ¬(divides (p ^ l) n)
  → rewrite target=fact divides_def sym=false goal=22 facts=[23]
    #[25] ∃k1. n = p ^ k * k1
  elim m goal=22 facts=[25]
    #[26] _VAR m
    #[27] n = p ^ k * m
    #[28] ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
    #[29] ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
    #[30] ∃p. ∃k::nat. ∃m. prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  ← inst m goal=28
    #[31] prime p ∧ 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  ← rule conjI goal=31
    #[32] 1 ≤ k ∧ coprime p m ∧ n = p ^ k * m
  ← rule conjI goal=32
    #[33] 1 ≤ k
    #[34] coprime p m ∧ n = p ^ k * m
  → inst target=fact k+1 goal=33 facts=[24]
    #[35] k < k + 1 ⟶ ¬(divides (p ^ (k + 1)) n)
  cut "k < k + 1" goal=33
    #[36] k < k + 1
  ← z3 goal=36
    #[37] k < k + 1
  → forward goal=33 facts=[35,37]
    #[38] ¬(divides (p ^ (k + 1)) n)
  → rewrite target=fact nat_power_add sym=false goal=33 facts=[38]
    #[39] ¬(divides (p ^ k * p ^ (1::nat)) n)
  → rewrite target=fact exp_1 sym=false goal=33 facts=[39]
    #[40] ¬(divides (p ^ k * p) n)
  ← rule le_1_2 goal=33
    #[41] ¬(k = 0)
  ← rule contrapositive goal=41 facts=[40]
    #[42] ¬¬(k = 0) ⟶ ¬¬(divides (p ^ k * p) n)
  ← rewrite double_neg sym=false goal=42
    #[43] k = 0 ⟶ divides (p ^ k * p) n
  ← intro goal=43
    #[44] k = 0
    #[45] divides (p ^ k * p) n
  ← rewrite source=prev goal=45 facts=[44]
    #[46] divides (p ^ (0::nat) * p) n
  ← rewrite nat_nat_power_def_1 sym=false goal=46
    #[47] divides (1 * p) n
  ← rewrite mult_1_left sym=false goal=47
  ← rule conjI goal=34
    #[48] coprime p m
  → forward prime_coprime_strong param_n=m goal=48 facts=[14]
    #[49] divides p m ∨ coprime p m
  ← rule disjE goal=48 facts=[49]
    #[50] divides p m ⟶ coprime p m
    #[51] coprime p m ⟶ coprime p m
  ← intro goal=50
    #[52] divides p m
    #[53] coprime p m
  → rewrite target=fact source=prev goal=53 facts=[27,40]
    #[54] ¬(divides (p ^ k * p) (p ^ k * m))
  ← rule negE_gen goal=53 facts=[54]
    #[55] divides (p ^ k * p) (p ^ k * m)
  ← rule divides_mult_left goal=55
qed

theorem primepow_divisors_divides
  fixes m :: nat, n :: nat
  prop divides m n <--> (!p. !k::nat. prime p --> divides (p ^ k) m --> divides (p ^ k) n)
  [hint_rewrite]

theorem primepow_divisors_eq
  fixes m :: nat, n :: nat
  prop m = n <--> (!p. !k::nat. prime p --> divides (p ^ k) m <--> divides (p ^ k) n)
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] m = n ⟶ (∀p. ∀k::nat. prime p ⟶ divides (p ^ k) m ⟷ divides (p ^ k) n)
    #[2] (∀p. ∀k::nat. prime p ⟶ divides (p ^ k) m ⟷ divides (p ^ k) n) ⟶ m = n
  ← intro goal=1
    #[3] m = n
    #[4] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) m ⟷ divides (p ^ k) n
  ← rewrite source=prev goal=4 facts=[3]
    #[5] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) n ⟷ divides (p ^ k) n
  ← intro p,k goal=5
    #[6] _VAR p
    #[7] _VAR k
    #[8] prime p
    #[9] divides (p ^ k) n ⟷ divides (p ^ k) n
  ← rule eq_refl goal=9
    #[10] divides (p ^ k) n ⟷ divides (p ^ k) n
  ← intro goal=2
    #[11] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) m ⟷ divides (p ^ k) n
    #[12] m = n
  ← rule divides_antisym goal=12
    #[13] divides m n
    #[14] divides n m
  ← rewrite primepow_divisors_divides sym=false goal=13
    #[15] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) m ⟶ divides (p ^ k) n
  ← intro p,k goal=15
    #[16] divides (p ^ k) m
    #[17] divides (p ^ k) n
  → forward goal=17 facts=[11,8]
    #[18] ∀k::nat. divides (p ^ k) m ⟷ divides (p ^ k) n
  → rewrite target=fact source=prev goal=17 facts=[18,16]
  ← rewrite primepow_divisors_divides sym=false goal=14
    #[19] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) n ⟶ divides (p ^ k) m
  ← intro p,k goal=19
    #[20] divides (p ^ k) n
    #[21] divides (p ^ k) m
  → forward goal=21 facts=[11,8]
  ← rewrite source=prev goal=21 facts=[18]
qed

theorem finite_exp_le
  fixes P :: nat => bool, n :: nat, p :: nat
  prop 2 <= p --> finite ({j. P j & p ^ j <= n})
  [hint_backward]

theorem finite_indices
  fixes P :: nat => bool, n :: nat, p :: nat
  prop 2 <= p --> ~(n = 0) --> finite ({j. P j & divides (p ^ j) n})
  [hint_backward,hint_backward1]

def index :: nat ⇒ nat ⇒ nat = index p n = (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) [hint_rewrite]

theorem index_0
  fixes p :: nat
  prop index p 0 = 0
  [hint_rewrite]
proof
  ← rewrite index_def sym=false goal=0
    #[1] (if p ≤ 1 ∨ (0::nat) = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) 0})) = 0
  cut "p ≤ 1 ∨ (0::nat) = 0" goal=1
    #[2] p ≤ 1 ∨ (0::nat) = 0
  ← rule disjI2 goal=2
    #[3] (0::nat) = 0
  ← rule eq_refl goal=3
  ← rewrite if_P sym=false goal=1 facts=[2]
qed

theorem primepow_divides_index
  fixes k :: nat, n :: nat, p :: nat
  prop divides (p ^ k) n <--> n = 0 | p = 1 | k <= index p n
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
    #[2] ¬(n = 0) ⟶ divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← intro goal=1
    #[3] n = 0
    #[4] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rewrite source=prev goal=4 facts=[3]
    #[5] divides (p ^ k) 0 ⟷ (0::nat) = 0 ∨ p = 1 ∨ k ≤ index p 0
  ← rule iffI goal=5
    #[6] divides (p ^ k) 0 ⟶ (0::nat) = 0 ∨ p = 1 ∨ k ≤ index p 0
    #[7] (0::nat) = 0 ∨ p = 1 ∨ k ≤ index p 0 ⟶ divides (p ^ k) 0
  ← intro goal=6
    #[8] divides (p ^ k) 0
    #[9] (0::nat) = 0 ∨ p = 1 ∨ k ≤ index p 0
  ← rule disjI1 goal=9
    #[10] (0::nat) = 0
  ← rule eq_refl goal=10
    #[11] (0::nat) = 0
  ← intro goal=7
    #[12] (0::nat) = 0 ∨ p = 1 ∨ k ≤ index p 0
    #[13] divides (p ^ k) 0
  ← rule divides_zero goal=13
    #[14] divides (p ^ k) 0
  ← intro goal=2
    #[15] ¬(n = 0)
    #[16] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← cases "p=0" goal=16
    #[17] p = 0 ⟶ divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
    #[18] ¬(p = 0) ⟶ divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← intro goal=17
    #[19] p = 0
    #[20] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rewrite source=prev goal=20 facts=[19]
    #[21] divides (0 ^ k) n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← rewrite exp_zero sym=false goal=21
    #[22] divides (if k = 0 then 1 else 0) n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← cases "k = 0" goal=22
    #[23] k = 0 ⟶ divides (if k = 0 then 1 else 0) n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
    #[24] ¬(k = 0) ⟶ divides (if k = 0 then 1 else 0) n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← intro goal=23
    #[25] k = 0
    #[26] divides (if k = 0 then 1 else 0) n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← rewrite if_P sym=false goal=26 facts=[25]
    #[27] divides 1 n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← rewrite source=prev goal=27 facts=[25]
    #[28] divides 1 n ⟷ n = 0 ∨ (0::nat) = 1 ∨ 0 ≤ index 0 n
  ← rule iffI goal=28
    #[29] divides 1 n ⟶ n = 0 ∨ (0::nat) = 1 ∨ 0 ≤ index 0 n
    #[30] n = 0 ∨ (0::nat) = 1 ∨ 0 ≤ index 0 n ⟶ divides 1 n
  ← intro goal=29
    #[31] divides 1 n
    #[32] n = 0 ∨ (0::nat) = 1 ∨ 0 ≤ index 0 n
  ← rule disjI2 goal=32
    #[33] (0::nat) = 1 ∨ 0 ≤ index 0 n
  ← rule disjI2 goal=33
    #[34] 0 ≤ index 0 n
  ← rule le_0 goal=34
    #[35] 0 ≤ index 0 n
  ← intro goal=30
    #[36] n = 0 ∨ (0::nat) = 1 ∨ 0 ≤ index 0 n
    #[37] divides 1 n
  ← rule divides_1 goal=37
    #[38] divides 1 n
  ← intro goal=24
    #[39] ¬(k = 0)
    #[40] divides (if k = 0 then 1 else 0) n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← rewrite if_not_P sym=false goal=40 facts=[39]
    #[41] divides 0 n ⟷ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← rule iffI goal=41
    #[42] divides 0 n ⟶ n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
    #[43] n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n ⟶ divides 0 n
  ← intro goal=42
    #[44] divides 0 n
    #[45] n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  → rewrite target=fact divides_0 sym=false goal=45 facts=[44]
    #[46] n = 0
  ← rule negE_gen goal=45 facts=[15,46]
    #[47] n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
  ← intro goal=43
    #[48] n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n
    #[49] divides 0 n
  cut "~(n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n)" goal=49
    #[50] ¬(n = 0 ∨ (0::nat) = 1 ∨ k ≤ index 0 n)
  ← rewrite de_morgan_thm2 sym=false goal=50
    #[51] ¬(n = 0) ∧ ¬((0::nat) = 1 ∨ k ≤ index 0 n)
  ← rewrite de_morgan_thm2 sym=false goal=51
    #[52] ¬(n = 0) ∧ ¬((0::nat) = 1) ∧ ¬(k ≤ index 0 n)
  ← rule conjI goal=52
    #[53] ¬((0::nat) = 1) ∧ ¬(k ≤ index 0 n)
  ← rule conjI goal=53
    #[54] ¬((0::nat) = 1)
    #[55] ¬(k ≤ index 0 n)
  nat_const_ineq goal=54
    #[56] ¬((0::nat) = 1)
  ← rewrite index_def sym=false goal=55
    #[57] ¬(k ≤ (if (0::nat) ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (0 ^ j) n})))
  cut "(0::nat) ≤ 1 ∨ n = 0" goal=57
    #[58] (0::nat) ≤ 1 ∨ n = 0
  ← rule disjI1 goal=58
    #[59] (0::nat) ≤ 1
  ← rule le_0 goal=59
    #[60] (0::nat) ≤ 1
  ← rewrite if_P sym=false goal=57 facts=[58]
    #[61] ¬(k ≤ 0)
  ← rewrite not_le sym=false goal=61
    #[62] 0 < k
  ← rule le_1_1 goal=62
  ← rule negE_gen goal=49 facts=[50,48]
  ← intro goal=18
    #[63] ¬(p = 0)
    #[64] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← cases "p = 1" goal=64
    #[65] p = 1 ⟶ divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
    #[66] ¬(p = 1) ⟶ divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← intro goal=65
    #[67] p = 1
    #[68] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rule iffI goal=68
    #[69] divides (p ^ k) n ⟶ n = 0 ∨ p = 1 ∨ k ≤ index p n
    #[70] n = 0 ∨ p = 1 ∨ k ≤ index p n ⟶ divides (p ^ k) n
  ← intro goal=69
    #[71] divides (p ^ k) n
    #[72] n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rule disjI2 goal=72
    #[73] p = 1 ∨ k ≤ index p n
  ← rule disjI1 goal=73
  ← intro goal=70
    #[74] n = 0 ∨ p = 1 ∨ k ≤ index p n
    #[75] divides (p ^ k) n
  ← rewrite source=prev goal=75 facts=[67]
    #[76] divides (1 ^ k) n
  ← rewrite exp_one sym=false goal=76
    #[77] divides 1 n
  ← rule divides_1 goal=77
  ← intro goal=66
    #[78] ¬(p = 1)
    #[79] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  cut "2 <= p" goal=79
    #[80] 2 ≤ p
  ← z3 goal=80 facts=[78,63]
    #[81] 2 ≤ p
  → forward factorization_index param_n=n param_p=p goal=79
    #[82] ¬(n = 0) ⟶ 2 ≤ p ⟶ (∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n)))
  → forward goal=79 facts=[82,15]
    #[83] 2 ≤ p ⟶ (∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n)))
  → forward goal=79 facts=[83,81]
    #[84] ∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n))
  elim a goal=79 facts=[84]
    #[85] _VAR a
    #[86] divides (p ^ a) n ∧ (∀l. a < l ⟶ ¬(divides (p ^ l) n))
    #[87] divides (p ^ k) n ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  cut "!k. divides (p ^ k) n ⟷  k <= a" goal=87
    #[88] ∀k. divides (p ^ k) n ⟷ k ≤ a
  ← intro k1 goal=88
    #[89] _VAR k1
    #[90] divides (p ^ k1) n ⟷ k1 ≤ a
  ← rule iffI goal=90
    #[91] divides (p ^ k1) n ⟶ k1 ≤ a
    #[92] k1 ≤ a ⟶ divides (p ^ k1) n
  ← intro goal=91
    #[93] divides (p ^ k1) n
    #[94] k1 ≤ a
  → forward conjD2 goal=94 facts=[86]
    #[95] ∀l. a < l ⟶ ¬(divides (p ^ l) n)
  ← rewrite not_lt sym=true goal=94
    #[96] ¬(a < k1)
  ← rule contrapositive goal=96 facts=[93]
    #[97] ¬¬(a < k1) ⟶ ¬(divides (p ^ k1) n)
  ← rewrite double_neg sym=false goal=97
    #[98] a < k1 ⟶ ¬(divides (p ^ k1) n)
  ← intro goal=98
    #[99] a < k1
    #[100] ¬(divides (p ^ k1) n)
  ← apply_prev goal=100 facts=[95]
  ← intro goal=92
    #[101] k1 ≤ a
    #[102] divides (p ^ k1) n
  → forward conjD1 goal=102 facts=[86]
    #[103] divides (p ^ a) n
  ← rule divides_trans param_m="(p ^ a)" goal=102
    #[104] divides (p ^ k1) (p ^ a)
  ← rule divides_exp_le_imp goal=104
  ← rewrite source=prev goal=87 facts=[88]
    #[105] k ≤ a ⟷ n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rewrite index_def sym=false goal=105
    #[106] k ≤ a ⟷ n = 0 ∨ p = 1 ∨ k ≤ (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}))
  cut "~(p ≤ 1 ∨ n = 0)" goal=106
    #[107] ¬(p ≤ 1 ∨ n = 0)
  ← rewrite de_morgan_thm2 sym=false goal=107
    #[108] ¬(p ≤ 1) ∧ ¬(n = 0)
  ← rule conjI goal=108
    #[109] ¬(p ≤ 1)
  ← z3 goal=109 facts=[78,63]
    #[110] ¬(p ≤ 1)
  ← rewrite if_not_P sym=false goal=106 facts=[107]
    #[111] k ≤ a ⟷ n = 0 ∨ p = 1 ∨ k ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})
  ← rewrite source=prev goal=111 facts=[88]
    #[112] k ≤ a ⟷ n = 0 ∨ p = 1 ∨ k ≤ card ({j. 1 ≤ j ∧ j ≤ a})
  ← rewrite nat_interval_def sym=true goal=112
    #[113] k ≤ a ⟷ n = 0 ∨ p = 1 ∨ k ≤ card ({1..a})
  ← rewrite card_natseg_1 sym=false goal=113
    #[114] k ≤ a ⟷ n = 0 ∨ p = 1 ∨ k ≤ a
  ← rule iffI goal=114
    #[115] k ≤ a ⟶ n = 0 ∨ p = 1 ∨ k ≤ a
    #[116] n = 0 ∨ p = 1 ∨ k ≤ a ⟶ k ≤ a
  ← intro goal=115
    #[117] k ≤ a
    #[118] n = 0 ∨ p = 1 ∨ k ≤ a
  ← rule disjI2 goal=118
    #[119] p = 1 ∨ k ≤ a
  ← rule disjI2 goal=119
  ← intro goal=116
    #[120] n = 0 ∨ p = 1 ∨ k ≤ a
    #[121] k ≤ a
  ← rule disjE goal=121 facts=[120]
    #[122] n = 0 ⟶ k ≤ a
    #[123] p = 1 ∨ k ≤ a ⟶ k ≤ a
  ← intro goal=122
    #[124] k ≤ a
  ← rule negE_gen goal=124 facts=[15,3]
    #[125] k ≤ a
  ← intro goal=123
    #[126] p = 1 ∨ k ≤ a
    #[127] k ≤ a
  ← rule disjE goal=127 facts=[126]
    #[128] p = 1 ⟶ k ≤ a
    #[129] k ≤ a ⟶ k ≤ a
  ← intro goal=128
    #[130] k ≤ a
  ← rule negE_gen goal=130 facts=[78,67]
    #[131] k ≤ a
qed

theorem le_index
  fixes k :: nat, n :: nat, p :: nat
  prop k <= index p n <--> (n = 0 | p = 1 --> k = 0) & divides (p ^ k) n
  [hint_rewrite]
proof
  ← rewrite primepow_divides_index sym=false goal=0
    #[1] k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← cases "n=0" goal=1
    #[2] n = 0 ⟶ k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
    #[3] ¬(n = 0) ⟶ k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← intro goal=2
    #[4] n = 0
    #[5] k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← rewrite source=prev goal=5 facts=[4]
    #[6] k ≤ index p 0 ⟷ ((0::nat) = 0 ∨ p = 1 ⟶ k = 0) ∧ ((0::nat) = 0 ∨ p = 1 ∨ k ≤ index p 0)
  ← rewrite index_0 sym=false goal=6
    #[7] k ≤ 0 ⟷ ((0::nat) = 0 ∨ p = 1 ⟶ k = 0) ∧ ((0::nat) = 0 ∨ p = 1 ∨ k ≤ 0)
  ← rule iffI goal=7
    #[8] k ≤ 0 ⟶ ((0::nat) = 0 ∨ p = 1 ⟶ k = 0) ∧ ((0::nat) = 0 ∨ p = 1 ∨ k ≤ 0)
    #[9] ((0::nat) = 0 ∨ p = 1 ⟶ k = 0) ∧ ((0::nat) = 0 ∨ p = 1 ∨ k ≤ 0) ⟶ k ≤ 0
  ← intro goal=8
    #[10] k ≤ 0
    #[11] ((0::nat) = 0 ∨ p = 1 ⟶ k = 0) ∧ ((0::nat) = 0 ∨ p = 1 ∨ k ≤ 0)
  ← rule conjI goal=11
    #[12] (0::nat) = 0 ∨ p = 1 ⟶ k = 0
    #[13] (0::nat) = 0 ∨ p = 1 ∨ k ≤ 0
  ← intro goal=12
    #[14] (0::nat) = 0 ∨ p = 1
    #[15] k = 0
  → forward nat_le_zero goal=15 facts=[10]
    #[16] k = 0
  ← rule disjI2 goal=13
    #[17] p = 1 ∨ k ≤ 0
  ← rule disjI2 goal=17
  ← intro goal=9
    #[18] ((0::nat) = 0 ∨ p = 1 ⟶ k = 0) ∧ ((0::nat) = 0 ∨ p = 1 ∨ k ≤ 0)
    #[19] k ≤ 0
  → forward conjD1 goal=19 facts=[18]
    #[20] (0::nat) = 0 ∨ p = 1 ⟶ k = 0
  → forward conjD2 goal=19 facts=[18]
    #[21] (0::nat) = 0 ∨ p = 1 ∨ k ≤ 0
  → forward disj_assoc goal=19 facts=[21]
    #[22] ((0::nat) = 0 ∨ p = 1) ∨ k ≤ 0
  ← rule disjE goal=19 facts=[22]
    #[23] (0::nat) = 0 ∨ p = 1 ⟶ k ≤ 0
    #[24] k ≤ 0 ⟶ k ≤ 0
  ← intro goal=23
    #[25] k ≤ 0
  → forward goal=25 facts=[20,14]
    #[26] k = 0
  ← rewrite nat_less_eq_def_1 sym=false goal=25
  ← intro goal=3
    #[27] ¬(n = 0)
    #[28] k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← cases "p=1" goal=28
    #[29] p = 1 ⟶ k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
    #[30] ¬(p = 1) ⟶ k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← intro goal=29
    #[31] p = 1
    #[32] k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← rewrite index_def sym=false goal=32
    #[33] k ≤ (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})))
  cut "p ≤ 1 ∨ n = 0" goal=33
    #[34] p ≤ 1 ∨ n = 0
  ← rule disjI1 goal=34
    #[35] p ≤ 1
  ← rewrite le_lt sym=false goal=35
    #[36] p < 1 ∨ p = 1
  ← rule disjI2 goal=36
  ← rewrite if_P sym=false goal=33 facts=[34]
    #[37] k ≤ 0 ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ 0)
  ← rewrite nat_less_eq_def_1 sym=false goal=37
    #[38] k = 0 ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k = 0)
  ← rule iffI goal=38
    #[39] k = 0 ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k = 0)
    #[40] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k = 0) ⟶ k = 0
  ← intro goal=39
    #[41] k = 0
    #[42] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k = 0)
  ← rule conjI goal=42
    #[43] n = 0 ∨ p = 1 ⟶ k = 0
    #[44] n = 0 ∨ p = 1 ∨ k = 0
  ← intro goal=43
    #[45] n = 0 ∨ p = 1
  ← rule disjI2 goal=44
    #[46] p = 1 ∨ k = 0
  ← rule disjI2 goal=46
  ← intro goal=40
    #[47] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k = 0)
    #[48] k = 0
  → forward conjD1 goal=48 facts=[47]
    #[49] n = 0 ∨ p = 1 ⟶ k = 0
  → forward conjD2 goal=48 facts=[47]
    #[50] n = 0 ∨ p = 1 ∨ k = 0
  → forward disj_assoc goal=48 facts=[50]
    #[51] (n = 0 ∨ p = 1) ∨ k = 0
  ← rule disjE goal=48 facts=[51]
    #[52] k = 0 ⟶ k = 0
  ← intro goal=30
    #[53] ¬(p = 1)
    #[54] k ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← rule iffI goal=54
    #[55] k ≤ index p n ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
    #[56] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n) ⟶ k ≤ index p n
  ← intro goal=55
    #[57] k ≤ index p n
    #[58] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← rule conjI goal=58
    #[59] n = 0 ∨ p = 1 ⟶ k = 0
    #[60] n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← intro goal=59
    #[61] k = 0
  ← rule contrapositive goal=61 facts=[45]
    #[62] ¬(k = 0) ⟶ ¬(n = 0 ∨ p = 1)
  ← intro goal=62
    #[63] ¬(k = 0)
    #[64] ¬(n = 0 ∨ p = 1)
  ← rewrite de_morgan_thm2 sym=false goal=64
    #[65] ¬(n = 0) ∧ ¬(p = 1)
  ← rule conjI goal=65
  ← rule disjI2 goal=60
    #[66] p = 1 ∨ k ≤ index p n
  ← rule disjI2 goal=66
  ← intro goal=56
    #[67] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
    #[68] k ≤ index p n
  → forward conjD1 goal=68 facts=[67]
    #[69] n = 0 ∨ p = 1 ⟶ k = 0
  → forward conjD2 goal=68 facts=[67]
    #[70] n = 0 ∨ p = 1 ∨ k ≤ index p n
  → forward disj_assoc goal=68 facts=[70]
    #[71] (n = 0 ∨ p = 1) ∨ k ≤ index p n
  ← rule disjE goal=68 facts=[71]
    #[72] n = 0 ∨ p = 1 ⟶ k ≤ index p n
    #[73] k ≤ index p n ⟶ k ≤ index p n
  ← intro goal=72
    #[74] k ≤ index p n
  → forward goal=74 facts=[69,45]
    #[75] k = 0
  ← rewrite source=prev goal=74 facts=[75]
    #[76] 0 ≤ index p n
  ← rule le_0 goal=76
    #[77] 0 ≤ index p n
qed

theorem exp_index_divides
  fixes n :: nat, p :: nat
  prop divides (p ^ index p n) n
  [hint_backward]
proof
  → forward le_index param_k="index p n" param_n=n param_p=p goal=0
    #[1] index p n ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ index p n = 0) ∧ divides (p ^ index p n) n
  cut "index p n ≤ index p n" goal=0
    #[2] index p n ≤ index p n
  ← rule less_eq_refl goal=2
  → rewrite target=fact source=prev goal=0 facts=[1,2]
    #[3] (n = 0 ∨ p = 1 ⟶ index p n = 0) ∧ divides (p ^ index p n) n
  ← rule conjD2 goal=0 facts=[3]
qed

theorem index_1
  fixes p :: nat
  prop index p 1 = 0
  [hint_rewrite]

theorem index_mul
  fixes m :: nat, n :: nat, p :: nat
  prop prime p --> ~(m = 0) --> ~(n = 0) --> index p (m * n) = index p m + index p n
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ¬(m = 0)
    #[3] ¬(n = 0)
    #[4] index p (m * n) = index p m + index p n
  ← rewrite le_antisym sym=true goal=4
    #[5] index p (m * n) ≤ index p m + index p n ∧ index p m + index p n ≤ index p (m * n)
  → rewrite target=fact prime_def sym=false goal=5 facts=[1]
    #[6] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=5 facts=[6]
    #[7] ¬(p = 1)
  ← rule conjI goal=5
    #[8] index p (m * n) ≤ index p m + index p n
    #[9] index p m + index p n ≤ index p (m * n)
  cut "!m. !n. (∀k::nat. k ≤ m ⟶ k ≤ n) ⟶ m ≤ n" goal=8
    #[10] ∀m::nat. ∀n. (∀k. k ≤ m ⟶ k ≤ n) ⟶ m ≤ n
  ← z3 goal=10
    #[11] ∀m::nat. ∀n. (∀k. k ≤ m ⟶ k ≤ n) ⟶ m ≤ n
  ← apply_prev goal=8 facts=[11]
    #[12] ∀k. k ≤ index p (m * n) ⟶ k ≤ index p m + index p n
  ← intro k goal=12
    #[13] _VAR k
    #[14] k ≤ index p (m * n)
    #[15] k ≤ index p m + index p n
  → forward primepow_divides_prod param_k=k param_m=m param_n=n goal=15 facts=[1]
    #[16] divides (p ^ k) (m * n) ⟶ (∃i. ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j)
  → rewrite target=fact le_index sym=false goal=15 facts=[14]
    #[17] (m * n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) (m * n)
  → forward conjD2 goal=15 facts=[17]
    #[18] divides (p ^ k) (m * n)
  → forward goal=15 facts=[16,18]
    #[19] ∃i. ∃j. divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
  elim i,j goal=15 facts=[19]
    #[20] _VAR i
    #[21] _VAR j
    #[22] divides (p ^ i) m ∧ divides (p ^ j) n ∧ k = i + j
    #[23] k ≤ index p m + index p n
  → forward conjD1 goal=23 facts=[22]
    #[24] divides (p ^ i) m
  → forward conjD2 goal=23 facts=[22]
    #[25] divides (p ^ j) n ∧ k = i + j
  → forward conjD1 goal=23 facts=[25]
    #[26] divides (p ^ j) n
  → forward conjD2 goal=23 facts=[25]
    #[27] k = i + j
  ← rewrite source=prev goal=23 facts=[27]
    #[28] i + j ≤ index p m + index p n
  ← rule le_add2 goal=28
    #[29] i ≤ index p m
    #[30] j ≤ index p n
  ← rewrite le_index sym=false goal=29
    #[31] (m = 0 ∨ p = 1 ⟶ i = 0) ∧ divides (p ^ i) m
  ← rule conjI goal=31
    #[32] m = 0 ∨ p = 1 ⟶ i = 0
  ← intro goal=32
    #[33] m = 0 ∨ p = 1
    #[34] i = 0
  ← rule contrapositive goal=34 facts=[33]
    #[35] ¬(i = 0) ⟶ ¬(m = 0 ∨ p = 1)
  ← intro goal=35
    #[36] ¬(i = 0)
    #[37] ¬(m = 0 ∨ p = 1)
  ← rewrite de_morgan_thm2 sym=false goal=37
    #[38] ¬(m = 0) ∧ ¬(p = 1)
  ← rule conjI goal=38
  ← rewrite le_index sym=false goal=30
    #[39] (n = 0 ∨ p = 1 ⟶ j = 0) ∧ divides (p ^ j) n
  ← rule conjI goal=39
    #[40] n = 0 ∨ p = 1 ⟶ j = 0
  ← intro goal=40
    #[41] n = 0 ∨ p = 1
    #[42] j = 0
  ← rule contrapositive goal=42 facts=[41]
    #[43] ¬(j = 0) ⟶ ¬(n = 0 ∨ p = 1)
  ← intro goal=43
    #[44] ¬(j = 0)
    #[45] ¬(n = 0 ∨ p = 1)
  ← rewrite de_morgan_thm2 sym=false goal=45
    #[46] ¬(n = 0) ∧ ¬(p = 1)
  ← rule conjI goal=46
  ← rewrite le_index sym=false goal=9
    #[47] (m * n = 0 ∨ p = 1 ⟶ index p m + index p n = 0) ∧ divides (p ^ (index p m + index p n)) (m * n)
  ← rule conjI goal=47
    #[48] m * n = 0 ∨ p = 1 ⟶ index p m + index p n = 0
    #[49] divides (p ^ (index p m + index p n)) (m * n)
  ← intro goal=48
    #[50] m * n = 0 ∨ p = 1
    #[51] index p m + index p n = 0
  → rewrite target=fact mult_eq_0 sym=false goal=51 facts=[50]
    #[52] (m = 0 ∨ n = 0) ∨ p = 1
  ← rule contrapositive goal=51 facts=[52]
    #[53] ¬(index p m + index p n = 0) ⟶ ¬((m = 0 ∨ n = 0) ∨ p = 1)
  ← intro goal=53
    #[54] ¬(index p m + index p n = 0)
    #[55] ¬((m = 0 ∨ n = 0) ∨ p = 1)
  ← rewrite de_morgan_thm2 sym=false goal=55
    #[56] ¬(m = 0 ∨ n = 0) ∧ ¬(p = 1)
  ← rewrite de_morgan_thm2 sym=false goal=56
    #[57] (¬(m = 0) ∧ ¬(n = 0)) ∧ ¬(p = 1)
  ← rule conjI goal=57
    #[58] ¬(m = 0) ∧ ¬(n = 0)
  ← rule conjI goal=58
  ← rewrite nat_power_add sym=false goal=49
    #[59] divides (p ^ index p m * p ^ index p n) (m * n)
  ← rule divides_mul2 goal=59
    #[60] divides (p ^ index p m) m
    #[61] divides (p ^ index p n) n
  ← rule exp_index_divides goal=60
    #[62] divides (p ^ index p m) m
  ← rule exp_index_divides goal=61
    #[63] divides (p ^ index p n) n
qed

theorem index_exp
  fixes k :: nat, n :: nat, p :: nat
  prop prime p --> index p (n ^ k) = k * index p n
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] index p (n ^ k) = k * index p n
  ← cases "n=0" goal=2
    #[3] n = 0 ⟶ index p (n ^ k) = k * index p n
    #[4] ¬(n = 0) ⟶ index p (n ^ k) = k * index p n
  ← intro goal=3
    #[5] n = 0
    #[6] index p (n ^ k) = k * index p n
  ← rewrite source=prev goal=6 facts=[5]
    #[7] index p (0 ^ k) = k * index p 0
  ← rewrite exp_zero sym=false goal=7
    #[8] index p (if k = 0 then 1 else 0) = k * index p 0
  ← cases "k = 0" goal=8
    #[9] k = 0 ⟶ index p (if k = 0 then 1 else 0) = k * index p 0
    #[10] ¬(k = 0) ⟶ index p (if k = 0 then 1 else 0) = k * index p 0
  ← intro goal=9
    #[11] k = 0
    #[12] index p (if k = 0 then 1 else 0) = k * index p 0
  ← rewrite if_P sym=false goal=12 facts=[11]
    #[13] index p 1 = k * index p 0
  ← rewrite index_1 sym=false goal=13
    #[14] 0 = k * index p 0
  ← rewrite index_0 sym=false goal=14
    #[15] 0 = k * 0
  ← rewrite mult_0_right sym=false goal=15
    #[16] 0 = k * 0
  ← intro goal=10
    #[17] ¬(k = 0)
    #[18] index p (if k = 0 then 1 else 0) = k * index p 0
  ← rewrite if_not_P sym=false goal=18 facts=[17]
    #[19] index p 0 = k * index p 0
  ← rewrite index_0 sym=false goal=19
    #[20] 0 = k * 0
  ← rewrite mult_0_right sym=false goal=20
  ← intro goal=4
    #[21] ¬(n = 0)
    #[22] index p (n ^ k) = k * index p n
  ← induct k nat_induct goal=22
    #[23] index p (n ^ (0::nat)) = 0 * index p n
    #[24] ∀n1. index p (n ^ n1) = n1 * index p n ⟶ index p (n ^ Suc n1) = Suc n1 * index p n
  ← rewrite nat_nat_power_def_1 sym=false goal=23
    #[25] index p 1 = 0 * index p n
  ← rewrite nat_times_def_1 sym=false goal=25
    #[26] index p 1 = 0
  ← rewrite index_1 sym=false goal=26
    #[27] index p 1 = 0
  ← intro k goal=24
    #[28] _VAR k
    #[29] index p (n ^ k) = k * index p n
    #[30] index p (n ^ Suc k) = Suc k * index p n
  ← rewrite nat_nat_power_def_2 sym=false goal=30
    #[31] index p (n * n ^ k) = Suc k * index p n
  ← rewrite add_1_right sym=true goal=31
    #[32] index p (n * n ^ k) = (k + 1) * index p n
  ← rewrite distrib_r sym=false goal=32
    #[33] index p (n * n ^ k) = k * index p n + 1 * index p n
  ← rewrite mult_1_left sym=false goal=33
    #[34] index p (n * n ^ k) = k * index p n + index p n
  → rewrite target=fact eq_sym_eq sym=false goal=34 facts=[29]
    #[35] k * index p n = index p (n ^ k)
  ← rewrite source=prev goal=34 facts=[35]
    #[36] index p (n * n ^ k) = index p (n ^ k) + index p n
  ← rewrite mult_comm sym=false goal=36
    #[37] index p (n ^ k * n) = index p (n ^ k) + index p n
  ← rule index_mul goal=37
    #[38] ¬(n ^ k = 0)
  ← rewrite exp_eq_0 sym=false goal=38
    #[39] ¬(n = 0 ∧ ¬(k = 0))
  ← rewrite de_morgan_thm1 sym=false goal=39
    #[40] ¬(n = 0) ∨ ¬¬(k = 0)
  ← rule disjI1 goal=40
qed

theorem index_fact
  fixes n :: nat, p :: nat
  prop prime p --> index p (fact n) = nsum ({1..n}) (%m. index p m)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] index p (fact n) = nsum ({1..n}) (λm. index p m)
  ← induct n nat_induct goal=2
    #[3] index p (fact 0) = nsum ({1..0}) (λm. index p m)
    #[4] ∀n. index p (fact n) = nsum ({1..n}) (λm. index p m) ⟶ index p (fact (Suc n)) = nsum ({1..Suc n}) (λm. index p m)
  ← rewrite fact_def_1 sym=false goal=3
    #[5] index p 1 = nsum ({1..0}) (λm. index p m)
  ← rewrite index_1 sym=false goal=5
    #[6] 0 = nsum ({1..0}) (λm. index p m)
  ← rewrite eq_sym_eq sym=false goal=6
    #[7] nsum ({1..0}) (λm. index p m) = 0
  ← rewrite nsum_eq_0_iff_natseg sym=false goal=7
    #[8] ∀i. 1 ≤ i ⟶ i ≤ 0 ⟶ index p i = 0
  ← z3 goal=8
    #[9] ∀i. 1 ≤ i ⟶ i ≤ 0 ⟶ index p i = 0
  ← intro n goal=4
    #[10] _VAR n
    #[11] index p (fact n) = nsum ({1..n}) (λm. index p m)
    #[12] index p (fact (Suc n)) = nsum ({1..Suc n}) (λm. index p m)
  ← rewrite fact_def_2 sym=false goal=12
    #[13] index p (Suc n * fact n) = nsum ({1..Suc n}) (λm. index p m)
  ← rewrite nsum_Suc_natseg sym=false goal=13
    #[14] index p (Suc n * fact n) = (if 1 ≤ Suc n then nsum ({1..n}) (λm. index p m) + index p (Suc n) else nsum ({1..n}) (λm. index p m))
  cut "1 ≤ Suc n" goal=14
    #[15] 1 ≤ Suc n
  ← rule le_1_2 goal=15
    #[16] ¬(Suc n = 0)
  ← rule ineq_sym goal=16
    #[17] ¬(0 = Suc n)
  ← rule nat_zero_Suc_neq goal=17
    #[18] ¬(0 = Suc n)
  ← rewrite if_P sym=false goal=14 facts=[15]
    #[19] index p (Suc n * fact n) = nsum ({1..n}) (λm. index p m) + index p (Suc n)
  → rewrite target=fact eq_sym_eq sym=false goal=19 facts=[11]
    #[20] nsum ({1..n}) (λm. index p m) = index p (fact n)
  ← rewrite source=prev goal=19 facts=[20]
    #[21] index p (Suc n * fact n) = index p (fact n) + index p (Suc n)
  ← rewrite add_comm sym=false goal=21
    #[22] index p (Suc n * fact n) = index p (Suc n) + index p (fact n)
  ← rule index_mul goal=22
    #[23] ¬(fact n = 0)
  ← rule fact_nz goal=23
    #[24] ¬(fact n = 0)
qed

theorem index_fact_alt
  fixes n :: nat, p :: nat
  prop prime p --> index p (fact n) = nsum ({j::nat. 1 <= j & p ^ j <= n}) (%j. n DIV p ^ j)
  [hint_backward,hint_backward1]

theorem index_fact_unbounded
  fixes n :: nat, p :: nat
  prop prime p --> index p (fact n) = nsum ({j::nat. 1 <= j}) (%j. n DIV p ^ j)
  [hint_backward]

theorem primepow_divides_fact
  fixes k :: nat, n :: nat, p :: nat
  prop prime p --> divides (p ^ k) (fact n) <--> k <= nsum ({j::nat. 1 <= j & p ^ j <= n}) (%j. n DIV p ^ j)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] divides (p ^ k) (fact n) ⟷ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rewrite primepow_divides_index sym=false goal=2
    #[3] fact n = 0 ∨ p = 1 ∨ k ≤ index p (fact n) ⟷ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  → forward index_fact_alt param_n=n goal=3 facts=[1]
    #[4] index p (fact n) = nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rewrite source=prev goal=3 facts=[4]
    #[5] fact n = 0 ∨ p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j) ⟷ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rule iffI goal=5
    #[6] fact n = 0 ∨ p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j) ⟶ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
    #[7] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j) ⟶ fact n = 0 ∨ p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← intro goal=6
    #[8] fact n = 0 ∨ p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
    #[9] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rule disjE goal=9 facts=[8]
    #[10] fact n = 0 ⟶ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
    #[11] p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j) ⟶ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← intro goal=10
    #[12] fact n = 0
    #[13] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  → forward fact_nz param_n=n goal=13
    #[14] ¬(fact n = 0)
  ← rule negE_gen goal=13 facts=[14,12]
    #[15] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← intro goal=11
    #[16] p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
    #[17] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rule disjE goal=17 facts=[16]
    #[18] p = 1 ⟶ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
    #[19] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j) ⟶ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← intro goal=18
    #[20] p = 1
    #[21] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  → rewrite target=fact prime_def sym=false goal=21 facts=[1]
    #[22] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=21 facts=[22]
    #[23] ¬(p = 1)
  ← rule negE_gen goal=21 facts=[23,20]
    #[24] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← intro goal=7
    #[25] k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
    #[26] fact n = 0 ∨ p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rule disjI2 goal=26
    #[27] p = 1 ∨ k ≤ nsum ({j::nat. 1 ≤ j ∧ p ^ j ≤ n}) (λj. n DIV p ^ j)
  ← rule disjI2 goal=27
qed

theorem index_refl
  fixes n :: nat
  prop index n n = (if n <= 1 then 0 else 1)
  [hint_rewrite]
proof
  ← rewrite index_def sym=false goal=0
    #[1] (if n ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n})) = (if n ≤ 1 then 0 else 1)
  ← cases "n ≤ 1 ∨ n = 0" goal=1
    #[2] n ≤ 1 ∨ n = 0 ⟶ (if n ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n})) = (if n ≤ 1 then 0 else 1)
    #[3] ¬(n ≤ 1 ∨ n = 0) ⟶ (if n ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n})) = (if n ≤ 1 then 0 else 1)
  ← intro goal=2
    #[4] n ≤ 1 ∨ n = 0
    #[5] (if n ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n})) = (if n ≤ 1 then 0 else 1)
  ← rewrite if_P sym=false goal=5 facts=[4]
    #[6] (0::nat) = (if n ≤ 1 then 0 else 1)
  ← rule disjE goal=6 facts=[4]
    #[7] n ≤ 1 ⟶ (0::nat) = (if n ≤ 1 then 0 else 1)
    #[8] n = 0 ⟶ (0::nat) = (if n ≤ 1 then 0 else 1)
  ← intro goal=7
    #[9] n ≤ 1
    #[10] (0::nat) = (if n ≤ 1 then 0 else 1)
  ← rewrite if_P sym=false goal=10 facts=[9]
    #[11] (0::nat) = (if n ≤ 1 then 0 else 1)
  ← intro goal=8
    #[12] n = 0
    #[13] (0::nat) = (if n ≤ 1 then 0 else 1)
  cut "n <= 1" goal=13
    #[14] n ≤ 1
  ← z3 goal=14 facts=[12]
    #[15] n ≤ 1
  ← rewrite if_P sym=false goal=13 facts=[15]
    #[16] (0::nat) = (if n ≤ 1 then 0 else 1)
  ← intro goal=3
    #[17] ¬(n ≤ 1 ∨ n = 0)
    #[18] (if n ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n})) = (if n ≤ 1 then 0 else 1)
  ← rewrite if_not_P sym=false goal=18 facts=[17]
    #[19] card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n}) = (if n ≤ 1 then 0 else 1)
  → rewrite target=fact de_morgan_thm2 sym=false goal=19 facts=[17]
    #[20] ¬(n ≤ 1) ∧ ¬(n = 0)
  → forward conjD1 goal=19 facts=[20]
    #[21] ¬(n ≤ 1)
  ← rewrite if_not_P sym=false goal=19 facts=[21]
    #[22] card ({j::nat. 1 ≤ j ∧ divides (n ^ j) n}) = 1
  cut "2 <= n" goal=22
    #[23] 2 ≤ n
  ← z3 goal=23 facts=[21]
  → forward divides_exp_le param_m="" param_n="(1::nat)" goal=22 facts=[23]
    #[24] ∀m::nat. divides (n ^ m) (n ^ (1::nat)) ⟷ m ≤ 1
  → rewrite target=fact exp_1 sym=false goal=22 facts=[24]
    #[25] ∀m::nat. divides (n ^ m) n ⟷ m ≤ 1
  ← rewrite source=prev goal=22 facts=[25]
    #[26] card ({j::nat. 1 ≤ j ∧ j ≤ 1}) = 1
  ← rewrite nat_interval_def sym=true goal=26
    #[27] card ({1..1}) = 1
  ← rewrite card_natseg_1 sym=false goal=27
    #[28] card ({1..1}) = 1
qed

theorem index_eq_0
  fixes n :: nat, p :: nat
  prop index p n = 0 <--> n = 0 | p = 1 | ~(divides p n)
  [hint_rewrite]
proof
  cut "∀n::nat. n = 0 ⟷ ¬(1 ≤ n)" goal=0
    #[1] ∀n::nat. n = 0 ⟷ ¬(1 ≤ n)
  ← z3 goal=1
  ← rule iffI goal=0
    #[2] index p n = 0 ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[3] n = 0 ∨ p = 1 ∨ ¬(divides p n) ⟶ index p n = 0
  ← intro goal=2
    #[4] index p n = 0
    #[5] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  → rewrite target=fact source=prev goal=5 facts=[1,4]
    #[6] ¬(1 ≤ index p n)
  → rewrite target=fact le_index sym=false goal=5 facts=[6]
    #[7] ¬((n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∧ divides (p ^ (1::nat)) n)
  → rewrite target=fact exp_1 sym=false goal=5 facts=[7]
    #[8] ¬((n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∧ divides p n)
  → rewrite target=fact de_morgan_thm1 sym=false goal=5 facts=[8]
    #[9] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∨ ¬(divides p n)
  ← rule disjE goal=5 facts=[9]
    #[10] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[11] ¬(divides p n) ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← intro goal=10
    #[12] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0)
    #[13] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  → rewrite target=fact not_imp sym=false goal=13 facts=[12]
    #[14] (n = 0 ∨ p = 1) ∧ ¬((1::nat) = 0)
  → forward conjD1 goal=13 facts=[14]
    #[15] n = 0 ∨ p = 1
  ← rule disjE goal=13 facts=[15]
    #[16] n = 0 ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[17] p = 1 ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← intro goal=16
    #[18] n = 0
    #[19] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI1 goal=19 facts=[18]
    #[20] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← intro goal=17
    #[21] p = 1
    #[22] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=22
    #[23] p = 1 ∨ ¬(divides p n)
  ← rule disjI1 goal=23
  ← intro goal=11
    #[24] ¬(divides p n)
    #[25] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=25
    #[26] p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=26
  ← intro goal=3
    #[27] n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[28] index p n = 0
  ← rewrite source=prev goal=28 facts=[1]
    #[29] ¬(1 ≤ index p n)
  ← rewrite le_index sym=false goal=29
    #[30] ¬((n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∧ divides (p ^ (1::nat)) n)
  ← rewrite de_morgan_thm1 sym=false goal=30
    #[31] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∨ ¬(divides (p ^ (1::nat)) n)
  → forward disj_assoc goal=31 facts=[27]
    #[32] (n = 0 ∨ p = 1) ∨ ¬(divides p n)
  ← rule disjE goal=31 facts=[32]
    #[33] n = 0 ∨ p = 1 ⟶ ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∨ ¬(divides (p ^ (1::nat)) n)
    #[34] ¬(divides p n) ⟶ ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∨ ¬(divides (p ^ (1::nat)) n)
  ← intro goal=33
    #[35] n = 0 ∨ p = 1
    #[36] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∨ ¬(divides (p ^ (1::nat)) n)
  ← rule disjI1 goal=36
    #[37] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0)
  ← rewrite not_imp sym=false goal=37
    #[38] (n = 0 ∨ p = 1) ∧ ¬((1::nat) = 0)
  ← rule conjI goal=38
    #[39] ¬((1::nat) = 0)
  nat_const_ineq goal=39
    #[40] ¬((1::nat) = 0)
  ← intro goal=34
    #[41] ¬(n = 0 ∨ p = 1 ⟶ (1::nat) = 0) ∨ ¬(divides (p ^ (1::nat)) n)
  ← rule disjI2 goal=41
    #[42] ¬(divides (p ^ (1::nat)) n)
  ← rewrite exp_1 sym=false goal=42
qed

theorem index_zero
  fixes n :: nat, p :: nat
  prop ~(divides p n) --> index p n = 0
  [hint_backward]
proof
  ← intro goal=0
    #[1] ¬(divides p n)
    #[2] index p n = 0
  ← rewrite index_eq_0 sym=false goal=2
    #[3] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=3
    #[4] p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=4
qed

theorem index_pow
  fixes k :: nat, n :: nat, p :: nat
  prop index (p ^ k) n = index p n DIV k
  [hint_rewrite]
proof
  ← cases "k=0" goal=0
    #[1] k = 0 ⟶ index (p ^ k) n = index p n DIV k
    #[2] ¬(k = 0) ⟶ index (p ^ k) n = index p n DIV k
  ← intro goal=1
    #[3] k = 0
    #[4] index (p ^ k) n = index p n DIV k
  ← rewrite source=prev goal=4 facts=[3]
    #[5] index (p ^ (0::nat)) n = index p n DIV 0
  ← rewrite nat_nat_power_def_1 sym=false goal=5
    #[6] index 1 n = index p n DIV 0
  ← rewrite div_zero sym=false goal=6
    #[7] index 1 n = 0
  ← rewrite index_eq_0 sym=false goal=7
    #[8] n = 0 ∨ (1::nat) = 1 ∨ ¬(divides 1 n)
  ← rule disjI2 goal=8
    #[9] (1::nat) = 1 ∨ ¬(divides 1 n)
  ← rule disjI1 goal=9
    #[10] (1::nat) = 1
  ← rule eq_refl goal=10
    #[11] (1::nat) = 1
  ← intro goal=2
    #[12] ¬(k = 0)
    #[13] index (p ^ k) n = index p n DIV k
  cut "!m::nat. !n. (m = n) <--> !d. d <= m <--> d <= n" goal=13
    #[14] ∀m::nat. ∀n. m = n ⟷ (∀d. d ≤ m ⟷ d ≤ n)
  ← z3 goal=14
    #[15] ∀m::nat. ∀n. m = n ⟷ (∀d. d ≤ m ⟷ d ≤ n)
  ← rewrite source=prev goal=13 facts=[15]
    #[16] ∀d. d ≤ index (p ^ k) n ⟷ d ≤ index p n DIV k
  ← intro d goal=16
    #[17] _VAR d
    #[18] d ≤ index (p ^ k) n ⟷ d ≤ index p n DIV k
  → forward le_rdiv_eq param_b="index p n" param_n=d goal=18 facts=[12]
    #[19] d ≤ index p n DIV k ⟷ k * d ≤ index p n
  ← rewrite source=prev goal=18 facts=[19]
    #[20] d ≤ index (p ^ k) n ⟷ k * d ≤ index p n
  ← rewrite le_index sym=false goal=20
    #[21] (n = 0 ∨ p ^ k = 1 ⟶ d = 0) ∧ divides (p ^ k ^ d) n ⟷ (n = 0 ∨ p = 1 ⟶ k * d = 0) ∧ divides (p ^ (k * d)) n
  ← rule iffI goal=21
    #[22] (n = 0 ∨ p ^ k = 1 ⟶ d = 0) ∧ divides (p ^ k ^ d) n ⟶ (n = 0 ∨ p = 1 ⟶ k * d = 0) ∧ divides (p ^ (k * d)) n
    #[23] (n = 0 ∨ p = 1 ⟶ k * d = 0) ∧ divides (p ^ (k * d)) n ⟶ (n = 0 ∨ p ^ k = 1 ⟶ d = 0) ∧ divides (p ^ k ^ d) n
  ← intro goal=22
    #[24] (n = 0 ∨ p ^ k = 1 ⟶ d = 0) ∧ divides (p ^ k ^ d) n
    #[25] (n = 0 ∨ p = 1 ⟶ k * d = 0) ∧ divides (p ^ (k * d)) n
  → forward conjD1 goal=25 facts=[24]
    #[26] n = 0 ∨ p ^ k = 1 ⟶ d = 0
  → forward conjD2 goal=25 facts=[24]
    #[27] divides (p ^ k ^ d) n
  ← rule conjI goal=25
    #[28] n = 0 ∨ p = 1 ⟶ k * d = 0
    #[29] divides (p ^ (k * d)) n
  ← intro goal=28
    #[30] n = 0 ∨ p = 1
    #[31] k * d = 0
  ← rewrite mult_eq_0 sym=false goal=31
    #[32] k = 0 ∨ d = 0
  ← rule disjI2 goal=32
    #[33] d = 0
  ← apply_prev goal=33 facts=[26]
    #[34] n = 0 ∨ p ^ k = 1
  ← rule disjE goal=34 facts=[30]
    #[35] n = 0 ⟶ n = 0 ∨ p ^ k = 1
    #[36] p = 1 ⟶ n = 0 ∨ p ^ k = 1
  ← intro goal=35
    #[37] n = 0
    #[38] n = 0 ∨ p ^ k = 1
  ← rule disjI1 goal=38
  ← intro goal=36
    #[39] p = 1
    #[40] n = 0 ∨ p ^ k = 1
  ← rule disjI2 goal=40
    #[41] p ^ k = 1
  ← rewrite exp_eq_1 sym=false goal=41
    #[42] p = 1 ∨ k = 0
  ← rule disjI1 goal=42
  ← rewrite exp_mult sym=false goal=29
  ← intro goal=23
    #[43] (n = 0 ∨ p = 1 ⟶ k * d = 0) ∧ divides (p ^ (k * d)) n
    #[44] (n = 0 ∨ p ^ k = 1 ⟶ d = 0) ∧ divides (p ^ k ^ d) n
  → forward conjD1 goal=44 facts=[43]
    #[45] n = 0 ∨ p = 1 ⟶ k * d = 0
  → forward conjD2 goal=44 facts=[43]
    #[46] divides (p ^ (k * d)) n
  → rewrite target=fact exp_mult sym=false goal=44 facts=[46]
    #[47] divides (p ^ k ^ d) n
  ← rule conjI goal=44
    #[48] n = 0 ∨ p ^ k = 1 ⟶ d = 0
  ← intro goal=48
    #[49] n = 0 ∨ p ^ k = 1
    #[50] d = 0
  → rewrite target=fact mult_eq_0 sym=false goal=50 facts=[45]
    #[51] n = 0 ∨ p = 1 ⟶ k = 0 ∨ d = 0
  cut "n = 0 ∨ p = 1" goal=50
    #[52] n = 0 ∨ p = 1
  ← rule disjE goal=52 facts=[49]
    #[53] n = 0 ⟶ n = 0 ∨ p = 1
    #[54] p ^ k = 1 ⟶ n = 0 ∨ p = 1
  ← intro goal=53
    #[55] n = 0 ∨ p = 1
  ← rule disjI1 goal=55
  ← intro goal=54
    #[56] p ^ k = 1
    #[57] n = 0 ∨ p = 1
  ← rule disjI2 goal=57
    #[58] p = 1
  → rewrite target=fact exp_eq_1 sym=false goal=58 facts=[56]
    #[59] p = 1 ∨ k = 0
  ← rule disjE goal=58 facts=[59]
    #[60] p = 1 ⟶ p = 1
    #[61] k = 0 ⟶ p = 1
  ← intro goal=61
    #[62] p = 1
  ← rule negE_gen goal=62 facts=[12,3]
    #[63] p = 1
  → forward goal=50 facts=[51,52]
    #[64] k = 0 ∨ d = 0
  ← rule disjE goal=50 facts=[64]
    #[65] k = 0 ⟶ d = 0
    #[66] d = 0 ⟶ d = 0
  ← intro goal=65
    #[67] d = 0
  ← rule negE_gen goal=67 facts=[12,3]
    #[68] d = 0
qed

theorem index_prime
  fixes a :: nat, p :: nat
  prop prime p --> index a p = (if p = a then 1 else 0)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] index a p = (if p = a then 1 else 0)
  ← cases "p = a" goal=2
    #[3] p = a ⟶ index a p = (if p = a then 1 else 0)
    #[4] ¬(p = a) ⟶ index a p = (if p = a then 1 else 0)
  ← intro goal=3
    #[5] p = a
    #[6] index a p = (if p = a then 1 else 0)
  ← rewrite if_P sym=false goal=6 facts=[5]
    #[7] index a p = 1
  ← rewrite source=prev goal=7 facts=[5]
    #[8] index a a = 1
  ← rewrite index_refl sym=false goal=8
    #[9] (if a ≤ 1 then (0::nat) else 1) = 1
  cut "~(a ≤ 1)" goal=9
    #[10] ¬(a ≤ 1)
  → forward prime_ge_2 goal=10 facts=[1]
    #[11] 2 ≤ p
  → rewrite target=fact source=prev goal=10 facts=[5,11]
    #[12] 2 ≤ a
  ← z3 goal=10 facts=[12]
  ← rewrite if_not_P sym=false goal=9 facts=[10]
  ← intro goal=4
    #[13] ¬(p = a)
    #[14] index a p = (if p = a then 1 else 0)
  ← rewrite if_not_P sym=false goal=14 facts=[13]
    #[15] index a p = 0
  ← rewrite index_eq_0 sym=false goal=15
    #[16] p = 0 ∨ a = 1 ∨ ¬(divides a p)
  ← rule disjI2 goal=16
    #[17] a = 1 ∨ ¬(divides a p)
  → rewrite target=fact prime_def sym=false goal=17 facts=[1]
    #[18] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD2 goal=17 facts=[18]
    #[19] ∀x. divides x p ⟶ x = 1 ∨ x = p
  ← cases "a=1" goal=17
    #[20] a = 1 ⟶ a = 1 ∨ ¬(divides a p)
    #[21] ¬(a = 1) ⟶ a = 1 ∨ ¬(divides a p)
  ← intro goal=20
    #[22] a = 1
    #[23] a = 1 ∨ ¬(divides a p)
  ← rule disjI1 goal=23
  ← intro goal=21
    #[24] ¬(a = 1)
    #[25] a = 1 ∨ ¬(divides a p)
  ← rule disjI2 goal=25
    #[26] ¬(divides a p)
  ← rule contrapositive goal=26 facts=[24]
    #[27] ¬¬(divides a p) ⟶ ¬¬(a = 1)
  ← rewrite double_neg sym=false goal=27
    #[28] divides a p ⟶ a = 1
  ← intro goal=28
    #[29] divides a p
    #[30] a = 1
  → forward goal=30 facts=[19,29]
    #[31] a = 1 ∨ a = p
  ← rule disjE goal=30 facts=[31]
    #[32] a = 1 ⟶ a = 1
    #[33] a = p ⟶ a = 1
  ← intro goal=33
    #[34] a = p
    #[35] a = 1
  → rewrite target=fact eq_sym_eq sym=false goal=35 facts=[34]
    #[36] p = a
  ← rule negE_gen goal=35 facts=[13,36]
    #[37] a = 1
qed

theorem index_trivial_bound
  fixes n :: nat, p :: nat
  prop index p n <= n
  [hint_backward]
proof
  → forward primepow_divides_index param_k=n param_n=n param_p=p goal=0
    #[1] divides (p ^ n) n ⟷ n = 0 ∨ p = 1 ∨ n ≤ index p n
  ← rewrite index_def sym=false goal=0
    #[2] (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ n
  → rewrite target=fact index_def sym=false goal=2 facts=[1]
    #[3] divides (p ^ n) n ⟷ n = 0 ∨ p = 1 ∨ n ≤ (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}))
  ← cases "p ≤ 1 ∨ n = 0" goal=2
    #[4] p ≤ 1 ∨ n = 0 ⟶ (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ n
    #[5] ¬(p ≤ 1 ∨ n = 0) ⟶ (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ n
  ← intro goal=4
    #[6] p ≤ 1 ∨ n = 0
    #[7] (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ n
  ← rewrite if_P sym=false goal=7 facts=[6]
    #[8] 0 ≤ n
  ← rule le_0 goal=8
    #[9] 0 ≤ n
  ← intro goal=5
    #[10] ¬(p ≤ 1 ∨ n = 0)
    #[11] (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ n
  ← rewrite if_not_P sym=false goal=11 facts=[10]
    #[12] card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}) ≤ n
  → rewrite target=fact if_not_P sym=false goal=12 facts=[3,10]
    #[13] divides (p ^ n) n ⟷ n = 0 ∨ p = 1 ∨ n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})
  → rewrite target=fact de_morgan_thm2 sym=false goal=12 facts=[10]
    #[14] ¬(p ≤ 1) ∧ ¬(n = 0)
  → forward conjD1 goal=12 facts=[14]
    #[15] ¬(p ≤ 1)
  → forward conjD2 goal=12 facts=[14]
    #[16] ¬(n = 0)
  → rewrite target=fact not_le sym=false goal=12 facts=[15]
    #[17] 1 < p
  → rewrite target=fact le_lt sym=false goal=12 facts=[15]
    #[18] ¬(p < 1 ∨ p = 1)
  → rewrite target=fact de_morgan_thm2 sym=false goal=12 facts=[18]
    #[19] ¬(p < 1) ∧ ¬(p = 1)
  → forward conjD2 goal=12 facts=[19]
    #[20] ¬(p = 1)
  ← rule lt_imp_le goal=12
    #[21] card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}) < n
  ← rewrite not_le sym=true goal=21
    #[22] ¬(n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}))
  cut "~(divides (p ^ n) n)" goal=22 facts=[13]
    #[23] ¬(divides (p ^ n) n)
  ← rule contrapositive goal=23 facts=[16]
    #[24] ¬¬(divides (p ^ n) n) ⟶ ¬¬(n = 0)
  ← rewrite double_neg sym=false goal=24
    #[25] divides (p ^ n) n ⟶ n = 0
  ← intro goal=25
    #[26] divides (p ^ n) n
    #[27] n = 0
  → forward divides_le goal=27 facts=[26]
    #[28] p ^ n ≤ n ∨ n = 0
  ← rule disjE goal=27 facts=[28]
    #[29] p ^ n ≤ n ⟶ n = 0
    #[30] n = 0 ⟶ n = 0
  ← intro goal=29
    #[31] p ^ n ≤ n
    #[32] n = 0
  ← rule contrapositive goal=32 facts=[31]
    #[33] ¬(n = 0) ⟶ ¬(p ^ n ≤ n)
  ← rewrite not_le sym=false goal=33
    #[34] ¬(n = 0) ⟶ n < p ^ n
  ← intro goal=34
    #[35] ¬(n = 0)
    #[36] n < p ^ n
  ← rule lte_trans param_m="(2::nat) ^ n" goal=36
    #[37] n < 2 ^ n
    #[38] 2 ^ n ≤ p ^ n
  ← rule lt_pow2_refl goal=37
    #[39] n < 2 ^ n
  ← rewrite exp_mono_le sym=false goal=38
    #[40] 2 ≤ p ∨ n = 0
  ← rule disjI1 goal=40
    #[41] 2 ≤ p
  ← z3 goal=41 facts=[17]
    #[42] 2 ≤ p
  → rewrite target=fact source=prev goal=22 facts=[13,23]
    #[43] ¬(n = 0 ∨ p = 1 ∨ n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}))
  ← rule contrapositive goal=22 facts=[43]
    #[44] ¬¬(n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ⟶ ¬¬(n = 0 ∨ p = 1 ∨ n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}))
  ← rewrite double_neg sym=false goal=44
    #[45] n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n}) ⟶ n = 0 ∨ p = 1 ∨ n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})
  ← intro goal=45
    #[46] n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})
    #[47] n = 0 ∨ p = 1 ∨ n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})
  ← rule disjI2 goal=47
    #[48] p = 1 ∨ n ≤ card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})
  ← rule disjI2 goal=48
qed

theorem index_decomposition
  fixes n :: nat, p :: nat
  prop ?m. p ^ index p n * m = n & (n = 0 | p = 1 | ~(divides p m))
  [hint_backward]
proof
  → forward le_index param_k="index p n" param_n=n param_p=p goal=0
    #[1] index p n ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ index p n = 0) ∧ divides (p ^ index p n) n
  cut "index p n ≤ index p n" goal=0
    #[2] index p n ≤ index p n
  ← rule less_eq_refl goal=2
  → rewrite target=fact source=prev goal=0 facts=[1,2]
    #[3] (n = 0 ∨ p = 1 ⟶ index p n = 0) ∧ divides (p ^ index p n) n
  → forward conjD1 goal=0 facts=[3]
    #[4] n = 0 ∨ p = 1 ⟶ index p n = 0
  → forward conjD2 goal=0 facts=[3]
    #[5] divides (p ^ index p n) n
  → rewrite target=fact divides_def sym=false goal=0 facts=[5]
    #[6] ∃k. n = p ^ index p n * k
  elim m goal=0 facts=[6]
    #[7] _VAR m
    #[8] n = p ^ index p n * m
    #[9] ∃m. p ^ index p n * m = n ∧ (n = 0 ∨ p = 1 ∨ ¬(divides p m))
  ← inst m goal=9
    #[10] p ^ index p n * m = n ∧ (n = 0 ∨ p = 1 ∨ ¬(divides p m))
  ← rule conjI goal=10
    #[11] p ^ index p n * m = n
    #[12] n = 0 ∨ p = 1 ∨ ¬(divides p m)
  → rewrite target=fact eq_sym_eq sym=false goal=11 facts=[8]
  → forward le_index param_k="index p n +1" param_n=n param_p=p goal=12
    #[13] index p n + 1 ≤ index p n ⟷ (n = 0 ∨ p = 1 ⟶ index p n + 1 = 0) ∧ divides (p ^ (index p n + 1)) n
  cut "~(index p n + 1 ≤ index p n)" goal=12
    #[14] ¬(index p n + 1 ≤ index p n)
  ← rewrite not_le sym=false goal=14
    #[15] index p n < index p n + 1
  ← rewrite lt_add sym=false goal=15
    #[16] (0::nat) < 1
  ← z3 goal=16
    #[17] (0::nat) < 1
  → rewrite target=fact source=prev goal=12 facts=[13,14]
    #[18] ¬((n = 0 ∨ p = 1 ⟶ index p n + 1 = 0) ∧ divides (p ^ (index p n + 1)) n)
  ← cases "n=0" goal=12
    #[19] n = 0 ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p m)
    #[20] ¬(n = 0) ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← intro goal=19
    #[21] n = 0
    #[22] n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule disjI1 goal=22
  ← intro goal=20
    #[23] ¬(n = 0)
    #[24] n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← cases "p=1" goal=24
    #[25] p = 1 ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p m)
    #[26] ¬(p = 1) ⟶ n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← intro goal=25
    #[27] p = 1
    #[28] n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule disjI2 goal=28
    #[29] p = 1 ∨ ¬(divides p m)
  ← rule disjI1 goal=29
  ← intro goal=26
    #[30] ¬(p = 1)
    #[31] n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule disjI2 goal=31
    #[32] p = 1 ∨ ¬(divides p m)
  ← rule disjI2 goal=32
    #[33] ¬(divides p m)
  ← rule contrapositive goal=33 facts=[18]
    #[34] ¬¬(divides p m) ⟶ ¬¬((n = 0 ∨ p = 1 ⟶ index p n + 1 = 0) ∧ divides (p ^ (index p n + 1)) n)
  ← rewrite double_neg sym=false goal=34
    #[35] divides p m ⟶ (n = 0 ∨ p = 1 ⟶ index p n + 1 = 0) ∧ divides (p ^ (index p n + 1)) n
  ← intro goal=35
    #[36] divides p m
    #[37] (n = 0 ∨ p = 1 ⟶ index p n + 1 = 0) ∧ divides (p ^ (index p n + 1)) n
  ← rule conjI goal=37
    #[38] n = 0 ∨ p = 1 ⟶ index p n + 1 = 0
    #[39] divides (p ^ (index p n + 1)) n
  ← intro goal=38
    #[40] n = 0 ∨ p = 1
    #[41] index p n + 1 = 0
  ← z3 goal=41 facts=[40,30,23]
    #[42] index p n + 1 = 0
  ← rewrite nat_power_add sym=false goal=39
    #[43] divides (p ^ index p n * p ^ (1::nat)) n
  ← rewrite exp_1 sym=false goal=43
    #[44] divides (p ^ index p n * p) n
  cut "divides (p ^ index p n * p) (p ^ index p n * m)" goal=44
    #[45] divides (p ^ index p n * p) (p ^ index p n * m)
  ← rule divides_mult_left goal=45
  → rewrite target=fact eq_sym_eq sym=false goal=44 facts=[8]
  → rewrite target=fact source=prev goal=44 facts=[11,45]
qed

theorem index_decomposition_prime
  fixes n :: nat, p :: nat
  prop prime p --> (?m. p ^ index p n * m = n & (n = 0 | coprime p m))
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] ∃m. p ^ index p n * m = n ∧ (n = 0 ∨ coprime p m)
  → forward index_decomposition param_n=n param_p=p goal=2
    #[3] ∃m. p ^ index p n * m = n ∧ (n = 0 ∨ p = 1 ∨ ¬(divides p m))
  elim m goal=2 facts=[3]
    #[4] _VAR m
    #[5] p ^ index p n * m = n ∧ (n = 0 ∨ p = 1 ∨ ¬(divides p m))
    #[6] ∃m. p ^ index p n * m = n ∧ (n = 0 ∨ coprime p m)
  ← inst m goal=6
    #[7] p ^ index p n * m = n ∧ (n = 0 ∨ coprime p m)
  → forward conjD1 goal=7 facts=[5]
    #[8] p ^ index p n * m = n
  → forward conjD2 goal=7 facts=[5]
    #[9] n = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule conjI goal=7
    #[10] n = 0 ∨ coprime p m
  ← rule disjE goal=10 facts=[9]
    #[11] n = 0 ⟶ n = 0 ∨ coprime p m
    #[12] p = 1 ∨ ¬(divides p m) ⟶ n = 0 ∨ coprime p m
  ← intro goal=11
    #[13] n = 0
    #[14] n = 0 ∨ coprime p m
  ← rule disjI1 goal=14
  ← intro goal=12
    #[15] p = 1 ∨ ¬(divides p m)
    #[16] n = 0 ∨ coprime p m
  ← rule disjE goal=16 facts=[15]
    #[17] p = 1 ⟶ n = 0 ∨ coprime p m
    #[18] ¬(divides p m) ⟶ n = 0 ∨ coprime p m
  ← intro goal=17
    #[19] p = 1
    #[20] n = 0 ∨ coprime p m
  → rewrite target=fact prime_def sym=false goal=20 facts=[1]
    #[21] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=20 facts=[21]
    #[22] ¬(p = 1)
  ← rule negE_gen goal=20 facts=[22,19]
    #[23] n = 0 ∨ coprime p m
  ← intro goal=18
    #[24] ¬(divides p m)
    #[25] n = 0 ∨ coprime p m
  ← rule disjI2 goal=25
    #[26] coprime p m
  → forward prime_coprime_strong param_n=m goal=26 facts=[1]
    #[27] divides p m ∨ coprime p m
  ← rule disjE goal=26 facts=[27]
    #[28] divides p m ⟶ coprime p m
    #[29] coprime p m ⟶ coprime p m
  ← intro goal=28
    #[30] divides p m
    #[31] coprime p m
  ← rule negE_gen goal=31 facts=[24,30]
    #[32] coprime p m
qed

theorem index_decomposition_le
  fixes e1 :: nat, e2 :: nat, m1 :: nat, m2 :: nat, p :: nat
  prop p ^ e1 * m1 = p ^ e2 * m2 --> ~(p = 0) --> ~(divides p m2) --> e1 <= e2
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] p ^ e1 * m1 = p ^ e2 * m2
    #[2] ¬(p = 0)
    #[3] ¬(divides p m2)
    #[4] e1 ≤ e2
  ← rule contrapositive goal=4 facts=[3]
    #[5] ¬(e1 ≤ e2) ⟶ ¬¬(divides p m2)
  ← rewrite double_neg sym=false goal=5
    #[6] ¬(e1 ≤ e2) ⟶ divides p m2
  ← intro goal=6
    #[7] ¬(e1 ≤ e2)
    #[8] divides p m2
  → rewrite target=fact not_le sym=false goal=8 facts=[7]
    #[9] e2 < e1
  → rewrite target=fact less_exist sym=false goal=8 facts=[9]
    #[10] ∃d. e1 = e2 + Suc d
  elim d goal=8 facts=[10]
    #[11] _VAR d
    #[12] e1 = e2 + Suc d
    #[13] divides p m2
  → rewrite target=fact source=prev goal=13 facts=[12,1]
    #[14] p ^ (e2 + Suc d) * m1 = p ^ e2 * m2
  → rewrite target=fact nat_power_add sym=false goal=13 facts=[14]
    #[15] p ^ e2 * p ^ Suc d * m1 = p ^ e2 * m2
  → rewrite target=fact mult_assoc sym=false goal=13 facts=[15]
    #[16] p ^ e2 * (p ^ Suc d * m1) = p ^ e2 * m2
  → rewrite target=fact eq_mult_lcancel sym=false goal=13 facts=[16]
    #[17] p ^ e2 = 0 ∨ p ^ Suc d * m1 = m2
  ← rule disjE goal=13 facts=[17]
    #[18] p ^ e2 = 0 ⟶ divides p m2
    #[19] p ^ Suc d * m1 = m2 ⟶ divides p m2
  ← intro goal=18
    #[20] p ^ e2 = 0
    #[21] divides p m2
  → rewrite target=fact exp_eq_0 sym=false goal=21 facts=[20]
    #[22] p = 0 ∧ ¬(e2 = 0)
  → forward conjD1 goal=21 facts=[22]
    #[23] p = 0
  ← rule negE_gen goal=21 facts=[2,23]
    #[24] divides p m2
  ← intro goal=19
    #[25] p ^ Suc d * m1 = m2
    #[26] divides p m2
  → rewrite target=fact nat_nat_power_def_2 sym=false goal=26 facts=[25]
    #[27] p * p ^ d * m1 = m2
  → rewrite target=fact eq_sym_eq sym=false goal=26 facts=[27]
    #[28] m2 = p * p ^ d * m1
  ← rewrite source=prev goal=26 facts=[28]
    #[29] divides p (p * p ^ d * m1)
  ← rewrite mult_assoc sym=false goal=29
    #[30] divides p (p * (p ^ d * m1))
  ← rule divides_rmul goal=30
    #[31] divides p p
  ← rule divides_self goal=31
    #[32] divides p p
qed

theorem index_decomposition_unique
  fixes e1 :: nat, e2 :: nat, m1 :: nat, m2 :: nat, p :: nat
  prop p ^ e1 * m1 = p ^ e2 * m2 --> ~(p = 0) --> ~(divides p m1) --> ~(divides p m2) --> e1 = e2
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] p ^ e1 * m1 = p ^ e2 * m2
    #[2] ¬(p = 0)
    #[3] ¬(divides p m1)
    #[4] ¬(divides p m2)
    #[5] e1 = e2
  ← rewrite le_antisym sym=true goal=5
    #[6] e1 ≤ e2 ∧ e2 ≤ e1
  ← rule conjI goal=6
    #[7] e1 ≤ e2
    #[8] e2 ≤ e1
  ← rule index_decomposition_le goal=7 facts=[1,2,4]
    #[9] e1 ≤ e2
  → rewrite target=fact eq_sym_eq sym=false goal=8 facts=[1]
    #[10] p ^ e2 * m2 = p ^ e1 * m1
  ← rule index_decomposition_le goal=8 facts=[10,2,3]
    #[11] e2 ≤ e1
qed

theorem index_unique
  fixes e :: nat, m :: nat, n :: nat, p :: nat
  prop p ^ e * m = n --> (p = 0 --> e = 0) --> ~(divides p m) --> index p n = e
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] p ^ e * m = n
    #[2] p = 0 ⟶ e = 0
    #[3] ¬(divides p m)
    #[4] index p n = e
  cut "!i. i = e <--> e <= i & ~(e + 1 <= i)" goal=4
    #[5] ∀i. i = e ⟷ e ≤ i ∧ ¬(e + 1 ≤ i)
  ← z3 goal=5
    #[6] ∀i. i = e ⟷ e ≤ i ∧ ¬(e + 1 ≤ i)
  ← rewrite source=prev goal=4 facts=[6]
    #[7] e ≤ index p n ∧ ¬(e + 1 ≤ index p n)
  ← rule conjI goal=7
    #[8] e ≤ index p n
    #[9] ¬(e + 1 ≤ index p n)
  ← rewrite le_index sym=false goal=8
    #[10] (n = 0 ∨ p = 1 ⟶ e = 0) ∧ divides (p ^ e) n
  → rewrite target=fact eq_sym_eq sym=false goal=10 facts=[1]
    #[11] n = p ^ e * m
  ← rewrite source=prev goal=10 facts=[11]
    #[12] (p ^ e * m = 0 ∨ p = 1 ⟶ e = 0) ∧ divides (p ^ e) (p ^ e * m)
  ← rule conjI goal=12
    #[13] p ^ e * m = 0 ∨ p = 1 ⟶ e = 0
    #[14] divides (p ^ e) (p ^ e * m)
  ← intro goal=13
    #[15] p ^ e * m = 0 ∨ p = 1
    #[16] e = 0
  ← rule disjE goal=16 facts=[15]
    #[17] p ^ e * m = 0 ⟶ e = 0
    #[18] p = 1 ⟶ e = 0
  ← intro goal=17
    #[19] p ^ e * m = 0
    #[20] e = 0
  → rewrite target=fact mult_eq_0 sym=false goal=20 facts=[19]
    #[21] p ^ e = 0 ∨ m = 0
  ← rule disjE goal=20 facts=[21]
    #[22] p ^ e = 0 ⟶ e = 0
    #[23] m = 0 ⟶ e = 0
  ← intro goal=22
    #[24] p ^ e = 0
    #[25] e = 0
  → rewrite target=fact exp_eq_0 sym=false goal=25 facts=[24]
    #[26] p = 0 ∧ ¬(e = 0)
  ← apply_prev goal=25 facts=[2]
    #[27] p = 0
  ← rule conjD1 goal=27 facts=[26]
    #[28] p = 0
  ← intro goal=23
    #[29] m = 0
    #[30] e = 0
  → rewrite target=fact source=prev goal=30 facts=[29,3]
    #[31] ¬(divides p 0)
  ← rule negE_gen goal=30 facts=[31]
    #[32] divides p 0
  ← rule divides_zero goal=32
    #[33] divides p 0
  ← intro goal=18
    #[34] p = 1
    #[35] e = 0
  → rewrite target=fact source=prev goal=35 facts=[34,3]
    #[36] ¬(divides 1 m)
  ← rule negE_gen goal=35 facts=[36]
    #[37] divides 1 m
  ← rule divides_1 goal=37
    #[38] divides 1 m
  ← rule divides_rmul goal=14
    #[39] divides (p ^ e) (p ^ e)
  ← rule divides_self goal=39
    #[40] divides (p ^ e) (p ^ e)
  ← rewrite le_index sym=false goal=9
    #[41] ¬((n = 0 ∨ p = 1 ⟶ e + 1 = 0) ∧ divides (p ^ (e + 1)) n)
  ← rewrite de_morgan_thm1 sym=false goal=41
    #[42] ¬(n = 0 ∨ p = 1 ⟶ e + 1 = 0) ∨ ¬(divides (p ^ (e + 1)) n)
  ← rule disjI2 goal=42
    #[43] ¬(divides (p ^ (e + 1)) n)
  ← rewrite nat_power_add sym=false goal=43
    #[44] ¬(divides (p ^ e * p ^ (1::nat)) n)
  ← rewrite exp_1 sym=false goal=44
    #[45] ¬(divides (p ^ e * p) n)
  ← rule contrapositive goal=45 facts=[3]
    #[46] ¬¬(divides (p ^ e * p) n) ⟶ ¬¬(divides p m)
  ← rewrite double_neg sym=false goal=46
    #[47] divides (p ^ e * p) n ⟶ divides p m
  ← intro goal=47
    #[48] divides (p ^ e * p) n
    #[49] divides p m
  → rewrite target=fact source=prev goal=49 facts=[11,48]
    #[50] divides (p ^ e * p) (p ^ e * m)
  ← rule divides_cmul2 goal=49 facts=[50]
    #[51] ¬(p ^ e = 0)
  ← rewrite exp_eq_0 sym=false goal=51
    #[52] ¬(p = 0 ∧ ¬(e = 0))
  ← rewrite de_morgan_thm1 sym=false goal=52
    #[53] ¬(p = 0) ∨ ¬¬(e = 0)
  ← cases "p=0" goal=53
    #[54] p = 0 ⟶ ¬(p = 0) ∨ ¬¬(e = 0)
    #[55] ¬(p = 0) ⟶ ¬(p = 0) ∨ ¬¬(e = 0)
  ← intro goal=54
    #[56] p = 0
    #[57] ¬(p = 0) ∨ ¬¬(e = 0)
  ← rule disjI2 goal=57
    #[58] ¬¬(e = 0)
  ← rewrite double_neg sym=false goal=58
    #[59] e = 0
  ← apply_prev goal=59 facts=[2]
  ← intro goal=55
    #[60] ¬(p = 0)
    #[61] ¬(p = 0) ∨ ¬¬(e = 0)
  ← rule disjI1 goal=61
qed

theorem index_unique_eq
  fixes k :: nat, n :: nat, p :: nat
  prop index p n = k <--> (if p = 1 | n = 0 then k = 0 else !j. divides (p ^ j) n <--> j <= k)
  [hint_rewrite]
proof
  ← cases "p = 1 ∨ n = 0" goal=0
    #[1] p = 1 ∨ n = 0 ⟶ index p n = k ⟷ (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k)
    #[2] ¬(p = 1 ∨ n = 0) ⟶ index p n = k ⟷ (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k)
  ← intro goal=1
    #[3] p = 1 ∨ n = 0
    #[4] index p n = k ⟷ (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k)
  ← rewrite if_P sym=false goal=4 facts=[3]
    #[5] index p n = k ⟷ k = 0
  ← rewrite index_def sym=false goal=5
    #[6] (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) = k ⟷ k = 0
  cut "p ≤ 1 ∨ n = 0" goal=6
    #[7] p ≤ 1 ∨ n = 0
  ← z3 goal=7 facts=[3]
  ← rewrite if_P sym=false goal=6 facts=[7]
    #[8] 0 = k ⟷ k = 0
  ← z3 goal=8
    #[9] 0 = k ⟷ k = 0
  ← intro goal=2
    #[10] ¬(p = 1 ∨ n = 0)
    #[11] index p n = k ⟷ (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k)
  ← rewrite if_not_P sym=false goal=11 facts=[10]
    #[12] index p n = k ⟷ (∀j. divides (p ^ j) n ⟷ j ≤ k)
  ← rewrite primepow_divides_index sym=false goal=12
    #[13] index p n = k ⟷ (∀j. n = 0 ∨ p = 1 ∨ j ≤ index p n ⟷ j ≤ k)
  → rewrite target=fact disj_comm sym=false goal=13 facts=[10]
    #[14] ¬(n = 0 ∨ p = 1)
  ← rule iffI goal=13
    #[15] index p n = k ⟶ (∀j. n = 0 ∨ p = 1 ∨ j ≤ index p n ⟷ j ≤ k)
    #[16] (∀j. n = 0 ∨ p = 1 ∨ j ≤ index p n ⟷ j ≤ k) ⟶ index p n = k
  ← intro goal=15
    #[17] index p n = k
    #[18] ∀j. n = 0 ∨ p = 1 ∨ j ≤ index p n ⟷ j ≤ k
  ← rewrite source=prev goal=18 facts=[17]
    #[19] ∀j. n = 0 ∨ p = 1 ∨ j ≤ k ⟷ j ≤ k
  ← intro j goal=19
    #[20] _VAR j
    #[21] n = 0 ∨ p = 1 ∨ j ≤ k ⟷ j ≤ k
  ← rule iffI goal=21
    #[22] n = 0 ∨ p = 1 ∨ j ≤ k ⟶ j ≤ k
    #[23] j ≤ k ⟶ n = 0 ∨ p = 1 ∨ j ≤ k
  ← intro goal=22
    #[24] n = 0 ∨ p = 1 ∨ j ≤ k
    #[25] j ≤ k
  → forward disj_assoc goal=25 facts=[24]
    #[26] (n = 0 ∨ p = 1) ∨ j ≤ k
  ← rule disjE goal=25 facts=[26]
    #[27] n = 0 ∨ p = 1 ⟶ j ≤ k
    #[28] j ≤ k ⟶ j ≤ k
  ← intro goal=27
    #[29] n = 0 ∨ p = 1
    #[30] j ≤ k
  ← rule negE_gen goal=30 facts=[14,29]
    #[31] j ≤ k
  ← intro goal=23
    #[32] j ≤ k
    #[33] n = 0 ∨ p = 1 ∨ j ≤ k
  ← rule disjI2 goal=33
    #[34] p = 1 ∨ j ≤ k
  ← rule disjI2 goal=34
  ← intro goal=16
    #[35] ∀j. n = 0 ∨ p = 1 ∨ j ≤ index p n ⟷ j ≤ k
    #[36] index p n = k
  ← rewrite le_antisym sym=true goal=36
    #[37] index p n ≤ k ∧ k ≤ index p n
  → rewrite target=fact eq_sym_eq sym=false goal=37 facts=[35]
    #[38] ∀j. j ≤ k ⟷ n = 0 ∨ p = 1 ∨ j ≤ index p n
  ← rule conjI goal=37
    #[39] index p n ≤ k
    #[40] k ≤ index p n
  ← rewrite source=prev goal=39 facts=[38]
    #[41] n = 0 ∨ p = 1 ∨ index p n ≤ index p n
  ← rule disjI2 goal=41
    #[42] p = 1 ∨ index p n ≤ index p n
  ← rule disjI2 goal=42
    #[43] index p n ≤ index p n
  ← rule less_eq_refl goal=43
    #[44] index p n ≤ index p n
  cut "k <= k" goal=40
    #[45] k ≤ k
  ← rule less_eq_refl goal=45
    #[46] k ≤ k
  → rewrite target=fact source=prev goal=40 facts=[38,46]
    #[47] n = 0 ∨ p = 1 ∨ k ≤ index p n
  → forward disj_assoc goal=40 facts=[47]
    #[48] (n = 0 ∨ p = 1) ∨ k ≤ index p n
  ← rule disjE goal=40 facts=[48]
    #[49] n = 0 ∨ p = 1 ⟶ k ≤ index p n
    #[50] k ≤ index p n ⟶ k ≤ index p n
  ← intro goal=49
    #[51] k ≤ index p n
  ← rule negE_gen goal=51 facts=[14,29]
    #[52] k ≤ index p n
qed

theorem index_unique_alt
  fixes k :: nat, n :: nat, p :: nat
  prop index p n = k <--> (if p = 1 | n = 0 then k = 0 else divides (p ^ k) n & ~(divides (p ^ (k + 1)) n))
  [hint_rewrite]
proof
  ← rewrite index_unique_eq sym=false goal=0
    #[1] (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k) ⟷ (if p = 1 ∨ n = 0 then k = 0 else divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n))
  ← cases "p = 1 ∨ n = 0" goal=1
    #[2] p = 1 ∨ n = 0 ⟶ (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k) ⟷ (if p = 1 ∨ n = 0 then k = 0 else divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n))
    #[3] ¬(p = 1 ∨ n = 0) ⟶ (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k) ⟷ (if p = 1 ∨ n = 0 then k = 0 else divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n))
  ← intro goal=2
    #[4] p = 1 ∨ n = 0
    #[5] (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k) ⟷ (if p = 1 ∨ n = 0 then k = 0 else divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n))
  ← rewrite if_P sym=false goal=5 facts=[4]
  ← intro goal=3
    #[6] ¬(p = 1 ∨ n = 0)
    #[7] (if p = 1 ∨ n = 0 then k = 0 else ∀j. divides (p ^ j) n ⟷ j ≤ k) ⟷ (if p = 1 ∨ n = 0 then k = 0 else divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n))
  ← rewrite if_not_P sym=false goal=7 facts=[6]
    #[8] (∀j. divides (p ^ j) n ⟷ j ≤ k) ⟷ divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n)
  ← rule iffI goal=8
    #[9] (∀j. divides (p ^ j) n ⟷ j ≤ k) ⟶ divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n)
    #[10] divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n) ⟶ (∀j. divides (p ^ j) n ⟷ j ≤ k)
  ← intro goal=9
    #[11] ∀j. divides (p ^ j) n ⟷ j ≤ k
    #[12] divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n)
  ← rule conjI goal=12
    #[13] divides (p ^ k) n
    #[14] ¬(divides (p ^ (k + 1)) n)
  ← rewrite source=prev goal=13 facts=[11]
    #[15] k ≤ k
  ← rule less_eq_refl goal=15
    #[16] k ≤ k
  ← rewrite source=prev goal=14 facts=[11]
    #[17] ¬(k + 1 ≤ k)
  ← z3 goal=17
    #[18] ¬(k + 1 ≤ k)
  ← intro goal=10
    #[19] divides (p ^ k) n ∧ ¬(divides (p ^ (k + 1)) n)
    #[20] ∀j. divides (p ^ j) n ⟷ j ≤ k
  → forward conjD1 goal=20 facts=[19]
    #[21] divides (p ^ k) n
  → forward conjD2 goal=20 facts=[19]
    #[22] ¬(divides (p ^ (k + 1)) n)
  ← intro j goal=20
    #[23] _VAR j
    #[24] divides (p ^ j) n ⟷ j ≤ k
  ← rule iffI goal=24
    #[25] divides (p ^ j) n ⟶ j ≤ k
    #[26] j ≤ k ⟶ divides (p ^ j) n
  ← intro goal=25
    #[27] divides (p ^ j) n
    #[28] j ≤ k
  ← rewrite not_lt sym=true goal=28
    #[29] ¬(k < j)
  ← rule contrapositive goal=29 facts=[22]
    #[30] ¬¬(k < j) ⟶ ¬¬(divides (p ^ (k + 1)) n)
  ← rewrite double_neg sym=false goal=30
    #[31] k < j ⟶ divides (p ^ (k + 1)) n
  ← intro goal=31
    #[32] k < j
    #[33] divides (p ^ (k + 1)) n
  ← rule divides_trans param_m="(p ^ j)" goal=33
    #[34] divides (p ^ (k + 1)) (p ^ j)
  ← rule divides_exp_le_imp goal=34
    #[35] k + 1 ≤ j
  ← z3 goal=35 facts=[32]
    #[36] k + 1 ≤ j
  ← intro goal=26
    #[37] j ≤ k
    #[38] divides (p ^ j) n
  ← rule divides_trans param_m="(p ^ k)" goal=38
    #[39] divides (p ^ j) (p ^ k)
  ← rule divides_exp_le_imp goal=39
qed

theorem index_add_min
  fixes m :: nat, n :: nat, p :: nat
  prop min (index p m) (index p n) <= index p (m + n)
  [hint_backward]
proof
  ← cases "p=1" goal=0
    #[1] p = 1 ⟶ min (index p m) (index p n) ≤ index p (m + n)
    #[2] ¬(p = 1) ⟶ min (index p m) (index p n) ≤ index p (m + n)
  ← intro goal=1
    #[3] p = 1
    #[4] min (index p m) (index p n) ≤ index p (m + n)
  ← rewrite index_def sym=false goal=4
    #[5] min (if p ≤ 1 ∨ m = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) m})) (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ (if p ≤ 1 ∨ m + n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) (m + n)}))
  cut "!m::nat. p ≤ 1 ∨ m = 0" goal=5
    #[6] ∀m::nat. p ≤ 1 ∨ m = 0
  ← intro a goal=6
    #[7] _VAR a
    #[8] p ≤ 1 ∨ a = 0
  ← rule disjI1 goal=8
    #[9] p ≤ 1
  ← rewrite le_lt sym=false goal=9
    #[10] p < 1 ∨ p = 1
  ← rule disjI2 goal=10
  → inst target=fact m goal=5 facts=[6]
    #[11] p ≤ 1 ∨ m = 0
  → inst target=fact n goal=5 facts=[6]
    #[12] p ≤ 1 ∨ n = 0
  → inst target=fact m+n goal=12 facts=[6]
    #[13] p ≤ 1 ∨ m + n = 0
  ← rewrite if_P sym=false goal=5 facts=[11]
    #[14] min 0 (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ (if p ≤ 1 ∨ m + n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) (m + n)}))
  ← rewrite if_P sym=false goal=14 facts=[13]
    #[15] min 0 (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ 0
  ← rewrite if_P sym=false goal=15 facts=[12]
    #[16] min (0::nat) 0 ≤ 0
  ← rewrite nat_less_eq_def_1 sym=false goal=16
    #[17] min (0::nat) 0 = 0
  ← rewrite min_def sym=false goal=17
    #[18] (if (0::nat) ≤ 0 then (0::nat) else 0) = 0
  ← z3 goal=18
    #[19] (if (0::nat) ≤ 0 then (0::nat) else 0) = 0
  ← intro goal=2
    #[20] ¬(p = 1)
    #[21] min (index p m) (index p n) ≤ index p (m + n)
  ← rewrite le_index sym=false goal=21
    #[22] (m + n = 0 ∨ p = 1 ⟶ min (index p m) (index p n) = 0) ∧ divides (p ^ min (index p m) (index p n)) (m + n)
  ← rule conjI goal=22
    #[23] m + n = 0 ∨ p = 1 ⟶ min (index p m) (index p n) = 0
    #[24] divides (p ^ min (index p m) (index p n)) (m + n)
  cut "!a::nat. !b. min a b = 0 <--> a = 0 | b = 0" goal=23
    #[25] ∀a::nat. ∀b. min a b = 0 ⟷ a = 0 ∨ b = 0
  ← z3 goal=25
    #[26] ∀a::nat. ∀b. min a b = 0 ⟷ a = 0 ∨ b = 0
  ← rewrite source=prev goal=23 facts=[26]
    #[27] m + n = 0 ∨ p = 1 ⟶ index p m = 0 ∨ index p n = 0
  ← rewrite index_eq_0 sym=false goal=27
    #[28] m + n = 0 ∨ p = 1 ⟶ (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← intro goal=28
    #[29] m + n = 0 ∨ p = 1
    #[30] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjE goal=30 facts=[29]
    #[31] m + n = 0 ⟶ (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[32] p = 1 ⟶ (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← intro goal=31
    #[33] m + n = 0
    #[34] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  → forward add_is_zero goal=34 facts=[33]
    #[35] m = 0 ∧ n = 0
  ← rule disjI1 goal=34
    #[36] m = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule disjI1 goal=36
    #[37] m = 0
  ← rule conjD1 goal=37 facts=[35]
    #[38] m = 0
  ← intro goal=32
    #[39] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI1 goal=39
    #[40] m = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule disjI2 goal=40
    #[41] p = 1 ∨ ¬(divides p m)
  ← rule disjI1 goal=41
  ← rule divides_sum goal=24
    #[42] divides (p ^ min (index p m) (index p n)) m
    #[43] divides (p ^ min (index p m) (index p n)) n
  ← rule divides_trans param_m="p ^ (index p m)" goal=42
    #[44] divides (p ^ min (index p m) (index p n)) (p ^ index p m)
    #[45] divides (p ^ index p m) m
  ← rule divides_exp_le_imp goal=44
    #[46] min (index p m) (index p n) ≤ index p m
  ← rewrite min_def sym=false goal=46
    #[47] (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← cases "index p m ≤ index p n" goal=47
    #[48] index p m ≤ index p n ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p m
    #[49] ¬(index p m ≤ index p n) ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← intro goal=48
    #[50] index p m ≤ index p n
    #[51] (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← rewrite if_P sym=false goal=51 facts=[50]
    #[52] index p m ≤ index p m
  ← rule less_eq_refl goal=52
    #[53] index p m ≤ index p m
  ← intro goal=49
    #[54] ¬(index p m ≤ index p n)
    #[55] (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← rewrite if_not_P sym=false goal=55 facts=[54]
    #[56] index p n ≤ index p m
  → rewrite target=fact not_le sym=false goal=56 facts=[54]
    #[57] index p n < index p m
  ← rule lt_imp_le goal=56
  ← rule exp_index_divides goal=45
    #[58] divides (p ^ index p m) m
  ← rule divides_trans param_m="p ^ index p n" goal=43
    #[59] divides (p ^ min (index p m) (index p n)) (p ^ index p n)
    #[60] divides (p ^ index p n) n
  ← rule divides_exp_le_imp goal=59
    #[61] min (index p m) (index p n) ≤ index p n
  ← rewrite min_def sym=false goal=61
    #[62] (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← cases "index p m ≤ index p n" goal=62
    #[63] index p m ≤ index p n ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p n
    #[64] ¬(index p m ≤ index p n) ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← intro goal=63
    #[65] (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← rewrite if_P sym=false goal=65 facts=[50]
  ← intro goal=64
    #[66] (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← rewrite if_not_P sym=false goal=66 facts=[54]
    #[67] index p n ≤ index p n
  ← rule less_eq_refl goal=67
    #[68] index p n ≤ index p n
  ← rule exp_index_divides goal=60
    #[69] divides (p ^ index p n) n
qed

theorem index_sub_min
  fixes m :: nat, n :: nat, p :: nat
  prop n < m --> min (index p m) (index p n) <= index p (m - n)
  [hint_backward]
proof
  ← intro goal=0
    #[1] n < m
    #[2] min (index p m) (index p n) ≤ index p (m - n)
  ← cases "p = 1" goal=2
    #[3] p = 1 ⟶ min (index p m) (index p n) ≤ index p (m - n)
    #[4] ¬(p = 1) ⟶ min (index p m) (index p n) ≤ index p (m - n)
  ← intro goal=3
    #[5] p = 1
    #[6] min (index p m) (index p n) ≤ index p (m - n)
  ← rewrite index_def sym=false goal=6
    #[7] min (if p ≤ 1 ∨ m = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) m})) (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ (if p ≤ 1 ∨ m - n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) (m - n)}))
  cut "!a::nat. p <= 1 | a = 0" goal=7
    #[8] ∀a::nat. p ≤ 1 ∨ a = 0
  ← intro a goal=8
    #[9] _VAR a
    #[10] p ≤ 1 ∨ a = 0
  ← rule disjI1 goal=10
    #[11] p ≤ 1
  ← rewrite le_lt sym=false goal=11
    #[12] p < 1 ∨ p = 1
  ← rule disjI2 goal=12
  → inst target=fact m goal=7 facts=[8]
    #[13] p ≤ 1 ∨ m = 0
  → inst target=fact n goal=7 facts=[8]
    #[14] p ≤ 1 ∨ n = 0
  → inst target=fact m-n goal=7 facts=[8]
    #[15] p ≤ 1 ∨ m - n = 0
  ← rewrite if_P sym=false goal=7 facts=[13]
    #[16] min 0 (if p ≤ 1 ∨ n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) n})) ≤ (if p ≤ 1 ∨ m - n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) (m - n)}))
  ← rewrite if_P sym=false goal=16 facts=[14]
    #[17] min 0 0 ≤ (if p ≤ 1 ∨ m - n = 0 then 0 else card ({j::nat. 1 ≤ j ∧ divides (p ^ j) (m - n)}))
  ← rewrite if_P sym=false goal=17 facts=[15]
    #[18] min (0::nat) 0 ≤ 0
  ← z3 goal=18
    #[19] min (0::nat) 0 ≤ 0
  ← intro goal=4
    #[20] ¬(p = 1)
    #[21] min (index p m) (index p n) ≤ index p (m - n)
  ← rewrite le_index sym=false goal=21
    #[22] (m - n = 0 ∨ p = 1 ⟶ min (index p m) (index p n) = 0) ∧ divides (p ^ min (index p m) (index p n)) (m - n)
  ← rewrite sub_eq_0 sym=false goal=22
    #[23] (m ≤ n ∨ p = 1 ⟶ min (index p m) (index p n) = 0) ∧ divides (p ^ min (index p m) (index p n)) (m - n)
  ← rule conjI goal=23
    #[24] m ≤ n ∨ p = 1 ⟶ min (index p m) (index p n) = 0
    #[25] divides (p ^ min (index p m) (index p n)) (m - n)
  ← intro goal=24
    #[26] m ≤ n ∨ p = 1
    #[27] min (index p m) (index p n) = 0
  ← rule disjE goal=27 facts=[26]
    #[28] m ≤ n ⟶ min (index p m) (index p n) = 0
    #[29] p = 1 ⟶ min (index p m) (index p n) = 0
  ← intro goal=28
    #[30] m ≤ n
    #[31] min (index p m) (index p n) = 0
  → rewrite target=fact not_lt sym=true goal=31 facts=[30]
    #[32] ¬(n < m)
  ← rule negE_gen goal=31 facts=[32,1]
    #[33] min (index p m) (index p n) = 0
  ← intro goal=29
    #[34] min (index p m) (index p n) = 0
  ← rule negE_gen goal=34 facts=[20,5]
    #[35] min (index p m) (index p n) = 0
  ← rule divides_sub goal=25
    #[36] divides (p ^ min (index p m) (index p n)) m
    #[37] divides (p ^ min (index p m) (index p n)) n
  ← rule divides_trans param_m="p ^ (index p m)" goal=36
    #[38] divides (p ^ min (index p m) (index p n)) (p ^ index p m)
    #[39] divides (p ^ index p m) m
  ← rule divides_exp_le_imp goal=38
    #[40] min (index p m) (index p n) ≤ index p m
  ← rewrite min_def sym=false goal=40
    #[41] (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← cases "index p m ≤ index p n" goal=41
    #[42] index p m ≤ index p n ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p m
    #[43] ¬(index p m ≤ index p n) ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← intro goal=42
    #[44] index p m ≤ index p n
    #[45] (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← rewrite if_P sym=false goal=45 facts=[44]
    #[46] index p m ≤ index p m
  ← rule less_eq_refl goal=46
    #[47] index p m ≤ index p m
  ← intro goal=43
    #[48] ¬(index p m ≤ index p n)
    #[49] (if index p m ≤ index p n then index p m else index p n) ≤ index p m
  ← rewrite if_not_P sym=false goal=49 facts=[48]
    #[50] index p n ≤ index p m
  → rewrite target=fact not_le sym=false goal=50 facts=[48]
    #[51] index p n < index p m
  ← rule lt_imp_le goal=50
  ← rule exp_index_divides goal=39
    #[52] divides (p ^ index p m) m
  ← rule divides_trans param_m="p ^ (index p n)" goal=37
    #[53] divides (p ^ min (index p m) (index p n)) (p ^ index p n)
    #[54] divides (p ^ index p n) n
  ← rule divides_exp_le_imp goal=53
    #[55] min (index p m) (index p n) ≤ index p n
  ← rewrite min_def sym=false goal=55
    #[56] (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← cases "index p m ≤ index p n" goal=56
    #[57] index p m ≤ index p n ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p n
    #[58] ¬(index p m ≤ index p n) ⟶ (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← intro goal=57
    #[59] (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← rewrite if_P sym=false goal=59 facts=[44]
  ← intro goal=58
    #[60] (if index p m ≤ index p n then index p m else index p n) ≤ index p n
  ← rewrite if_not_P sym=false goal=60 facts=[48]
    #[61] index p n ≤ index p n
  ← rule less_eq_refl goal=61
    #[62] index p n ≤ index p n
  ← rule exp_index_divides goal=54
    #[63] divides (p ^ index p n) n
qed

theorem index_nsum_le
  fixes f :: 'a => nat, k :: 'a set, n :: nat, p :: nat
  prop finite k --> ~(k = {}) --> (!a. a Mem k --> n <= index p (f a)) --> n <= index p (nsum k f)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] finite k
    #[2] ¬(k = ∅)
    #[3] ∀a. a ∈ k ⟶ n ≤ index p (f a)
    #[4] n ≤ index p (nsum k f)
  → forward nsum_closed_nonempty param_P="(%m. n <= index p m)" param_f=f goal=4 facts=[1,2]
    #[5] (∀x. ∀y. n ≤ index p x ⟶ n ≤ index p y ⟶ n ≤ index p (x + y)) ⟶ (∀a. a ∈ k ⟶ n ≤ index p (f a)) ⟶ n ≤ index p (nsum k f)
  ← apply_prev goal=4 facts=[5]
    #[6] ∀x. ∀y. n ≤ index p x ⟶ n ≤ index p y ⟶ n ≤ index p (x + y)
  ← intro x,y goal=6
    #[7] _VAR x
    #[8] _VAR y
    #[9] n ≤ index p x
    #[10] n ≤ index p y
    #[11] n ≤ index p (x + y)
  ← rule less_eq_trans param_m="min (index p x) (index p y)" goal=11
    #[12] n ≤ min (index p x) (index p y)
    #[13] min (index p x) (index p y) ≤ index p (x + y)
  ← rewrite min_def sym=false goal=12
    #[14] n ≤ (if index p x ≤ index p y then index p x else index p y)
  ← cases "index p x ≤ index p y" goal=14
    #[15] index p x ≤ index p y ⟶ n ≤ (if index p x ≤ index p y then index p x else index p y)
    #[16] ¬(index p x ≤ index p y) ⟶ n ≤ (if index p x ≤ index p y then index p x else index p y)
  ← intro goal=15
    #[17] index p x ≤ index p y
    #[18] n ≤ (if index p x ≤ index p y then index p x else index p y)
  ← rewrite if_P sym=false goal=18 facts=[17]
  ← intro goal=16
    #[19] ¬(index p x ≤ index p y)
    #[20] n ≤ (if index p x ≤ index p y then index p x else index p y)
  ← rewrite if_not_P sym=false goal=20 facts=[19]
  ← rule index_add_min goal=13
    #[21] min (index p x) (index p y) ≤ index p (x + y)
qed

theorem divides_index
  fixes m :: nat, n :: nat
  prop divides m n <--> n = 0 | ~(m = 0) & (!p. prime p --> index p m <= index p n)
  [hint_rewrite]
proof
  ← cases "n=0" goal=0
    #[1] n = 0 ⟶ divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[2] ¬(n = 0) ⟶ divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← intro goal=1
    #[3] n = 0
    #[4] divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← rule iffI goal=4
    #[5] divides m n ⟶ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[6] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n) ⟶ divides m n
  ← intro goal=5
    #[7] divides m n
    #[8] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← rule disjI1 goal=8
  ← intro goal=6
    #[9] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[10] divides m n
  ← rewrite source=prev goal=10 facts=[3]
    #[11] divides m 0
  ← rule divides_zero goal=11
    #[12] divides m 0
  ← intro goal=2
    #[13] ¬(n = 0)
    #[14] divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← cases "m=0" goal=14
    #[15] m = 0 ⟶ divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[16] ¬(m = 0) ⟶ divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← intro goal=15
    #[17] m = 0
    #[18] divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← rule iffI goal=18
    #[19] divides m n ⟶ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[20] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n) ⟶ divides m n
  ← intro goal=19
    #[21] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  → rewrite target=fact source=prev goal=21 facts=[17,7]
    #[22] divides 0 n
  → rewrite target=fact divides_0 sym=false goal=21 facts=[22]
    #[23] n = 0
  ← rule disjI1 goal=21
  ← intro goal=20
    #[24] divides m n
  ← rule disjE goal=24 facts=[9]
    #[25] n = 0 ⟶ divides m n
    #[26] ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n) ⟶ divides m n
  ← intro goal=25
    #[27] divides m n
  ← rule negE_gen goal=27 facts=[13,3]
    #[28] divides m n
  ← intro goal=26
    #[29] ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[30] divides m n
  → forward conjD1 goal=30 facts=[29]
    #[31] ¬(m = 0)
  ← rule negE_gen goal=30 facts=[31,17]
    #[32] divides m n
  ← intro goal=16
    #[33] ¬(m = 0)
    #[34] divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  cut "!m. !n. (m <= n) <--> !k::nat. k <= m --> k <= n" goal=34
    #[35] ∀m::nat. ∀n. m ≤ n ⟷ (∀k. k ≤ m ⟶ k ≤ n)
  ← z3 goal=35
    #[36] ∀m::nat. ∀n. m ≤ n ⟷ (∀k. k ≤ m ⟶ k ≤ n)
  ← rewrite source=prev goal=34 facts=[36]
    #[37] divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k. k ≤ index p m ⟶ k ≤ index p n))
  ← rewrite le_index sym=false goal=37
    #[38] divides m n ⟷ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n))
  cut "∀p::nat. ∀k::nat. prime p --> (m = 0 ∨ p = 1 ⟶ k = 0)" goal=38
    #[39] ∀p. ∀k::nat. prime p ⟶ m = 0 ∨ p = 1 ⟶ k = 0
  ← intro p,k goal=39
    #[40] _VAR p
    #[41] _VAR k
    #[42] prime p
    #[43] m = 0 ∨ p = 1
    #[44] k = 0
  ← rule disjE goal=44 facts=[43]
    #[45] m = 0 ⟶ k = 0
    #[46] p = 1 ⟶ k = 0
  ← intro goal=45
    #[47] k = 0
  ← rule negE_gen goal=47 facts=[33,17]
    #[48] k = 0
  ← intro goal=46
    #[49] p = 1
    #[50] k = 0
  → rewrite target=fact prime_def sym=false goal=50 facts=[42]
    #[51] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=50 facts=[51]
    #[52] ¬(p = 1)
  ← rule negE_gen goal=50 facts=[52,49]
    #[53] k = 0
  ← rule iffI goal=38
    #[54] divides m n ⟶ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n))
    #[55] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n)) ⟶ divides m n
  ← intro goal=54
    #[56] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n))
  ← rule disjI2 goal=56
    #[57] ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n))
  ← rule conjI goal=57
    #[58] ∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n)
  ← intro p goal=58
    #[59] ∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n
  ← intro k goal=59
    #[60] (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m
    #[61] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n
  → forward conjD1 goal=61 facts=[60]
    #[62] m = 0 ∨ p = 1 ⟶ k = 0
  → forward conjD2 goal=61 facts=[60]
    #[63] divides (p ^ k) m
  ← rule conjI goal=61
    #[64] n = 0 ∨ p = 1 ⟶ k = 0
    #[65] divides (p ^ k) n
  ← intro goal=64
    #[66] n = 0 ∨ p = 1
    #[67] k = 0
  ← apply_prev goal=67 facts=[62]
    #[68] m = 0 ∨ p = 1
  ← rule disjE goal=68 facts=[66]
    #[69] n = 0 ⟶ m = 0 ∨ p = 1
    #[70] p = 1 ⟶ m = 0 ∨ p = 1
  ← intro goal=69
    #[71] m = 0 ∨ p = 1
  ← rule negE_gen goal=71 facts=[13,3]
    #[72] m = 0 ∨ p = 1
  ← intro goal=70
    #[73] m = 0 ∨ p = 1
  ← rule disjI2 goal=73
  ← rule divides_trans param_m=m goal=65
  ← intro goal=55
    #[74] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n))
    #[75] divides m n
  ← rule disjE goal=75 facts=[74]
    #[76] n = 0 ⟶ divides m n
    #[77] ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n)) ⟶ divides m n
  ← intro goal=76
    #[78] divides m n
  ← rule negE_gen goal=78 facts=[13,3]
  ← intro goal=77
    #[79] ¬(m = 0) ∧ (∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n))
    #[80] divides m n
  → forward conjD2 goal=80 facts=[79]
    #[81] ∀p. prime p ⟶ (∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n)
  ← rewrite primepow_divisors_divides sym=false goal=80
    #[82] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) m ⟶ divides (p ^ k) n
  ← intro p,k goal=82
    #[83] divides (p ^ k) m
    #[84] divides (p ^ k) n
  → forward goal=84 facts=[81,42]
    #[85] ∀k::nat. (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m ⟶ (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n
  → forward goal=84 facts=[39,42]
    #[86] ∀k::nat. m = 0 ∨ p = 1 ⟶ k = 0
  → inst target=fact k goal=84 facts=[86]
    #[87] m = 0 ∨ p = 1 ⟶ k = 0
  → forward conjI goal=84 facts=[87,83]
    #[88] (m = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) m
  → forward goal=84 facts=[85,88]
    #[89] (n = 0 ∨ p = 1 ⟶ k = 0) ∧ divides (p ^ k) n
  ← rule conjD2 goal=84 facts=[89]
    #[90] divides (p ^ k) n
qed

theorem eq_index
  fixes m :: nat, n :: nat
  prop m = n <--> (m = 0 <--> n = 0) & (!p. prime p --> index p m = index p n)
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] m = n ⟶ (m = 0 ⟷ n = 0) ∧ (∀p. prime p ⟶ index p m = index p n)
    #[2] (m = 0 ⟷ n = 0) ∧ (∀p. prime p ⟶ index p m = index p n) ⟶ m = n
  ← intro goal=1
    #[3] m = n
    #[4] (m = 0 ⟷ n = 0) ∧ (∀p. prime p ⟶ index p m = index p n)
  ← rewrite source=prev goal=4 facts=[3]
    #[5] (n = 0 ⟷ n = 0) ∧ (∀p. prime p ⟶ index p n = index p n)
  ← rule conjI goal=5
    #[6] n = 0 ⟷ n = 0
    #[7] ∀p. prime p ⟶ index p n = index p n
  ← rule eq_refl goal=6
    #[8] n = 0 ⟷ n = 0
  ← intro p goal=7
    #[9] _VAR p
    #[10] prime p
    #[11] index p n = index p n
  ← rule eq_refl goal=11
    #[12] index p n = index p n
  ← intro goal=2
    #[13] (m = 0 ⟷ n = 0) ∧ (∀p. prime p ⟶ index p m = index p n)
    #[14] m = n
  ← rule divides_antisym goal=14
    #[15] divides m n
    #[16] divides n m
  → forward conjD1 goal=15 facts=[13]
    #[17] m = 0 ⟷ n = 0
  → forward conjD2 goal=15 facts=[13]
    #[18] ∀p. prime p ⟶ index p m = index p n
  ← rewrite divides_index sym=false goal=15
    #[19] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← cases "m=0" goal=19
    #[20] m = 0 ⟶ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
    #[21] ¬(m = 0) ⟶ n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← intro goal=20
    #[22] m = 0
    #[23] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← rule disjI1 goal=23
    #[24] n = 0
  → rewrite target=fact source=prev goal=24 facts=[17,22]
  ← intro goal=21
    #[25] ¬(m = 0)
    #[26] n = 0 ∨ ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← rule disjI2 goal=26
    #[27] ¬(m = 0) ∧ (∀p. prime p ⟶ index p m ≤ index p n)
  ← rule conjI goal=27
    #[28] ∀p. prime p ⟶ index p m ≤ index p n
  ← intro p goal=28
    #[29] index p m ≤ index p n
  → forward goal=29 facts=[18,10]
    #[30] index p m = index p n
  ← rewrite source=prev goal=29 facts=[30]
    #[31] index p n ≤ index p n
  ← rule less_eq_refl goal=31
    #[32] index p n ≤ index p n
  ← rewrite divides_index sym=false goal=16
    #[33] m = 0 ∨ ¬(n = 0) ∧ (∀p. prime p ⟶ index p n ≤ index p m)
  ← cases "n=0" goal=33
    #[34] n = 0 ⟶ m = 0 ∨ ¬(n = 0) ∧ (∀p. prime p ⟶ index p n ≤ index p m)
    #[35] ¬(n = 0) ⟶ m = 0 ∨ ¬(n = 0) ∧ (∀p. prime p ⟶ index p n ≤ index p m)
  ← intro goal=34
    #[36] n = 0
    #[37] m = 0 ∨ ¬(n = 0) ∧ (∀p. prime p ⟶ index p n ≤ index p m)
  ← rule disjI1 goal=37
    #[38] m = 0
  ← rewrite source=prev goal=38 facts=[17]
  ← intro goal=35
    #[39] ¬(n = 0)
    #[40] m = 0 ∨ ¬(n = 0) ∧ (∀p. prime p ⟶ index p n ≤ index p m)
  ← rule disjI2 goal=40
    #[41] ¬(n = 0) ∧ (∀p. prime p ⟶ index p n ≤ index p m)
  ← rule conjI goal=41
    #[42] ∀p. prime p ⟶ index p n ≤ index p m
  ← intro p goal=42
    #[43] index p n ≤ index p m
  → forward goal=43 facts=[18,10]
  ← rewrite source=prev goal=43 facts=[30]
    #[44] index p n ≤ index p n
  ← rule less_eq_refl goal=44
qed

theorem coprime_index
  fixes m :: nat, n :: nat
  prop coprime m n <--> (m = 0 --> n = 1) & (n = 0 --> m = 1) & (!p. prime p --> index p m = 0 | index p n = 0)
  [hint_rewrite]
proof
  ← rule iffI goal=0
    #[1] coprime m n ⟶ (m = 0 ⟶ n = 1) ∧ (n = 0 ⟶ m = 1) ∧ (∀p. prime p ⟶ index p m = 0 ∨ index p n = 0)
    #[2] (m = 0 ⟶ n = 1) ∧ (n = 0 ⟶ m = 1) ∧ (∀p. prime p ⟶ index p m = 0 ∨ index p n = 0) ⟶ coprime m n
  ← intro goal=1
    #[3] coprime m n
    #[4] (m = 0 ⟶ n = 1) ∧ (n = 0 ⟶ m = 1) ∧ (∀p. prime p ⟶ index p m = 0 ∨ index p n = 0)
  ← rule conjI goal=4
    #[5] m = 0 ⟶ n = 1
    #[6] (n = 0 ⟶ m = 1) ∧ (∀p. prime p ⟶ index p m = 0 ∨ index p n = 0)
  ← intro goal=5
    #[7] m = 0
    #[8] n = 1
  → rewrite target=fact source=prev goal=8 facts=[7,3]
    #[9] coprime 0 n
  → rewrite target=fact coprime_sym sym=false goal=8 facts=[9]
    #[10] coprime n 0
  → rewrite target=fact coprime_0 sym=false goal=8 facts=[10]
  ← rule conjI goal=6
    #[11] n = 0 ⟶ m = 1
    #[12] ∀p. prime p ⟶ index p m = 0 ∨ index p n = 0
  ← intro goal=11
    #[13] n = 0
    #[14] m = 1
  → rewrite target=fact source=prev goal=14 facts=[13,3]
    #[15] coprime m 0
  → rewrite target=fact coprime_0 sym=false goal=14 facts=[15]
  → rewrite target=fact coprime_prime_eq sym=false goal=12 facts=[3]
    #[16] ∀p. ¬(prime p ∧ divides p m ∧ divides p n)
  ← intro p goal=12
    #[17] _VAR p
    #[18] prime p
    #[19] index p m = 0 ∨ index p n = 0
  → rewrite target=fact de_morgan_thm1 sym=false goal=19 facts=[16]
    #[20] ∀p. ¬(prime p) ∨ ¬(divides p m ∧ divides p n)
  → rewrite target=fact disj_conv_imp sym="" goal=19 facts=[20]
    #[21] ∀p. prime p ⟶ ¬(divides p m ∧ divides p n)
  → forward goal=19 facts=[21,18]
    #[22] ¬(divides p m ∧ divides p n)
  → rewrite target=fact de_morgan_thm1 sym=false goal=19 facts=[22]
    #[23] ¬(divides p m) ∨ ¬(divides p n)
  ← rewrite index_eq_0 sym=false goal=19
    #[24] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjE goal=24 facts=[23]
    #[25] ¬(divides p m) ⟶ (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[26] ¬(divides p n) ⟶ (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← intro goal=25
    #[27] ¬(divides p m)
    #[28] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI1 goal=28
    #[29] m = 0 ∨ p = 1 ∨ ¬(divides p m)
  ← rule disjI2 goal=29
    #[30] p = 1 ∨ ¬(divides p m)
  ← rule disjI2 goal=30
  ← intro goal=26
    #[31] ¬(divides p n)
    #[32] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=32
    #[33] n = 0 ∨ p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=33
    #[34] p = 1 ∨ ¬(divides p n)
  ← rule disjI2 goal=34
  ← intro goal=2
    #[35] (m = 0 ⟶ n = 1) ∧ (n = 0 ⟶ m = 1) ∧ (∀p. prime p ⟶ index p m = 0 ∨ index p n = 0)
    #[36] coprime m n
  → forward conjD1 goal=36 facts=[35]
    #[37] m = 0 ⟶ n = 1
  → forward conjD2 goal=36 facts=[35]
    #[38] (n = 0 ⟶ m = 1) ∧ (∀p. prime p ⟶ index p m = 0 ∨ index p n = 0)
  → forward conjD1 goal=36 facts=[38]
    #[39] n = 0 ⟶ m = 1
  → forward conjD2 goal=36 facts=[38]
    #[40] ∀p. prime p ⟶ index p m = 0 ∨ index p n = 0
  ← cases "m=0" goal=36
    #[41] m = 0 ⟶ coprime m n
    #[42] ¬(m = 0) ⟶ coprime m n
  ← intro goal=41
    #[43] coprime m n
  → forward goal=43 facts=[37,7]
    #[44] n = 1
  ← rewrite source=prev goal=43 facts=[44]
    #[45] coprime m 1
  ← rule coprime_1 goal=45
    #[46] coprime m 1
  ← intro goal=42
    #[47] ¬(m = 0)
    #[48] coprime m n
  ← cases "n=0" goal=48
    #[49] n = 0 ⟶ coprime m n
    #[50] ¬(n = 0) ⟶ coprime m n
  ← intro goal=49
    #[51] coprime m n
  → forward goal=51 facts=[39,13]
    #[52] m = 1
  ← rewrite source=prev goal=51 facts=[52]
    #[53] coprime 1 n
  ← rewrite coprime_sym sym=false goal=53
    #[54] coprime n 1
  ← rule coprime_1 goal=54
    #[55] coprime n 1
  ← intro goal=50
    #[56] ¬(n = 0)
    #[57] coprime m n
  ← rewrite coprime_prime_eq sym=false goal=57
    #[58] ∀p. ¬(prime p ∧ divides p m ∧ divides p n)
  ← intro p goal=58
    #[59] ¬(prime p ∧ divides p m ∧ divides p n)
  ← rewrite de_morgan_thm1 sym=false goal=59
    #[60] ¬(prime p) ∨ ¬(divides p m ∧ divides p n)
  ← rewrite disj_conv_imp sym="" goal=60
    #[61] prime p ⟶ ¬(divides p m ∧ divides p n)
  ← intro goal=61
    #[62] ¬(divides p m ∧ divides p n)
  → forward goal=62 facts=[40,18]
    #[63] index p m = 0 ∨ index p n = 0
  ← rewrite de_morgan_thm1 sym=false goal=62
    #[64] ¬(divides p m) ∨ ¬(divides p n)
  → rewrite target=fact index_eq_0 sym=false goal=64 facts=[63]
    #[65] (m = 0 ∨ p = 1 ∨ ¬(divides p m)) ∨ n = 0 ∨ p = 1 ∨ ¬(divides p n)
  → rewrite target=fact prime_def sym=false goal=64 facts=[18]
    #[66] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=64 facts=[66]
    #[67] ¬(p = 1)
  ← rule disjE goal=64 facts=[65]
    #[68] m = 0 ∨ p = 1 ∨ ¬(divides p m) ⟶ ¬(divides p m) ∨ ¬(divides p n)
    #[69] n = 0 ∨ p = 1 ∨ ¬(divides p n) ⟶ ¬(divides p m) ∨ ¬(divides p n)
  ← intro goal=68
    #[70] m = 0 ∨ p = 1 ∨ ¬(divides p m)
    #[71] ¬(divides p m) ∨ ¬(divides p n)
  ← rule disjI1 goal=71
    #[72] ¬(divides p m)
  ← rule disjE goal=72 facts=[70]
    #[73] m = 0 ⟶ ¬(divides p m)
    #[74] p = 1 ∨ ¬(divides p m) ⟶ ¬(divides p m)
  ← intro goal=73
    #[75] ¬(divides p m)
  ← rule negE_gen goal=75 facts=[47,7]
    #[76] ¬(divides p m)
  ← intro goal=74
    #[77] p = 1 ∨ ¬(divides p m)
    #[78] ¬(divides p m)
  ← rule disjE goal=78 facts=[77]
    #[79] p = 1 ⟶ ¬(divides p m)
    #[80] ¬(divides p m) ⟶ ¬(divides p m)
  ← intro goal=79
    #[81] p = 1
    #[82] ¬(divides p m)
  ← rule negE_gen goal=82 facts=[67,81]
    #[83] ¬(divides p m)
  ← intro goal=69
    #[84] n = 0 ∨ p = 1 ∨ ¬(divides p n)
    #[85] ¬(divides p m) ∨ ¬(divides p n)
  ← rule disjI2 goal=85
    #[86] ¬(divides p n)
  ← rule disjE goal=86 facts=[84]
    #[87] n = 0 ⟶ ¬(divides p n)
    #[88] p = 1 ∨ ¬(divides p n) ⟶ ¬(divides p n)
  ← intro goal=87
    #[89] ¬(divides p n)
  ← rule negE_gen goal=89 facts=[56,13]
    #[90] ¬(divides p n)
  ← intro goal=88
    #[91] p = 1 ∨ ¬(divides p n)
    #[92] ¬(divides p n)
  ← rule disjE goal=92 facts=[91]
    #[93] p = 1 ⟶ ¬(divides p n)
    #[94] ¬(divides p n) ⟶ ¬(divides p n)
  ← intro goal=93
    #[95] ¬(divides p n)
  ← rule negE_gen goal=95 facts=[67,81]
    #[96] ¬(divides p n)
qed

theorem index_gcd
  fixes m :: nat, n :: nat, p :: nat
  prop prime p --> index p (gcd m n) = (if m = 0 then index p n else if n = 0 then index p m else min (index p m) (index p n))
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime p
    #[2] index p (gcd m n) = (if m = 0 then index p n else if n = 0 then index p m else min (index p m) (index p n))
  ← cases "m=0" goal=2
    #[3] m = 0 ⟶ index p (gcd m n) = (if m = 0 then index p n else if n = 0 then index p m else min (index p m) (index p n))
    #[4] ¬(m = 0) ⟶ index p (gcd m n) = (if m = 0 then index p n else if n = 0 then index p m else min (index p m) (index p n))
  ← intro goal=3
    #[5] m = 0
    #[6] index p (gcd m n) = (if m = 0 then index p n else if n = 0 then index p m else min (index p m) (index p n))
  ← rewrite if_P sym=false goal=6 facts=[5]
    #[7] index p (gcd m n) = index p n
  ← rewrite source=prev goal=7 facts=[5]
    #[8] index p (gcd 0 n) = index p n
  ← rewrite gcd_zero_left sym=false goal=8
    #[9] index p (gcd 0 n) = index p n
  ← intro goal=4
    #[10] ¬(m = 0)
    #[11] index p (gcd m n) = (if m = 0 then index p n else if n = 0 then index p m else min (index p m) (index p n))
  ← rewrite if_not_P sym=false goal=11 facts=[10]
    #[12] index p (gcd m n) = (if n = 0 then index p m else min (index p m) (index p n))
  ← cases "n=0" goal=12
    #[13] n = 0 ⟶ index p (gcd m n) = (if n = 0 then index p m else min (index p m) (index p n))
    #[14] ¬(n = 0) ⟶ index p (gcd m n) = (if n = 0 then index p m else min (index p m) (index p n))
  ← intro goal=13
    #[15] n = 0
    #[16] index p (gcd m n) = (if n = 0 then index p m else min (index p m) (index p n))
  ← rewrite if_P sym=false goal=16 facts=[15]
    #[17] index p (gcd m n) = index p m
  ← rewrite source=prev goal=17 facts=[15]
    #[18] index p (gcd m 0) = index p m
  ← rewrite gcd_zero_right sym=false goal=18
    #[19] index p (gcd m 0) = index p m
  ← intro goal=14
    #[20] ¬(n = 0)
    #[21] index p (gcd m n) = (if n = 0 then index p m else min (index p m) (index p n))
  ← rewrite if_not_P sym=false goal=21 facts=[20]
    #[22] index p (gcd m n) = min (index p m) (index p n)
  cut "∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n" goal=22
    #[23] ∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n
  ← z3 goal=23
    #[24] ∀m::nat. ∀n. (∀k. k ≤ m ⟷ k ≤ n) ⟶ m = n
  ← apply_prev goal=22 facts=[24]
    #[25] ∀k. k ≤ index p (gcd m n) ⟷ k ≤ min (index p m) (index p n)
  ← intro k goal=25
    #[26] _VAR k
    #[27] k ≤ index p (gcd m n) ⟷ k ≤ min (index p m) (index p n)
  → forward divides_gcd param_a=m param_b=n param_d="p ^ k" goal=27
    #[28] divides (p ^ k) (gcd m n) ⟷ divides (p ^ k) m ∧ divides (p ^ k) n
  → rewrite target=fact primepow_divides_index sym=false goal=27 facts=[28]
    #[29] gcd m n = 0 ∨ p = 1 ∨ k ≤ index p (gcd m n) ⟷ (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  → rewrite target=fact prime_def sym=false goal=27 facts=[1]
    #[30] ¬(p = 1) ∧ (∀x. divides x p ⟶ x = 1 ∨ x = p)
  → forward conjD1 goal=27 facts=[30]
    #[31] ¬(p = 1)
  ← rule iffI goal=27
    #[32] k ≤ index p (gcd m n) ⟶ k ≤ min (index p m) (index p n)
    #[33] k ≤ min (index p m) (index p n) ⟶ k ≤ index p (gcd m n)
  ← intro goal=32
    #[34] k ≤ index p (gcd m n)
    #[35] k ≤ min (index p m) (index p n)
  cut "gcd m n = 0 ∨ p = 1 ∨ k ≤ index p (gcd m n)" goal=35
    #[36] gcd m n = 0 ∨ p = 1 ∨ k ≤ index p (gcd m n)
  ← rule disjI2 goal=36
    #[37] p = 1 ∨ k ≤ index p (gcd m n)
  ← rule disjI2 goal=37
  → rewrite target=fact source=prev goal=35 facts=[29,36]
    #[38] (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  → forward conjD1 goal=35 facts=[38]
    #[39] m = 0 ∨ p = 1 ∨ k ≤ index p m
  → forward conjD2 goal=35 facts=[38]
    #[40] n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rule disjE goal=35 facts=[39]
    #[41] m = 0 ⟶ k ≤ min (index p m) (index p n)
    #[42] p = 1 ∨ k ≤ index p m ⟶ k ≤ min (index p m) (index p n)
  ← intro goal=41
    #[43] k ≤ min (index p m) (index p n)
  ← rule negE_gen goal=43 facts=[10,5]
    #[44] k ≤ min (index p m) (index p n)
  ← intro goal=42
    #[45] p = 1 ∨ k ≤ index p m
    #[46] k ≤ min (index p m) (index p n)
  ← rule disjE goal=46 facts=[45]
    #[47] p = 1 ⟶ k ≤ min (index p m) (index p n)
    #[48] k ≤ index p m ⟶ k ≤ min (index p m) (index p n)
  ← intro goal=47
    #[49] p = 1
    #[50] k ≤ min (index p m) (index p n)
  ← rule negE_gen goal=50 facts=[31,49]
    #[51] k ≤ min (index p m) (index p n)
  ← intro goal=48
    #[52] k ≤ index p m
    #[53] k ≤ min (index p m) (index p n)
  ← rule disjE goal=53 facts=[40]
    #[54] n = 0 ⟶ k ≤ min (index p m) (index p n)
    #[55] p = 1 ∨ k ≤ index p n ⟶ k ≤ min (index p m) (index p n)
  ← intro goal=54
    #[56] k ≤ min (index p m) (index p n)
  ← rule negE_gen goal=56 facts=[20,15]
    #[57] k ≤ min (index p m) (index p n)
  ← intro goal=55
    #[58] p = 1 ∨ k ≤ index p n
    #[59] k ≤ min (index p m) (index p n)
  ← rule disjE goal=59 facts=[58]
    #[60] k ≤ index p n ⟶ k ≤ min (index p m) (index p n)
  ← intro goal=60
    #[61] k ≤ index p n
    #[62] k ≤ min (index p m) (index p n)
  ← rewrite min_def sym=false goal=62
    #[63] k ≤ (if index p m ≤ index p n then index p m else index p n)
  ← cases "index p m ≤ index p n" goal=63
    #[64] index p m ≤ index p n ⟶ k ≤ (if index p m ≤ index p n then index p m else index p n)
    #[65] ¬(index p m ≤ index p n) ⟶ k ≤ (if index p m ≤ index p n then index p m else index p n)
  ← intro goal=64
    #[66] index p m ≤ index p n
    #[67] k ≤ (if index p m ≤ index p n then index p m else index p n)
  ← rewrite if_P sym=false goal=67 facts=[66]
  ← intro goal=65
    #[68] ¬(index p m ≤ index p n)
    #[69] k ≤ (if index p m ≤ index p n then index p m else index p n)
  ← rewrite if_not_P sym=false goal=69 facts=[68]
  ← intro goal=33
    #[70] k ≤ min (index p m) (index p n)
    #[71] k ≤ index p (gcd m n)
  cut "gcd m n = 0 ∨ p = 1 ∨ k ≤ index p (gcd m n)" goal=71
    #[72] gcd m n = 0 ∨ p = 1 ∨ k ≤ index p (gcd m n)
  ← rewrite source=prev goal=72 facts=[29]
    #[73] (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  → rewrite target=fact min_def sym=false goal=73 facts=[70]
    #[74] k ≤ (if index p m ≤ index p n then index p m else index p n)
  ← cases "index p m ≤ index p n" goal=73
    #[75] index p m ≤ index p n ⟶ (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
    #[76] ¬(index p m ≤ index p n) ⟶ (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  ← intro goal=75
    #[77] (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  → rewrite target=fact if_P sym=false goal=77 facts=[74,66]
    #[78] k ≤ index p m
  ← rule conjI goal=77
    #[79] m = 0 ∨ p = 1 ∨ k ≤ index p m
    #[80] n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rule disjI2 goal=79
    #[81] p = 1 ∨ k ≤ index p m
  ← rule disjI2 goal=81
  ← rule disjI2 goal=80
    #[82] p = 1 ∨ k ≤ index p n
  ← rule disjI2 goal=82
    #[83] k ≤ index p n
  ← rule less_eq_trans param_m="index p m" goal=83
  ← intro goal=76
    #[84] (m = 0 ∨ p = 1 ∨ k ≤ index p m) ∧ (n = 0 ∨ p = 1 ∨ k ≤ index p n)
  → rewrite target=fact if_not_P sym=false goal=84 facts=[74,68]
    #[85] k ≤ index p n
  ← rule conjI goal=84
    #[86] m = 0 ∨ p = 1 ∨ k ≤ index p m
    #[87] n = 0 ∨ p = 1 ∨ k ≤ index p n
  ← rule disjI2 goal=86
    #[88] p = 1 ∨ k ≤ index p m
  ← rule disjI2 goal=88
    #[89] k ≤ index p m
  ← rule less_eq_trans param_m="index p n" goal=89
    #[90] index p n ≤ index p m
  → rewrite target=fact not_le sym=false goal=90 facts=[68]
    #[91] index p n < index p m
  ← rule lt_imp_le goal=90
  ← rule disjI2 goal=87
    #[92] p = 1 ∨ k ≤ index p n
  ← rule disjI2 goal=92
  ← rule disjE goal=71 facts=[72]
    #[93] gcd m n = 0 ⟶ k ≤ index p (gcd m n)
    #[94] p = 1 ∨ k ≤ index p (gcd m n) ⟶ k ≤ index p (gcd m n)
  ← intro goal=93
    #[95] gcd m n = 0
    #[96] k ≤ index p (gcd m n)
  → rewrite target=fact gcd_zero sym=false goal=96 facts=[95]
    #[97] m = 0 ∧ n = 0
  → forward conjD1 goal=96 facts=[97]
    #[98] m = 0
  ← rule negE_gen goal=96 facts=[10,98]
    #[99] k ≤ index p (gcd m n)
  ← intro goal=94
    #[100] p = 1 ∨ k ≤ index p (gcd m n)
    #[101] k ≤ index p (gcd m n)
  ← rule disjE goal=101 facts=[100]
    #[102] p = 1 ⟶ k ≤ index p (gcd m n)
    #[103] k ≤ index p (gcd m n) ⟶ k ≤ index p (gcd m n)
  ← intro goal=102
    #[104] k ≤ index p (gcd m n)
  ← rule negE_gen goal=104 facts=[31,49]
    #[105] k ≤ index p (gcd m n)
qed

theorem index_fact_prime_mult
  fixes n :: nat, p :: nat
  prop prime p --> index p (fact (p * n)) = n + index p (fact n)
  [hint_backward]

theorem prime_factorization_index
  fixes k :: nat => nat
  prop finite ({p. prime p & ~(k p = 0)}) --> (?n. ~(n = 0) & (!p. prime p --> index p n = k p))
  [hint_backward]

theorem prime_power_exists
  fixes n :: nat, q :: nat
  prop prime q --> (?i::nat. n = q ^ i) <--> (!p. prime p --> divides p n --> p = q)
  [hint_backward]
proof
  ← intro goal=0
    #[1] prime q
    #[2] (∃i::nat. n = q ^ i) ⟷ (∀p. prime p ⟶ divides p n ⟶ p = q)
  ← rule iffI goal=2
    #[3] (∃i::nat. n = q ^ i) ⟶ (∀p. prime p ⟶ divides p n ⟶ p = q)
    #[4] (∀p. prime p ⟶ divides p n ⟶ p = q) ⟶ (∃i::nat. n = q ^ i)
  ← intro goal=3
    #[5] ∃i::nat. n = q ^ i
    #[6] ∀p. prime p ⟶ divides p n ⟶ p = q
  elim i goal=6 facts=[5]
    #[7] _VAR i
    #[8] n = q ^ i
    #[9] ∀p. prime p ⟶ divides p n ⟶ p = q
  → rewrite target=fact primepow_divisors_eq sym=false goal=9 facts=[8]
    #[10] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) n ⟷ divides (p ^ k) (q ^ i)
  ← intro p goal=9
    #[11] _VAR p
    #[12] prime p
    #[13] divides p n
    #[14] p = q
  → forward divides_prime_exp_le param_m="" param_n=i goal=14 facts=[12,1]
    #[15] ∀m. divides (p ^ m) (q ^ i) ⟷ m = 0 ∨ p = q ∧ m ≤ i
  → forward goal=14 facts=[10,12]
    #[16] ∀k::nat. divides (p ^ k) n ⟷ divides (p ^ k) (q ^ i)
  → rewrite target=fact source=prev goal=14 facts=[15,16]
    #[17] ∀k. divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ i
  → inst target=fact "(1::nat)" goal=14 facts=[17]
    #[18] divides (p ^ (1::nat)) n ⟷ (1::nat) = 0 ∨ p = q ∧ 1 ≤ i
  → rewrite target=fact exp_1 sym=false goal=14 facts=[18]
    #[19] divides p n ⟷ (1::nat) = 0 ∨ p = q ∧ 1 ≤ i
  → rewrite target=fact source=prev goal=14 facts=[19,13]
    #[20] (1::nat) = 0 ∨ p = q ∧ 1 ≤ i
  ← rule disjE goal=14 facts=[20]
    #[21] (1::nat) = 0 ⟶ p = q
    #[22] p = q ∧ 1 ≤ i ⟶ p = q
  ← intro goal=21
    #[23] (1::nat) = 0
    #[24] p = q
  ← z3 goal=24 facts=[23]
    #[25] p = q
  ← intro goal=22
    #[26] p = q ∧ 1 ≤ i
    #[27] p = q
  ← rule conjD1 goal=27 facts=[26]
    #[28] p = q
  ← intro goal=4
    #[29] ∀p. prime p ⟶ divides p n ⟶ p = q
    #[30] ∃i::nat. n = q ^ i
  ← rewrite primepow_divisors_eq sym=false goal=30
    #[31] ∃i::nat. ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) n ⟷ divides (p ^ k) (q ^ i)
  ← inst "index q n" goal=31
    #[32] ∀p. ∀k::nat. prime p ⟶ divides (p ^ k) n ⟷ divides (p ^ k) (q ^ index q n)
  ← intro p,k goal=32
    #[33] _VAR k
    #[34] divides (p ^ k) n ⟷ divides (p ^ k) (q ^ index q n)
  → forward divides_prime_exp_le param_m=k param_n="index q n" goal=34 facts=[12,1]
    #[35] divides (p ^ k) (q ^ index q n) ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← rewrite source=prev goal=34 facts=[35]
    #[36] divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← cases "k=0" goal=36
    #[37] k = 0 ⟶ divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
    #[38] ¬(k = 0) ⟶ divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← intro goal=37
    #[39] k = 0
    #[40] divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← rule iffI goal=40
    #[41] divides (p ^ k) n ⟶ k = 0 ∨ p = q ∧ k ≤ index q n
    #[42] k = 0 ∨ p = q ∧ k ≤ index q n ⟶ divides (p ^ k) n
  ← intro goal=41
    #[43] divides (p ^ k) n
    #[44] k = 0 ∨ p = q ∧ k ≤ index q n
  ← rule disjI1 goal=44
  ← intro goal=42
    #[45] k = 0 ∨ p = q ∧ k ≤ index q n
    #[46] divides (p ^ k) n
  ← rewrite source=prev goal=46 facts=[39]
    #[47] divides (p ^ (0::nat)) n
  ← rewrite nat_nat_power_def_1 sym=false goal=47
    #[48] divides 1 n
  ← rule divides_1 goal=48
    #[49] divides 1 n
  ← intro goal=38
    #[50] ¬(k = 0)
    #[51] divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← cases "divides (p ^ k) n" goal=51
    #[52] divides (p ^ k) n ⟶ divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
    #[53] ¬(divides (p ^ k) n) ⟶ divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← intro goal=52
    #[54] divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← rule iffI goal=54
    #[55] divides (p ^ k) n ⟶ k = 0 ∨ p = q ∧ k ≤ index q n
    #[56] k = 0 ∨ p = q ∧ k ≤ index q n ⟶ divides (p ^ k) n
  ← intro goal=55
    #[57] k = 0 ∨ p = q ∧ k ≤ index q n
  ← rule disjI2 goal=57
    #[58] p = q ∧ k ≤ index q n
  → forward goal=58 facts=[29,12]
    #[59] divides p n ⟶ p = q
  ← rule conjI goal=58
    #[60] p = q
    #[61] k ≤ index q n
  ← apply_prev goal=60 facts=[59]
    #[62] divides p n
  ← rule divides_exp2 param_n=k goal=62
  → rewrite target=fact source=prev goal=61 facts=[60,43]
    #[63] divides (q ^ k) n
  → rewrite target=fact primepow_divides_index sym=false goal=61 facts=[63]
    #[64] n = 0 ∨ q = 1 ∨ k ≤ index q n
  ← rule disjE goal=61 facts=[64]
    #[65] n = 0 ⟶ k ≤ index q n
    #[66] q = 1 ∨ k ≤ index q n ⟶ k ≤ index q n
  ← intro goal=65
    #[67] n = 0
    #[68] k ≤ index q n
  ← rewrite double_neg sym=true goal=68
    #[69] ¬¬(k ≤ index q n)
  ← rule negI goal=69
    #[70] ¬(k ≤ index q n) ⟶ false
  ← intro goal=70
    #[71] ¬(k ≤ index q n)
    #[72] false
  → inst target=fact "(2::nat)" goal=72 facts=[29]
    #[73] prime 2 ⟶ divides 2 n ⟶ 2 = q
  → inst target=fact "(3::nat)" goal=72 facts=[29]
    #[74] prime 3 ⟶ divides 3 n ⟶ 3 = q
  cut "2 = q" goal=72
    #[75] 2 = q
  ← apply_prev goal=75 facts=[73]
    #[76] prime 2
    #[77] divides 2 n
  ← rule prime_2 goal=76
    #[78] prime 2
  ← rewrite source=prev goal=77 facts=[67]
    #[79] divides 2 0
  ← rule divides_zero goal=79
    #[80] divides 2 0
  cut "3 = q" goal=72
    #[81] 3 = q
  ← apply_prev goal=81 facts=[74]
    #[82] prime 3
    #[83] divides 3 n
  ← rewrite prime_alt sym=false goal=82
    #[84] ¬((3::nat) = 0) ∧ ¬((3::nat) = 1) ∧ (∀n. 1 < n ⟶ n < 3 ⟶ ¬(divides n 3))
  ← rule conjI goal=84
    #[85] ¬((3::nat) = 0)
    #[86] ¬((3::nat) = 1) ∧ (∀n. 1 < n ⟶ n < 3 ⟶ ¬(divides n 3))
  nat_const_ineq goal=85
    #[87] ¬((3::nat) = 0)
  ← rule conjI goal=86
    #[88] ¬((3::nat) = 1)
    #[89] ∀n. 1 < n ⟶ n < 3 ⟶ ¬(divides n 3)
  nat_const_ineq goal=88
    #[90] ¬((3::nat) = 1)
  ← intro m goal=89
    #[91] _VAR m
    #[92] 1 < m
    #[93] m < 3
    #[94] ¬(divides m 3)
  cut "m=2" goal=94
    #[95] m = 2
  ← z3 goal=95 facts=[92,93]
    #[96] m = 2
  ← rewrite source=prev goal=94 facts=[96]
    #[97] ¬(divides 2 3)
  ← rewrite divides_def sym=false goal=97
    #[98] ¬(∃k::nat. 3 = 2 * k)
  ← z3 goal=98
    #[99] ¬(∃k::nat. 3 = 2 * k)
  ← rewrite source=prev goal=83 facts=[67]
    #[100] divides 3 0
  ← rule divides_zero goal=100
    #[101] divides 3 0
  → rewrite target=fact eq_sym_eq sym=false goal=72 facts=[81]
    #[102] q = 3
  → rewrite target=fact source=prev goal=72 facts=[102,75]
    #[103] (2::nat) = 3
  ← z3 goal=72 facts=[103]
  ← intro goal=66
    #[104] q = 1 ∨ k ≤ index q n
    #[105] k ≤ index q n
  ← rule disjE goal=105 facts=[104]
    #[106] q = 1 ⟶ k ≤ index q n
    #[107] k ≤ index q n ⟶ k ≤ index q n
  ← intro goal=106
    #[108] q = 1
    #[109] k ≤ index q n
  → rewrite target=fact prime_def sym=false goal=109 facts=[1]
    #[110] ¬(q = 1) ∧ (∀x. divides x q ⟶ x = 1 ∨ x = q)
  → forward conjD1 goal=109 facts=[110]
    #[111] ¬(q = 1)
  ← rule negE_gen goal=109 facts=[111,108]
    #[112] k ≤ index q n
  ← intro goal=56
  ← intro goal=53
    #[113] ¬(divides (p ^ k) n)
    #[114] divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ k ≤ index q n
  ← rewrite le_index sym=false goal=114
    #[115] divides (p ^ k) n ⟷ k = 0 ∨ p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
  ← rule iffI goal=115
    #[116] divides (p ^ k) n ⟶ k = 0 ∨ p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
    #[117] k = 0 ∨ p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n ⟶ divides (p ^ k) n
  ← intro goal=116
    #[118] k = 0 ∨ p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
  ← rule negE_gen goal=118 facts=[113,43]
    #[119] k = 0 ∨ p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
  ← intro goal=117
    #[120] k = 0 ∨ p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
    #[121] divides (p ^ k) n
  ← rule disjE goal=121 facts=[120]
    #[122] k = 0 ⟶ divides (p ^ k) n
    #[123] p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n ⟶ divides (p ^ k) n
  ← intro goal=122
    #[124] divides (p ^ k) n
  ← rule negE_gen goal=124 facts=[50,39]
    #[125] divides (p ^ k) n
  ← intro goal=123
    #[126] p = q ∧ (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
    #[127] divides (p ^ k) n
  → forward conjD1 goal=127 facts=[126]
    #[128] p = q
  → forward conjD2 goal=127 facts=[126]
    #[129] (n = 0 ∨ q = 1 ⟶ k = 0) ∧ divides (q ^ k) n
  → forward conjD2 goal=127 facts=[129]
    #[130] divides (q ^ k) n
  → rewrite target=fact source=prev goal=127 facts=[128,113]
    #[131] ¬(divides (q ^ k) n)
  ← rule negE_gen goal=127 facts=[131,130]
    #[132] divides (p ^ k) n
qed

theorem prime_factorization_alt
  fixes n :: nat
  prop ~(n = 0) --> nproduct ({p. prime p}) (%p. p ^ index p n) = n
  [hint_backward]
proof
  ← intro goal=0
    #[1] ¬(n = 0)
    #[2] nproduct ({p. prime p}) (λp. p ^ index p n) = n
  cut "!n. ¬(n = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p n) = n" goal=2
    #[3] ∀n. ¬(n = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p n) = n
  ← rule complete_factor_induct goal=3
    #[4] ¬((0::nat) = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p 0) = 0
    #[5] ¬((1::nat) = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p 1) = 1
    #[6] ∀p. prime p ⟶ ¬(p = 0) ⟶ nproduct ({p1. prime p1}) (λp1. p1 ^ index p1 p) = p
    #[7] ∀m. ∀n. (¬(m = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p m) = m) ⟶ (¬(n = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p n) = n) ⟶ ¬(m * n = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p (m * n)) = m * n
  ← intro goal=4
    #[8] ¬((0::nat) = 0)
    #[9] nproduct ({p. prime p}) (λp. p ^ index p 0) = 0
  ← rule negE_gen goal=9 facts=[8]
    #[10] (0::nat) = 0
  ← rule eq_refl goal=10
    #[11] (0::nat) = 0
  ← intro goal=5
    #[12] ¬((1::nat) = 0)
    #[13] nproduct ({p. prime p}) (λp. p ^ index p 1) = 1
  ← rewrite index_1 sym=false goal=13
    #[14] nproduct ({p. prime p}) (λp. p ^ (0::nat)) = 1
  ← rewrite nat_nat_power_def_1 sym=false goal=14
    #[15] nproduct ({p. prime p}) (λp. 1) = 1
  ← rewrite nproduct_one sym=false goal=15
    #[16] nproduct ({p. prime p}) (λp. 1) = 1
  ← intro p goal=6
    #[17] _VAR p
    #[18] prime p
    #[19] ¬(p = 0)
    #[20] nproduct ({p1. prime p1}) (λp1. p1 ^ index p1 p) = p
  → forward index_prime param_a="" goal=20 facts=[18]
    #[21] ∀a. index a p = (if p = a then 1 else 0)
  ← rewrite source=prev goal=20 facts=[21]
    #[22] nproduct ({p1. prime p1}) (λp1. p1 ^ (if p = p1 then (1::nat) else 0)) = p
  cut "∀p1. p1 ^ (if p = p1 then (1::nat) else 0) = (if p1 = p then p else 1)" goal=22
    #[23] ∀p1. p1 ^ (if p = p1 then (1::nat) else 0) = (if p1 = p then p else 1)
  ← intro x goal=23
    #[24] _VAR x
    #[25] x ^ (if p = x then (1::nat) else 0) = (if x = p then p else 1)
  ← cases "p = x" goal=25
    #[26] p = x ⟶ x ^ (if p = x then (1::nat) else 0) = (if x = p then p else 1)
    #[27] ¬(p = x) ⟶ x ^ (if p = x then (1::nat) else 0) = (if x = p then p else 1)
  ← intro goal=26
    #[28] p = x
    #[29] x ^ (if p = x then (1::nat) else 0) = (if x = p then p else 1)
  ← rewrite if_P sym=false goal=29 facts=[28]
    #[30] x ^ (1::nat) = (if x = p then p else 1)
  → rewrite target=fact eq_sym_eq sym=false goal=30 facts=[28]
    #[31] x = p
  ← rewrite if_P sym=false goal=30 facts=[31]
    #[32] x ^ (1::nat) = p
  ← rewrite exp_1 sym=false goal=32
  ← intro goal=27
    #[33] ¬(p = x)
    #[34] x ^ (if p = x then (1::nat) else 0) = (if x = p then p else 1)
  ← rewrite if_not_P sym=false goal=34 facts=[33]
    #[35] x ^ (0::nat) = (if x = p then p else 1)
  → rewrite target=fact eq_sym_eq sym=false goal=35 facts=[33]
    #[36] ¬(x = p)
  ← rewrite if_not_P sym=false goal=35 facts=[36]
    #[37] x ^ (0::nat) = 1
  ← rewrite nat_nat_power_def_1 sym=false goal=37
    #[38] x ^ (0::nat) = 1
  ← rewrite source=prev goal=22 facts=[23]
    #[39] nproduct ({p1. prime p1}) (λp1. if p1 = p then p else 1) = p
  ← rewrite nproduct_delta sym=false goal=39
    #[40] (if p ∈ {p1. prime p1} then p else 1) = p
  ← rewrite member_collect sym=false goal=40
    #[41] (if prime p then p else 1) = p
  ← rewrite if_P sym=false goal=41 facts=[18]
    #[42] (if prime p then p else 1) = p
  ← intro a,b goal=7
    #[43] _VAR a
    #[44] _VAR b
    #[45] ¬(a = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p a) = a
    #[46] ¬(b = 0) ⟶ nproduct ({p. prime p}) (λp. p ^ index p b) = b
    #[47] ¬(a * b = 0)
    #[48] nproduct ({p. prime p}) (λp. p ^ index p (a * b)) = a * b
  → rewrite target=fact mult_eq_0 sym=false goal=48 facts=[47]
    #[49] ¬(a = 0 ∨ b = 0)
  → rewrite target=fact de_morgan_thm2 sym=false goal=48 facts=[49]
    #[50] ¬(a = 0) ∧ ¬(b = 0)
  → forward conjD1 goal=48 facts=[50]
    #[51] ¬(a = 0)
  → forward conjD2 goal=48 facts=[50]
    #[52] ¬(b = 0)
  → forward goal=48 facts=[45,51]
    #[53] nproduct ({p. prime p}) (λp. p ^ index p a) = a
  → forward goal=48 facts=[46,52]
    #[54] nproduct ({p. prime p}) (λp. p ^ index p b) = b
  cut "nproduct ({p. prime p}) (λp. p ^ (index p a + index p b)) = nproduct ({p. prime p}) (λp. p ^ index p a) * nproduct ({p. prime p}) (λp. p ^ index p b)" goal=48
    #[55] nproduct ({p. prime p}) (λp. p ^ (index p a + index p b)) = nproduct ({p. prime p}) (λp. p ^ index p a) * nproduct ({p. prime p}) (λp. p ^ index p b)
  ← rewrite nat_power_add sym=false goal=55
    #[56] nproduct ({p. prime p}) (λp. p ^ index p a * p ^ index p b) = nproduct ({p. prime p}) (λp. p ^ index p a) * nproduct ({p. prime p}) (λp. p ^ index p b)
  ← rule nproduct_mul_gen goal=56
    #[57] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p ^ index p a = 1)})
    #[58] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p ^ index p b = 1)})
  ← rewrite member_collect sym=false goal=57
    #[59] finite ({p. prime p ∧ ¬(p ^ index p a = 1)})
  ← rewrite exp_eq_1 sym=false goal=59
    #[60] finite ({p. prime p ∧ ¬(p = 1 ∨ index p a = 0)})
  ← rewrite index_eq_0 sym=false goal=60
    #[61] finite ({p. prime p ∧ ¬(p = 1 ∨ a = 0 ∨ p = 1 ∨ ¬(divides p a))})
  ← rewrite de_morgan_thm2 sym=false goal=61
    #[62] finite ({p. prime p ∧ ¬(p = 1) ∧ ¬(a = 0 ∨ p = 1 ∨ ¬(divides p a))})
  ← rewrite de_morgan_thm2 sym=false goal=62
    #[63] finite ({p. prime p ∧ ¬(p = 1) ∧ ¬(a = 0) ∧ ¬(p = 1 ∨ ¬(divides p a))})
  ← rewrite de_morgan_thm2 sym=false goal=63
    #[64] finite ({p. prime p ∧ ¬(p = 1) ∧ ¬(a = 0) ∧ ¬(p = 1) ∧ ¬¬(divides p a)})
  ← rewrite double_neg sym=false goal=64
    #[65] finite ({p. prime p ∧ ¬(p = 1) ∧ ¬(a = 0) ∧ ¬(p = 1) ∧ divides p a})
  ← rewrite conj_assoc sym=false goal=65
    #[66] finite ({p. (prime p ∧ ¬(p = 1)) ∧ ¬(a = 0) ∧ ¬(p = 1) ∧ divides p a})
  ← rewrite conj_assoc sym=false goal=66
    #[67] finite ({p. ((prime p ∧ ¬(p = 1)) ∧ ¬(a = 0)) ∧ ¬(p = 1) ∧ divides p a})
  ← rewrite conj_assoc sym=false goal=67
    #[68] finite ({p. (((prime p ∧ ¬(p = 1)) ∧ ¬(a = 0)) ∧ ¬(p = 1)) ∧ divides p a})
  ← rule finite_special_divisors goal=68
  ← rewrite exp_eq_1 sym=false goal=58
    #[69] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p = 1 ∨ index p b = 0)})
  ← rewrite index_eq_0 sym=false goal=69
    #[70] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p = 1 ∨ b = 0 ∨ p = 1 ∨ ¬(divides p b))})
  ← rewrite de_morgan_thm2 sym=false goal=70
    #[71] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p = 1) ∧ ¬(b = 0 ∨ p = 1 ∨ ¬(divides p b))})
  ← rewrite de_morgan_thm2 sym=false goal=71
    #[72] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p = 1) ∧ ¬(b = 0) ∧ ¬(p = 1 ∨ ¬(divides p b))})
  ← rewrite de_morgan_thm2 sym=false goal=72
    #[73] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p = 1) ∧ ¬(b = 0) ∧ ¬(p = 1) ∧ ¬¬(divides p b)})
  ← rewrite double_neg sym=false goal=73
    #[74] finite ({p. p ∈ {p1. prime p1} ∧ ¬(p = 1) ∧ ¬(b = 0) ∧ ¬(p = 1) ∧ divides p b})
  ← rewrite conj_assoc sym=false goal=74
    #[75] finite ({p. (p ∈ {p1. prime p1} ∧ ¬(p = 1)) ∧ ¬(b = 0) ∧ ¬(p = 1) ∧ divides p b})
  ← rewrite conj_assoc sym=false goal=75
    #[76] finite ({p. ((p ∈ {p1. prime p1} ∧ ¬(p = 1)) ∧ ¬(b = 0)) ∧ ¬(p = 1) ∧ divides p b})
  ← rewrite conj_assoc sym=false goal=76
    #[77] finite ({p. (((p ∈ {p1. prime p1} ∧ ¬(p = 1)) ∧ ¬(b = 0)) ∧ ¬(p = 1)) ∧ divides p b})
  ← rule finite_special_divisors goal=77
  cut "nproduct ({p. prime p}) (λp. p ^ index p (a * b)) = nproduct ({p. prime p}) (λp. p ^ (index p a + index p b))" goal=48
    #[78] nproduct ({p. prime p}) (λp. p ^ index p (a * b)) = nproduct ({p. prime p}) (λp. p ^ (index p a + index p b))
  ← rule nproduct_eq goal=78
    #[79] ∀x. x ∈ {p. prime p} ⟶ x ^ index x (a * b) = x ^ (index x a + index x b)
  ← intro p goal=79
    #[80] p ∈ {p1. prime p1}
    #[81] p ^ index p (a * b) = p ^ (index p a + index p b)
  → rewrite target=fact member_collect sym=false goal=81 facts=[80]
    #[82] prime p
  cut "index p (a * b) = (index p a + index p b)" goal=81
    #[83] index p (a * b) = index p a + index p b
  ← rule index_mul goal=83
  ← rewrite source=prev goal=81 facts=[83]
  ← rewrite source=prev goal=48 facts=[78]
    #[84] nproduct ({p. prime p}) (λp. p ^ (index p a + index p b)) = a * b
  ← rewrite source=prev goal=84 facts=[55]
    #[85] nproduct ({p. prime p}) (λp. p ^ index p a) * nproduct ({p. prime p}) (λp. p ^ index p b) = a * b
  ← rewrite source=prev goal=85 facts=[53]
    #[86] a * nproduct ({p. prime p}) (λp. p ^ index p b) = a * b
  ← rewrite source=prev goal=86 facts=[54]
    #[87] a * nproduct ({p. prime p}) (λp. p ^ index p b) = a * b
  ← apply_prev goal=2 facts=[3]
qed

theorem prime_factorization
  fixes n :: nat
  prop ~(n = 0) --> nproduct ({p. prime p & divides p n}) (%p. p ^ index p n) = n
  [hint_backward]

theorem induct_coprime
  fixes P :: nat => bool
  prop (!a. !b. 1 < a --> 1 < b --> coprime a b --> P a --> P b --> P (a * b)) --> (!p. !k::nat. prime p --> P (p ^ k)) --> (!n. 1 < n --> P n)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] ∀a. ∀b. 1 < a ⟶ 1 < b ⟶ coprime a b ⟶ P a ⟶ P b ⟶ P (a * b)
    #[2] ∀p. ∀k::nat. prime p ⟶ P (p ^ k)
    #[3] ∀n. 1 < n ⟶ P n
  ← rule nat_WF goal=3
    #[4] ∀n. (∀m. m < n ⟶ 1 < m ⟶ P m) ⟶ 1 < n ⟶ P n
  ← intro n goal=4
    #[5] _VAR n
    #[6] ∀m. m < n ⟶ 1 < m ⟶ P m
    #[7] 1 < n
    #[8] P n
  cut "~(n=1)" goal=8
    #[9] ¬(n = 1)
  ← z3 goal=9 facts=[7]
    #[10] ¬(n = 1)
  → forward prime_factor goal=8 facts=[10]
    #[11] ∃p. prime p ∧ divides p n
  elim p goal=8 facts=[11]
    #[12] _VAR p
    #[13] prime p ∧ divides p n
    #[14] P n
  → forward conjD1 goal=14 facts=[13]
    #[15] prime p
  → forward conjD2 goal=14 facts=[13]
    #[16] divides p n
  cut "~(n=0)" goal=14
    #[17] ¬(n = 0)
  ← z3 goal=17 facts=[7]
    #[18] ¬(n = 0)
  → forward prime_ge_2 goal=14 facts=[15]
    #[19] 2 ≤ p
  → forward factorization_index goal=14 facts=[18,19]
    #[20] ∃k::nat. divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n))
  elim k goal=14 facts=[20]
    #[21] _VAR k
    #[22] divides (p ^ k) n ∧ (∀l. k < l ⟶ ¬(divides (p ^ l) n))
    #[23] P n
  → forward conjD1 goal=23 facts=[22]
    #[24] divides (p ^ k) n
  → forward conjD2 goal=23 facts=[22]
    #[25] ∀l. k < l ⟶ ¬(divides (p ^ l) n)
  → rewrite target=fact divides_def sym=false goal=23 facts=[24]
    #[26] ∃k1. n = p ^ k * k1
  elim m goal=23 facts=[26]
    #[27] _VAR m
    #[28] n = p ^ k * m
    #[29] P n
  ← cases "m=1" goal=29
    #[30] m = 1 ⟶ P n
    #[31] ¬(m = 1) ⟶ P n
  ← intro goal=30
    #[32] m = 1
    #[33] P n
  → rewrite target=fact source=prev goal=33 facts=[32,28]
    #[34] n = p ^ k * 1
  → rewrite target=fact mult_1_right sym=false goal=33 facts=[34]
    #[35] n = p ^ k
  ← rewrite source=prev goal=33 facts=[35]
    #[36] P (p ^ k)
  ← apply_prev goal=36 facts=[2]
  ← intro goal=31
    #[37] ¬(m = 1)
    #[38] P n
  ← rewrite source=prev goal=38 facts=[28]
    #[39] P (p ^ k * m)
  ← apply_prev goal=39 facts=[1]
    #[40] 1 < p ^ k
    #[41] 1 < m
    #[42] coprime (p ^ k) m
    #[43] P (p ^ k)
    #[44] P m
  cut "~(k=0)" goal=40
    #[45] ¬(k = 0)
  ← rule negI goal=45
    #[46] k = 0 ⟶ false
  ← intro goal=46
    #[47] k = 0
    #[48] false
  → rewrite target=fact source=prev goal=48 facts=[47,25]
    #[49] ∀l::nat. 0 < l ⟶ ¬(divides (p ^ l) n)
  → inst target=fact "(1::nat)" goal=48 facts=[49]
    #[50] (0::nat) < 1 ⟶ ¬(divides (p ^ (1::nat)) n)
  → rewrite target=fact exp_1 sym=false goal=48 facts=[50]
    #[51] (0::nat) < 1 ⟶ ¬(divides p n)
  ← rule contrapositive goal=48 facts=[16]
    #[52] ¬false ⟶ ¬(divides p n)
  ← intro goal=52
    #[53] ¬false
    #[54] ¬(divides p n)
  ← apply_prev goal=54 facts=[51]
    #[55] (0::nat) < 1
  ← z3 goal=55
    #[56] (0::nat) < 1
  → rewrite target=fact source=prev goal=40 facts=[28,7]
    #[57] 1 < p ^ k * m
  cut "!x::nat. 1 < x <--> ~(x = 0) & ~(x = 1)" goal=40
    #[58] ∀x::nat. 1 < x ⟷ ¬(x = 0) ∧ ¬(x = 1)
  ← z3 goal=58
    #[59] ∀x::nat. 1 < x ⟷ ¬(x = 0) ∧ ¬(x = 1)
  → rewrite target=fact source=prev goal=40 facts=[59,57]
    #[60] ¬(p ^ k * m = 0) ∧ ¬(p ^ k * m = 1)
  → forward conjD1 goal=40 facts=[60]
    #[61] ¬(p ^ k * m = 0)
  → rewrite target=fact mult_eq_0 sym=false goal=40 facts=[61]
    #[62] ¬(p ^ k = 0 ∨ m = 0)
  → rewrite target=fact de_morgan_thm2 sym=false goal=40 facts=[62]
    #[63] ¬(p ^ k = 0) ∧ ¬(m = 0)
  → forward conjD1 goal=40 facts=[63]
    #[64] ¬(p ^ k = 0)
  → forward conjD2 goal=40 facts=[63]
    #[65] ¬(m = 0)
  ← rewrite source=prev goal=40 facts=[59]
    #[66] ¬(p ^ k = 0) ∧ ¬(p ^ k = 1)
  ← rule conjI goal=66
    #[67] ¬(p ^ k = 1)
  ← rewrite exp_eq_1 sym=false goal=67
    #[68] ¬(p = 1 ∨ k = 0)
  ← rewrite de_morgan_thm2 sym=false goal=68
    #[69] ¬(p = 1) ∧ ¬(k = 0)
  ← rule conjI goal=69
    #[70] ¬(p = 1)
  ← z3 goal=70 facts=[19]
    #[71] ¬(p = 1)
  ← rewrite source=prev goal=41 facts=[59]
    #[72] ¬(m = 0) ∧ ¬(m = 1)
  ← rule conjI goal=72
  ← rewrite coprime_sym sym=false goal=42
    #[73] coprime m (p ^ k)
  ← rule coprime_exp goal=73
    #[74] coprime m p
  → forward prime_coprime param_n=m goal=74 facts=[15]
    #[75] m = 1 ∨ divides p m ∨ coprime p m
  ← rewrite coprime_sym sym=false goal=74
    #[76] coprime p m
  ← rule disjE goal=76 facts=[75]
    #[77] m = 1 ⟶ coprime p m
    #[78] divides p m ∨ coprime p m ⟶ coprime p m
  ← intro goal=77
    #[79] coprime p m
  ← rule negE_gen goal=79 facts=[37,32]
    #[80] coprime p m
  ← intro goal=78
    #[81] divides p m ∨ coprime p m
    #[82] coprime p m
  ← rule disjE goal=82 facts=[81]
    #[83] divides p m ⟶ coprime p m
    #[84] coprime p m ⟶ coprime p m
  ← intro goal=83
    #[85] divides p m
    #[86] coprime p m
  → inst target=fact k+1 goal=86 facts=[25]
    #[87] k < k + 1 ⟶ ¬(divides (p ^ (k + 1)) n)
  cut "k < k + 1" goal=86
    #[88] k < k + 1
  ← z3 goal=88
    #[89] k < k + 1
  → forward goal=86 facts=[87,89]
    #[90] ¬(divides (p ^ (k + 1)) n)
  → rewrite target=fact nat_power_add sym=false goal=86 facts=[90]
    #[91] ¬(divides (p ^ k * p ^ (1::nat)) n)
  → rewrite target=fact exp_1 sym=false goal=86 facts=[91]
    #[92] ¬(divides (p ^ k * p) n)
  ← rule negE_gen goal=86 facts=[92]
    #[93] divides (p ^ k * p) n
  ← rewrite source=prev goal=93 facts=[28]
    #[94] divides (p ^ k * p) (p ^ k * m)
  ← rule divides_mult_left goal=94 facts=[85]
    #[95] divides (p ^ k * p) (p ^ k * m)
  ← apply_prev goal=43 facts=[2]
  ← apply_prev goal=44 facts=[6]
    #[96] m < n
  ← rewrite source=prev goal=96 facts=[28]
    #[97] m < p ^ k * m
  ← rule let_trans param_m="1 * m" goal=97
    #[98] m ≤ 1 * m
    #[99] 1 * m < p ^ k * m
  ← rewrite mult_1_left sym=false goal=98
    #[100] m ≤ m
  ← rule less_eq_refl goal=100
    #[101] m ≤ m
  ← rewrite lt_mult_rcancel sym=false goal=99
    #[102] 1 < p ^ k ∧ ¬(m = 0)
  ← rule conjI goal=102
qed

theorem induct_coprime_strong
  fixes P :: nat => bool
  prop (!a. !b. 1 < a --> 1 < b --> coprime a b --> P a --> P b --> P (a * b)) --> (!p. !k::nat. prime p --> ~(k = 0) --> P (p ^ k)) --> (!n. 1 < n --> P n)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] ∀a. ∀b. 1 < a ⟶ 1 < b ⟶ coprime a b ⟶ P a ⟶ P b ⟶ P (a * b)
    #[2] ∀p. ∀k::nat. prime p ⟶ ¬(k = 0) ⟶ P (p ^ k)
    #[3] ∀n. 1 < n ⟶ P n
  cut "∀n. 1 < n ⟶ 1 < n ⟶ P n" goal=3
    #[4] ∀n. 1 < n ⟶ 1 < n ⟶ P n
  ← rule induct_coprime goal=4
    #[5] ∀a. ∀b. 1 < a ⟶ 1 < b ⟶ coprime a b ⟶ (1 < a ⟶ P a) ⟶ (1 < b ⟶ P b) ⟶ 1 < a * b ⟶ P (a * b)
    #[6] ∀p. ∀k::nat. prime p ⟶ 1 < p ^ k ⟶ P (p ^ k)
  ← intro a,b goal=5
    #[7] _VAR a
    #[8] _VAR b
    #[9] 1 < a
    #[10] 1 < b
    #[11] coprime a b
    #[12] 1 < a ⟶ P a
    #[13] 1 < b ⟶ P b
    #[14] 1 < a * b
    #[15] P (a * b)
  ← apply_prev goal=15 facts=[1]
    #[16] P a
    #[17] P b
  ← apply_prev goal=16 facts=[12]
  ← apply_prev goal=17 facts=[13]
  ← intro p,k goal=6
    #[18] _VAR p
    #[19] _VAR k
    #[20] prime p
    #[21] 1 < p ^ k
    #[22] P (p ^ k)
  ← apply_prev goal=22 facts=[2]
    #[23] ¬(k = 0)
  ← rule contrapositive goal=23 facts=[21]
    #[24] ¬¬(k = 0) ⟶ ¬(1 < p ^ k)
  ← rewrite double_neg sym=false goal=24
    #[25] k = 0 ⟶ ¬(1 < p ^ k)
  ← intro goal=25
    #[26] k = 0
    #[27] ¬(1 < p ^ k)
  ← rewrite source=prev goal=27 facts=[26]
    #[28] ¬(1 < p ^ (0::nat))
  ← rewrite nat_nat_power_def_1 sym=false goal=28
    #[29] ¬((1::nat) < 1)
  ← rewrite not_lt sym=false goal=29
    #[30] (1::nat) ≤ 1
  ← rule less_eq_refl goal=30
    #[31] (1::nat) ≤ 1
  ← intro n goal=3
    #[32] _VAR n
    #[33] 1 < n
    #[34] P n
  ← apply_prev goal=34 facts=[4]
qed

theorem induct_coprime_alt
  fixes P :: nat => bool
  prop P 0 --> (!a. !b. 1 < a --> 1 < b --> coprime a b --> P a --> P b --> P (a * b)) --> (!p. !k::nat. prime p --> P (p ^ k)) --> (!n. P n)
  [hint_backward,hint_backward1]
proof
  ← intro goal=0
    #[1] P 0
    #[2] ∀a. ∀b. 1 < a ⟶ 1 < b ⟶ coprime a b ⟶ P a ⟶ P b ⟶ P (a * b)
    #[3] ∀p. ∀k::nat. prime p ⟶ P (p ^ k)
    #[4] ∀n. P n
  cut "(∀n. 1 < n ⟶ P n)" goal=4
    #[5] ∀n. 1 < n ⟶ P n
  ← rule induct_coprime goal=5
  cut "∀n. ~(1 < n) ⟶ P n" goal=4
    #[6] ∀n. ¬(1 < n) ⟶ P n
  ← intro n goal=6
    #[7] _VAR n
    #[8] ¬(1 < n)
    #[9] P n
  cut "n = 0 | n=1" goal=9
    #[10] n = 0 ∨ n = 1
  ← z3 goal=10 facts=[8]
    #[11] n = 0 ∨ n = 1
  ← rule disjE goal=9 facts=[11]
    #[12] n = 0 ⟶ P n
    #[13] n = 1 ⟶ P n
  ← intro goal=12
    #[14] n = 0
    #[15] P n
  ← rewrite source=prev goal=15 facts=[14]
  ← intro goal=13
    #[16] n = 1
    #[17] P n
  ← rewrite source=prev goal=17 facts=[16]
    #[18] P 1
  → inst target=fact "(2::nat)" goal=18 facts=[3]
    #[19] ∀k::nat. prime 2 ⟶ P (2 ^ k)
  → inst target=fact "(0::nat)" goal=18 facts=[19]
    #[20] prime 2 ⟶ P (2 ^ (0::nat))
  → rewrite target=fact nat_nat_power_def_1 sym=false goal=18 facts=[20]
    #[21] prime 2 ⟶ P 1
  ← apply_prev goal=18 facts=[21]
    #[22] prime 2
  ← rule prime_2 goal=22
    #[23] prime 2
  ← z3 goal=4 facts=[5,6]
qed

